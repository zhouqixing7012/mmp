name: Archived asset inventory cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-off asset inventory cleanup has been archived."
