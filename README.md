# 企业资产管理系统（Asset Management System）

## 项目简介

这是一个用于产品演示的企业资产管理前端项目，不是生产系统。当前覆盖员工资产申请、审批、配给、领用、借用、更换、退库、合约号码退库、后台资产管理、库存管理入口、报废和机房资产等场景。

## 技术架构

- React 19 + Create React App
- React Router 7
- Ant Design 6
- Tailwind CSS
- Lucide React
- Recharts
- Playwright

主要分层：

```text
页面组件
  ↓
业务 Service
  ↓
demoStorage
  ↓
localStorage 演示数据
```

查询区域优先复用 `QueryBar / QueryItem`，状态优先复用 `StatusTag`，选择类字段优先复用 `SelectModal`。

## 项目结构

```text
src/
├── components/                     # 通用查询、状态、选择弹窗等组件
├── mock/                           # 演示数据
├── services/                       # 演示数据与流程读写
├── pages/
│   ├── assetManagement/            # 后台资产管理
│   ├── inventoryManagement/        # 库存管理菜单与页面入口
│   ├── employeeSelfService/        # 资产申请、审批、配给等
│   ├── assetBorrowing/             # 资产借用
│   ├── assetReplacement/           # 资产更换
│   ├── assetReturn/                # 资产退库、合约号码退库
│   └── yewurules/                  # 后台框架、菜单与配置
├── App.js
└── index.js
```

## 后台资产管理菜单

```text
资产管理
├─ 资产维护
├─ 耗材维护
├─ 合约号码维护
├─ 标签打印
├─ 公司间转移
├─ 资产报废
├─ 账面报废
├─ 资产处置
└─ 员工资产信息查询
```

已完成资产维护、耗材维护、合约号码维护、标签打印、资产报废查询列表、账面报废查询列表、资产处置查询列表、员工资产信息查询；公司间转移等待截图和字段。

资产报废、账面报废、资产处置统一采用单据查询列表：申请单号、单据状态、公司、制单时间筛选；列表展示申请单、状态、公司、制单人、制单时间、资产数量、备注和操作，并提供对应的新建、删除按钮。

## 库存管理菜单

一级菜单“库存管理”位于“无形资产”下方：

```text
库存管理
├─ 资产接收
├─ 耗材接收
├─ 入库
├─ 出库
├─ 移库
├─ 转移
└─ 库管员工作台
```

当前仅建立菜单和页面入口，业务字段等待截图确认，不自行补造。

## 核心流程

```text
资产申请 → 业务审批 → 资产配给
                         ├─ 库存领用 → ES前台领用 → 员工领用确认
                         └─ 统一采购 → 统一申请汇总-资产
```

```text
资产借用 → 借用配给 → 借用审批 → 借用发放 → 员工借用确认
```

```text
资产更换申请 → MIS鉴定 → 旧资产退回确认 → 新资产发放 → 新资产领取确认
```

```text
资产退库 → 领导/MIS审批 → 资产退库办理 → 员工退库确认 → 入库
```

```text
合约号码退库 → 合约号码退库办理 → 员工号码退库确认 → 入库
```

## 本地运行

```bash
npm install
npm start
```

默认访问：`http://localhost:3000`

## 构建与部署

```bash
npm run build
npm run deploy
```

## 测试

```bash
npm test
```

关键人工验收路径见 `CONTEXT.md`。

## 已完成功能

- 后台资产管理九个子菜单入口。
- 资产维护、耗材维护、合约号码维护查询/编辑/导出。
- 标签打印、预打印、打印历史。
- 资产报废、账面报废、资产处置单据查询列表。
- 员工资产信息查询。
- 库存管理一级菜单及 7 个子菜单入口。
- 资产申请、审批、配给、领用、借用、更换、退库和合约号码退库演示流程。
- 后台基础配置主要页面。
- 报废和机房资产演示页面。

## 待办事项

- 根据截图完成公司间转移页面。
- 根据截图完成库存管理 7 个子页面。
- 根据字段补充资产处置新建申请页。
- 按页面继续验收现有流程字段和交互。
- 补充关键流程 Playwright 冒烟测试。

## 主要文档

- `AI_RULES.md`：AI 开发行为约束
- `CLAUDE.md`：代码和组件规范
- `CONTEXT.md`：当前进度、关键决定和验收路径
- `ARCHITECTURE.md`：模块职责、调用关系和数据流

## 搜索记录

- 后台资产维护及报废查询列表复用仓库现有 `QueryBar`、Ant Design Table、`StatusTag` 等能力，没有新增第三方依赖。
- 库存管理当前仅新增菜单和页面入口，直接复用现有后台菜单渲染结构，没有引入外部方案或新增依赖。
- 资产借用、更换、退库等流程继续复用项目现有 service + `demoStorage` 结构。
