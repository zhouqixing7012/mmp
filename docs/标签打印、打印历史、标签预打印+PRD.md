# 标签打印、打印历史、标签预打印 PRD

**标签打印**：打印已经存在的资产标签。**标签预打印**：在资产实际形成前，按标签编码规则提前批量生成标签号并打印。**打印历史**：查询预打印批次以及标签实际打印记录。

## 标签打印、打印历史、标签预打印 PRD

## 一、标签打印

## 功能说明

“标签打印”用于从当前系统已有的资产标签打印数据中查询资产，并通过本地标签打印机打印资产标签。

整体流程：

**进入资产标签打印**

→ 查询需要打印的资产

→ 勾选指定资产 / 打印当前查询范围全部资产

→ 设置打印份数

→ 调用本地 Brother b-PAC 打印组件

→ 按现有标签模板执行打印

→ 更新标签打印次数

## 页面查询条件

当前资产标签打印页面提供：

| **查询项** | **交互** | **当前规则** |
| --- | --- | --- |
| 公司中文名称 | 文本输入 | 按现有查询框架过滤 |
| 公司英文名称 | 文本输入 | 按现有查询框架过滤 |
| 部门 | 部门选择 | 支持选择部门范围 |
| 板块中文名称 | 文本输入 | 按现有字段查询 |
| 资产状态 | 下拉 | 使用 `ASSET_STATUS` |
| 资产标签号 | 文本输入 | 按标签号查询 |
| 序列号 | 文本输入 | 按 SN 查询 |
| 使用人 | 弹窗选择 | 按责任人员工编号查询 |
| 城市 | 下拉/选择 | 来源现有 City 数据 |

## 默认排序

**默认按照City → 使用人编号升序 → 资产标签号升序**

## 标签打印列表

| **字段** | **说明** |
| --- | --- |
| 选择 | Checkbox，可多选 |
| 资产标签号 | 当前资产标签 |
| 资产名称 | 当前资产说明 |
| 部门 |  |
| 公司中文名称 | 标签打印数据中的公司中文名 |
| 公司英文名称 | 标签打印数据中的公司英文名 |
| 板块中文名 |  |
| 序列号 |  |
| 资产类别 | 当前资产类别 |
| 资产状态 | 当前资产状态 |
| 使用人 | 工号-姓名 |
| 打印次数 | 当前累计打印次数 |

页面支持分页及表头排序

## 页面操作

当前有效操作：

| **操作** | **是否保留** |
| --- | --- |
| 打印所选 | Y |
| 打印全部 | Y |
| 预打印 | Y |

## 打印所选

用户勾选一条或多条资产，点击：

> **打印所选**

未选择资产：

> 请选择要打印的资产标签！

系统逐条校验。

### 公司英文名

公司英文名称为空：

> 资产标签号为“XXX”项没有公司英文名！

### 公司中文名

优先取：

> 板块中文名称

为空时回退：

> 公司中文名称

仍为空：

> 资产标签号为“XXX”项没有公司中文名！

## 打印份数

开始打印前，系统打开：

> **打印几份**

弹窗。

用户设置本次打印份数后，将数量传递给本地打印组件。

## 本地打印环境

当前资产标签打印不是由服务器生成 PDF，而是直接调用用户电脑上的打印组件。

运行依赖：

### 浏览器

当前主要要求：

> **Google Chrome**

### 浏览器插件

需要安装：

> **Brother b-PAC 浏览器打印组件**

未安装插件时：

Chrome：

> 请安装插件

其他浏览器：

> 请使用谷歌浏览器且安装插件

## 打印次数

“打印所选”完成后，针对本次选中的资产，将：

> **累计打印次数 += 本次打印份数**

因此打印次数统计的是标签被要求打印的累计份数。

## 打印全部

点击：**打印全部**含义不是打印当前分页，而是：**打印当前查询条件范围内全部标签数据。页面同样打开打印次数弹窗**

## 二、标签预打印

## 功能说明

“标签预打印”用于在资产实际接收或正式建卡前，根据现有资产标签编码规则：

1. 选择标签编码规则；

1. 预先生成一批资产标签号；

1. 形成标签批次；

1. 查看批次内标签；

1. 批量打印标签。

其核心对象不是正式资产，而是：

> **标签批次 + 标签明细**

## 标签预打印整体流程

**进入标签打印历史/批次列表**

→ 点击“预打印”

→ 进入“标签打印-预打印”

→ 选择账套

→ 选择标签编码规则

→ 输入生成标签数量

→ 设置标签大小、部件数量

→ 点击“生成标签”

→ 系统生成标签批次

→ 按序号规则生成标签明细

→ 展示批次号

→ 点击“打印标签”

→ 打开本批次标签清单

→ 打印所选 / 打印全部

→ 更新批次、标签打印状态

→ 写入打印日志

## 预打印页面

页面标题：

> **标签打印-预打印**

页面操作：

| **操作** | **当前规则** |
| --- | --- |
| 返回 | 返回上一页 |
| 生成标签 | 根据当前标签设置生成标签 |
| 打印标签 | 标签生成成功前隐藏，成功后展示 |

## 选择账套

“选择账套”为必填。

候选数据来源：

> `MMP_ASSETS_TAG_ACCOUNT`

展示：

> 账套编码 - 账套名称

标签的编码规则和最终打印中英文名称均可能与所选账套相关。

未选择账套时，不允许正常生成标签。

## 标签编码规则

当前支持 5 类标签规则。

| **规则** | **需要设置的信息** |
| --- | --- |
| 常规标签规则 | 资产类型 + 年份 |
| 高耗标签规则 | 高耗大类 + 高耗小类 + 年份 |
| 特殊规则-家具 | 家具类型 |
| 特殊规则-手机 | 固定使用手机标签规则 |
| 特殊规则-备件 | 备件类型 |

系统匹配现有标签流水号规则，再确定当前最大流水号及下一标签编号。

## 常规标签规则

选择：

> **常规标签规则**

需要维护：

- 资产类型；

- 年份。

资产类型来源：

> `ASSETS_TYPE`

页面展示：

> 当前最大序号

生成时根据：

> 账套 + 资产类型 + 年份

找到对应流水号配置。

## 高耗标签规则

选择：

> **高耗标签规则**

需要：

- 高耗大类；

- 高耗小类；

- 年份。

普通高耗标签使用当前：

> `QT-` 类规则

部分特定高耗类别按照小类使用不同标签前缀。

## 特殊规则—家具

家具标签使用：

> `RULE_SPECIAL_FURNITURE`

对应规则。

需要选择家具类型。

该规则下“当前最大序号”在当前页面存在人工调整入口。

如果人工输入的流水号小于系统已有最大流水号，后台会检查该范围是否已经被使用。

如果将产生流水号重叠：

> 当前输入的流水号，存在已经被使用的情况！

不允许生成。

## 特殊规则—手机

手机特殊标签：

> 固定前缀 `NE`

页面展示当前最大流水号。

生成时继续按照现有手机标签流水规则递增。

## 特殊规则—备件

需要选择：

> 备件类型

使用当前：

> `RULE_SPECIAL_SPARE_PARTS`

对应的标签序号规则生成。

## 标签生成设置

| **字段** | **当前规则** |
| --- | --- |
| 打印标签数量 | 默认 1，必须为正整数 |
| 部件数量 | 默认 1，允许 1～99 |
| 标签批次 | 生成前显示“生成标签后自动生成” |
| 操作员 | 当前登录用户 |
| 当前日期 | 当前系统日期 |
| 备注说明 | 用户填写 |

## 标签数量与部件数量

### 打印标签数量表示本次需要生成多少个主标签。

### 部件数量含义是：每个主资产总共需要X-1张关联标签。

**标签明细总数量 = 主标签数量 × 部件数量**

## 生成标签

点击：**生成标签**

后台首先生成标签批次号。

批次号采用当前编号规则。

批次初始：

| **字段** | **初始值** |
| --- | --- |
| 批次号 | 系统生成 |
| 是否已打印 | 否 |
| 创建人 | 当前登录用户 |
| 创建时间 | 当前时间 |
| 标签数量 | 本次生成数量 |
| 标签大小 | 当前选择 |
| 标签规则 | 当前选择规则 |
| 备注 | 当前输入 |

## 标签明细生成

对于每一个主标签，系统生成标签明细：

| **字段** | **初始规则** |
| --- | --- |
| 标签批次 | 当前批次 |
| 标签号 | 按当前流水规则生成 |
| 是否已打印 | 否 |
| 打印次数 | 0 |
| 流水号 | 当前生成流水 |
| 标签规则 | 当前规则 |

如果存在部件标签，则在主标签基础上继续生成对应部件标签。

主标签流水生成结束后：

> 更新当前标签序号规则的 CurrentValue

并将：

> NextValue = CurrentValue + 1

供下一次生成标签继续使用。

## 标签生成结果

成功：

- 页面回填标签批次号；

- 页面刷新当前最大流水号；

- “打印标签”按钮显示。

失败：

> 标签生成失败

已生成成功的批次作为独立标签批次保存在系统中。

## 打印预生成标签

点击：

> **打印标签**

不会直接在当前设置页面开始打印。

系统首先打开：

> **标签清单**

即当前批次的标签明细页面。

## 标签清单

查询条件主要包括：

| **查询项** | **查询条件控件** | **说明** |
| --- | --- | --- |
| 标签号 | 文本框 | 支持模糊检索 |
| 是否已打印 | 下拉框 | 是/否 |

列表：

| **字段** | **说明** |
| --- | --- |
| 选择 | Checkbox |
| 标签批次 | 当前批次 |
| 标签号 | 已生成标签 |
| 打印次数 | 当前累计打印次数 |
| 是否已打印 | 是 / 否 |
| 操作 | 打印历史 |

默认按照标签号升序`➡️`流水号升序展示

## 预打印标签—打印所选

未选择：

> 请选择要打印的资产标签！

选择标签后：

1. 设置打印份数；

1. 后台查询选中标签明细；

1. 后台更新打印状态及打印次数；

1. 生成打印日志；

1. 返回标签数据；

1. 浏览器调用插件执行实际打印。

## 预打印标签—打印全部

打印全部按：

> 当前标签批次

取得全部标签明细。

不要求用户逐条勾选。

标签按标签号升序`➡️`流水号升序打印。

## 部件标签模板

预打印标签中，如果标签号属于部件标签：

> 标签号包含 `-`，同时不是 `-V` 特殊形式

系统将其识别为：

> 部件标签

打印时使用部件标签模版，普通标签继续使用标准标签模板。

## 打印状态更新

执行预打印标签打印时：

### 批次

整个标签批次是否已打印更新为是

### 标签明细

每一条本次打印的标签是否已打印更新为是

同时更新打印次数：

> `printCount += 本次打印份数`

因此标签批次上的“已打印”表示：**该批次已经发生过打印行为，**并不表示批次中每一张标签都一定已经打印过。

## 三、打印历史

## 功能说明

“打印历史”实际上存在两个层次：

### 第一层：标签批次历史

查看：哪一批标签什么时候生成、生成多少、是否发生过打印。

### 第二层：具体标签打印日志

查看：某张标签由谁、在什么时间、从哪台电脑执行过打印。

因此打印历史不是只有一张列表。

## 标签批次历史

**标签打印历史**同时它也是进入“标签预打印”的入口。

## 查询条件

| **查询项** | **规则** |
| --- | --- |
| 标签批次 | 按批次号查询 |
| 是否已打印 | 是 / 否 |
| 创建人 | 从现有标签批次创建人中选择 |
| 订单编号 | 按订单号查询 |
| 创建时间 | 结束时间不能早于开始日期 |

默认按照**标签批次记录主键倒序**

## 标签批次历史列表

| **字段** | **说明** |
| --- | --- |
| 标签批次 | 当前标签批次号 |
| 订单编号 | 对应业务订单号 |
| 生成标签数量 | 当前批次生成数量 |
| 是否已打印 | 批次是否发生过打印 |
| 创建人 | 标签生成操作人 |
| 创建时间 | `yyyy-MM-dd` |
| 详细 | 打开标签清单 |
| 备注说明 | 批次备注 |

当前页面还提供**预打印**按钮。

因此该页面同时承担：**历史批次查询 + 新建预打印入口**两个职责。

## 标签打印日志列表

查询条件：

| **查询项** |
| --- |
| 标签批次 |
| 标签号 |
| 打印人 |
| 打印时间 |

列表：

| **字段** |
| --- |
| 标签批次 |
| 标签号 |
| 打印时间 |
| 打印IP |
| 打印人 |

默认排序：最近打印记录优先。

## 单标签打印历史

在预打印标签清单中，每一条标签都提供：

> **打印历史**

入口。

点击后进入该标签自己的打印日志。

查询条件：

- 打印人；

- 打印时间

列表继续展示：

- 标签批次；

- 标签号；

- 打印时间；

- 打印 IP；

- 打印人。

因此用户可以从：

> 批次 → 标签清单 → 单标签打印历史

逐层追查。

## 打印日志生成规则

预打印标签执行打印时，系统针对本次打印的每一条标签创建一条打印日志。

记录：

| **字段** | **当前规则** |
| --- | --- |
| 标签号 | 本次标签 |
| 标签批次 | 当前批次 |
| 打印时间 | 当前系统时间 |
| 打印IP | 当前客户端 IP |
| 打印人 | 当前登录用户姓名 |
| 打印次数 | 当前日志记录固定写 1 |

## 打印状态更新时机

**后台先更新“已打印”、累计次数和打印日志，再把数据返回浏览器执行本地打印。**

## 普通“标签打印”与“打印历史”的关系

**资产标签打印**页面打印现有资产标签时，主要做的是：更新标签打印数据中的累计打印次数

不同步创建：日志记录。

**标签预打印 → 标签清单 → 打印**

这条链路明确会创建打印日志。

**打印日志体系主要记录标签批次/预打印标签的打印行为；普通资产标签打印页面主要维护累计打印次数。**

## 三个功能的数据关系

最终三个功能关系可以整理为：

| **功能** | **处理的数据** | **是否生成标签号** | **是否记录打印日志** |
| --- | --- | --- | --- |
| 标签打印 | 已有资产标签打印数据 | N | 当前普通打印链路不生成详细日志 |
| 标签预打印 | 标签批次、标签流水 | **Y** | 打印时 Y |
| 打印历史 | 标签批次、标签明细打印日志 | N | 查询日志 |

#### 预打印标签

1. 预打印功能、资产接收功能，标签号生成规则相同，并且共用一个流水号，跨年不清零。

1. 系统支持标签预打印功能。用于维护生成标签号和标签打印模板信息来源。

1. “生成标签”按钮：点击生成标签按钮后，根据页面设置，自动调用【标签生成规则】生成标签明细，并将标签明细存入【标签清单】表里，然后显示“打印标签”按钮。

1. “打印标签”按钮：点击打印标签按钮后，根据当前页面保存的“标签批次”调出【标签清单】编辑页面。

1. 生成当前最大序号逻辑:

  1. 根据【规则类型】（1-常规标签、2-家具、3-手机、4-备件、5-低值耐用品）和【规则描述】（由帐套编码、资产类型编码、和年份的后两位组成，例如1011121是有帐套编码101和资产编码11以及年份2021年的后两位21组成），查询规则表（MMP_ASSETS_TAG_SEQ_RULE）的当前值最为当前最大序号，具体逻辑如下：

    1. 常规、低值耐用品：

      1. 规则存在：取当前值

      1. 规则不存在：尝试找前一年的规则，

        1. 如果找到了，若前一年规则已用完（currentValue >= maxValue），从新规则的最小值开始，若没用完，接着前一年的值继续编号。

        1. 如果找不到任何历史规则，直接从最小值开始编号。

    1. 家具、手机、备件：

      1. 规则存在：取当前值。

      1. 规则不存在：不查找历史规则，直接创建新规则，从最小值开始编号。

      1. **最大序号保留位数：**

| **规则类型** | **保留位数** |
| --- | --- |
| 常规 | 5位（例如：00001） |
| 家具 | 6位（例如：000001） |
| 手机 | 4位（例如：0001） |
| 备件 | 5位（例如：00001） |
| 低值耐用品 | 4位（例如：0001） |

1. 标签生成规则（以接收单生成标签号为例，需先判断是否适用特殊规则 - 家具或特殊规则 - 手机，若不适用，则遵循常规 / 低值耐用品标签规则）

  1. 常规标签规则

    1. 1112-PC.显示器编码规则：

      1. 标签号=选择帐套的编码＋11＋选择年份后两位+5位最大流水号+ V（例：123112000277-V）

    1. 正常标签号：

      1. 选择帐套的编码＋选择资产类型的编码+选择年份后两位+5位最大流水号（例：11216122300746）

    1. 先选标签里的内容，对应好编码和要选的资产类型，再确定一组流水号，这组流水号不用清零，继续往下用就行。

    1. 如果标签号长度超过15位，标签号去掉两位年份。

    1. 有部件数量，部件标签号=对应资产标签号+“-01”、“-02”依次排序（例：1231100278-V-01）。

    1. 主资产上有二维码，部件不生成卡片且无二维码。

  1. 低值耐用品标签规则（对应物料第一级分类编码为 “2”）

    1. 低值耐用品大类和小类对应关系:

| **低值耐用品大类** | **低值耐用品小类** |
| --- | --- |
| 电脑配件 | 内存 |
| 电脑配件 | 内置硬盘 |
| 电脑配件 | 板卡 |
| 电脑外设 | 移动硬盘 |
| 电脑外设 | 移动光驱 |
| 办公设备 | 会议白板 |
| 办公设备 | 验钞机 |
| 办公设备 | 考勤机 |
| 办公设备 | 碎纸机 |
| 家电（ES权限） | 微波炉 |
| 家电（ES权限） | 电扇\|空调扇 |
| 家电（ES权限） | 饮水机 |
| 家电（ES权限） | 咖啡机 |
| 家电（ES权限） | 电暖器 |
| 家电（ES权限） | 机顶盒 |
| 合约机 | 合约电话卡 |

    1. 若低值耐用品大类为合约机且低值耐用品小类为合约手机，标签号=P-+选择年份后两位+4位最大流水号（例：P-230003），这条规则确定一组流水号，不清零，不区分公司；

    1. 若低值耐用品大类为合约机且低值耐用品小类为合约电话卡，标签号=N-+选择年份后两位+4位最大流水号（例：N-230001），这条规则确定一组流水号，不清零，不区分公司；

    1. 否则，标签号=QT-+选择年份后两位+4位最大流水号（例：QT-214434），这条规则确定一组流水号，不清零，不区分公司

  1. 特殊规则-家具

    1. 标签号=6位流水号＋家具类型编码（例：617957A）

    1. 所有家具，确定一组流水号，不清零

    1. 家具类型：工位-A、柜子-D、桌子-E、椅子-Y、其他-Q

  1. 特殊规则-手机（对应物料第二级分类编码为“13”且对应物料第三级分类编码为〝004”）

    1. 标签号=NE+4位流水号（例：NE3722），固定NE

    1. 所有手机，确定一组流水号，不清零

  1. 特殊规则-备件

    1. 标签号=备件编码+5位流水号（例：CPU00007）

    1. 所有备件，确定一组流水号，不清零

    1. 备件类型：备件-CPU、板卡配件-Card、硬盘备件-HD、内存备件-ME、板块备件-SP

#### 标签打印

1. 动态获取所有资产卡片和耗材卡片，显示最新卡片信息，用于标签补打，默认显示所有卡片信息，可以根据已选择查询条件过滤点击查询后显示相应查询结果。

1. 支持页面打印标签。

1. “导出”按钮：导出当前页面查询出所有数据或者导出所选数据。

1. 标签生成时间以点击 “生成” 按钮的时刻为准。

#### 标签格式

资产标签

| 二维码 | 标签英文名 |
| --- | --- |
| 二维码 | 标签中文名 |
| 二维码 | 资产标签号 |

![图片 1](标签打印、打印历史、标签预打印+PRD_image1.jpeg)

部件标签

![图片 1](标签打印、打印历史、标签预打印+PRD_image2.jpeg)

## 标签打印

### 流程图

### 业务特殊说明

1. 功能说明：

  1. 动态获取所有资产卡片与耗材卡片，展示最新卡片信息，支持标签补打功能与批量打印功能。默认显示全部卡片信息，可通过选择查询条件进行筛选，点击 “查询” 后呈现对应查询结果。

1. 功能说明：预打印生成标签和接收时生成标签，都会生成一条标签清单数据。

1. 资产台账信息变更后，根据资产标签号同步字段信息内容到标签打印列表中。

1. 资产接收并完成机房资产标签标识后，系统将在标签打印模块自动生成对应记录。

## 标签打印历史

### 流程图

### 业务特殊说明

1. 功能说明：

  1. 标签打印生成资产编号后，预打印、打印、接收入库打印的标签记录会按照标签批次号统一存入该表，实现资产标签打印全流程的可追溯管理，便于资产溯源、流程审计与数据统计。
