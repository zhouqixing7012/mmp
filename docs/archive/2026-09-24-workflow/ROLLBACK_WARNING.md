# 2026-06-24/25 yewurules.js 大规模改动 — 防止再次回滚

> 这次会话对整个 src/pages/yewurules.js 做了大量改动，中途因另一个对话误操作被回滚。
> 记录下所有改动的完整清单，方便重新恢复。

## 改动清单（会话全量）

### 已存活（当前 git diff 中仍存在）
1. DatePicker 加入 antd import
2. formData 新增 enableDate、stopDate 等字段（handleAdd 同步）
3. 维度组合描述改为自动生成：品牌.型号名称，输入框只读
4. 参考价格字段加入核心字段（单位右侧）
5. 启用日期/停用日期加入状态字段（DatePicker）

### 已丢失（被另一对话回滚 — 需重新应用）
6. 物料大类+物料小类 → 合并为"物料大类/小类"（点击打开物料小类弹窗）
7. 品牌+规格型号 → 合并为"品牌/规格型号"（点击打开型号弹窗）
8. 权限控制字段重构：正式员工可申请改为"是/否/临时可申请"单选按钮
9. 临时可申请时展示：生效时间(RangePicker)、可申请部门弹窗、可申请员工弹窗
10. 实习生可申请：正式员工可申请 != 临时可申请时展示，否则隐藏
11. 业务规则字段+权限控制字段合并为同一"业务规则字段"区块
12. 删除"级别"和"非技术申请超标"字段
13. 仓库信息新增弹窗样式统一（去除工具栏按钮、增加保存/返回底部按钮）
14. 仓库类型枚举值改为：ES库、MIS库、外地站、机房库
15. 启用日期/停用日期移入仓库弹窗是否启用字段后
16. 地点基础数据维护页面隐藏操作栏（新增/删除/启用/停用按钮）
17. 新增 isSubCategoryModalOpen/isDeptModalOpen/isEmployeeModalOpen 三个状态
18. 新增三个 SelectModal（物料小类选择、部门选择、员工选择）
19. 业务规则区块重构为数据驱动方式：renderFieldRows + businessRuleFields
20. 两个选项的下拉框改为单选按钮样式
21. 移除了资产/耗材低值耐用品专属字段区块

### 重要设计决定
- 业务规则字段改用数据驱动：字段配置为数组（key/label/required/visible/content），先 filter 再排列
- 所有 Select 只有两个选项的改成 Radio.Group 单选按钮
- 维度组合自动生成格式：品牌.型号名称
- 仓库弹窗放弃独立样式，统一使用 min-h-[40px]/p-2 标准
