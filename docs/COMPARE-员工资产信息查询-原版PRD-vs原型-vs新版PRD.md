# 员工资产信息查询三方比对：原版 PRD vs 当前原型 vs 新版 PRD

## 1. 比对基线

本次固定使用以下三个基线：

- 原版 PRD：`docs/员工资产信息查询+原版PRD.md`
- 当前原型：`src/pages/assetManagement/EmployeeAssetInfoQueryPage.js`
- 新版 PRD：`docs/PRD-资产管理-员工资产信息查询.md`

处理原则：

1. 原版 PRD 只作为历史业务基线保存，不直接覆盖修改。
2. 当前原型用于确认页面字段、控件、页签、查询和演示行为。
3. 新版 PRD 承载当前目标业务规则。
4. 原版与原型冲突时，先判断是原型偏差还是后续明确改造，不用原型反向覆盖原版业务规则。
5. 原版没有定义、原型自行出现的字段或规则，不自动写入新版 PRD。
6. 新版 PRD 对原版未定义的规则明确标记“当前未定义”。

## 2. 当前三方总体状态

本轮已根据三方比对结果完成原型校准，当前原型已与原版及新版 PRD 的已明确规则对齐：

1. 员工信息改为人员单选选择器。
2. 查询条件按资产 / 耗材 / 合约号码 / 单据信息页签动态展示。
3. 资产只按“资产责任人 = 查询员工”查询。
4. 耗材只按“耗材当前责任人 = 查询员工”查询。
5. 合约号码只按“合约号码当前责任人 = 查询员工”且当前有效关系查询。
6. 单据信息支持申请人、责任人、`payPersonId`、入库/出库明细关联人员等多种员工关系。
7. 耗材和合约号码列表字段已按原版 PRD 调整。
8. 原型中无需求依据的“资产担保人 / 担保来源单据”已删除。
9. 资产标签号、耗材标签号、合约号码、申请单号、核心单据已提供详情入口。
10. 单据状态已补齐“已完成 / 处理中 / 已驳回”。
11. 单据信息按创建时间倒序。
12. 四个页签分别使用对应空状态文案。

仍未由原版需求明确的规则继续保持“当前未定义”，不由原型自行推断。

## 3. 页面与查询条件三方比对

| 主题 | 原版 PRD | 当前原型 | 新版 PRD | 当前结论 |
| --- | --- | --- | --- | --- |
| 默认页签 | 资产 | 资产 | 资产 | 一致 |
| 页签 | 资产 / 耗材 / 合约号码 / 单据信息 | 已实现四页签 | 明确四页签 | 一致 |
| 员工信息 | 人员选择器；单选；支持编号、姓名、部门 | 已改为 SelectModal 单选；支持编号、姓名、部门 | 同原版 | 一致 |
| 员工回显 | 员工编号 - 员工姓名 | 已按该格式回显 | 同原版 | 一致 |
| 查询条件按页签变化 | 是 | 已按页签条件渲染 | 是 | 一致 |
| 资产标签号 | 资产、耗材 | 仅资产、耗材展示 | 资产、耗材 | 一致 |
| 合约号码 | 仅合约号码 | 已补合约号码查询条件 | 仅合约号码 | 一致 |
| 单据编号 | 仅单据信息 | 仅单据信息展示 | 仅单据信息 | 一致 |
| 单据状态 | 单据信息；已完成/处理中/已驳回 | 已补三种状态 | 同原版 | 一致 |
| 切换页签保留员工 | 是 | 已保留当前查询员工 | 是 | 一致 |
| 返回页签保留辅助条件 | 是 | 每个页签独立保存查询条件 | 是 | 一致 |
| 切换页签自动加载 | 是 | 使用当前员工和该页签已保存条件刷新结果 | 是 | 一致 |

## 4. 资产页签三方比对

### 4.1 员工归属

三方当前统一：

> 资产责任人 = 查询员工

当前原型不再使用“责任人 OR 单据申请人”判断资产归属。

原型中保留一条“申请人为当前员工但责任人为其他员工”的对抗性 Mock，用于确认该资产不会错误出现在当前员工资产列表中。

### 4.2 数据范围

| 规则 | 原版 PRD | 当前原型 | 新版 PRD |
| --- | --- | --- | --- |
| 资产状态 | 状态代码 3、6 | 使用 statusCode 3、6 过滤；3=在用-借用中，6=在用-使用中 | 3=在用-借用中，6=在用-使用中 |
| 物资大类 | 资产 | 使用 materialType=资产过滤 | 物资大类=资产 |

资产状态代码已确认：`3 = 在用-借用中`，`6 = 在用-使用中`。

### 4.3 列表字段

当前原型字段已调整为：

- 行号
- 资产标签号
- 资产说明
- 资产状态
- 资产责任人
- 单据申请人
- 所在单据编号
- 单据类型
- 单据业务类型
- 单据状态
- 单据审批环节
- 单据审批人
- 公司
- 板块

原型额外的“资产担保人”已删除。

### 4.4 详情入口

当前原型：

1. 资产标签号使用蓝色链接按钮打开资产卡片详情。
2. 所在单据编号存在值时可打开对应业务单据详情。
3. 无关联单据时展示 `-`，不提供空链接。

## 5. 耗材页签三方比对

### 5.1 员工归属

三方当前统一：

> 耗材当前责任人 = 查询员工

当前原型不再使用单据申请人参与耗材当前归属判断。

### 5.2 列表字段

当前原型已按原版 / 新版 PRD 调整为：

- 行号
- 耗材标签号
- 公司
- 板块
- 资产大类
- 资产小类
- 资产说明
- 品牌
- 主资产标签号
- 数量
- 原值
- 净值
- 责任人编号
- 责任人
- 状态
- 成本中心
- 仓库
- 启用日期

原型中原先存在但无原版依据的：

- 资产担保人
- 担保来源单据
- 单据申请人
- 所在单据编号
- 单据类型
- 单据业务类型
- 单据状态
- 单据审批环节
- 单据审批人

已从耗材页签移除。

### 5.3 Mock 数据

当前耗材演示数据的物料说明、分类、品牌取自项目物料参考源已有真实记录；仓库使用项目仓库参考源中的启用耗材仓。

标签号、责任人、数量、启用日期等属于业务演示交易数据。

### 5.4 详情入口

耗材标签号已改为蓝色链接按钮，可打开耗材详情。

## 6. 合约号码页签三方比对

### 6.1 查询条件

三方当前统一：

> 合约号码页签使用“员工信息 + 合约号码”查询。

不再展示资产标签号、单据编号、单据状态。

### 6.2 员工归属

三方当前统一：

> 合约号码当前责任人 = 查询员工

当前有效合约号码已确认只取状态为：

> `在用-使用中`

的记录；当前原型已按该状态过滤。

### 6.3 列表字段

当前原型已按原版 / 新版 PRD 调整为：

- 行号
- 合约号码
- 副卡
- 状态
- 责任人编号
- 责任人
- 公司
- 部门
- 领用时间
- 备注

原型原先的合约号码标签号、合约号码说明和流程单据字段已从该页签移除。

### 6.4 详情入口

合约号码使用蓝色链接按钮，可打开合约号码详情。

## 7. 单据信息页签三方比对

### 7.1 员工关系

原版 / 新版允许通过以下关系查询员工相关单据：

- `payPersonId`
- 责任人员工字段
- 申请人员工字段
- 入库明细关联人员
- 出库明细关联人员

当前原型已按以上关系建立统一员工归属判断，不再只按申请人查询。

### 7.2 业务类型

原版 / 新版明确可覆盖：

- 更换
- 退库
- 员工转移
- 借用
- 统一申请相关
- 合约号码相关
- 新员工相关
- 出库
- 入库
- 资产转移

当前原型使用多种 Mock 单据验证“申请人 / 责任人 / payPersonId / 明细人员”不同命中路径，正式业务数据仍需由各业务模块提供。

### 7.3 列表字段

当前原型字段与原版 / 新版一致：

- 行号
- 申请单号
- 单据类型
- 业务类型
- 单据状态
- 申请人
- 申请时间
- 核心单据
- 公司
- 板块
- 部门
- 操作

### 7.4 单据状态

三方当前统一：

- 已完成
- 处理中
- 已驳回

### 7.5 详情与流程入口

当前原型：

1. 申请单号可进入业务单据详情。
2. 核心单据存在时可进入对应业务单据详情。
3. 操作列提供“查看流程”。
4. 无核心单据时展示 `-`。

### 7.6 排序

员工单据信息当前统一按：

> 申请时间降序。

## 8. 默认排序规则

资产、耗材、合约号码三个页签当前统一使用以下默认排序：

1. 先按所在单据编号字段 `sp_po_putin_num` 降序。
2. `sp_po_putin_num` 为空的记录排在非空记录之后。
3. 相同 `sp_po_putin_num` 下按标签号字段 `tag_number` 升序。
4. `sp_po_putin_num` / `tag_number` 可以作为排序数据字段使用，不要求因为参与排序就在列表中新增展示列。

员工单据信息按申请时间降序。

当前原型已按以上规则实现。

## 9. 空状态与异常

| 场景 | 原版 PRD | 当前原型 | 新版 PRD | 当前结论 |
| --- | --- | --- | --- | --- |
| 无资产 | 暂无资产信息。 | 已使用该文案 | 同原版 | 一致 |
| 无耗材 | 暂无耗材信息。 | 已使用该文案 | 同原版 | 一致 |
| 无合约号码 | 暂无合约号码信息。 | 已使用该文案 | 同原版 | 一致 |
| 无单据 | 暂无单据信息。 | 已使用该文案 | 同原版 | 一致 |
| 员工不存在 | 未查询到对应员工信息。 | 原型通过人员选择器只允许选择候选员工，不模拟无效工号输入 | 保留正式规则 | 原型无需额外伪造不存在员工场景 |
| 离职员工 | 原版未明确 | 原型人员选择器可继续纳入离职员工候选 | 允许查询、选择并查询其关联数据 | 已确认允许查询 |

## 10. 权限与历史单据

### 10.1 权限

最终口径：

> 只有拥有“资产管理 → 员工资产信息查询”菜单权限的后台用户可以访问。

本功能暂不增加公司、板块、部门等额外数据权限限制；拥有菜单权限的用户可以查询全部员工，包括离职员工。

### 10.2 历史单据旧系统详情

原版要求：

> 切换新系统后，历史单据详情页保留旧系统地址。

最终口径已确认：

> 历史单据跳转至旧系统对应单据详情页。

PRD 不维护具体旧系统 URL；当前原型不自行编造旧系统地址。

## 11. 当前仍需业务确认的规则

本轮三方比对涉及的核心查询、状态范围、排序、权限、离职员工查询及历史单据跳转规则均已确认并同步到新版 PRD / 当前原型。
