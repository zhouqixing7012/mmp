# Lessons Learned

## 通用页面规范

- 用户未明确指定旁分支时，以当前任务明确目标分支为准（本轮为 `main`）；不能因最近提及或正在查看的分支自动将改造写入旁分支。

- 个人工作台业务页保持单层画布；页面标题不使用蓝色竖块，业务 Card 标题才使用蓝色竖块。
- 只读字段直接展示文本或状态，不用 readOnly/disabled 输入框模拟只读；真实可编辑字段才保留输入控件。
- `DetailGrid` 的 `span={3}` 整行字段必须从完整新行开始，不能接在只填满一到两列的普通字段后，否则会出现标签和值串行。
- 用户明确给出字段集合时，页面只展示该集合，不保留“方便查看”的额外字段。
- 用户纠正“哪些页面不要字段”时，只按明确页面移除，不把列表查询/列表字段等其他位置一起删掉。当前资产接收维护页、资产接收详情、低值耐用品维护页不展示制单人/制单时间，但接收单列表仍可保留制单信息用于查询和追溯。
- 表格行【编辑】若业务上是“对原新增/选择结果继续维护”，必须复用原添加/选择弹窗及完整字段，只带入当前行已保存值；不要另做字段缩水版编辑弹窗。
- 审批处理页和状态查看弹窗必须职责分离：审批处理页展示业务审批所需字段和审批操作；列表单据状态入口只展示审批记录，不再重复塞入单据详情或物资明细。
- 业务 Modal 内再打开 SelectModal 时，不得把 SelectModal 挂在会因 `open=false + destroyOnHidden` 被卸载的父 Modal 内容树中；采用与入库/出库一致的同级渲染结构，由选择状态控制父 Modal 暂时隐藏、选择 Modal 独立打开。
- 员工/虚拟员工选择器的列必须按业务给出的展示口径实现。新增入库责任人选择固定为员工（员工工号.姓名）+公司（公司编码.名称）两列，所在部门统一为 SOHU0001.虚拟组织。
- 当仓库决定业务数据范围时，应先选择仓库，再根据仓库所属公司硬过滤候选数据，不能反过来根据第一条物资自动猜仓库。手工采购接收入库按此规则执行；系统自动生成采购草稿仍保留来源仓库。
- 项目已有资产台账 Mock 和正式枚举时，库存流程应直接复用，不再新造“借出”“办公”等不在正式字典中的状态或用途。借用归还候选固定为“在用-借用中”，用途使用部门公用/员工用机/其他用途/专业用途，资产标记使用硬件老化/组件缺失/设备故障/物理损伤。
- 用户明确后续会提供 Excel 模板时，只先建立真实文件选择入口和流程占位，不伪造模板字段或随机生成“导入成功”业务数据。
- 字段“可编辑”不等于必须使用普通 Input：当业务对象来自人员、资产、供应商等基础数据时，应使用统一选择弹窗；条件可编辑字段要按业务条件切换选择/只读状态。新增入库的原资产仅“报废新增”可选择，责任人仅允许虚拟库管员。
- 有派生关系的日期字段应在源日期变化时重新计算默认值，但派生字段允许独立调整时不得反向改写源字段。新增入库购买日期 1～25 日对应同日启用，26 日至月末对应次月 1 日启用。
- 组合编码字段必须由各段编码生成，不能把中文描述拼成业务编码。新增入库费用账户固定按公司、板块、成本中心、科目、子目、业务线、项目、往来公司、备用段九段编码组合。
- 同一公共展示组件被多个业务页面复用，但用户确认的字段集合已经不同后，必须显式拆分展示模式，不能为了复用继续让两类页面共用完全相同字段。当前耗材接收单详情固定 15 项单头字段，低值耐用品逐件维护页在此基础上额外展示制单人、制单时间。
- 资产接收的说明字段统一使用“资产说明”，不要在接收单详情、逐件维护表格或扫描核对区混用“物料说明 / 物资说明”。
- 表格“操作”列存在多个独立业务入口时，按钮文案必须分别使用真实动作名，不能根据状态把多个入口合并成一个动态按钮。
- 业务列表 Card 默认在右上角展示当前筛选结果数量“共 X 条”；即使右上角同时存在新增、删除等分区操作，也应让数量与操作并列展示，不因有按钮而省略结果数量。
- 详情/只读 Modal 默认沿用项目统一的 **3 列 DetailGrid**；只有用户明确提出业务或视觉上的特殊列数时才做例外，不能仅因为历史截图信息密度较高就单独保留 4 列布局。
- 弹窗整体密度偏紧凑：普通业务 Modal 的 Header / Footer 统一使用 `12px 20px`，Body 使用 `16px 20px`；不再采用 24px 通用内容边距，避免标题区、内容区、Footer 之间竖向留白过大，也避免左右页边距过宽。
- 业务 Modal 不应通过 1120/1200px 或 94vw 解决宽表格问题；普通弹窗默认最大宽度为 **960px**。四列表单型弹窗可例外放宽至 **1000px**，但必须通过 DetailGrid 栅格本身适配，表单区不使用横向滚动。选择型弹窗统一复用 `SelectModal`：QueryBar 按自身实际可用宽度自动采用 1 / 2 / 3 列（<504px=1列，504～767px=2列，≥768px=3列）；Table 是否横向滚动按各列合理最小宽度之和是否超过可用宽度决定，统一优先使用 `scroll={{ x: 'max-content' }}`，不按列数判断；Footer 默认右对齐，顺序为取消、确定。
- 蓝色编号、标签号、单据号等 link Button 除了承担点击下钻入口，还必须支持用户用光标拖选和复制文本，不能因为做成按钮而丢失基础文本复制能力。
- 依赖某张表格当前勾选结果才能执行的动作，应优先放在该表格所属 Card 的操作区，与选择对象保持同一上下文；不要把这类动作放到页面底部作为全局动作。资产接收 PO 详情的“创建接收单”和机房资产“接收确认”都放在“PO物资明细”Card 右上角。
- 资产接收 PO 列表的操作列固定同时展示 **接收 / 查看**：PO 单号与“接收”都进入 PO 详情；“查看”进入该 PO 的接收单列表，不能根据 PO 接收状态改变 PO 单号的去向。
- 资产接收 PO 详情按采购类型控制主动作：**电子设备使用“创建接收单”**；**服务器 / 服务器备件 / 网络设备 / 网络设备备件使用“接收确认”**。机房资产“接收确认”必须作用于当前勾选 PO 物资行，并一次生成 **已完成接收单 + 草稿采购接收入库单**，不能再沿用“执行入库”文案或直接把入库单做成已完成。
- 资产接收接收单状态固定为 **草稿 / 已完成**。创建电子设备草稿接收单后，当前接收数量必须立即计入 PO 的“草稿数量”，可接收数量固定按 `采购数量 - 已接收数量 - 草稿数量` 计算；删除接收行、删除逐件拆分明细、删除草稿接收单、取消接收都必须同步释放草稿占用。
- 资产接收 PO 状态必须按整张 PO 的累计数量判断：部分接收继续保持 **待接收**，全部 PO 行累计接收完成后才是 **已接收**，全部后续入库完成后才是 **已入库**；不能因为执行过一次接收确认就直接把整张 PO 改成已接收。
- 资产接收“取消接收”固定表示 **作废当前草稿接收单并释放该单全部 PO 草稿占用数量**；接收单本身不因此新增“已取消/已作废”第三种业务状态。
- 资产接收逐件维护中，一条拆分明细代表 1 台资产；删除一条拆分明细时，必须同步让来源接收行“本次接收数量”减 1，并释放 1 个 PO 草稿占用数量，不能只删展示行。
- 资产接收的“维护SN号”只给 **SN 为空**的明细写“缺省”，已有真实 SN 不得被批量覆盖；`缺省`算已维护但不参与唯一性冲突，其他真实 SN 必须唯一，并在扫描写入、明细编辑保存和接收确认时校验重复。
- 资产接收必须区分制单与接收两个时间点：**制单人/制单时间**取草稿创建时，**接收人/接收时间**取接收确认成功时；草稿阶段接收人/接收时间为空。申请批次为选填，但有值时必须随接收单继续传入采购接收入库单及入库物资明细；板块同样随链路传递。
- 资产接收与耗材接收的 PO 行“编辑接收信息”只统一共性字段与接收数量校验，不强求字段完全一致：耗材/低值耐用品接收链路不再维护任何部件字段；资产接收仍保留部件能力。
- PO 行编辑需拆成两个判断：①【编辑】是否展示只看剩余可接收数量（资产还要求待接收状态），剩余为 0 时不因草稿引用额外展示；②物料、配置及资产部件信息是否可改只看当前行是否被草稿接收单引用，已完成接收记录不作为字段锁定条件。草稿引用期间仍可调整本次接收数量并同步草稿占用。
- 耗材板块默认值来自采购订单使用部门：按“部门→成本中心→板块”规则自动带出；库管在创建接收单前允许人工调整，人工调整值优先并在后续分批接收中沿用，重新进入 PO 不得被自动计算覆盖。
- 当用户要求操作“常显但不可用时置灰”时，必须稳定渲染按钮并通过 disabled 表达状态，不能继续通过条件渲染隐藏。当前电子设备与低值耐用品维护页的删除行、生成标签号、维护SN号、打印标签号均按此规则。
- 耗材/低值耐用品链路移除某业务概念时，要从 UI、Mock、快照、逐件明细、标签/SN逻辑和下游入库数据一起清理，不能只删除表单字段。
- Card 级上下文选择项若用户明确要求放在标题区，例如入库单“当前仓库”，应放到 Card extra；不要同时在 DetailGrid 中保留重复字段。
- 耗材接收采购类型固定为 **低值耐用品 / 耗材**，不得再使用“低耗耗材”作为采购类型名称。
- 耗材接收单列表若“查看”与接收单号跳转完全重复，则不保留操作列；制单时间统一使用单个日期范围查询条件。
- 耗材接收必须区分 **低值耐用品 / 普通耗材**：删除接收行始终是勾选接收行后的行级动作；低值耐用品的“维护接收明细”是页面级动作，覆盖整张接收单并按本次接收数量逐件拆行；普通耗材仍按选中的接收行维护，不能为了共用页面把两者强行统一成同一种粒度。
- 低值耐用品接收单状态只保留 **草稿 / 已完成**。草稿期生成标签号、维护缺省 SN、删除明细、扫码写入 SN 都实时保存；生成标签号后才能扫码，按“标签号 → SN号”两步处理；接收确认前每条逐件明细都必须有标签号和 SN，`缺省`算已维护、空值不算，确认后生成采购接收入库单并变为已完成。普通耗材继续按数量型接收，不要求逐件标签号/SN。
- 耗材接收明细字段不展示“资产标记”；字段口径应按耗材业务本身确认，不因为低值耐用品复用资产式逐件维护就把资产专属展示字段一并带入。
- 耗材接收详情不展示后台自动入库结果区块；即使接收确认已经生成入库单，也只更新业务状态，不额外在详情页堆一块自动入库结果。
- 入库单列表的制单日期使用单个日期范围查询条件；操作列按单据状态显示真实动作：**草稿=编辑，已完成=打印**。
- 入库草稿编辑页必须有页面级 **执行入库**；Excel 导入只对非采购接收入库展示，采购接收通过“待入库物资”选择已有接收资产。机房资产 PO “接收确认”自动生成的采购接收入库单也必须以**草稿**进入入库列表，继续沿用同一套草稿编辑/执行入库流程。
- 原型列表已经提供可点击详情入口时，Mock 不能只造单据头：必须同步给详情所依赖的行数据，并覆盖每个主要业务分支。当前入库至少要保证 **新增入库 / 采购接收 / 退库入库 / 借用归还** 四类都有可点击且非空的详情演示数据。
- 已完成单据进入详情后应切换成真正的查看态：所有不可编辑字段直接使用文本/状态展示，不能继续保留灰色 disabled Select/Input。当前入库的“入库类型 / 是否计费 / 当前仓库”都按此处理；物资表中的关键标识“资产标签号”使用蓝色链接作为下钻入口，点击打开只读物资详情。
- 只读物资详情如果“选择物资”区域与后续“物资信息”重复，不保留选择区；选择区只服务新增/编辑动作。详情字段默认与对应维护场景保持业务口径一致，但用户提供独立历史详情字段集时，以该已确认字段集为准。当前采购接收入库只读详情不再单独展示“NO设备信息”：删除品牌、规格型号、库区、货位、级别、合同编号、小服务、Cabinet、ILO、ILOPW、IP1、IP2、IP3，保留的 NO位置紧跟 Floor、服务紧跟费用账户并并入“物资信息”。
- 同一业务对象同时存在新增/编辑弹窗和只读详情弹窗时，用户确认的字段顺序、字段名称和 `span` 必须同步调整；除非用户明确要求两种状态采用不同字段口径，不能只改当前看到的一处。
- 只读详情中的长标签（如“原资产标签号 / 主资产标签号”）必须预留足够标签宽度完整展示，不能为了压缩栅格把字段名截断。
- 纯查看型 Modal 如果没有确认、保存等业务动作，只保留右上角 X 即可，不再额外放 footer“关闭”按钮形成两个等价关闭入口；新增、编辑、确认类 Modal 继续保留业务 footer。
- 新建出库单的“出库类型”当前固定默认 **领用出库**并只读；历史已有出库单继续按原出库类型只读展示，不能因为新建默认值覆盖借用出库历史单据。
- 列表所在菜单已经明确业务对象时，主创建按钮可使用简短“创建”，避免重复菜单名；当前“库存管理 → 转移”列表按钮统一为“创建”，不再写“创建转移单”。
- 扫码枪输入如果本质只是查询字段的快速录入方式，就放进统一 QueryBar 并支持回车查询，不单独做扫描 Card；多选弹窗需要持续确认选择集合时，优先使用“左侧结果列表 + 右侧已选择标签号”布局，右侧删除与左侧勾选同步。
- “资产扫描”必须按所在业务语义区分：移库发起列表/接收列表中的扫描属于查询条件，使用 QueryBar；创建/编辑移库单中的扫描属于快捷添加动作，回车直接添加；移库接收详情中的扫描属于快捷验证动作，按资产标签号回车直接完成验证。快捷动作均不使用【查询 / 重置】，也不通过扫描去筛选下方明细。
- 移库列表中如果移库单号已能进入详情，则不再保留重复“操作”列；进入接收详情后也不继续展示仅用于列表切换的“发起单据 / 接收单据”页签。
- 移库接收确认和移库驳回属于行级动作，必须作用于当前勾选物资；不能因为单据只有一条演示数据就实现成整单直接确认或驳回。
- 物资说明确定后可唯一带出的物资主数据字段应只读展示；仓库、责任人等上游字段带出的结果再按业务是否允许覆盖决定只读或可编辑，不能为了“字段都有输入框”把衍生字段全部做成可编辑。
- 当用户给出的字段名与当前业务对象或上下文语义明显冲突、疑似手误，或存在两个都合理的解释时，不要机械照改：如果上下文已能唯一判断预期口径，应先提醒用户再按一致语义处理；如果仍不能唯一判断，再向用户确认后修改。若同一句指令把两个原本不同的业务字段改成同一个名称、且与上一轮刚确认的字段集合冲突，也按疑似手误处理，先提醒并优先沿用已确认的唯一一致口径。
- 删除字段后先判断下一行字段是否适合自然上移：普通短字段可以补位，长文本/整行字段保持原行；不允许为了填满一行擅自拉宽相邻字段。确需保留空位时使用真正的空结构单元，不使用 `\u00a0`、空格文本等占位字符，避免页面显示转义文本。
- Modal 内使用 Table 时，提交前必须核算**选择列 / 固定列 / 普通列**的总宽度与 Modal 实际内容宽度；总列宽超过内容区时，必须配置与列宽匹配的 `scroll.x`，或明确调整 Modal/列宽，并检查最右列背景、边框完整，禁止让末列直接溢出内容区造成背景透明或裁切异常。
- 表格单元格内的 `Input / InputNumber / Select` 等编辑控件必须受单元格可用宽度约束；尤其 `InputNumber` 不要沿用组件默认固定宽度，应显式 `width: 100%` 并允许收缩，避免窗口变窄后控件跨出单元格。
- Modal 内只有 1 个 QueryBar 查询条件时，不能继续沿用默认三列宽度把输入区压窄；应让该条件占满查询区域的可用宽度，并核对标签、输入内容和 placeholder 均完整可见。
- 库存管理权限统一采用“公司/板块控制数据范围 + 仓库权限控制操作”；工作台属于明确例外，当前固定开放给 `114111-杨芊`、`213852-孙志强`。
- 员工领用自动出库追加“员工领用信息”，员工退库自动入库追加本次“员工退库确认信息”；借用出库、借用归还不追加操作附页，继续使用各自打印单。
- 移库当前只支持资产和低值耐用品逐件处理，不支持普通耗材数量型移库；移动端仍属于移库主 PRD范围，通知和7天超期每日催办属于正式能力。
- 员工前台申请形成的转移单，制单人展示申请人；转移明细统一导出 Excel `.xlsx`，不使用 CSV。
- 员工确认全面取消电子签；刷卡/员工工号和扫码都必须校验到真实申请人。
- 辅助查看入口如果用户强调既要轻量又要看得出能点，优先使用 **明确图标 + link Button**。本项目“查看名下资产”当前统一为眼睛图标 + 蓝色 link Button，并继续打开原有弹窗。
- 公共详情组件中的字段不代表所有页面都必须展示；业务审批、资产配给明确不需要员工状态时，应通过页面参数隐藏，避免为了复用组件而牺牲页面字段口径。
- 普通业务页面里的“公司”字段使用日常公司展示口径，按 `编码.公司名` 展示，例如 `114.新媒体`；**集团 / 媒体 / 视频 / 焦点是子公司/组织层级，不是普通“公司”字段值**。只有号码控制等明确展示子公司层级的组织路径时，才以集团/媒体/视频/焦点作为路径起点。
- 所有业务页面的“申请人信息”默认字段集合和顺序固定为：**申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门**。默认不展示板块、退库类型、员工状态等身份扩展字段；但业务流程已经明确需要的业务原因字段（如退库原因、申请原因）仍应保留，放在默认字段之后并优先独占完整新行，不能因为“默认 7 字段”而误删。
- 业务页面及用户信息里的“联系电话”Mock 统一使用座机号格式（如 `010-xxxxxxxx`），不要使用 11 位手机号或手机号掩码；合约号码、电话号码等号码资产字段属于业务资源本身，不受该规则影响。
- 用户在本项目迭代语境下说“给我一个展示的页面”时，默认理解为**直接在现有项目对应页面中构造可见演示态**，不要另起独立 HTML；只有用户明确要求独立文件时才单独产出 HTML。

## UI 重画边界

- 用户要求“按新 UI 设计规范重画”且强调“功能不变”时，优先只调整页面画布、标题、Card、详情栅格、表格密度、状态展示、按钮视觉和弹窗排版。
- 不顺手改字段业务含义、必填规则、资格校验、按钮触发条件、状态机节点、service 调用、数据拆单规则或原型持久化语义；如果某项旧逻辑本身不符合设计规范但用户明确要求功能不变，应先保留逻辑，再单独提请用户决定是否改业务。
- 对一组历史页面做统一重画时，可以在 `WorkspaceContent` 下使用作用域明确的公共样式消除重复画布和旧页头，但要排除工作台首页、移动端等有意独立设计的页面，避免全局样式误伤。

## 附件组件偏好

- 用户后续说“用附件组件”时，默认复用当前 `ReturnAttachmentCard` 的信息结构和视觉：Card 标题为“附件信息”，**上传附件按钮放在 Card 右上角**，下方使用 small bordered Table 展示 **附件名称 / 附件大小 / 操作**。
- 附件删除仍按节点和上传人控制权限，危险删除使用二次确认；不因为复用视觉组件而削弱底层权限校验。
- “喜欢这个附件组件”不代表所有页面都必须展示附件；如果用户明确要求某页面删除附件信息，应整块移除该页面的附件 Card、上传入口及仅为该组件服务的页面逻辑，同时保留该组件作为后续其他页面的标准附件样式。
- 同一条原则也适用于退库审批：用户明确删除附件信息后，应同时清掉审批页上传/删除回调和附件组件引用，不留不可达 UI 代码。

## 个人工作台资源列表

- 工作台首页头部除了问候语，还应承担轻量概览职责：显示当前日期、星期，以及“名下资产与耗材数量 + 待办数量”的摘要，避免用户进入页面后还要额外寻找当天和待办信息。
- 当用户给了明确 Banner 参考图时，结构层级优先按参考图还原：日期/星期在最上，主问候语居中层级最大，资源与待办摘要放底部，快捷操作固定在右侧；不要只保留信息内容而忽略层级关系。
- 概览文案不要擅自增加业务状态判断；用户只要求“名下共有 X 项资产与耗材”时，不额外补“运行正常”等未要求状态描述。
- 资产操作退库/转移/更换按物料编码配置规则控制；借用资产只展示退库。
- 批量操作要基于所选资产共同支持的动作，并把资产标签号真正带入目标新增单据。
- 资产信息主层级为品牌.型号，次层级为大类-小类；资产用途独立展示。
- 耗材页签沿用相同信息层级，但字段语义独立：耗材说明/耗材小类/耗材标签号/主资产标签号/主资产说明。
- 合约号码业务字段可以只有合约号码/合约号码说明/操作，但复选框属于选择交互，可以继续支持批量退库。

## 审批与配给

- “ES建议”和“审批意见”是两个不同语义字段：ES建议属于配给处理信息，审批意见属于审批操作；用户要求审批操作可填写意见时，应新增独立输入，不要把 ES 建议改名或挪用。
- 审批区域标题应使用用户确认后的业务名称；本项目资产申请审批统一使用“审批操作”，不再出现“当前审批操作”。
- 统一申请汇总提交后进入的“统一申请汇总审批”仍是审批页，底部必须保留 **同意 / 驳回 / 返回**；不能因为页面主体是只读展示就误判成纯详情页并删掉审批动作。

## 资产转移审批

- 不能仅因为截图展示了完整审批历史就把页面判断为“详情页”；只要该节点仍有审批意见和审批操作，就应按审批页处理并保留对应操作按钮。
- 当前个人工作台下三套转移页面均为审批页：**资产转移审批 / 转出部门经理审批 / 接收部门经理审批**，审批操作统一为 **同意 / 驳回 / 返回**。
- 用户要求从一组同构页面删除同一字段时，应同时删除共享数据模型中的演示字段和公共表格列，避免只隐藏列但仍残留无用数据。当前三页“转移资产信息”均不展示“转移备注说明”。

## ES前台领用

- 申请人信息只保留当前办理需要核对和维护的字段；用户明确删除“单据备注”时，应同时移除该页面 Form 初始值和输入区域，不留不可见表单字段。
- 申请资产信息里的上游申请字段要按业务语义排序：“申请物资说明”后紧跟“申请配置”；“详细说明”在该办理节点使用业务名称“申请原因”，并单独占一整行，避免和其他短字段挤在同一行。

## 耗材领用方案

- 耗材领用方案一和方案二虽然共用同一页面组件，但字段口径可以不同；用户只修改其中一个方案时，应通过 variant 条件控制，不能为了方便把另一方案一起改掉。
- 方案一申请人信息不展示单据备注；申请耗材信息不展示序列号、使用用途。方案一、方案二的“备注”均为只读文本（无值显示 `-`），只有“使用说明”继续使用单行 Input。
- 方案二也不展示“使用用途”，删除该字段时必须同步删除相关 state、枚举和提交必填校验，不能留下页面不可见但仍拦截提交的逻辑。
- 方案一的“物料编码”和方案二的“耗材标签号”都是只读上游结果，直接展示文本，不提供可编辑 Input，也不做用户输入必填校验。
- 方案一“申请参考信息”字段名按业务语义固定为：申请耗材说明 / 申请用途 / 申请原因；不要沿用旧的“申请原因 / 详细说明”命名。
- 方案一、方案二的地理位置字段标签统一使用 `city / building / floor`，不再显示“城市 / 建筑 / 楼层”。
- 两个方案允许字段口径不同，但同属同一领用办理节点时，审批操作应保持一致；当前两者统一为：**领用确认 / 弃领 / 加签 / 返回 / 发送领用通知**，不要因 variant 分支造成操作集合漂移。

## 号码控制

- “所属部门”的长部门从子公司层开始展示，子公司口径固定为 **集团 / 媒体 / 焦点 / 视频**，不包含法人公司全称或“新媒体”等公司展示名；格式示例：`集团.ERP部.业务产品二组`。
## 合约号码申请与配给

- 合约号码 ES 配给“同意”后必须让**同一张申请单**流转到“合约号码配给主管审批”，申请人信息、号码配给结果、员工申请附件、ES 配给附件和审批历史都沿同一记录传递；禁止再用独立预置的主管审批 Mock 顶替真实流转记录。
- 合约号码配给主管审批完成后，该主管待办必须立即消失；当前原型按演示口径在单次 SPA 会话内顺序流转，整页刷新后回到初始演示态，便于重复演示完整链路。
- 员工申请附件与 ES 配给附件是两组不同附件：员工申请阶段支持多附件并同步到 ES 配给和主管审批；ES 配给附件也支持多附件，主管审批页只读展示两组附件，不能把两组附件合并或互相覆盖。
- 合约号码 ES 配给和主管审批“申请信息”里的员工申请附件按普通详情字段展示：`附件`必须真正从新行开始；当前实现让前一项“身份证号码”直接使用整行 `span={3}`，下一整行再放“附件”，不再依赖裸空 `div` 占位。多个附件在字段值区域逐行显示“附件名称 + 下载”超链，不使用表格、不展示附件大小或“操作”列；ES 配给阶段自己上传的“附件信息”继续保留附件大小。
- 合约号码 ES 配给“选择合约号码”弹窗的列表字段口径固定为：**序号 / 标签号 / 合约号码 / 套餐金额 / 备注**；其中 `phoneNumber` 在该业务中统一展示为“合约号码”，不再使用“会员号码”。候选数据没有备注值时显示 `-`。

## 合约号码领用

- 合约号码库管员待办的字段口径应严格跟随当前节点需要：申请合约号码信息只保留标签号、合约号码、合约号码说明、金额、城市、申请原因、备注，不因为底层数据里还有数量、子公司、领用原因就继续展示。
- “当前仓库”在该办理页统一称为“仓库”，避免把普通业务字段写成状态化文案。
- 库管员办理动作使用“领用确认 / 弃领 / 返回”；“弃领”不是“驳回”的换文案，应终止流程并把已配给号码释放回在库状态。
- 员工合约号码领取确认只展示员工需要核对的最小信息：标签号、合约号码、合约号码说明、套餐内容、申请原因；申请数量、领用数量等不再展示。
- 员工合约号码领取确认的保管职责提示与正文属于业务确认文案，用户给出明确原文时必须原样采用，不自行补充“电话卡”、刷卡等未在指定文案中的措辞；当前提示固定为“提示：我已核对并确认领取上述合约号码，已阅读并确认保管职责，特此扫码确认！”，保管职责正文按最新确认原文展示。

## MIS鉴定与资产更换办理

- MIS鉴定页面若用户明确删除“鉴定结果”，不要保留与该字段绑定的隐藏状态或同意/驳回联动校验；当前只保留“鉴定说明”作为业务鉴定字段。
- 鉴定说明当前固定为枚举下拉：**无 / 主板故障 / 键盘故障 / 屏幕故障 / 硬盘故障**，默认空；它属于“同意时必填”的条件必填字段，驳回不要求选择。
- MIS鉴定确认后的鉴定说明属于下游办理需要核对的只读结果，应沿 `application.mis.description` 原值传递到资产更换办理，不能在办理页重新录入或二次维护。
- “退库确认”和“领用确认”属于两个不同节点动作。点击后进入同一个员工资产确认页时，必须携带来源场景，员工确认页按来源只展示对应明细，不能再依赖 Tabs 让员工自行切换。
- 退库确认入口只展示退库资产明细；领用确认入口只展示待发放资产明细，避免员工误确认另一阶段。
- 员工资产确认的领用确认场景在“确认提示及保管职责”中使用红字展示保管职责正文；该正文属于确认业务文案，用户给出明确原文时应原样采用，退库确认场景不展示该领用保管职责。
- 更换物资信息的仓库维护顺序按业务口径固定：备注/耗材在仓库前，仓库后依次责任人、鉴定说明、资产标记、退库日期、使用说明。
- 更换物资信息和待发放资产信息的位置字段标签统一使用 `city / building / floor`，不再显示“城市 / 建筑 / 楼层”。
- 资产更换办理申请人信息中的“查看名下资产”使用眼睛图标 + link Button，并打开统一“员工名下资产明细”弹窗。
- 退库日期取员工退库确认完成日期，不允许库管员手工改；使用说明要求一行时使用普通 Input。
- 待发放资产选错后重新打开选择弹窗覆盖即可，不额外提供删除按钮。
- 盘点人/盘点状态是条件字段：只有同时存在真实非空盘点数据时才展示；不存在时整组隐藏，不能用 `-` 占位冒充盘点期。

## 资产更换申请

- 资产更换申请“退回资产信息”中的资产说明只展示“品牌.型号”，不展示资产小类前缀；优先使用 `brand / model` 字段组合，避免通过截字符串误伤型号内容。
- “更换须知”当前只保留第一条提示；删除其他提示只影响展示，不顺手修改资产资格校验逻辑。

## 资产退库办理与审批

- 退库信息维护只保留当前节点真正需要维护的字段；MIS鉴定、鉴定结果、鉴定说明、维修记录已经从该区域删除，不因为上游流程或资产历史存在这些数据就继续展示。
- 当前资产退库办理页面不展示附件信息；页面层的附件上传、删除、维修记录弹窗等仅为已删除 UI 服务的代码也应同步清理，避免留下不可见的冗余交互逻辑。
- 资产退库办理的退库日期属于流程结果字段，不是库管员录入字段：应直接取员工退库确认完成时间对应日期，只读展示；员工未确认前显示 `-`，不能用“今天”预填。
- 资产退库办理的使用说明要求一行时使用普通 Input，不保留多行 TextArea。
- 资产退库办理“申请人信息”使用默认 7 字段：**申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门**，并在最后单独整行保留**退库原因**；不展示板块、退库类型等身份扩展字段。当前公司展示为 `114.新媒体`。申请人旁提供眼睛图标 + “查看名下资产”，打开基于 `getAssetReturnAssets()` 的员工名下资产明细弹窗。
- 资产退库办理“退库资产信息”的位置字段标签统一使用 `city / building / floor`，不再显示“城市 / 建筑 / 楼层”。
- 退库审批的退库资产信息也已删除维修记录；资产说明放在资产状态之前。页面不展示附件信息，附件上传/删除及维修弹窗页面逻辑同步清理。
- 领导退库审批的“申请人信息”与资产申请业务审批保持同一字段集合和顺序：**申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门**；“退库原因”在最后单独占一整行。公司使用普通业务公司口径，例如 `114.新媒体`，不能写成集团/媒体/视频/焦点。

## 合约号码退库

- 合约号码退库申请页的政策提示已下线；删除提示时直接删除，不换位置或换样式保留。
- 申请信息若不需要内部滚动，避免 DetailGrid/固定高度 TextArea产生单独滚动条；退库原因可自动增高。
- “添加物资”弹窗和已选明细必须严格使用用户给出的最小字段集；当前都只有合约号码、合约号码说明。
- 合约号码退库办理“申请人信息”同样使用默认 7 字段：**申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门**，删除板块、成本中心等扩展字段，最后单独整行保留**退库原因**；普通公司口径当前展示为 `114.新媒体`。
- 合约号码退库办理的号码信息当前只展示合约号码、合约号码说明、号码状态。
- 办理页退库日期取员工确认完成日期，只读展示；使用说明为单行输入。
- 员工合约号码退库确认只展示员工确认需要核对的最小字段：合约号码、合约号码说明、退库原因。

## 资产借用与汇总

- 资产借用申请阶段只确定大类/小类，因此申请页字段名称使用“资产类别”，展示“资产大类.资产小类”；配给选定资产标签号后，具体实物信息再使用“资产说明”展示品牌.型号。
- 借用发放删除单据备注时，不再让页面编辑或覆盖 `issueNote`，让已有数据随原记录自然保留；“使用说明”要求一行时使用普通 Input，不保留多行 TextArea。
- 耗材汇总的“汇总明细”视图不放导出；“耗材汇总申请”和“耗材汇总审批”里的“申请明细”Card 右上角都提供导出，导出表头与页面当前字段口径一致。
- 耗材汇总明细表以及后续“申请明细”的字段口径统一为“耗材小类 / 耗材说明 / 采购数量 / 预计费用（元） / 申请原因 / ES建议”；底层继续使用 `category / materialDesc / detail`。
- 资产汇总与耗材汇总出现同类时间字段时，应统一字段名“汇总时间”，并使用 `开始日期至结束日期` 的区间格式；不要一处叫汇总时间、一处叫汇总周期。

## 原型标注

- PRD 已经在项目仓库里时，不要再设计“前端上传 PRD / 前端调用 AI 自动匹配”的链路。正确流程是 Agent 直接读取仓库 PRD 和 React 页面代码，生成语义锚点与初始标注；页面端只负责人工校准。
- 原型标注保存不能把整页 annotation 快照覆盖代码基线，否则 Agent 后续根据 PRD 新增/更新标注会被旧 localStorage 挡住。必须采用“代码基线 + 用户覆盖层”：同 id 用户修改优先，新 id 基线自动出现，用户新增和删除单独持久化。
- 页面内移动标注时继续保存相对锚点的 `side / align / offset`，不要退化成绝对 x/y 坐标。
- 标注面板出现“内部无法滚动 / Ant Design Tooltip 被面板遮挡”时，不要继续只堆 `overflow` 或全局 CSS z-index。面板应直接 Portal 到 `document.body`，内部使用明确高度的单一滚动容器；Tooltip / Popconfirm / Select popup 也显式挂载 body 并设置独立 popup z-index。
- 新增/重绑标注不能只允许命中模块级 `data-prototype-anchor`。产品标注需要覆盖单个按钮、QueryItem 查询条件、Table 表头字段、FormItem 等细粒度元素；细粒度 target 必须由稳定业务语义重建，禁止 `nth-child`、绝对 CSS path 或绝对 x/y。
- 中文字段生成稳定 target 时，编码后必须统一大小写再过滤字符，避免 URL 编码中的 A-F 被误删导致不同中文字段 key 退化或冲突。
- 对于同一路由内部通过菜单/页签切换的后台，不可只用 pathname 作为标注 pageKey；必须显式暴露当前业务页面 scope，否则不同页面会共享或丢失标注。普通路由按 pathname 隔离，`/yewurules` 按 `activeMenu / activeSubMenu / activeTab` 隔离。
- 精准标注选择不能依赖“进入页面时预扫描已经成功”。鼠标经过时应实时从当前 DOM 判断 Button、表头、QueryItem、DetailItem、FormField、FormItem、Input 等目标，再回退到 Card/Table/Form/业务块；预扫描只负责持久化 target 重建，不负责决定当前能不能选。
- Select、DatePicker、Radio、Checkbox、Tabs、Segmented、Switch 等交互控件不能被外层 QueryItem/DetailItem/FormItem 抢走命中。选择目标时应先判断具体交互控件，只有点击字段标签或普通只读值区域时才回退到字段级标注；复合控件内部隐藏 input 不能单独成为伪目标。
- 标注点层级不能只靠一个超高 z-index 解决，否则底层页面标注会穿透到 Modal/Drawer 上方。应感知当前顶层业务浮层：弹窗外目标隐藏，弹窗内目标可继续展示；公共自定义 Modal 显式声明 overlay，Ant Design Modal/Drawer 自动识别。
- `SelectModal` 这类项目自定义选择弹窗必须显式声明业务 overlay，并给弹窗整体、搜索条件、结果列表稳定语义；否则只能偶然命中里面的 input/button，无法把“选择弹窗”作为稳定业务对象标注。标注选择状态下如果需要先打开弹窗或下拉，允许按住 Alt/Option 点击执行原业务交互且保持 bindingMode，再在浮层内部完成绑定。
- 查看态的标注详情本身已经支持“点击条目展开 / 再点同一条收起”，不要再在详情区重复放一个独立“收起”按钮占据内容高度；编辑态的“收起编辑”仍保留，因为它控制的是编辑器区域。
- 标注内容优先复用现有 `section.title + items[]` 结构：查看态将普通 item 自动渲染为项目符号，并在末尾保留来源标识。需要富文本时使用受控 Markdown 子集（加粗、斜体、行内代码、标题、列表、表格、常用转义），不解析原始 HTML、不改变 annotation JSON / localStorage 数据结构。
- 标注粒度按规则实际归属拆分：单字段的必填、只读、脱敏、枚举、输入限制、附件格式/大小等要求必须优先绑定到具体字段或控件，不要塞进整个 Card/模块标注；模块级标注只承载跨字段统一规则、准入/统一校验、状态流转、公共交互和系统副作用等规则。
- 标注的业务 `target` 与序号 `display anchor` 必须分离：`target` 继续负责规则归属、高亮和稳定重建；字段/控件的序号贴自身，Card/分组/模块的序号优先贴实际标题文字或标题组件。自定义模块必要时用 `data-prototype-display-anchor` 显式指定标题，避免模块宽度导致序号被推到页面最右侧。
- PRD 中如果同一段同时描述多个按钮、字段或不同业务分支，必须先拆成最小 Requirement Atom，再逐条绑定；不能按“它们都在同一个 Card”就合成模块标注。比如库管员“领用确认”和“弃领”属于两个按钮、两条业务分支，必须分别绑定对应 Button。
- 精确性和完整性必须分开校验：具体对象规则使用 Granularity Check 阻止 action/field/tab/table-column 规则回退到模块；同时维护 Requirement Coverage Ledger，所有研发重点必须明确为 `bound / review / skip`。找不到稳定目标或 PRD 与页面口径冲突时进入 `review`，不能静默遗漏，也不能为了有地方放而挂到错误模块。
- 标注 target 的稳定性不能依赖动态展示文案：Card 标题里的“共 X 件/条”、状态数字、统计副标题等不能进入 target 语义；优先取稳定主标题，并在 exact target 失配时只允许“同 kind + 同语义 key + 唯一上下文前缀”兼容回退。弹窗关闭造成的 DOM 暂不存在也不应统计为真正失配，应与“真未匹配”分开显示。
- 审批/办理动作按钮的文案通常比父级 Card 标题更稳定：当历史 target 因“审批操作 / 审批信息”等分组名称调整失配时，如果当前页面同文案 Button 唯一，可以按按钮文案恢复匹配；若存在多个同名 Button，则禁止自动猜测，继续依赖精确上下文或人工重绑。
- 表格行内重复动作是明确例外：如果规则本身就是“发送通知”这类每行重复的 Button 动作，仍必须绑定按钮，不得因为同名按钮不唯一就退回整个 Card。可使用该表格当前首个可见同语义按钮的无后缀 target 作为稳定展示锚点；这种处理必须由规则明确声明为重复行内动作，不能推广到普通重复按钮场景。
- Coverage `review` 评审必须区分“PRD 正确且原型需要补页面”和“PRD 正确但前端原型无需模拟的研发规则”。后者一旦用户确认，应退出 `review` 并继续保留研发标注，不要为了让原型看起来已实现而补员工类型、实时锁、定时刷新等假前端逻辑；只有用户可见页面和交互缺失时才补原型。
- 介绍或比对 PRD / 功能规则时，只陈述已明确的具体业务规则及尚未明确的规则，不使用“完整 / 较完整 / 成熟度高”等评价性或分级式表述。
- PRD/规则文档不得使用“沿用既有规则 / 继续按现有规则 / 使用现有能力 / 原逻辑不变”等依赖隐含背景的表述。必须展开写明实际规则；若仓库和资料中无法确认具体规则，则明确标记该规则尚未定义/当前文档未明确，不能用“沿用现有”掩盖规则缺口。
- 用户提供原版 PRD 作为历史需求基线后，原版 PRD 只读保存，不直接覆盖或重写。后续需求校准固定执行“原版 PRD vs 当前原型 vs 新版 PRD”三方比对：原版用于确认历史业务规则，原型用于确认当前页面字段/文案/交互和已实现规则，新版 PRD 承载最终目标口径；发现差异先区分“原版规则被遗漏”与“原版规则已被后续改造替代”，只修改新版 PRD，并在比对记录中说明最终口径。
- 主 PRD 必须写成可执行的业务规则，不要把“已完成、当前原型、文件路径、Mock 实现方式”混在需求正文里；正式规则与演示环境口径要明确分开。
- 工作台类 PRD 必须单独写清页面职责边界、待办准入、每个查询字段的匹配方式、多个条件的组合关系、回车触发、0/1/多条结果行为、办理入口和异常；不单独设置“验收标准”章节，也不在正文固定演示账号或演示数据。除非是数据映射章节，不在正文使用代码字段名代替业务字段名。

- 库管员工作台主 PRD 只保留正向业务规则：职责边界写成一句正向范围说明；扫码、刷卡、业务类型等按实际交互写规则；重复的“不出现 / 不负责 / 不直接处理”不在正文反复罗列；“当前原型状态”“Mock 实现”“后端确认清单”移到演示说明或项目任务记录。
- 用户明确确认的详情/表单弹窗默认使用 3 列 DetailGrid；转移“添加转移物资”弹窗采用 3 列、最大 960px，不能继续保留为四列 1000px 特殊布局。
- 表格展示“行号”必须按当前列表/明细的展示顺序生成：详情明细从 1 开始，分页列表按页码连续编号；不能直接绑定 PO 行或业务数据的 `id`。
- 用户提供的 Excel 模板是批量导入字段和处理链路的直接来源：下拉选项只做填写辅助，不能替代服务端校验；转移导入按标签号优先、整批原子失败、错误回写第一列、上传后暂时锁定、确认时二次校验并在成功后解锁。
- 普通业务弹窗不应因全局 `overflow-x: auto` 计算出无意义的纵向滚动条；滚动只放到确实需要浏览的表格容器。

- PO 行“可接收数量=0”不等于没有编辑入口：如果 0 是因为当前草稿接收单已经占满剩余数量，用户仍需要通过【编辑】调整该草稿的本次接收数量；此时可调整上限应为“当前未占用可接收数量 + 当前草稿自身占用数量”，保存后同步更新草稿接收行和 PO 草稿占用。
- 当用户明确删除某字段时，要同时检查 PO、接收单、Mock、下游单据和 PRD；本次耗材接收的申请批次已从整条耗材接收链路删除。
- Excel 批量导入必须以用户提供的正式模板为准：按入库类型校验表头，任意一行错误整批不落库，错误原因回写模板第一列；上传成功只生成草稿明细，业务结果仍以执行入库为准。

- PRD 主结构固定为“背景 → 权限 → 通用规则 → 页面字段及交互 → 校验 → 后续操作 / 业务结果 / 补充说明”。规则只在“通用规则”完整定义一次；页面章节只描述该规则在页面上的表现，校验章节只描述提交拦截条件，避免同一规则在多处复制后发生口径漂移。重构旧 PRD 时不得以“精简”为目标，必须先做细节清单，确保状态、数量占用、字段锁定、异常、下游结果、打印和特殊场景完整迁移。

- 同一业务域下的兄弟 PRD（如资产接收、耗材接收）应使用相同一级骨架和同类规则命名，例如“PO 级字段固化”“PO 行编辑与草稿引用锁定”“可接收数量与草稿占用”，但不能为了结构一致而强行复制业务字段；耗材链路明确无申请批次、无部件，普通耗材还有接收确认即原子自动入库的独立结果。

- 入库 PRD 的 Excel 模板必须以当前正式业务字段为准，不保留“Excel 专有字段”或老模板历史字段；页面与模板同义日期统一使用“购买日期”。采购接收拆单规则统一为：资产按“位置 + 资产大类”，耗材按接收单维度不再拆分。草稿当前仓库（含系统生成采购草稿）由用户选择，选择后按仓库公司限定可处理物资。

- 出库列表不提供导出功能；【模板下载 / Excel导入】属于草稿出库物资维护能力，不能与列表导出混淆。出库列表操作固定为创建、删除、出库打印、领用打印；PRD、页面和回归检查必须保持一致。

- 正式 PRD 避免“当前原型、Mock、后端实现、沿用现状”等过程性或模糊表述，应直接陈述最终业务规则。页面字段名可以与业务语义不同，但必须明确映射，例如“公司”字段的业务含义为财务公司。

- 工作台 PRD 若用户明确确认具体账号白名单就是正式业务权限，可以在正文直接列出账号和姓名；“正文不固定演示账号”的规则只适用于演示/Mock账号。库管员工作台查询结果无论 0、1 或多条都保留在列表，申请单编号是唯一办理入口，不设置【处理】操作列；员工页面确认内容需结合业务类型和当前办理场景判断，不能仅按单据类型做静态白名单。
- 查询页或详情入口的 PRD 不写“本 PRD 不维护具体 URL”“不修改任何业务数据”“不生成操作历史”“不改变业务状态”等显而易见且没有新增业务约束的否定句。正文只保留真实业务行为、必要限制和操作结果；禁止事项只有在确实可能发生且需要系统拦截时才写。
- 移库物资通过扫码或手工验证成功后，不提供取消验证；PC 与移动端规则一致，已验证的待接收物资后续只进入接收或驳回处理。
- 移库明细不使用“已取消”状态。草稿删除物资时直接移除明细并释放本流程增加的锁；正式明细状态固定为草稿、待接收、已接收、已驳回。
- 移库添加物资后实时保存，不设置【保存草稿】按钮。草稿单号生成时点、单头字段实时保存范围及离开页面后的锁保留规则需在移库 PRD 整改时一并明确。
- 同一轮 PRD 评审存在多个待确认点时，一次性汇总成决策表让用户集中确认，不逐条追问。
- 维护类批量模板统一使用“定位标签号必填；其他可编辑字段空白保留原值、非空覆盖”，保存前按合并后的最终卡片执行必填和业务校验。耗材模板不保留板块、耗材说明、数量等只读核对列。
- 查询列表的详情继续使用弹窗：标签号或业务编号打开只读详情弹窗；有编辑权限且记录允许编辑时显示【编辑】，在当前弹窗进入编辑态；保存或取消后返回只读详情态；关闭后保留列表查询条件、更多条件展开状态、分页、每页条数和排序。该规则适用于所有模块，后续修改其他 PRD时同步写入。
- 耗材有关联主资产时随主资产正式报废，报废日期、报废类型、报废原因和耗材状态由主资产报废结果带入；无主资产耗材允许在单条编辑中直接报废，状态和报废三字段同时保存。主资产联动是否生成耗材操作历史待确认，未确认前不写成正式规则。
- 合约号码维护不再展示或维护身份证号码：卡片、单条编辑、批量模板、导出和操作历史均删除该字段；历史底层值保留但不对维护功能开放。
- 移库发起列表无论 PC 还是移动端，都按用户拥有出库权限的移出仓库展示单据，不限制为本人创建。页面字段统一使用“移出仓库、移入仓库”，不再混用当前仓库、对方仓库、目标仓库、接收仓库。
- 移库页面不展示通知状态、催办状态；狐小E验证属于正式范围。打印仅对已完成单据开放，已驳回单据不显示打印。

- 资产维护类 PRD 重构时，不能因结构收敛丢失历史查询/审计/批量/导出细节：开放编辑字段应在通用规则统一定义，单条与 Excel 共用；数量类字段按业务原值展示不强制千分位；资产操作历史展示字段与后台审计字段可以不同；RFID 历史中的“小服务”属于独立历史数据，不因其他业务删除“子服务”而联动删除。

- 耗材维护 PRD 中单条编辑与 Excel 批量修改可以使用不同字段白名单，必须分别定义；批量模板覆盖策略下，必填字段空白应直接失败而不是清空，其中耗材状态属于必填。耗材维护 Excel 正式支持 .xls/.xlsx；操作历史与资产维护统一按新到旧稳定排序，并由较新事务和下一条较早事务比较变化字段。

- 合约号码维护与合约机必须分开建模：当前维护范围只含合约号码；权限为固定人员而非公司/板块数据权限。批量修改字段应以单条编辑白名单为准，身份证号码属于独立手工维护字段，不从责任人主数据派生；合约号码无占位例外且全局唯一；操作历史按新到旧稳定排序并对全部业务快照字段做差异比较。
