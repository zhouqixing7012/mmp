# 可复用纠错经验

业务规则见对应正式 PRD；写作、UI、工程要求分别见 [AGENTS.md 的规范索引](AGENTS.md)。本文件只保留跨任务可复用的纠错经验。

| 易错点 | 后续如何避免 |
|---|---|
| 仓库根链接被误读为上次使用的功能分支 | 先核对链接实际分支、仓库默认分支及 AGENTS/CONTEXT 指定目标；未指定时不能沿用上轮分支，引用源码和写入记录前再次核对 ref |
| 用历史记录或旧代码替代当前要求 | 先查正式目标和明确确认来源；不能用条目排列位置判断新旧 |
| 把 PC 和移动端不同要求当冲突 | 先核对适用端、页面、角色和状态，再判断是否需要同步 |
| 字段只改页面，其他入口仍能写入 | 核对保存、批量、导出、历史及下游；实际影响范围由业务决定 |
| 复用时把两个业务强行变成同一套字段 | 共用结构可以保留，字段和操作按已确认业务分开 |
| 把“取消勾选”和“撤销验证”混为一谈 | 分别核对选择状态和业务状态，不能互相推导 |
| 静态复核或构建通过被说成整条流程通过 | 分开记录源码、运行、浏览器和线上验证；未执行如实说明 |
| 用户截图被当作原型运行截图 | 来源截图只作参考，运行截图必须来自实际操作 |
| 同名文件导致改动没有进入实际页面 | 沿菜单、路由、import 核对最终执行文件，不只查文件名 |
| 同一问题不断追加，旧结论仍可被误读 | 修正权威位置，标记被替代内容并归档；不重复追加相反规则 |
| 读远端成功就承诺能提交或已经上线 | 核对实际写入返回与分支结果，线上状态单独验证 |

2026-09-24 整理前的全部原文见 [历史 lessons](docs/archive/2026-09-24-workflow/lessons.md)。[迁移对照](docs/archive/2026-09-24-workflow/README.md) 区分已替代规则、现行规范入口和仍待核实条目；归档不表示所有旧业务结论失效。
