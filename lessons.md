# Lessons Learned

## 2026-06-25 yewurules.js 物料维度组合弹窗改造

### 字段布局
- 核心字段：维度组合编码+描述(自动) / 物料总类+大类小类 / 品牌规格+配置 / 单位+参考价格
- 业务规则字段改为数组驱动渲染：定义 fields 数组（label/visible/content），filter 可见字段后每2个一行渲染
- 排序：正式员工可申请 → 条件字段(资产/耗材) → 是否需要盘点 → 是否允许退库
- 条件字段用条件拼接而非 visible，保证顺序正确

### 字段显隐规则
- 退库MIS鉴定、允许更换/转移 → 仅资产
- MIS审核、关联主资产、主资产小类 → 仅耗材/低值
- 正式员工可申请选"临时可申请" → 隐藏实习生可申请，显示生效时间(RangePicker)/可申请部门/可申请员工

### 技术要点
- 生效时间用 DatePicker.RangePicker + dayjs
- 可申请部门/员工绑定 onClick 弹出 SelectModal
- 表格列用"fixed:'left'/'right'"冻结，scroll x 设固定值
- 每次变更必须 git push 并告知版本号

## 2026-06-28 业务映射规则+费用账户+成本中心页面改造

### 费用账户规则页面
- 查询条件新增"是否启用"下拉框，操作栏去掉删除按钮
- 弹窗"是否启用"由下拉框改为单选按钮
- 全部输入字段改为弹窗选择样式(readOnly + Search图标 + onClick)
- 缺失Search图标的字段(成本中心/科目/子目/业务线/项目)需自动补齐
- 新增11个SelectModal弹窗组件

### 成本中心与科目映射页面
- 精简弹窗字段，保留成本中心/大类/公司/科目/是否启用5个
- 表格列使用"编码.描述"格式显示(onConfirm存record.code + "." + record.desc)
- 弹窗字段用SelectModal弹窗选择

### SelectModal多选
- SelectModal新增multiple属性，多选模式下用复选框替代单选按钮
- onConfirm返回选中记录数组

### 技术坑
- Python脚本写UTF-8文件: 用[System.IO.File]::WriteAllText而非Out-File
- apply_patch: 所有行都用空格前缀时为纯上下文匹配(不会修改文件)，需用-/+
- 多行replace匹配first occurrence，容易改错组件，推荐行号或unique marker定位
- 行号偏移问题: insert操作后field_lines字典需重新计算
- PowerShell @''@ heredoc中文管道会乱码，须避免pipe直接写文件

## 2026-07-19 页面精简与弹窗改造

### 页面区块移除
- 从大页面中移除区块时，避免用全局正则替换（会破坏 JSX），优先用 Python 逐行处理 + 明确的行范围
- 布局结构调整时注意外层 `<div>` 和内层 `<div>` 的闭合关系，移除外层开头后需同步确认结尾 `</div>` 是否匹配
- TSX 转 JS 时：`type`/`interface` 声明、`<Type>` 泛型、`as Type` 断言需要逐类处理

### 弹窗改造（左侧面板→Modal）
- 将侧边面板转为弹窗时，给渲染函数加 `inModal` 参数控制容器样式（去掉边框/圆角/背景）
- 弹窗需要独立处理点击背景关闭（`stopPropagation`）、确定/返回按钮、已选数量提示
- 注意函数签名从 `() => (...)`（隐式 return）改为 `() => { ... return (...); }`（显式 return）时，结尾 `);` 需同步改为 `};`

### 组件复用模式
- 角色管理页（rolemgt.js）的 UserLinkModal 可直接复制到其他页面，适配 mockAllUsers 数据和 onConfirm 回调即可
- StatusTag 组件（src/components/StatusTag.jsx）统一管理状态标签样式，避免各页面重复写 Tag
- 弹窗按钮栏样式统一：`flex justify-center gap-3 + Button type="primary" + Button`

## 2026-08-03 资产借用页面收敛

- 新业务页面不要仅参考 PRD 独立设计，先找到系统内相同角色、相同操作的成熟页面，直接复用其结构和交互。
- 借用申请应以资产申请为基准：不额外展示申请人信息，物资选择保持同一五级分栏样式，起止日期合并为 RangePicker。
- 借用配给应以资产配给为基准：申请人字段、页面分区、库存资产选择弹窗保持一致，仅保留借用特有字段。
- 审批页面默认精简展示，顶部规则提示只有用户明确要求时才增加；关键字段优先排列，非必要的 SN 号、资产状态不要重复展示。

## 2026-08-03 原型交付方式纠正

- 用户在项目语境中说“GitHub 上的原型”或“放到个人工作台下”时，交付物必须是仓库内可运行页面，不使用图片生成。
- “原型图”默认理解为项目原型页面；只有用户明确要求图片、截图或视觉稿时才生成静态图片。

## 2026-08-03 截图还原字段优先级

- 用户提供旧系统截图时，不能只参考业务流程重新设计页面；必须先逐项核对截图中的区块名称、字段、顺序和按钮文案。
- 页面视觉结构按截图还原，既有流程能力通过同一按钮分阶段执行，不额外暴露截图中不存在的操作区块。
- 字段精简以截图为准，避免因为现有数据对象字段较多而全部展示。

## 2026-08-03 截图还原边界

- 用户明确说某个区块“不用动”时，只调整外围信息结构、字段顺序和操作区，不改该区块内部字段、校验和交互。
- 已确认区块与新截图存在差异时，以用户明确保留指令为准，不擅自同步改造。

## 2026-08-04 工作台直接进入业务页

- 用户要求删除待办列表时，菜单必须直接渲染第一张待处理单据的详情、审批或确认页，不保留只有一个标签的空壳列表。
- 删除菜单页后必须同步清理提交、返回等内部跳转，禁止继续跳转到已移除菜单。
- 处理完成后刷新数据并自动切换下一张待办；无待办时只显示空状态和返回工作台入口。

## 2026-08-04 退库办理字段还原

- 人员详情中的关联入口应紧跟关键字段展示，例如“查看员工名下资产”放在申请人姓名后，不放卡片右上角。
- 组织层级分隔符必须按旧系统口径展示；数据仍为“/”时只做显示转换为“.”，不改底层数据。
- 用户明确要求补充盘点字段但数据源没有值时，只展示“-”，禁止虚构盘点状态和执行人。
- 页面隐藏确认状态字段不等于删除确认流程，按钮禁用和执行入库校验仍应保留。

## 2026-08-04 申请页信息层级

- 数量、总计等统计信息应放在对应明细卡片右上角，不占用申请表单字段。
- 必填字段必须同时有红色必填标识和提交校验，不能只靠提示文案。
- 用户要求去掉页面提示时应直接移除，不用换成其他说明或提示条。
- 阅读确认类复选框的对齐方式按用户要求处理，居中时使用独立容器，不影响须知正文布局。

## 2026-08-04 个人工作台画布统一

- 业务页已经嵌入后台框架时，页面根节点禁止再次设置 `min-h-screen`、独立背景色和外层内边距，否则会形成浅蓝画布套白色画布的双层结构。
- 页面背景、最外层留白和滚动应由 `AdminContent` 统一管理，业务页只负责标题、卡片和业务内容。
- 个人工作台与后台基础配置可以使用不同容器规则，但同一菜单体系内必须保持单层画布。

## 2026-08-04 审批页面信息顺序

- 申请人信息应直接收纳业务原因，不额外拆出只有一个字段的独立卡片。
- 详情信息默认最多三列，长文本字段使用跨列展示，避免四列导致内容过窄。
- 审批意见和审批操作应放在审批流程记录之后，先看历史再做决策。
- 共用流程记录组件的字段差异使用参数控制，禁止为单页要求直接影响其他业务页面。

## 2026-08-04 员工确认页字段拆分

- 同一流程存在多个确认场景时，可以保留标签页，但每个标签页必须按场景展示对应明细字段，不能共用一套笼统的详情字段。
- 员工基础信息作为公共区域只保留截图明确要求的字段；旧资产退回与新资产领取分别使用独立表格列。
- 标签页可以切换查看，但确认动作只能作用于当前真正待确认的场景，避免员工误确认另一阶段。

## 2026-08-04 员工确认页卡片与代确认交互

- 公共人员信息、场景标签页和场景明细属于同一业务上下文时，应合并在一个 Card 内，避免页面切成过多碎片。
- 提示和保管职责应放在同一 Card 中，先展示确认声明，再展示责任说明。
- 管理员代为录入员工工号时，业务结果按刷卡确认处理，但必须校验录入工号与申请人工号一致。
- 管理员完成扫码时，业务结果按员工扫码确认处理；原型可使用可点击二维码区域模拟，不新增二维码依赖。
- 即使允许管理员代确认，确认动作仍只能落在当前实际待确认的流程节点。

## 2026-08-04 页面归属确认

- “MIS鉴定”可能同时存在于资产更换与资产退库流程中，收到页面修改要求时必须先根据菜单名称、字段口径和上下文确认业务模块。
- 用户纠正页面归属后，应先恢复误改页面，再将同一组要求落实到正确页面，不能只补改目标页而保留错误变更。

## 2026-08-04 退库节点附件权限

- 流程附件必须记录业务单据、上传节点、上传人和上传时间，不能只保存文件名。
- 删除权限应同时校验“当前节点”和“当前审批人”，页面隐藏或禁用删除入口之外，Service 层也必须再次校验。
- 后续节点可以查看上一个节点附件，但不能删除上一个节点上传的附件。
- 危险删除操作必须使用二次确认；演示项目只保存附件元数据，不伪造真实文件地址或下载能力。

## 2026-08-04 单按钮保留多阶段流程

- 用户要求将办理操作收敛为一个“确认”按钮时，不能因此删除必要的员工确认节点。
- 单个确认按钮可以按状态分阶段执行：未发起时发起员工确认，待确认时提示等待，已确认时执行最终入库。
- 发起员工确认前应先保存当前办理字段，避免跨页面确认后编辑内容丢失。

## 2026-08-04 借用业务对象与枚举口径

- 资产借用不支持耗材时，页面、弹窗、空状态和提示文案必须统一使用“资产”，不能只改入口按钮而保留耗材标签页或“物资”文案。
- “借用”是业务场景，不是资产用途；资产用途仍应使用员工用机、部门公用、其他用途、专业用途等真实用途枚举。
- 历史演示数据可能保留已废弃枚举，页面回填时需要显式归一化为空值，强制用户重新选择有效选项。
- 同一业务的审批记录列应由共用组件统一维护；删除代理人字段时优先修改借用审批共用组件，保证配给和审批页面同时生效。
- 共用申请人卡片存在不同页面字段口径时，应通过模式参数控制字段集合和顺序，避免为单页要求影响其他页面。

## 2026-08-04 关联入口与附件展示层级

- 与资产字段同级的关联能力，如维修记录，应作为详情字段放在资产信息区域末尾，不默认占用 Card 右上角操作位。
- 按钮文案应直接表达业务对象，例如字段和按钮均使用“维修记录”，避免只写“查看”造成语义依赖上下文。
- 附件底层可以保留节点、上传人、上传时间等权限元数据，但页面只展示用户当前决策所需字段；展示精简不能削弱 Service 层权限校验。
- 设计走查应区分“业务差异”与“设计语言差异”：鉴定通过/不通过等业务文案可以不同，但页面标题、Card、字段命名、按钮图标和空状态文案应统一规范。

## 2026-08-04 设计规范从走查到项目约束

- 用户确认设计走查建议后，应按跨模块问题统一整改，不能只修复举例页面而保留同类不一致。
- 设计规范必须同时沉淀为三层约束：规范文档说明原则、AI_RULES/CLAUDE 强制新页面遵守、公共组件和工具函数降低偏离成本。
- 日期和部门展示应通过公共格式化函数处理，避免每个页面重复实现并逐渐产生不同口径。
- 审批状态必须由 StatusTag 统一映射；业务页面不得自行维护状态颜色对象。
- 流程按钮和工具按钮应明确区分：流程按钮使用纯文字，搜索、添加、上传、删除、刷新等工具操作才使用图标。
- 资产与物资命名必须根据页面真实业务对象决定，并同步覆盖 Card 标题、表格列、弹窗、空状态和提示文案。
- 新页面完成后必须执行固定设计检查清单，而不是等到集中走查时再发现跨页面差异。

## 2026-08-05 公共状态组件回归

- 将多个页面统一到公共状态组件后，必须同时验证数据值、状态文字和颜色，不能只检查组件已被引用。
- 公共组件应兼容已有页面的常见传值方式，并对空值、首尾空格和未知状态提供明确兜底。
- 状态文字是业务信息主体，颜色只是辅助；实现时必须显式渲染文字，不能把可读性完全依赖于 UI 库预设样式。
- 修改审批记录、状态标签等高复用组件后，必须回归资产更换、资产退库、资产借用的所有审批和办理页面。

## 2026-08-05 耗材流程页面收敛

- 用户删除个人工作台业务页面时，应先移除菜单入口和可见路由；仍被其他页面复用的同文件代码不要贸然整体删除。
- “库存领用、统一采购”属于配给结果，“驳回”属于流程决策，两者应使用不同的控件和校验链路，禁止混在同一个匹配状态枚举中。
- 汇总明细页负责按申请人维护 ES 建议并提供 Card 级整单驳回；汇总详情页负责形成最终采购单据，不能在明细表中重复增加“是否通过”。
- 用户要求页面与成熟页面“基本保持一致”时，应复用其信息架构、字段顺序和操作区，而不是仅复制 Card 样式。
- 审批详情字段应按当前节点决策需要精简；上游已完成的主资产、MIS 结果和行状态信息，用户明确删除后不得继续以“方便查看”为由保留。

## 2026-08-05 耗材确认入口与汇总操作层级

- 用户明确暂缓某项修改时，只处理仍被确认的其他事项，不提前落地暂缓项。
- 汇总明细若采用申请人 Card 级“驳回”，表格内不应重复增加“是否通过”字段；整单决策入口保持唯一。
- 办理页面的“领用确认”不是状态提示按钮：首次点击应保存当前字段、发起员工确认节点并直接进入员工确认页面；已处于待确认状态时仍应允许再次进入确认页。
- 确认方式属于员工确认页面能力，不应在上游办理信息区重复展示。
- 是否支持电子签必须以当前业务明确口径为准，不能因为其他资产确认页面存在电子签就自动复用。
- 当前员工耗材领用确认仅支持刷卡/员工工号和扫码确认，不使用标签页，也不提供电子签名区域。
- 用户纠正确认方式后，应同步更新页面代码、主 PRD、修订说明、CONTEXT 和 UAT 清单，避免旧口径从文档再次回流到页面。

## 2026-08-09 账面报废字段交互纠正

- 用户要求删除说明文案时直接删除，不要把同一说明换个位置继续展示；只有明确指定保留的信息才可改成 Tooltip 等弱提示。
- 用户明确要求“单选按钮”时使用普通 `Radio`，不要擅自改成 button-style Radio、Segmented 或 Switch。
- 板块、成本中心、City、Building、仓库等已有后台基础数据的字段，应优先复用统一 `SelectModal` 和现成数据源，不退化为自由输入。
- 地点存在上下游依赖时，上游字段变化应清空下游已选值，例如 City 变化清空 Building/Floor，Building 变化清空 Floor。

## 2026-08-11 原型待办页面独立验收

- 个人工作台新增一个流程节点页面时，不能只实现路由和状态筛选，还要确认默认演示数据中存在该节点的待办，否则菜单首次打开只会看到空状态。
- 同一份 localStorage 演示数据会记录用户上一次点击后的流程状态；调整节点或默认数据结构时应升级 storage key，避免旧状态让新页面误判为“暂无待办”。
- 需要分别验收多个流程节点页面时，应为每个节点准备独立的默认待办数据，保证菜单可直接打开查看；真实流程推进仍按 service 状态机执行。

## 2026-08-11 Descriptions 跨 Card 列对齐

- Ant Design `Descriptions column={3}` 只保证每个区块按三列排布，不会自动保证不同 Card 中每一列的起始边界完全一致；内容长度不同会让浏览器重新分配表格列宽。
- 用户明确要求“第二列与其他区域第二列对齐”时，不能只通过 `span` 调整位置，还需要为各详情区统一 label/content 宽度规则。
- 一个字段只是第二列本身时不要设置 `span={2}`；`span` 只用于确实需要跨列展示的长文本或整行字段。

## 2026-08-11 详情字段严格对齐修正

- 不要为了跨 Card 对齐直接给 Ant Design `Descriptions` 设置百分比 `labelStyle/contentStyle`；宽屏下百分比会把字段名称列拉得过宽，同时压缩内容列，且 `span`/自动表格布局下仍可能无法做到像素级对齐。
- 需要多个 Card 的三列字段严格对齐时，应采用统一的六列固定表格结构：三组“固定像素 label + 等分 content”，通过 `colgroup + table-layout: fixed` 固定每组边界。
- 字段名称宽度保持紧凑固定值，剩余宽度全部分配给内容；跨行或长字段只让内容单元格跨列，不改变第二、第三组字段的起始位置。

## 2026-08-13 物资申请命名与关联主资产

- 同一申请入口同时包含资产和耗材时，页面菜单、顶部标签、面包屑、预览标题及限制提示应统一使用“物资申请”，避免局部继续沿用“资产申请”。
- 大类、小类、品牌、型号、配置属于物料选择的核心决策信息，长名称不能用省略号隐藏；优先自动换行完整展示，可辅以 Tooltip 提升快速浏览体验。
- 耗材“关联主资产”是对当前员工本人名下既有资产的引用关系，不能混入“本次申请”的新主资产；应使用独立弹窗搜索和选择，不使用下拉框承载长列表。
- 关联主资产弹窗的数据口径必须与个人工作台当前用户的资产列表一致，不能为了演示另造一套与个人工作台不一致的资产样例。

## 2026-08-13 文案删除范围与长配置案例

- 用户说“去掉弹窗里的某个标题/文案”时，只删除明确点名的那段文字，不能扩大解释为删除整个弹窗、内容区或交互；涉及容器级删除前必须确认用户是否真的要求下线该能力。
- 用户明确说“配置名称叫某个长字符串”时，应把整段原文当作一个配置值展示，用于验证长文本换行/Tooltip；不能根据字符串里的品牌、接口、分隔符擅自拆成多品牌、多型号或多配置数据。

## 2026-08-13 移动端工作台与关联资产回显

- 关联主资产选择弹窗可以展示资产标签号、说明、配置、状态等丰富信息帮助用户做选择，但选择完成后的字段回显要服从字段语义；当用户明确要求精简时，只显示资产标签号，不把资产说明继续拼接在输入框或预览里。
- 用旧移动端截图重构新移动端时，要把“视觉与导航骨架”和“当前业务能力”拆开处理：可以复用青色主视觉、首页/历史/+ /盘点/扫一扫等移动端结构，但业务入口必须以当前个人工作台已经确认的功能为准，不能因为截图里出现担保、续借等旧按钮就恢复已下线能力。
- 桌面系统中的移动端原型应放在独立手机画布中，通过内部滚动、底部导航、弹层和页面状态模拟真实 App；不要为了模拟手机而破坏个人工作台外层的单层画布规范。
- 摄像头、扫码等依赖真实设备权限的能力，在产品演示原型中应明确采用标签号输入等可验证方式模拟，不伪造已接入真实摄像头能力。

## 2026-08-13 移动端业务分类与设计系统纠正

- 移动端首页的一级分类要按“员工当前持有什么资源”划分，而不是把业务动作混进资源分类；当前固定为“资产 / 耗材 / 合约号码”。“资产借用”属于发起/流程能力，应放在快捷操作或单据中，不应作为与资产、耗材、合约号码并列的数据 Tab。
- 用户提供旧 App 截图不等于要求复刻旧视觉。若用户明确希望现代化重绘，应保留其信息架构和交互习惯，同时改用当前项目设计系统；不要机械保留旧系统的大 Banner、高饱和青色和早期移动控件样式。
- “飞书风格”应理解为清爽、轻层级、白卡片、浅灰背景、主色克制、状态弱强调、间距规整等设计原则，不应复制飞书品牌素材、图标或具体页面。
- 仓库已经有 Ant Design 时，移动原型优先通过 `ConfigProvider` 和现有组件做移动端主题适配；除非真实产品需要复杂手势、ActionSheet、原生感导航等能力，否则不为原型额外引入 `antd-mobile`，避免增加依赖和两套组件体系。

## 2026-08-13 移动端一级导航与批量资产操作

- 全局“+”应只承载“新建/申请”语义的高频主动入口；合约号码退库属于已有资源的变更动作，应从号码卡片或详情进入，不应混在“物资申请 / 资产借用”的快捷发起中。
- 一级资源分类（资产 / 耗材 / 合约号码）属于平级内容区域切换，移动端优先使用 Ant Design 线条式 `Tabs`；大块 `Segmented` 更适合局部模式或轻量状态切换，用作一级导航容易显得厚重。
- 单条资产卡片的变更动作必须与业务能力完整对应：退库 / 转移 / 更换，详情页也应保持同一组动作，避免入口能力不一致。
- 长按资产进入批量模式时，应避免长按后继续触发普通点击进入详情；进入批量模式后以卡片勾选 + 固定底部操作栏承载批量退库 / 批量转移 / 批量更换，并提供明确退出入口。
- 顶部数量概览不能只是三个数字平铺；应结合资源图标、Statistic、轻量卡片层级，并允许点击概览项切换对应分类，让概览同时承担信息展示和快速导航价值。

## 2026-08-13 移动端说明文案、动作权重与五级物资目录

- 手势能力已经可以通过实际操作发现时，不要长期在首页堆叠“长按可批量操作”这类教学注释；原型验收阶段可以验证手势，但正式页面应保持信息区干净。
- 同一组资产变更动作（退库 / 转移 / 更换）业务权重一致时，按钮视觉也应同级；不要因为“更换”是新功能或更常用就默认给主色高亮，除非业务明确要求主次关系。
- PC 端五级目录“大类 / 小类 / 品牌 / 型号 / 配置”不能在移动端机械缩成五列。窄屏应改为逐级钻取：一次只展示一个层级，选择后自动进入下一级，同时保留可点击回跳的层级路径与面包屑。
- 移动端多物料选择适合用“目录选择 + 底部购物车 + 下一步填写”的两阶段模型：第一阶段只负责找物料和多选，第二阶段再逐物料填写数量、用途、关联主资产、申请原因，避免筛选和表单输入竞争同一屏幕空间。
- 购物车在移动端优先使用底部 `Drawer` / Bottom Sheet 展示已选物资，支持删除与清空；固定底栏只保留已选数量、查看购物车和下一步，避免复制 PC 端右侧已选栏。
- PC 与移动端即使交互形态不同，也必须共用同一目录语义和演示数据口径，特别是同品牌多型号、同型号多配置和超长配置名称案例。

## 2026-08-13 员工领取确认字段范围纠正

- “员工领取确认”页面不是库管办理页的完整镜像；确认页只展示员工在领取动作中需要核对的核心字段，不要把序列号、金额、城市、子公司、仓库、备注等办理信息机械复制过来。
- 合约号码员工领取确认的“领用物资明细”当前固定为：标签号、电话号码、说明、申请数量、领用数量、申请原因。
- 同一业务链路中，上游办理页可以字段较全，下游员工确认页应按“确认所需最小信息集”精简，避免信息噪音。

## 2026-08-13 号码控制列表页规范化纠正

- 用户再次要求“按现有 UI 设计规范重画”时，不能只做字段文案替换；列表页整体信息层级也要一起收敛到当前规范：单层画布、标准页面标题、QueryBar、小尺寸 Card、蓝色竖块 Card 标题、bordered small Table 和标准 Modal。
- 页面主标题不要额外使用大号蓝色装饰条；蓝色竖向标识用于业务 Card 标题即可，避免页面标题和 Card 标题同时过度强调。
- 表格操作区的业务动作尽量使用纯文字 Link 按钮，新增等工具动作可保留图标；状态统一走 StatusTag，不在页面内单独维护颜色。

## 2026-08-13 菜单对象区分与退库确认页纠正

- 用户点名“号码管理”时必须修改对应申请入口，不要因为前一轮正在讨论“号码控制”就继续改错相邻菜单；相邻菜单名称接近时先以 `workspaceMenuConfig` 的实际映射确认页面对象。
- “号码管理”当前统一改名为“合约号码申请”，菜单和页面标题必须同步，不能只改其中一个可见入口。
- 用户要求多个既有流程页“按照 UI 设计规范重画”时，不能仅替换标题样式；要同步清理二层灰底、阴影标题盒、旧 Descriptions 布局，并统一到单层画布 + 小 Card + 蓝色竖块标题 + DetailGrid/标准 Table。
- 员工退库确认的“申请人信息”是最小人员信息集，只保留申请人和部门；退库资产明细必须包含行号和退库原因。
- 员工退库确认必须同时提供刷卡/管理员录入工号与狐小 e 扫码两种方式，且继续执行全面取消电子签规则。
