# Lessons Learned

## 通用页面与 UI 规范
- 个人工作台业务页使用单层画布；不重复增加 `min-h-screen`、独立灰底、额外外层 padding 或阴影标题盒。
- 页面主标题使用标准 `Typography.Title level={4}`；蓝色竖块只用于业务 Card 标题。
- 详情页优先使用 `Card size="small" + DetailGrid + DetailItem`，列表统一 small bordered Table。
- 只读业务信息直接展示文本或 `StatusTag`，不要使用 readOnly/disabled Input、TextArea 伪装只读；只有真实可编辑字段才保留输入控件。
- `DetailGrid` 中 `span={3}` 整行字段必须从完整新行开始，不能跟在只填满一到两列的普通字段后，否则可能出现标签和值串行/错位。
- 流程按钮使用纯文字；搜索、添加、上传等工具动作可使用图标；危险操作需要二次确认。
- 员工确认全面取消电子签，统一保留刷卡/工号 + 狐小 e 扫码；管理员录入工号时必须校验与申请人一致。

## 业务对象与字段边界
- 用户明确给出字段集合时，页面只展示该集合，不因底层对象还有字段就机械全部暴露。
- 上游办理页可以字段较全，下游员工确认页按“确认所需最小信息集”精简。
- 资产、耗材、合约号码虽然流程相似，但字段和交互不能互相机械复用。
- 用户要求删除字段/提示时直接删除，不要换位置继续展示；用户要求“一行”时使用普通 Input 或单行 Alert，不继续使用多行组件。

## 个人工作台
- 资产信息：黑体为品牌.型号，浅灰为大类-小类；资产用途拆成独立字段；不使用电脑/显示器等装饰小图标。
- 资产单条操作退库/转移/更换必须按物料编码配置规则显隐；借用资产只展示退库。
- 批量操作必须基于所选资产共同支持的能力显示，并复用单条操作相同业务入口；目标新增页必须实际消费带入的资产标签号。
- 耗材页签：黑体为耗材说明，浅灰为耗材小类；字段为耗材标签号、主资产标签号、主资产说明。
- 合约号码页签即使业务字段只有合约号码/合约号码说明/操作，也可以通过表格复选框支持多选；批量退库必须真实带入退库申请页。

## 物资申请与资产借用
- 物资申请统一使用“大类 → 小类 → 品牌 → 型号 → 配置”五级目录；长配置名称完整展示，不用省略号隐藏。
- 耗材关联主资产必须从当前员工本人名下已有资产中选择，不能混入本次新申请资产。
- 资产借用申请只确定大类/小类，因此添加资产弹窗只保留两级；申请阶段资产说明显示“大类.小类”。
- 借用配给未选具体资产标签号时继续显示“大类.小类”；选择具体资产后改为所选实物的“品牌.型号”。

## 退库与合约号码退库
- 退库详情中整行字段前要先完成三列普通字段，避免配置/备注/关联耗材串行。
- 合约号码退库申请不提供附件；明细固定为合约号码、标签号、合约号码说明、套餐内容、金额、号码状态。
- 合约号码退库通过“添加物资”从本人名下号码选择，当前持有号码状态统一按“在用-使用中”展示。
- 政策提示属于辅助说明，使用紧凑单行 Alert，不作为普通 DetailGrid 字段。
- 合约号码退库办理不展示确认状态；确认状态可作为内部状态机驱动按钮，但不能暴露成页面字段。

## 流程动作与确认节点
- 页面收敛成单个节点动作按钮时，不能因此删除必要的员工确认状态机。
- 首次点击确认按钮应保存当前办理字段并发起员工确认；处于待确认状态时再次点击应允许重新进入员工确认页，而不是把按钮置灰；员工确认完成后再执行最终入库/出库。
- 前置条件不满足时，按钮可以保持可点击并给出明确提示，优先避免“看得见但点不了”的死按钮。

## 2026-08-14 资产更换办理纠正
- “更换物资信息”中删除鉴定结果、鉴定说明；“资产标签”统一更名为“资产标记”。
- 仓库后字段顺序为责任人、资产标记、退库日期、使用说明；责任人由所选仓库自动带出虚拟库管员工号-名称，退库日期取员工退库确认完成日期。
- 更换物资信息的退库确认按钮不能因“待确认”被禁用：首次点击发起员工退库确认并进入员工资产确认，待确认时再次点击可重新进入确认页，确认完成后点击执行入库。
- 待发放资产信息选择资产标签号后不出现删除按钮；选错时直接重新打开资产选择弹窗覆盖选择。
- 领用确认按钮保持可点击：旧资产退库未完成时点击提示前置条件，满足后发起员工领用确认并进入员工资产确认，待确认时可再次进入，确认完成后执行出库。
- 盘点人/盘点状态不是固定展示字段。只有对应资产同时存在非空的盘点人和盘点状态时才展示；无盘点数据时两个字段都不出现，禁止展示空值或 `-` 冒充盘点期数据。
