# AI 协作流程

项目入口和授权边界见 [AGENTS.md](AGENTS.md)。工程实现、UI 和 PRD 分别遵循其专项规范，本文件只维护任务推进方式。

## 先判断任务

| 任务 | 执行方式 | 何时需要确认 |
|---|---|---|
| 只读咨询、核对 | 读取相关资料，给结论和真实差异；不强制修改记录 | 缺失信息会改变结论时 |
| 已明确的文字、字段、样式修正 | 查影响，执行，检查，同步受影响文档 | 出现新的业务决定、越出范围或触及安全边界时 |
| 状态、权限、库存结果或跨模块改造 | 先列目标、范围、影响和验收；未完成事项维护在 task_plan | 仅确认新增决定，既有明确授权不重复提案 |
| 重大重构、多阶段任务 | 维护一个任务计划，必要时补调用关系或流程图 | 方案取舍会改变业务结果、范围或成本时 |

不为简单任务叠加 using-superpowers、planning-with-files、OpenSpec、gsd-method-guide 等多套流程。需要具体技能时检查当前环境是否可用；工具服务于任务，不把技能名称或文件数量当完成标准。

## 开始前

- 先完成不受歧义影响的读取和原因分析。缺失信息会改变业务规则、范围、验收或授权时才提问；多个问题集中成表，列出推荐答案和影响。
- 内部明确要改哪些文件、做什么、怎么验证、什么算完成。简单任务无需把内部检查逐条展示给用户。
- 修复问题先分析原因、上下游和同类问题；检索现有组件、服务及已记录方案，优先复用。
- 仅在现有资料不足时外部检索；时效性信息重新核实。README 的搜索记录只追加影响方案的结论。
- 写入和提交前核对目标分支及最新文件版本。无法读写、运行或验证时立即说明实际限制，不反复尝试无效路径，不声称已完成。

## 执行与确认

- 明确且已授权的子任务持续完成，不在每一步停下来请求许可。
- 原型字段、按钮、状态和流程经用户确认后，同次同步受影响的正式 PRD、页面和必要校验；不得把演示实现自动提升为正式规则。
- 排查字段变更影响：列表、详情、编辑、保存、批量导入、导出、历史、下游及 PC/移动端；按实际业务范围修改，不机械全量删除。
- 可独立开展的复杂研究或评审使用当前可用子代理，分清文件所有权并统一复核；不固定模型，未成功执行不得声称独立评审已完成。
- 遇到新反馈先纳入当前目标。已解决问题不重复提问，新增影响再集中处理。

## 每次修改后的自检

1. Review 查错：核对用户要求、范围、遗漏、上下游和同类问题，不覆盖原有工作。
2. 第一性原理复核：实现是否直接满足目标，是否新增了不必要的状态、分支、依赖或重复记录；有更简单可靠的实现时在授权范围内修正。
3. 按实际影响验证，达到完成标准后停止无关扩测。

| 变化 | 必要验证 |
|---|---|
| 纯文档 | 规则冲突、来源迁移、链接、差异范围；不宣称运行过程序测试 |
| 文案或样式 | 核对目标源码/页面及相应显示，不为无关业务增加测试 |
| 流程或数据行为 | 正常路径、此次影响的异常与重复操作、刷新后的结果、下游变化 |
| 公共组件 | 代表性的调用页面、相关布局和交互，避免只验证单页 |
| 发布或预览 | 区分本地构建、远端提交、线上实际状态；只有实际打开并核对相应版本后才能说预览可用 |

构建成功不代表业务验证完成。未能进行浏览器验证时明确记为未验证，用户提供的现状截图不能冒充本次原型运行截图。

根据已确认要求，不主动追踪 GitHub Actions，也不以其结果作为验收或构建成功依据；该约束不修改已有部署工作流。运行、构建、测试命令见 README。

## 记录与收口

- 实际发生修改或新的有效决定时更新 CONTEXT，仅保留交接摘要和链接，建议控制在一屏至两屏；只读咨询不强制写文件。
- task_plan 只维护未完成任务及状态。完成事项写入 progress 后，从当前任务视图移出；文件本身不删除，保留记录链接。
- findings 记录研究结论和问题来源。已确认业务规则进入对应 PRD，旧问题标明收口依据，不在多个文件重复维护问题正文。
- 用户纠正后先修改规则所属文件；只有可以复用的协作教训才写 lessons，不把每条业务规则变成全局经验。
- README 在模块、运行部署、依赖或重要文档入口变化时更新；ARCHITECTURE 在职责或调用关系变化时更新。
- 大量历史归档前保留原文及来源对照，未完成事项逐条核对去向；未经核实，不把历史未验证写成已通过，也不把已收口问题重新抛给用户。

## 对用户的输出

中文、结论先行。简单问题短段落，多项差异用表格，不用 P0/P1/P2，不复读背景，不输出完整内部检查表。

进度只说明重要发现、阻塞和下一步。完成时说明改动、结果和必要限制；测试仅报通过/失败数量，未执行的验证明确说明。只列实际使用的 skill，GitHub 等连接器单独说明，不伪称技能。完成后不主动追加优化建议。
