# AI_RULES.md — AI 行为规则

本文件定义 AI Agent 在项目中的行为约束，与 CLAUDE.md（代码规范）互补。
每次执行任务前必须阅读本文件。
涉及个人工作台业务页面时，还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

## 项目定位

这是一个用于产品演示的企业资产管理前端项目，不是生产系统。

项目目标：
- 页面在演示过程中稳定
- 关键业务流程可以连续操作
- 数据通过 localStorage 保持
- 所有主要按钮必须有明确反馈
- 页面视觉和交互保持一致
- 同一业务对象在所有页面中数据一致

## 技术约束

- 使用 React 函数组件
- UI 组件优先使用 Ant Design（Antd 6.x）
- 使用 React Router 管理路由
- 演示数据通过统一 mock 数据源访问
- 不直接在页面组件中修改 localStorage
- 不引入 Redux
- 不新增依赖，除非任务明确允许
- 不创建后端代码
- 不修改构建工具和 package.json，除非任务明确要求
- 状态颜色和 Tag 配置统一使用 StatusTag 组件，不在页面中重复声明

## 个人工作台 UI 约束（必须遵守）

详细规范见 `docs/UI_DESIGN_GUIDELINES.md`，以下规则不得省略：

- 页面只保留一层工作台画布，业务区块统一使用 `Card size="small"`，相邻区块间距 16px。
- 详情信息默认使用 `Descriptions bordered size="small" column={3}`，长文本单独一行。
- 申请人信息优先按“申请人、申请日期、公司、办公区、联系电话、邮箱、部门、业务原因”排列。
- 仅展示年月日时统一叫“日期”，并使用 `formatDateText`；部门使用 `formatDepartment` 将 `/` 转为 `.`。
- 只涉及固定资产时统一使用“资产”，同时包含资产和耗材时才使用“物资”。
- 提交、同意、驳回、确认、取消、返回、加签、转签等流程按钮不带图标；搜索、添加、上传、删除、刷新等工具按钮可带图标。
- 审批记录默认不展示代理人列；只有存在真实代理审批业务和数据时才展示。
- 维修记录、查看名下资产等字段级入口放在 Descriptions 内，使用小尺寸文字按钮。
- 状态统一使用 `StatusTag`；通用业务状态使用 `type="business"`。
- 弹窗只保留一个主标题，不增加重复的蓝色二级标题。
- 正常页面返回按钮使用“返回”，空状态返回按钮使用“返回工作台”。
- 新页面提交前必须逐项执行 `docs/UI_DESIGN_GUIDELINES.md` 中的检查清单。

## 页面结构约束

每个业务页面建议按职责拆分：

| 职责 | 拆分方式 |
|------|---------|
| 大型表格列配置 | 单独存放到 `columns` 常量 |
| Modal / Drawer 超过 80 行 | 拆分为独立组件 |
| mock 数据 | 统一放在 `src/mock/` |
| 枚举、状态映射、Select options | 单独管理，不散落在 JSX 中 |
| Service 层 | 统一调用 mock 数据，不直接在组件中操作 |

约束：
- 页面文件建议不超过 300 行
- 普通组件建议不超过 200 行
- 不允许新增 1000 行以上的单文件
- 不允许通过复制完整页面来创建相似页面

## 交互规范

所有用户操作必须有反馈：

1. **点击操作** → loading → 成功/失败提示 → 数据更新
2. **列表页面**必须支持：loading、空状态、查询无结果、查询、重置、分页
3. **表单**必须支持：必填校验、编辑回填、提交 loading、关闭后重置、防重复提交
4. **危险操作**（删除、报废、停用等）必须二次确认（Popconfirm）
5. **所有按钮必须有真实行为**，不能留下无行为的静态按钮

## 演示数据规范

- 页面不能直接 import 大块 mock 数组作为最终数据源
- 数据通过统一入口（`src/mock/businessRulesMock.js`）访问
- 新增、编辑、审批后必须在相关页面产生一致的数据变化
- 数据字段和值必须符合现有业务模型
- 严禁硬编码互相矛盾的统计数字（如仪表盘各指标必须同源计算）
- 不使用 `setTimeout` 模拟业务逻辑，除非明确用于演示 loading

## 修改原则

- 只修改任务涉及的文件
- 不进行无关重构
- 不改变已有路由和数据字段，除非任务明确要求
- 创建新组件或状态配置前，**必须先搜索项目**是否已有相同或相似实现
- 优先复用已有组件，不创建重复组件
- 不复制粘贴完整相似页面
- 不删除现有功能
- 不使用假接口地址
- 如果发现两个已有组件功能相似，不在本任务中合并，优先复用其中一个

## 输出要求

每次完成开发后必须说明：
1. 修改了哪些文件
2. 实现了哪些行为
3. 哪些内容仍然是演示模拟
4. 如何手动验收
5. 是否存在潜在影响

## 验收标准

每次完成后按以下路径验证：
1. 从哪个路由进入
2. 初始页面应看到什么
3. 输入什么查询条件
4. 点击查询后应看到什么
5. 打开哪个弹窗
6. 输入哪些演示数据
7. 提交后列表发生什么变化
8. 刷新浏览器后数据是否仍存在
9. 在哪个相关页面能看到这次变化
10. 如何重置演示数据恢复初始状态

## 禁止事项

- 创建超过 500 行的新页面文件
- 在 JSX 文件中写超过 20 条的大型 mock 数组
- 创建无行为的按钮
- 每个页面单独定义状态颜色映射
- 为同一实体创建不同字段名称
- 直接在组件中调用 localStorage
- 通过复制完整页面创建相似页面
- 无理由新增第三方库
- 修改无关路由
- 修改构建配置
- 删除已有功能
- 使用"采购1""审批2"等无意义页面名称
- 在代码中留下 lorem、TODO 或占位按钮
- 使用超宽表格展示所有字段（`scroll={{ x: 2000 }}` 暴力方式）
- 无意义动画、过度渐变和玻璃拟态
- 使用页面编号作为正式业务名称
- 在个人工作台页面使用自定义 section 模拟 Card
- 在没有真实代理数据时展示代理人列
- 在页面中直接展示 `/` 分隔的部门层级
- 为流程按钮随意添加图标

## 开始任务前检查列表

在编码之前，先输出以下内容：

1. **需求理解**：当前任务要做什么
2. **相似实现**：找到的已有相似页面/组件
3. **复用方案**：可以复用什么，需要新增什么
4. **文件清单**：计划新增或修改的文件
5. **状态模型**：页面需要哪些 state
6. **数据流**：数据从哪里来，操作后更新到哪里
7. **不会修改的内容**：明确不碰的文件
8. **验收清单**：怎么判断做完了
9. **设计规范检查**：逐项核对 `docs/UI_DESIGN_GUIDELINES.md`

# 附：任务生成模板（供参考）

以下模板在复杂页面任务时使用，简单任务无需套用。

## 列表页模板

```
请实现【页面名称】。

先阅读 AI_RULES.md、docs/UI_DESIGN_GUIDELINES.md，并搜索项目中质量最高的 Ant Design 表格页面作为基准。

业务目标：
【一句话说明用户要做什么】

查询条件：
- 字段A：模糊查询
- 字段B：下拉选择

默认表格列：
【6～10 个核心列】

操作：
- 查看详情：打开 Drawer
- 编辑：打开 Form Modal
- 其他：...

要求：
- 查询和重置必须真实生效
- 数据来自统一 mock 数据源
- 刷新页面后保留操作结果
- 表格列配置单独存放
- 状态颜色复用统一配置
- Drawer 和编辑 Modal 分别拆成组件
- 页面组件不超过 300 行
- 非核心字段放到详情 Drawer
- 操作成功后显示 message.success
- 提交期间禁用按钮
- 没有查询结果时显示 Empty
- 不新增依赖，不修改其他业务页面
```

## 审批页模板

```
请实现【页面名称】。

该页面属于资产变更审批流程，不要创建一套全新的审批页面结构。

开始前：
- 阅读 docs/UI_DESIGN_GUIDELINES.md
- 搜索现有审批页面
- 找出可以复用的组件
- 不允许复制完整审批页面后只替换标题

页面内容：
1. 单据基本信息
2. 申请人信息
3. 资产/变更信息
4. 变更前后对比
5. 变更原因
6. 审批时间线
7. 审批意见
8. 同意和驳回操作

业务规则：
- 只有 pending 状态显示审批操作
- 同意可不填写意见
- 驳回必须填写意见
- 操作后增加审批记录并更新状态
- 重复点击不能重复产生记录

工程要求：
- 审批操作不能直接写在展示组件中
- 审批状态和颜色不能在页面内重复声明
- 审批记录没有真实代理数据时不展示代理人列
- 流程操作按钮不带图标
```

## 仪表盘模板

```
请实现【页面名称】。

目标：展示当前演示数据中的资产运营情况。
所有数字必须由同一份数据计算得出，不能在不同卡片中分别硬编码。

核心指标：【资产总数、在用数、库存数、待报废数等】
图表：【状态分布、部门分布、趋势等】

数据约束：
- 指标由统计函数集中计算
- 不为每张图复制一份数据
- 日期计算统一使用现有工具（dayjs）
- 不新增图表库，优先使用项目已有能力

视觉要求：
- 保持现有 Ant Design 风格
- 颜色具有业务含义
- 桌面端演示优先，确保 1440×900 下完整可用
```
