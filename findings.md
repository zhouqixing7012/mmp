# 员工资产信息查询 PRD 整改发现

## 远端基线

- GitHub 分支：`feature/asset-inventory`。
- 本轮开始时本地 HEAD 与 `origin/feature/asset-inventory` 均为 `1e70d9db4704096f26a8592dc68db40179ab5214`。
- 主文档：`docs/PRD-资产管理-员工资产信息查询.md`。

## 用户确认的写作原则

- 固定结构：背景、权限、通用规则、页面字段及交互、校验、后续操作/业务结果/补充说明。
- PRD 使用业务语言，不写数据库表、SQL、类名、接口或技术字段。
- 字段需要写清业务含义、数据来源、展示或交互规则。
- 不使用“沿用现状”“相关字段”“后续处理”等模糊表述。
- 重构时保留细节，不合并不同业务场景，不删除异常。

## 已确认字段

### 耗材页签

1. 耗材标签号。
2. 耗材说明。
3. 耗材状态。
4. 耗材责任人。
5. 关联主资产标签号，仅展示文本。
6. 主资产资产说明。

### 合约号码页签

1. 行号。
2. 合约号码。
3. 合约号码说明。
4. 合约号码状态。
5. 责任人。
6. 责任人公司，取责任人人员主数据中的当前财务公司。
7. 所在单据编号。
8. 单据申请人。
9. 单据类型。
10. 单据业务类型。
11. 单据审批环节。
12. 单据审批人。

## 主要整改点

- 删除重复的“改造总结”。
- 将权限前置并改成最终规则。
- 删除状态代码、数据库排序字段、代码字段名和字典技术字段名。
- 补齐人员选择弹窗、查询、重置、页签切换、分页、空状态和详情入口规则。
- 明确耗材和合约号码新字段的来源、空值及点击规则。
- 将“申请单号”调整为能够覆盖独立业务单据的业务名称。
- 明确单据状态归类、一对多核心单据、历史单据和未配置详情的表现。

## 最终检查

- 主 PRD 六个一级章节与用户固定结构一致。
- 耗材列表字段 6 项，顺序与用户确认结果一致。
- 合约号码列表字段 12 项，空白占位列已删除，顺序与用户确认结果一致。
- 关联主资产标签号明确为普通文本。
- 责任人公司明确取人员主数据中的当前财务公司。
- 主 PRD 未残留旧状态代码、数据库排序字段、代码字段名和“改造总结”。
- 本轮没有修改 React 原型或其他生产代码。

## 移库 PRD 评审

- 评审基线为 GitHub `feature/asset-inventory` 分支上的 `docs/PRD-库存管理-移库.md` 和库存管理全模块三方走查文档。
- PC 规则明确验证完成后不可取消，移动端章节却写支持取消验证，规则冲突。
- 用户已确认冲突处理结果：PC 与移动端都不允许取消验证，验证后只能接收或驳回。
- 用户已确认删除“已取消”明细状态：草稿删除物资时直接移除明细并释放本流程锁，明细状态只保留草稿、待接收、已接收、已驳回。
- 用户已确认添加物资后实时保存，不保留【保存草稿】按钮；首次成功添加物资时生成草稿单号，后续单头、说明和明细增删改实时保存。
- 用户已确认发起列表在 PC 和移动端都按移出仓库权限展示，不限本人；仓库字段统一为移出仓库、移入仓库；通知状态、催办状态不展示；保留狐小E验证；只有已完成单据允许打印。
- 已在重构保留清单中补齐草稿单号生成、离开页面后的锁保留、接收列表、移动端页面、狐小E验证、通知催办、费用账户失败、接收 Excel 删除、反向移库路径和业务记录表述。
- 原正文中的组件名、变量名、事务类型代码、外部同步队列、历史布局对比和无效否定句不进入新版主 PRD；普通耗材不支持移库、资产锁禁止绕过等真实业务边界继续保留。

## 移库 PRD 重构保留清单

### 范围与权限

- 资产和低值耐用品按标签号或 SN 逐件移库，普通耗材不支持。
- PC 与移动端均支持发起、查看、添加、验证、分批接收和驳回；移动端不提供 Excel、打印和导出。
- 发起列表按移出仓库出库权限展示，接收列表按移入仓库入库权限展示，均不按创建人过滤。
- 公司、板块控制数据范围，仓库权限控制业务操作；打开单据和提交动作时重新校验。

### 仓库、状态与保存

- 一张单据固定一个移出仓库和一个移入仓库；两个仓库有效、同财务公司、均为一级仓库且不得相同。
- 正式调出后进入 V00001-集团在途总库，接收前不写入移入仓库位置。
- 单据状态：草稿、出库待接收、已完成、已驳回。
- 明细状态：草稿、待接收、已接收、已驳回。
- 首次成功添加物资生成单号并实时保存；后续单头、说明和明细增删改实时保存；无物资离开不生成空单。

### 锁、调出与接收

- 添加物资即加本流程锁；其他业务已锁定物资不得加入。
- 删除明细释放单行锁；删除草稿释放全单锁；已添加后离开页面继续保留草稿和锁。
- 提交前重新校验仓库、物资位置、状态、锁、类型、重复和在途库；任一失败整单不调出。
- 接收只处理已验证且待接收的勾选行；费用账户失败时本次勾选行整批不接收。
- 正常接收更新仓库、位置、虚拟库管员、费用账户、资产标记、接收人和接收时间，并生成移库调入记录。

### 验证、驳回与反向移库

- 支持扫码枪、手工输入、手工验证和狐小E验证；狐小E识别标签号后使用同一套扫码校验。
- 验证成功后不可取消；待接收物资后续只能接收或驳回。
- 待接收物资无需先验证即可驳回；驳回原因必填且最多 200 字。
- 每次确认驳回生成一张反向移库单，一次勾选多条合并生成一张。
- 反向单业务方向是原移入仓库退回原移出仓库，实际库存路径是 V00001 在途库进入原移出仓库，不再执行反向调出。

### 导入、导出、打印和通知

- 发起阶段 Excel 导入支持 `.xls`、`.xlsx`，整批校验；任一行失败全部不保存并输出错误结果。
- 删除接收 Excel，接收说明直接在接收明细维护。
- PC 列表和明细导出统一为 `.xlsx`，导出当前查询结果，不受勾选影响。
- 只有已完成单据显示打印；草稿、出库待接收、已驳回不显示打印。
- 正式调出后通知移入仓库全部有效负责人；反向单通知原移出仓库全部有效负责人。
- 从正式调出时间起算，第 8 天开始每日催办，全部明细完成接收或驳回后停止；页面不展示通知状态、催办状态。

### 历史与记录

- 历史详情固定展示移库发生时的信息，不随物资后续变化。
- 保留原单与反向单双向追溯。
- 正文使用“移库调出记录、移库调入记录”，不写事务类型代码。
- 删除未明确目标系统和数据内容的外部同步规则。

## 三类维护 PRD 联动整改

- GitHub 资产维护原规则：资产批量模板部分字段必填、可选字段空白会清空；资产卡片账务信息展示业务线和项目；详情曾被错误调整为独立页面。
- GitHub 耗材维护原规则：批量模板含板块、耗材说明、数量三项只读核对列；正式报废只能由报废业务产生；卡片原为 29 个字段。
- GitHub 合约号码维护原规则：身份证号码出现在单条编辑、卡片、批量模板、导出、操作历史和保存结果中；批量空白保留逻辑本身已经符合要求。
- 本轮只修改三份维护 PRD。其他模块的详情弹窗规则记录在 lessons，后续触及时同步。
- 主资产标签号关联耗材按资产维护实际变化同步成本中心、City、Building、Floor和状态；单条或批量任一同步失败时整体失败。
- 主资产联动是否生成耗材维护事务和操作历史待确认，本轮正式 PRD不新增该结论。
- 耗材无主资产时允许单条直接报废；有关联主资产时随主资产正式报废并继承报废信息。
- 合约号码历史身份证底层值保留，但维护功能不再展示、编辑、导出或写入操作历史。

## 2026-09-23 报废专项现状代码基线

- 资产管理正式菜单当前包含“资产报废 / 账面报废 / 资产处置”，统一由 `src/pages/assetManagement/index.js` 接入 `DocumentListPage` 列表，再进入对应编辑页。
- 资产报废编辑页为 `src/pages/ScrapApplicationEdit.js`；当前包含公司、资产大类、资产所在地、报废说明和可编辑报废资产明细，Excel 导入仍是演示占位。
- 账面报废编辑页为 `src/pages/AccountingScrapEdit.js`；当前代码包含报废类型、不同报废原因、附件以及跨公司转移相关字段/明细。
- 资产处置编辑页为 `src/pages/assetManagement/AssetDisposalEditPage.js`；当前包含处置说明、附件、资产选择和报废类型等字段。
- 仓库还保留独立旧报废链路：`ApprovalPage.js`、`ScrapInternalReview.js`、`ScrapProcurement1~4.js`，以及 `src/config/routes.js` 下的“报废管理”路由组。
- 机房资产旧页面 `AssetMaintenanceList.js` 左侧存在“资产报废”菜单，但当前主要是旧式静态页面结构；后续必须以用户提供的现状 PRD 和新流程重新确认机房报废正式入口、字段和审批链。
- 当前没有独立成体系的“资产报废 / 账面报废 / 资产处置”新版主 PRD，现有维护类 PRD只定义正式报废状态由报废业务产生，因此本轮需要从用户提供的现状 PRD建立正式基线。
