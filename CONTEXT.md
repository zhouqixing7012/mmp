# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成个人工作台资产申请、审批、配给、统一汇总及旧版领用流程的页面收口与交互调整。

## 1. 个人工作台菜单收口

`/yewurules` 下个人工作台仅保留以下菜单：

1. 工作台首页
2. 号码管理
3. 号码控制
4. 资产申请
5. 业务审批
6. 资产配给
7. 统一申请汇总-资产
8. ES前台领用
9. 员工领用确认

说明：
- “员工自助新版-资产申请”改名为“资产申请”。
- “员工自助新版-业务审批”改名为“业务审批”。
- “员工自助新版-资产配给”改名为“资产配给”。
- 未展示的旧页面和独立路由不删除。

## 2. 资产申请与业务审批

- 资产申请支持申请须知、资产/耗材多选、五级物料选择、申请用途、详细说明和超标判断。
- 预览为页面状态，超标资产数量按申请明细实时统计。
- 业务审批直接进入审批详情，展示申请人、申请物资和审批信息。
- 审批操作包括同意、驳回、返回和加签。

## 3. 资产配给

- 配给页面直接进入待配给详情。
- 支持库存领用、统一采购和匹配资产。
- 匹配资产弹窗支持标签号、序列号、板块和资产说明查询。
- 支持查看申请人名下资产。
- 页面区块顺序为：申请人信息、申请物资明细、ES配给处理、审批信息、审批操作。
- 同意、驳回、返回和加签操作栏统一放在页面最下方。

## 4. 统一申请汇总

- 列表页申请部门为蓝色链接，点击行为与“查看”一致。
- 汇总明细支持编辑 ES 建议、ES 汇总说明和项目用途说明。
- 超标申请和部门现资产参考使用量无数据时保留表头，并显示“无展示数据”。
- 导出按钮位于超标申请和非超标申请区域的公共上方。
- 导出内容为超标申请与非超标申请合并明细，字段为：申请人、物资类别、物料说明、配置、采购数量、预计费用(元)、详细说明、在用资产、ES建议。
- 点击提交进入统一申请汇总审批页，审批页复用汇总信息并全部只读展示。

## 5. ES前台领用

- 基于个人工作台原“ES前台领用”页面调整，不使用员工自助新版领用页。
- 申请人信息区域展示申请人基础信息，并包含可编辑的当前仓库和单据备注。
- 申请资产信息采用表格式展示：只读字段显示文本，可编辑字段直接在单元格内编辑。
- 资产标签号必填，点击放大镜打开物资列表弹窗修改。
- 物资列表弹窗复用资产配给的查询和单选交互，支持标签号、序列号、板块和资产说明查询。
- 城市、建筑、楼层使用地点数据维护口径；清空城市时同步清空建筑和楼层，修改建筑时清空楼层。
- 申请资产信息不展示“申请原因”字段。
- 点击领用确认时先执行表单必填校验，再校验资产卡片仓库与当前仓库：
  - 不一致时提示“资产不在当前库，请进行移库操作！”。
  - 一致时弹出“是否确认领用”。
  - 点击取消返回当前领用单页面。
  - 点击确定进入员工领用确认页面。

## 6. 员工领用确认

- 领用人信息仅保留使用人和部门。
- 展示领用物资明细、保管职责、刷卡输入和二维码。
- 固定提示为：`提示：我已阅读并确认保管职责说明，特此刷卡确认！`
- 员工卡号为空时不能确认领用。

## 7. 后台侧边栏

- 后台侧边栏菜单区域支持纵向滚动。
- 后台基础配置展开后，可正常滚动到组织管理及其下方菜单并进行选择。

# 本次关键决定

- 个人工作台采用精简菜单，旧页面和独立路由继续保留。
- 领用环节以个人工作台原 ES 前台领用和员工领用确认页面为本轮展示入口。
- 资产选择弹窗和查询栏优先复用现有资产配给页面交互。
- 当前仓库必须与选中资产所在仓库一致后才允许进入员工确认。
- 当前实现仍为前端演示，地点、仓库和资产列表数据来自统一 mock 数据。

# 上次停留位置

个人工作台资产申请至员工领用确认的主要展示页面已完成本轮收口，代码已提交到 `feat/business-rule-updates`。

下一步建议：

1. 本地执行构建和关键页面手工回归。
2. 检查精简菜单下 9 个页面的返回路径和页面标题是否统一。
3. 补充 ES 前台领用与员工领用确认的状态持久化。
4. 根据最终页面整理页面级 PRD 和上线验收清单。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 选择弹窗优先复用 `SelectModal` 或项目中已有同类弹窗。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能和独立路由。
- 入口文件最后更新，避免模块未齐时破坏分支构建。

# 验收路径

1. 打开 `/yewurules`，展开个人工作台，确认只显示 9 个目标菜单。
2. 展开后台基础配置，滚动侧边栏，确认组织管理及下方菜单可点击。
3. 进入“资产配给”，确认审批操作栏位于审批信息之后的页面最下方。
4. 进入“统一申请汇总-资产”，点击申请部门链接进入明细。
5. 检查导出按钮位于超标和非超标申请公共区域上方，并可导出合并明细。
6. 提交汇总单，确认进入只读审批页。
7. 进入“ES前台领用”，在申请人信息中维护当前仓库和单据备注。
8. 点击资产标签号放大镜，查询并选择资产。
9. 清空城市，确认建筑和楼层同步清空；修改建筑，确认楼层清空。
10. 选择与当前仓库不一致的资产并点击领用确认，确认出现移库提示。
11. 选择仓库一致的资产，点击领用确认并确定，确认进入员工领用确认页面。
12. 检查领用人信息仅展示使用人和部门，提示文案准确，未输入卡号时不能确认。
