# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图和已整理的历史逻辑文档只提取字段与业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 当前重点是 **库存管理 → 资产接收 / 耗材接收** 的接收链路，以及接收结果与入库草稿的衔接。

# 上次停留位置

- 资产接收 PO 详情创建接收单必须先勾选 PO 物资行；“创建接收单”放在 PO 物资明细 Card 右上角，创建成功直接进入接收单详情。
- 资产接收 PO 物资“编辑”弹窗已与耗材接收统一：物料、配置、接收数量、是否部件、部件数量、部件描述；接收数量受剩余可接收数量约束。
- 服务器、服务器备件、网络设备、网络设备备件的 PO 详情不再使用“执行入库”，改为 PO 物资明细 Card 右上角的“接收确认”；必须先勾选目标 PO 行。
- 机房资产“接收确认”一次生成 **已完成接收单 + 草稿采购接收入库单**；已处理 PO 行更新为已接收，草稿入库单可在「库存管理 → 入库」继续编辑并执行入库。
- 普通资产接收“维护接收明细”仍是页面级动作，按全部接收物资的本次接收数量逐件拆行；标签号/SN维护实时保存，接收确认要求标签号和 SN 均非空，“缺省”SN 视为已维护。
- 耗材接收采购类型固定为“低值耐用品 / 耗材”。低值耐用品沿用资产接收逐件维护链路；普通耗材仍按数量/接收行维护。
- 耗材接收单状态只保留“草稿 / 已完成”；接收明细已去除“资产标记”字段。
- 低值耐用品的生成标签号、维护SN号、删除明细、扫描写入均实时保存；接收确认前全部明细必须有标签号和 SN，SN 为“缺省”有效。
- 入库列表现有演示单据已补齐详情 Mock：**新增入库、采购接收、退库入库、借用归还**四种类型至少各有一张可进入详情并看到物资明细；现有两张退库入库演示单也都补有明细，避免点击任一历史演示单出现空详情。
- 创建出库单时“出库类型”固定默认为“领用出库”并只读；历史已有单据仍按原出库类型只读展示。
- 「库存管理 → 转移」列表的“创建转移单”按钮已改名为“创建”。

# 近期关键决定和原因

- 依赖表格勾选结果的动作放在对应表格 Card 操作区；因此电子设备“创建接收单”和机房资产“接收确认”都紧邻 PO 物资明细。
- 机房资产接收确认的业务结果是“接收已完成、入库待执行”，因此直接落一张已完成接收单，同时生成一张草稿采购接收入库单，而不是把两步混成已完成入库。
- 低值耐用品属于需要逐件形成标签号/SN的可管理实物，因此复用资产接收逐件维护逻辑；普通耗材仍保留数量型接收。
- 接收明细批量维护动作和扫码写入采用实时保存，避免再增加整页保存步骤。
- 原型中列表已经提供可点击详情入口时，演示数据必须覆盖所有主要业务分支并带完整详情行数据，不能只在列表层造单据头导致详情页为空。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”进入 PO 详情，“查看”进入接收单列表。
- 电子设备 PO 详情使用“创建接收单”；服务器、服务器备件、网络设备、网络设备备件使用勾选行后的“接收确认”，并生成已完成接收单 + 草稿采购接收入库单。
- 资产/耗材接收单列表的制单时间为单个日期范围查询，接收单号作为详情入口，不保留重复操作列。
- 入库、出库、移库、转移继续沿用此前已确认字段与状态规则。