# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成资产借用原型首版，并挂载到 `/yewurules` 的个人工作台。

## 资产借用菜单

新增以下 5 个个人工作台入口：

1. 资产借用
2. 借用配给
3. 借用审批
4. 借用发放
5. 员工借用确认

## 已实现页面与交互

- 资产借用申请：申请须知、申请人信息、允许借用物资多选、数量、借用期限快捷选择、借用原因、需求说明、保管职责确认和提交校验。
- 借用配给：办理仓库、实物资产匹配、员工名下资产查询、整单同意和整单驳回。
- 借用审批：申请与配给信息只读展示、同意、驳回、返回和加签。
- 借用发放：仓库和地点维护、重新匹配资产、确认方式、员工确认发起、执行出库和放弃领用。
- 员工借用确认：扫码、刷卡、输入工号三种确认展示，保管职责确认和身份一致性校验。
- 演示数据通过 `assetBorrowingService` 和 `demoStorage` 统一持久化，页面不直接操作 localStorage。

# 关键决定

- 资产借用相关原型全部放在个人工作台下，便于逐页验收。
- 物资选择、资产匹配和员工名下资产查询使用独立弹窗组件。
- 同一明细数量大于 1 时，提交后按数量拆成多行，但仍属于同一借用单。
- 续借不在本轮范围内；到期和逾期只保留规则，不改变单据状态或资产标记。
- 这是前端演示，通知、真实扫码、刷卡、库存锁定和出库接口均为页面流程模拟。

# 上次停留位置

资产借用 5 个页面、3 个查询选择弹窗、统一演示数据和菜单入口已完成代码实现。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能和独立路由。

# 验收路径

1. 打开 `/yewurules`，展开个人工作台，确认新增 5 个资产借用菜单。
2. 进入“资产借用”，完成须知、添加物资、期限、原因、说明和保管职责校验后提交。
3. 进入“借用配给”，选择仓库，可匹配资产或暂不匹配，查看员工名下资产并同意。
4. 进入“借用审批”，审批通过后生成库管员发放待办。
5. 进入“借用发放”，补齐实物资产、仓库、地点和确认方式，发起员工确认。
6. 进入“员工借用确认”，完成扫码/刷卡/工号确认。
7. 返回“借用发放”，刷新状态并执行出库，确认生成出库单号；也可验证放弃领用。
