# 当前状态

- 项目在 `main` 持续迭代，除用户明确要求外不新建分支或 worktree。
- 页面继续按已确认 PRD/截图校准；历史 ERP 截图只作为字段和业务规则来源，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 页面级保存/提交/确认/返回等操作统一放页面底部居中；Card 内只保留对应分区的添加、删除、导入、导出等操作。
- `DetailGrid` 的整行字段已统一修正：`span={3}` 必须从新行开始，标签和值保持同一视觉行；备注、使用说明等多行文本按此规则展示。
- 物资申请页字段口径已更新：原“申请用途”改为“申请原因”并移动到“关联主资产”之后，原“申请原因”改为“详细说明”；资产申请原因使用最多 400 字多行文本，耗材申请原因使用“日常办公使用 / 特殊项目采购 / 提升电脑配置使用 / 部门公共设备使用”四项下拉枚举。本轮只调整物资申请页，业务审批、资产配给等后续节点继续保持此前单独确认的字段展示口径。
- 库存管理“资产接收”PO 列表操作列固定同时展示“接收 / 查看”；PO 单号和“接收”都进入 PO 详情，“查看”进入该 PO 的接收单列表。
- 资产接收 PO 详情已按 UI V2.1 重画：页面主标题取消蓝色竖块，PO 单信息使用 `DetailGrid`，流程按钮统一放页面底部居中；PO 物资明细按最新确认的完整字段集合展示。
- 资产接收按采购类型区分底部动作：电子设备只显示“创建接收单”；服务器、服务器备件、网络设备、网络设备备件只显示“执行入库”。
- 资产接收“接收单列表”已改为单个“制单时间”日期范围查询；列表删除重复“操作”列，接收单号本身作为详情入口。
- 资产接收已新增“接收单详情”：接收单信息使用 `DetailGrid`，接收物资明细按最新截图字段展示。
- 库存管理“耗材接收”采购类型已统一为“低值耐用品 / 耗材”，不再使用“低耗耗材”命名。
- 耗材接收单列表已删除重复“操作”列，接收单号作为详情入口；制单时间使用单个日期范围查询条件。
- 耗材接收详情不展示自动入库结果；草稿态接收行可勾选，接收行级操作只保留“维护接收明细 / 删除接收行”。
- 低值耐用品接收明细页已将“扫描维护”改为常显单输入框，按“资产标签号 → SN号”顺序处理，扫码和手工录入共用同一入口。
- 库存管理“入库”列表的“制单日期从 / 至”已合并为单个“制单日期”日期范围查询；操作列按状态展示“草稿=编辑、已完成=打印”。
- 入库单草稿编辑页底部已新增页面级“执行入库”按钮；执行后单据状态改为“已完成”。
- 入库物资 Excel 导入入口仅对非“采购接收”类型展示；采购接收只能通过“待入库物资”选择现有接收资产。
- “选择待入库物资”弹窗已把“扫描光标”并入 QueryBar，支持扫码枪或手工录入资产标签号后回车查询；下方采用“左侧待入库列表 + 右侧已选择标签号”布局，右侧支持逐条删除并同步取消左侧勾选。
- “新增入库 → 添加物资”已删除库区、库位、资产级别、合同编号、用途；物资总类/大类/小类/品牌/型号/配置/单位由物资说明带出只读；City/公司按当前仓库带出只读，Building 可改；费用账户自动生成只读；成本中心随责任人带出但可编辑；服务器/网络设备及其备件额外展示“服务 / NO位置”。
- 新增入库、退库入库、借用归还的添加物资弹窗均提供“添加并继续 / 添加并关闭”；“添加并继续”新增物资后保持弹窗打开。
- “借用归还 → 添加物资”已删除印刷号、续借次数、库区、货位、鉴定结果；归还数量改为只读。
- “退库入库 → 添加物资”已删除印刷号、库区、货位、鉴定结果；退库数量改为只读，物资信息中的资产状态固定展示“在用-使用中”。
- 库存管理“出库”列表的“制单日期从 / 至”已合并为单个“制单日期”日期范围查询，列表已删除“操作”列，出库单号本身作为单据入口。
- 出库单草稿态页面底部固定为“保存草稿 / 执行出库 / 返回”；已完成状态固定为“打印 / 返回”，并按只读详情展示。
- 出库添加物资弹窗已删除库区、库位；业务区资产状态改为可编辑，出库数量改为只读带出资产数量。
- 库存管理“移库”发起/接收列表已将资产扫描并入查询条件；制单日期统一为单个日期范围查询；列表不保留重复操作列，移库单号作为详情入口。
- 移库单新增页已删除“移库类型”；新增时当前仓库可编辑；编辑页资产扫描改为移库物资的快速筛选查询条件。
- “移库 → 添加物资”已删除移库数量、接收仓库、库区、货位、品牌、规格型号、连续添加；新加物资的移库状态固定为“草稿”。
- 移库接收详情不再展示“发起单据 / 接收单据”页签；接收物资不保留操作列，资产标签号仍可进入资产明细。
- 移库接收确认与移库驳回均按勾选行处理，不再整单直接处理；仅当所有行处理完成时同步收敛单据状态。
- 库存管理“转移”已切到独立 `TransferPage.js`：保留原查询列表，新增转移单创建页和添加转移物资弹窗。
- ERP icon 库 `y8BydLFT5vQpnDMX9SpDzB` 节点 `1973:350` 已检查；当前后台四个一级菜单存在合适候选，本轮只做评估，尚未替换代码图标。

# 当前停留位置

- “库存管理 → 移库”当前发起/接收列表都使用“移库单号、单据状态、制单人、制单日期、资产扫描”作为主要查询条件；资产扫描本质是资产标签号/SN号快速筛选。
- 发起列表和接收列表均删除重复操作列，点击蓝色移库单号进入对应单据；接收详情打开后不再显示发起/接收页签。
- 新建移库单时当前仓库可编辑，接收仓库继续作为单据级字段；物资区资产扫描只筛选当前单据物资。
- 移库添加物资弹窗不展示移库数量、接收仓库、库区、货位、品牌、规格型号、连续添加；物资加入单据时移库状态为“草稿”。
- 移库接收详情的“移库接收确认 / 移库驳回”只作用于当前勾选行；行处理后保留在详情页继续处理其他行。
- “库存管理 → 出库”当前规则已同步到 `docs/PRD-库存管理-出库.md`：列表制单日期使用一个 RangePicker，不保留操作列；草稿通过出库单号进入编辑，已完成通过出库单号进入只读详情。
- 草稿出库单底部操作固定为“保存草稿 / 执行出库 / 返回”；执行出库后状态进入“已完成”；已完成单据底部只显示“打印 / 返回”。
- 出库添加物资弹窗当前不展示库区、库位；领用/借用业务区的资产状态均为可编辑字段，默认分别为“在用-使用中 / 在用-借用中”；出库数量只读带出当前资产数量。
- “库存管理 → 入库”当前规则已同步到 `docs/PRD-库存管理-入库.md`：列表查询使用单个“制单日期”范围控件；草稿操作为编辑，已完成操作为打印；草稿编辑页底部有“执行入库 / 保存草稿 / 返回”。
- “借用归还 → 添加物资”当前字段不含印刷号、续借次数、库区、货位、鉴定结果；归还数量直接带出为只读。
- “退库入库 → 添加物资”当前字段不含印刷号、库区、货位、鉴定结果；退库数量直接带出为只读；原资产状态展示“在用-使用中”，入库后的目标状态仍为“在库-待处理”。
- 非采购接收的添加物资弹窗统一提供“取消 / 添加并继续 / 添加并关闭”。
- “采购接收 → 选择待入库物资”弹窗的扫描光标属于普通查询条件，不再单独成 Card；右侧“已选择”只展示资产标签号并可逐条删除。
- “新增入库 → 添加物资”当前重点是字段可编辑性区分：物资主数据衍生字段只读，仓库衍生的 City/公司只读，Building 可编辑，责任人选择带出部门/成本中心，成本中心仍可修改，费用账户只读自动生成。
- “库存管理 → 耗材接收 → 接收单详情”信息字段按最新截图为：接收单号、接收人、接收时间、PO单号、供应商、供应商联系电话、PO单说明、采购单位、采购员联系电话、板块、部门、采购员、合同主体、订单日期、接收单状态、申请批次。
- 草稿态“接收行明细”选择框可直接勾选；“维护接收明细”和“删除接收行”都只作用于勾选行，删除时同步释放对应草稿占用数量。
- “接收明细”当前仍使用已确认字段；用户已说明后续字段会更多，但本轮未给完整新增字段清单，因此未补造，已记录在 `docs/PRD-库存管理-耗材接收.md`。
- “库存管理 → 资产接收 → 接收单列表”查询条件中的制单时间使用一个 RangePicker；列表只通过接收单号进入详情，不再保留仅含“查看”的操作列。
- “库存管理 → 资产接收 → PO详情”物资明细字段为：选择、行号、操作、接收状态、物资总类、资产大类、物料编码、物料说明、PO单说明、配置、部件数量、部件说明、本次接收数量、采购数量、不含税单价、不含税金额小计、总税额、含税单价、含税小计、税率、已接收数量、草稿数量、约定到货日期、PR单/行号、SA单/行号、申请单号、部门、业务线。
- `review/rd-review` 仍只保存阶段性已确认研发评审结论，不自动跟随 `main`。

# 近期关键决定

- 业务规则优先于设计系统；设计系统决定视觉，不反向改变字段、状态机和流程。
- 状态统一通过 `StatusTag`，详情字段优先使用 `DetailGrid`，查询区使用 `QueryBar + QueryItem`。
- 历史 ERP 页面继续只提取字段、表头和明确规则，不复刻旧版蓝色界面。
- 同一业务查询条件是一个日期区间时，统一使用一个日期范围控件，不拆成“从 / 至”两个 QueryItem。
- 列表若单据号本身已经是唯一详情/编辑入口，就不再增加只有一个重复动作的“操作”列；当前出库、移库列表按此规则处理。
- 扫码枪输入如果本质是已有查询字段的快速录入方式，则放入统一 QueryBar，并通过回车触发查询，不额外创建“扫描”业务分区；移库发起/接收列表和移库物资筛选均按此处理。
- 移库接收是行级处理：接收确认与驳回都必须基于勾选行执行，不再默认整单处理；资产标签号本身已能进入明细时不再额外保留“查看”操作列。
- 出库单状态决定页面级动作：草稿固定“保存草稿 / 执行出库 / 返回”，已完成固定“打印 / 返回”；已完成单据不再保留可编辑物资入口。
- 出库添加物资时，库区/库位不属于当前维护字段；出库数量由当前资产数量决定时只读带出，目标资产状态属于当前节点可维护字段。
- 表格操作列可按状态承载不同真实动作；当前入库单固定为“草稿=编辑、已完成=打印”。
- 多选弹窗需要持续确认当前选择集合时，优先采用“结果列表 + 右侧已选择区”布局；右侧删除与左侧勾选状态必须双向同步。
- 物资说明确定后能够唯一带出的物资主数据字段必须只读展示，不继续伪装成可编辑输入框；仓库/责任人等上游字段带出的结果同理按业务是否允许覆盖决定只读或可编辑。
- 采购接收入库不提供 Excel 导入；其他入库类型保留 Excel 导入入口。
- 入库草稿的最终业务动作是页面级“执行入库”，放底部居中，与保存草稿、返回同级。
- 退库入库和借用归还中的退库/归还数量由待处理资产数量决定时直接只读带出，不再提供可编辑数量输入框。
- 非采购接收的添加物资弹窗统一提供“添加并继续 / 添加并关闭”，避免用户连续添加时反复关闭重开弹窗。
- 资产接收 PO 列表操作列有两个独立入口：**接收 / 查看**；接收=PO 详情，查看=接收单列表。PO 单号本身也固定进入 PO 详情。
- 接收单列表若“查看”与接收单号跳转完全重复，则删除操作列，直接以接收单号作为详情入口。
- 资产接收 PO 详情的采购类型动作互斥：**电子设备 → 创建接收单；服务器 / 服务器备件 / 网络设备 / 网络设备备件 → 执行入库**。
- 耗材接收采购类型固定为 **低值耐用品 / 耗材**；“低耗耗材”不再作为采购类型名称。
- 耗材接收详情不展示自动入库结果；草稿接收详情的接收行级业务动作固定为 **维护接收明细 / 删除接收行**，两者都以当前勾选接收行为作用范围。
- 扫描维护属于接收明细页常驻输入能力，不再使用 Modal；同一输入框严格按“资产标签号 → SN号”两步处理，既支持扫码枪也支持人工键盘输入。
- PO 详情页面结构统一为“页面标题 → PO 单信息 Card → PO 物资明细 Card → 底部流程操作”，不把流程按钮塞进信息 Card 右上角，也不在页面主标题前加蓝色竖块。
- 必填编辑字段的星号必须单独用红色展示；只读字段直接展示文本，只有当前节点真实可维护字段使用输入控件。
- 多行文本字段必须独占完整新行，禁止通过空文本、拉宽相邻字段等方式补布局。
- Figma icon 库候选只有在明确决定替换后再导出/接入项目，不先混用临时图标。