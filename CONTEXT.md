# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 当前重点

后台资产管理开始按现系统截图逐页重构：
- 资产管理菜单包含：资产维护、耗材维护、合约号码维护、标签打印、公司间转移、资产报废、账面报废、资产处置、员工资产信息查询。
- 已完成“资产维护”：`src/pages/assetManagement/AssetMaintenancePage.js`。
- 已完成“耗材维护”：`src/pages/assetManagement/ConsumableMaintenancePage.js`。
- 已完成“合约号码维护”：`src/pages/assetManagement/ContractNumberMaintenancePage.js`。
- 三个维护类页面共用 `src/pages/assetManagement/LedgerMaintenancePage.js`，避免复制整页实现。
- 其余 6 个菜单仅建立入口，等待对应截图和字段后逐页补充，不自行猜测业务字段。

账面报废申请页与账面报废审批页此前已完成一轮重构：
- 申请页：`src/pages/AccountingScrapEdit.js`
- 审批页：`src/pages/ApprovalPage.js`

# 后台资产管理当前口径

## 菜单

- 现有一级菜单“资产管理”改为可展开菜单。
- 子菜单顺序固定为：资产维护、耗材维护、合约号码维护、标签打印、公司间转移、资产报废、账面报废、资产处置、员工资产信息查询。
- 点击“资产管理”默认进入“资产维护”。

## 资产维护

- 查询条件按旧系统截图还原：标签号、公司、部门、资产责任人、序列号、资产说明、资产类别、资产状态、板块、成本中心、用途、仓库、资产类型、主资产标签号。
- 公司、部门、资产责任人、资产类别、资产状态、成本中心、仓库使用统一选择弹窗；板块、用途、资产类型使用下拉框。
- 操作按钮仅保留“编辑”“导出”。
- 列表字段固定为：选择、标签号、公司、板块、资产大类、资产小类、资产说明、品牌、数量、原值、净值、资产责任人编号、资产责任人、资产状态、成本中心、仓库、启用日期、资产类型。
- 查询、重置真实生效；导出按当前查询结果生成 CSV。
- 编辑要求单选一条资产，演示修改通过 `assetManagementService` 写入 `demoStorage`，刷新后保留。
- 演示数据仅使用截图中能够确认的内容；截图未提供的字段展示为“-”，不补造业务数据。

## 耗材维护

- 查询条件按旧系统截图还原：耗材标签号、公司、部门、当前责任人、主资产标签号、耗材说明、耗材类别、耗材状态、板块、PO单号、用途、仓库。
- 公司、部门、当前责任人、耗材类别、耗材状态、仓库使用统一选择弹窗；板块、用途使用下拉框。
- 操作按钮仅保留“编辑”“导出”，不保留旧系统中的批量修改、模板下载、配置等按钮。
- 列表字段固定为：选择、耗材标签号、公司、板块、资产大类、资产小类、资产说明、品牌、主资产标签号、数量、原值、净值、资产责任人编号、资产责任人、资产状态、成本中心、仓库、启用日期。
- 查询、重置真实生效；导出按当前查询结果生成 CSV。
- 编辑要求单选一条耗材，演示修改通过 `assetManagementService` 写入 `demoStorage`，刷新后保留。
- 耗材演示数据来自用户截图可见内容；净值、成本中心、仓库、启用日期、部门、PO单号、用途等截图无法确认的数据统一展示“-”。

## 合约号码维护

- 查询条件按旧系统截图还原：标签号、金额、状态、说明、SN号、副卡、责任人、公司、部门、领用日期从、领用日期至、资产小类。
- 状态、资产小类使用下拉框；责任人、公司、部门使用统一选择弹窗；领用日期从/至使用日期选择器。
- 操作按钮沿用当前维护类页面规则，仅保留“编辑”“导出”。
- 列表字段固定为：选择、标签号、副卡、资产小类、品牌、资产说明、配置、金额、数量、状态、SN号、仓库、启用日期、资产责任人、身份证号、职级、子公司、部门、领用时间、备注。
- 查询、重置真实生效；领用日期支持起止范围筛选；导出按当前查询结果生成 CSV。
- 编辑要求单选一条合约号码，演示修改通过 `assetManagementService` 写入 `demoStorage`，刷新后保留。
- 合约号码演示数据仅使用截图可确认的标签号、副卡、资产小类、品牌、资产说明、配置、金额、数量、状态、SN号、仓库等内容；责任人、身份证号、职级、子公司、部门、领用时间、备注等无法确认的数据统一展示“-”。

# 账面报废当前口径

## 申请页

- 公司使用统一 `SelectModal` 弹窗选择。
- 新增：是否公司间转移、已到报废期报废原因、未到报废期报废原因、丢失报废原因。
- “是否公司间转移”使用普通单选按钮，默认“否”；选择“是”才展示公司间转移明细。
- 公司间转移明细字段：资产标签号、新责任人、新公司、新板块、新成本中心、City、Building、Floor、调账后仓库。
- 资产标签号由上方资产明细自动带入，不可编辑；公司间转移明细不能独立新增或删除资产，资产集合始终与资产明细一一对应。
- 除“调账后仓库”外，其余公司间转移字段均必填。
- 新公司、新板块、新成本中心、City、Building、调账后仓库均使用弹窗选择；Floor 使用下拉框。
- 新板块复用后台配置板块数据，新成本中心复用成本中心数据，City/Building 复用地点基础数据，调账后仓库复用仓库信息数据。
- City 变化时清空 Building/Floor；Building 变化时清空 Floor，避免保留不匹配地点。
- 页面顶部说明文字已删除；公司间转移明细原说明条已删除。“与资产明细保持一一对应”改为 Card 标题旁气泡提示，hover 展示。
- 申请页以 Ant Design Card / Descriptions / Table 为主，字段、必填规则、资产增删联动逻辑保持不变。
- 基本信息、报废原因、资产明细、公司间转移明细等 Card 标题与审批页统一使用“蓝色竖条 + 16px 加粗标题”样式。

## 审批页

- 审批页按申请页已填写结果只读展示，不再重复提供公司间转移相关字段编辑能力。
- 当前演示单据“是否公司间转移=是”。
- 基本信息只读展示：申请单号、公司、单据状态、制单人、制单时间、是否公司间转移、备注、附件。
- 报废原因只读展示三类：已到报废期报废原因、未到报废期报废原因、丢失报废原因。
- “公司间转移明细”只读展示资产标签号、新责任人、新公司、新板块、新成本中心、City、Building、Floor、调账后仓库；资产标签号按普通黑色文本展示。
- 报废资产明细展示字段保持原口径不变，并继续按“已到报废期 / 未到报废期 / 丢失”三类分别汇总和展示。
- 每个报废类型继续展示该类型总数量、原值合计、净值合计，并在类型内部继续按服务器、服务器（配件）、网络设备、网络设备（配件）等分组汇总。
- 服务器、服务器（配件）、网络设备、网络设备（配件）等分组默认折叠，只展示组名及数量/原值/净值汇总；点击分组后再展开原明细表格。切换报废类型时重新收起分组。
- 基本信息、报废原因、报废资产明细、公司间转移明细、审批意见等 Card 标题统一使用更明显的蓝色竖条 + 加粗大字号分区标题。
- 附件在 hover 时展示下载按钮，点击可触发演示下载并给出反馈。
- 审批意见保留；驳回时审批意见必填。

# 其他已确认规则

- 本次资产系统重构全面取消电子签确认，员工确认统一只保留刷卡/员工工号和狐小 e 扫码。
- 页面不能因为数据对象存在字段就全部展示，展示字段以当前确认口径为准。
- 老系统截图用于确认业务字段和流程，视觉结构按当前项目规范重构。
- 用户在项目语境中说“原型/原型图/放到个人工作台”时，默认交付仓库内可运行页面。

# 上次停留位置

后台“资产管理”九个子菜单已建立；“资产维护”“耗材维护”“合约号码维护”已完成。下一步等待用户提供“标签打印”等其余菜单的现系统截图和字段，继续逐页生成。

# 验收路径

## 后台资产管理 / 资产维护

1. 打开 `/yewurules`。
2. 点击左侧“资产管理”，应展开 9 个子菜单，并默认选中“资产维护”。
3. 资产维护查询区应展示 14 个查询条件；点击查询、重置均应真实生效。
4. 列表仅展示确认的 17 个业务字段，加最左侧“选择”列。
5. 操作区仅有“编辑”“导出”。
6. 未选择或选择多条资产时点击编辑应拦截；单选一条后可打开编辑弹窗并保存。
7. 点击导出应按当前筛选结果下载 CSV。

## 后台资产管理 / 耗材维护

1. 在“资产管理”下点击“耗材维护”。
2. 查询区应展示 12 个查询条件：耗材标签号、公司、部门、当前责任人、主资产标签号、耗材说明、耗材类别、耗材状态、板块、PO单号、用途、仓库。
3. 列表仅展示确认的 17 个业务字段，加最左侧“选择”列。
4. 操作区仅有“编辑”“导出”。
5. 未选择或选择多条耗材时点击编辑应拦截；单选一条后可打开编辑弹窗并保存。
6. 点击导出应按当前筛选结果下载 CSV。
7. 刷新页面后，已编辑的耗材演示数据仍应保留。

## 后台资产管理 / 合约号码维护

1. 在“资产管理”下点击“合约号码维护”。
2. 查询区应展示 12 个查询条件：标签号、金额、状态、说明、SN号、副卡、责任人、公司、部门、领用日期从、领用日期至、资产小类。
3. 领用日期从/至使用日期选择器，并按领用时间进行范围筛选。
4. 列表仅展示确认的 19 个业务字段，加最左侧“选择”列。
5. 操作区仅有“编辑”“导出”。
6. 未选择或选择多条合约号码时点击编辑应拦截；单选一条后可打开编辑弹窗并保存。
7. 点击导出应按当前筛选结果下载 CSV。
8. 刷新页面后，已编辑的合约号码演示数据仍应保留。
9. 点击其余 6 个未完成子菜单，应能正常切换入口，不展示未经确认的业务字段。

## 账面报废申请页

1. 打开 `/` → “账面报废申请单”。
2. 页面顶部不展示说明文案。
3. “是否公司间转移”为普通“否/是”单选按钮。
4. 选择“是”后展示公司间转移明细，标题旁信息图标 hover 显示“与资产明细保持一一对应”。
5. 新板块、新成本中心、City、Building、调账后仓库点击后均打开对应选择弹窗；Floor 为下拉框。
6. 公司间转移明细无说明条、无新增/删除按钮，资产标签号只读且随资产明细同步。
7. 除调账后仓库外，任一必填字段为空时提交应拦截。
8. 基本信息、报废原因、资产明细、公司间转移明细标题均展示蓝色竖条和 16px 加粗标题。

## 账面报废审批页

1. 打开 `/approval` → “账面报废审批”。
2. 基本信息、报废原因、报废资产明细、公司间转移明细、审批意见标题均有明显蓝色竖条和加粗标题。
3. 基本信息、三类报废原因均按申请结果只读展示，“是否公司间转移”为“是”。
4. 附件 hover 时出现下载按钮，点击有下载动作和成功反馈。
5. 报废资产明细仍分“已到报废期 / 未到报废期 / 丢失”三个标签页展示，并保留类型级数量/原值/净值汇总。
6. 各资产分组默认折叠，只显示组名及数量/原值/净值汇总；点击后展开原审批页明细表格。
7. 报废资产明细字段与原审批页保持一致。
8. 页面展示只读“公司间转移明细”，资产标签号使用普通黑色文本且与报废资产明细对应。
9. 审批意见可填写；驳回时未填写审批意见应拦截。
