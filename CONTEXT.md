# 当前状态

- 项目为企业资产管理产品演示前端，持续在 `main` 迭代；除用户明确要求外不新建开发分支或 worktree。
- 个人工作台、资产管理、库存管理、资产盘点主要原型已建立，继续按 PRD、截图和已确认业务口径逐页校准。
- 原型标注体系已进入研发评审阶段；规则继续遵循“PRD Requirement Atom → 最细业务 target → annotation → Coverage Ledger → Granularity/Coverage Check”，找不到稳定目标时进入 `review`，不粗绑 Card。
- 资产盘点正式入口已统一为“盘点项目 / 盘点报表”；盘点项目沿用去机房范围方案，历史方案仅保留代码不再展示；复盘全面取消内审监督人，仅保留财务监督人。
- 员工确认全面取消电子签，只保留刷卡/员工工号与狐小 e 扫码，并校验到真实申请人。
- 页面级操作（保存/提交/确认/返回/生成等）统一放页面底部居中；Card/表格内的导出、删除、局部查询等分区级操作保留在对应分区。
- 数量/计数统一千分位；金额/原值/净值等财务数字统一千分位 + 2 位小数。
- 2026-08-25 已将 ERP Figma `基础组件｜Library 修正版`（node `826:2`）新增规则补充到 `docs/UI_DESIGN_GUIDELINES.md`，规范升级为 V2.1。
- V2.1 已确认覆盖 Input、Radio、Checkbox、Switch、Breadcrumb、Dropdown、Pagination、Tag、Badge、Popover、Grid、Layout、Divider、Anchor、Affix、FloatButton；Select、Table、Form、DatePicker、Upload 仍未确认完整 ERP 本地规范。
- V2.1 新增关键边界：Dropdown ≠ Select；Tag 不替代 `StatusTag`；ERP Grid 管页面级布局、`DetailGrid` 管业务详情三列；Divider 优先让位于留白；语义 Token 优先于页面直接使用基础色号。
- 2026-08-25 开始按 V2.1 收敛正式资产盘点 UI：新增统一 `SectionCardTitle`，首批覆盖盘点项目列表、盘点计划、盘点进度、图片审核、V3 盘点范围；盘点计划项目信息统一改用 `DetailGrid`；图片审核概览取消 Card 套 Card，改为单 Card 三列留白结构。
- 2026-08-27 资产配给与借用配给的“员工名下资产明细”已统一删除“物资总类”查询条件和列表列；耗材配给同类弹窗原本就没有该字段，无需调整。
- 2026-08-27 资产配给申请人信息中的公司展示已改为“新媒体”，仅影响资产配给页面。
- 2026-08-27 统一申请汇总明细页字段口径调整：`详细说明` 改为 `申请原因`，`超标` 改为 `是否超标`；仅调整明细页展示名称，不改底层字段和流程逻辑。
- 2026-08-27 统一申请汇总编辑页“超标申请 / 非超标申请”新增申请部门列，部门沿用明细页同一申请人的申请部门；编辑页表头统一为“资产小类 / 资产说明 / 申请原因”，导出申请明细同步该口径，审批页表头保持原样。
- 2026-08-27 统一申请汇总提交后的“统一申请汇总审批”确认仍为审批页，底部操作恢复为“同意 / 驳回 / 返回”，不再按纯查看页处理。
- 2026-08-27 ES前台领用“申请资产信息”字段口径调整：城市/建筑/楼层改为 `city` / `building` / `floor`，删除“所在仓库”，并在“启用日期”后新增只读“备注”；申请人信息中的“当前仓库”及跨库校验逻辑保持不变。
- 2026-08-28 新员工领用“使用人信息”删除领用单号与备注，制单时间调整到使用人之后、单据状态之前，并保留 City 起始行不前移；“领用物资明细”删除物资总类和操作列，“大类”改为“资产大类”并新增“资产小类”，“物资说明”改为“资产说明”；资产标签号选择弹窗补充横向滚动，修正末列“所在仓库”超出弹窗内容宽度时的背景显示异常。
- 2026-08-28 Modal 内表格新增宽度检查规则：必须核算选择列、固定列和普通列总宽度；超过内容区时显式配置匹配的横向滚动或调整容器/列宽，并检查最右列背景和边框完整。
- 2026-08-28 合约号码 ES 配给“附件信息”升级为多附件：支持一次选择多个文件并持续追加，使用独立附件 Card + “附件名称 / 附件大小 / 操作”表格展示，支持逐条删除；同时兼容历史单附件 `allocationAttachment` 数据，审批提交仍要求至少存在一个配给附件。
- 2026-08-28 合约号码申请 → ES 配给 → 配给主管审批已改为同一张单据顺序流转：员工申请支持多附件并同步到 ES 配给，ES 同意后直接进入主管审批并完整带入申请附件、配给附件、号码与审批历史，主管审批完成后该主管待办消失；该演示链路改为单次 SPA 会话内保存状态，整页刷新回到初始演示态。选择号码弹窗查询条件改为“合约号码”，列表表头固定为“序号 / 标签号 / 合约号码 / 套餐金额”。
- 2026-08-28 合约号码选择弹窗的单一“合约号码”查询条件改为占满 QueryBar 可用查询宽度，避免默认三列栅格压缩；ES 配给与主管审批“申请信息”里的员工申请附件改为普通“附件”详情字段，多附件逐行展示“附件名称 + 下载”超链，不再使用附件表格或“操作”列，ES 配给阶段自身“附件信息”仍保留附件大小列。
- 2026-08-28 ES 配给与主管审批“申请信息”进一步明确附件布局：身份证号码改为整行，附件紧接下一整行，彻底避免附件字段名与身份证号码视觉同行；员工合约号码领取确认页的提示和保管职责已替换为最新确认文案。
- 2026-08-28 字段口径处理新增约束：当用户给出的名称与当前业务语义明显冲突、疑似手误或有多种合理解释时，不机械照改；能从上下文唯一判断时先提醒再按一致口径处理，不能唯一判断时再确认。
- 2026-08-31 号码控制“所属部门”长部门口径修正：从子公司层开始到末级部门，第一层只使用 `集团 / 媒体 / 焦点 / 视频`，不展示法人公司全称；展示示例为 `集团.ERP部.业务产品二组`，授权人员列表及新增授权人员弹窗口径一致。
- 2026-08-31 耗材审批“申请耗材信息”在数量后新增“参考价格”，直接复用申请行 `referencePrice` 并按金额两位小数展示；原“申请原因 / 详细说明”表头分别改为“申请用途 / 申请原因”，底层 `reason / detail` 数据和审批流程不变。
- 2026-08-31 耗材领用方案一、方案二的地理位置字段标签统一改为 `city` / `building` / `floor`；方案二进一步删除“使用用途”及其 state/枚举/提交校验；方案一“物料编码”和方案二“耗材标签号”改为只读文本展示，不再允许编辑或参与用户输入必填校验。
- 2026-08-31 耗材汇总明细表字段口径调整：原“物资类别 / 物料说明 / 详细说明”分别改为“耗材小类 / 耗材说明 / 申请原因”，底层继续使用 `category / materialDesc / detail`。
- 2026-08-31 耗材汇总后续“申请明细”进一步统一字段口径：原“物资类别 / 物料说明”改为“耗材小类 / 耗材说明”，导出表头同步；耗材汇总审批拆为独立 `ConsumableSummaryApprovalPage`，申请明细右上角新增“导出”按钮，导出审批中的已通过申请明细，审批动作仍为“同意 / 驳回 / 返回”。
- 2026-09-03 “员工名下资产明细”弹窗进一步统一删除“资产用途”查询条件及对应隐藏筛选逻辑；当前实际使用该弹窗的借用配给、资产配给均已同步，资产更换目录下未被页面引用的同名弹窗组件也同步清理，避免后续启用时恢复旧条件。
- 2026-09-03 借用发放“借用资产明细”的位置字段标签统一改为 `city / building / floor`；资产说明按“品牌.型号”展示，通过去除当前资产小类前缀实现，例如 `笔记本电脑.联想.ThinkPad T14` 展示为 `联想.ThinkPad T14`，不修改底层资产数据。
- 2026-09-03 资产更换申请“退回资产信息”的资产说明改为“品牌.型号”，不再展示资产小类前缀；更换须知仅保留第一条。资产更换办理“更换物资信息 / 待发放资产信息”的位置字段标签统一改为 `city / building / floor`，申请人姓名旁新增“查看名下资产”入口并正式接入员工名下资产明细弹窗。
- 2026-09-03 领导退库审批“申请人信息”已与资产申请业务审批对齐：字段及顺序统一为“申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门”，删除原“退库类型”，最后新增整行“退库原因”；当前公司展示固定为 `114.新媒体`。同时明确普通业务“公司”字段使用公司口径，集团/媒体/视频/焦点属于子公司/组织层级，不能作为普通公司字段值。
- 2026-09-03 “申请人信息”全局默认字段进一步固定为“申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门”，默认不再展示板块、退库类型、员工状态等身份扩展字段；业务流程明确需要的“退库原因 / 申请原因”等原因字段继续保留在默认字段后并独占整行。资产退库办理已恢复最后一整行“退库原因”，公司为 `114.新媒体`，申请人旁保留“查看名下资产”，退库资产信息位置字段为 `city / building / floor`；合约号码退库办理申请人信息也已统一为默认 7 字段 + 最后一整行“退库原因”，删除板块/成本中心并补齐邮箱，公司为 `114.新媒体`。
- 2026-09-03 已按当前工作台实际入口完成“申请人信息”静态全量审计，共核对 25 个页面；审计只记录缺失字段、字段名不一致和额外身份/业务字段，本轮不批量修改这些页面。统一申请汇总-资产的审批视图已在“超标申请”上方增加“导出申请明细”按钮，复用原有 CSV 导出逻辑，编辑视图原导出能力保持不变。
- 2026-09-04 申请人信息审计项继续收敛：ES前台领用删除成本中心；耗材领用方案一/二删除成本中心并将“使用人”改为“申请人”；员工借用确认、员工资产确认将“使用人”改回“申请人”；退库审批补齐公司/办公区并删除退库类型，退库原因保留；合约号码库管员待办删除板块/成本中心。联系电话 Mock 新增统一口径：业务申请人和用户信息的联系电话使用座机号，手机号/手机号掩码在运行态归一为座机；合约号码等号码资产本身不受影响。
- 2026-09-04 联系电话座机修复进一步下沉到 `demoStorage` 读写：历史 localStorage 中已保存的手机号/手机号掩码会在读取时自动转换并回写为座机，避免退库审批、资产退库办理等持久化页面继续显示旧手机号。借用发放“申请人信息”改用 `DetailGrid`，三列字段标签统一固定宽度；业务审批节点“申请人信息”的公司固定展示 `114.新媒体`，仅影响业务审批页面，不修改共享申请数据。
- 2026-09-04 借用发放跨 Card 三列对齐进一步修正：申请人信息与借用资产明细统一使用 `DetailGrid`，不再混用 Antd `Descriptions`；员工资产确认的领用确认场景在“确认提示及保管职责”中新增红字保管职责正文，退库确认场景不展示该领用职责。
- 2026-09-04 业务审批“申请资产信息”字段口径更新：原“详细说明”展示名改为“申请原因”，底层继续使用 `detail`，审批逻辑不变；`docs/PRD-EMPLOYEE-ASSET-SELF-SERVICE.md` 的业务审批章节已同步该字段名。

# 当前停留位置

- UI 规范已同步最新 Figma 基础组件规则，后续新增/重画页面默认按 `docs/UI_DESIGN_GUIDELINES.md` V2.1 执行。
- 资产盘点首批三项纯 UI 收敛已落代码，未修改字段、流程、状态机、Mock 和操作行为；后续继续按具体任务逐项收敛，不批量改业务。
- 统一申请汇总明细页、编辑页和提交后审批页已按最新口径同步：审批页底部保留“同意 / 驳回 / 返回”，且“超标申请”上方已提供“导出申请明细”。
- ES前台领用申请资产信息已按最新字段口径同步，当前仓库选择与资产跨库校验继续保留；申请人信息已删除成本中心。
- 新员工领用页已按最新使用人信息与领用物资明细字段口径同步；领用明细使用“资产大类 / 资产小类 / 资产说明”，资产选择弹窗横向溢出问题已处理并形成通用检查规则。
- 合约号码链路当前按“员工申请多附件 → ES 配给多附件 → 主管审批只读同步”演示；ES 同意自动切到同一单据的主管审批页，主管审批后该待办消失，整页刷新可重新开始演示。选择号码弹窗查询条件和列表号码列均统一为“合约号码”，列表为“序号 / 标签号 / 合约号码 / 套餐金额”；申请信息中的身份证号码和附件各自独占整行，多附件逐行显示名称和下载链接；员工领取确认提示与保管职责已同步最新文案。合约号码库管员待办申请人信息已删除板块、成本中心。
- 资产配给和借用配给当前实际使用的“员工名下资产明细”弹窗均不再提供“物资总类 / 资产用途”查询条件；资产更换办理与资产退库办理均已接入同类弹窗。借用发放页“借用资产明细”的位置字段为 `city / building / floor`，资产说明只展示“品牌.型号”；借用发放“申请人信息”和“借用资产明细”均使用同一 `DetailGrid`，三列标签与列边界支持跨 Card 对齐。资产更换办理两组位置字段、资产退库办理退库资产信息的位置字段同样使用 `city / building / floor`。员工借用确认、员工资产确认的人员字段均已统一显示为“申请人”；员工资产确认的领用确认场景在“确认提示及保管职责”中展示红字保管职责正文。
- “申请人信息”默认字段统一为“申请人 / 申请日期 / 公司 / 办公区 / 联系电话 / 邮箱 / 部门”；领导退库审批、资产退库办理、合约号码退库办理都在默认字段后单独整行展示“退库原因”。退库审批已补齐公司/办公区并删除退库类型。普通业务公司字段使用 `编码.公司名` 口径，例如 `114.新媒体`，集团/媒体/视频/焦点只作为子公司/组织层级；业务审批节点的公司当前固定展示 `114.新媒体`，其“申请资产信息”使用“申请原因”而不再显示“详细说明”。
- 号码控制页所属部门当前从 `集团 / 媒体 / 焦点 / 视频` 子公司层开始展示完整路径，不再带法人公司全称；耗材审批申请耗材信息当前字段为“耗材说明 / 配置 / 数量 / 参考价格 / 申请用途 / 申请原因”。耗材领用方案一、二均不展示“使用用途”，申请人信息已删除成本中心且人员字段统一为“申请人”；方案一物料编码和方案二耗材标签号为只读文本，地理字段统一为 `city / building / floor`；耗材汇总从汇总明细到后续申请明细统一使用“耗材小类 / 耗材说明 / 申请原因”口径，耗材汇总申请和耗材汇总审批的申请明细均支持导出。
- 业务申请人及用户信息的“联系电话”Mock 统一使用座机号；直接 Mock 在启动时归一，所有通过 `demoStorage` 读取/写入的历史演示记录也会自动迁移并回写座机，避免旧 localStorage 继续展示手机号；合约号码字段自身保留号码资产值。
- 原型标注下一阶段继续逐模块处理 Coverage `review`，先确认 PRD 口径，再决定改原型、改 PRD 或保留为后端/流程能力。
- `review/rd-review` 仅保存阶段性已确认研发评审结论，不自动跟随 `main`。

# 关键决定

- 业务规则优先级高于设计系统；设计系统提供能力，不自动改变资产系统业务交互。
- 基础视觉优先在 Theme / 公共组件层吸收 ERP Design System，禁止业务页面重复硬编码。
- Figma 中与资产系统无关的聊天、通讯录、消息气泡等示例只用于理解语义 Token，不复制成资产系统业务规则。
- 未被 Figma 明确覆盖的组件不猜测、不静默补齐、不以单页实现反向定义全局规范。
- 状态继续统一通过 `StatusTag` 输出；选择弹窗继续使用 `SelectModal`；查询区继续使用 `QueryBar` + `QueryItem`。
- 页面分组优先顺序：留白 → 分组结构 → 必要 Divider → 新增容器，避免 Card 套 Card、边框套边框。
- 资产盘点公共 Card 标题优先复用 `SectionCardTitle`，ERP 主色只在该公共组件集中维护，业务页不再重复写标题蓝色值。