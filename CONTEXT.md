# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 退库审批页面：
  - 维修记录作为资产信息的普通字段放在最下方，使用小尺寸文字按钮。
  - 附件表格只展示附件名称、附件大小、操作，底层节点和上传人元数据继续用于权限校验。
  - 删除无实际数据的代理人列。
- 资产更换、资产退库、资产借用三个流程已完成统一设计语言整改：
  - 详情信息默认三列，长文本单独一行。
  - 部门层级统一使用“.”连接。
  - 流程按钮统一使用纯文字，工具按钮保留图标。
  - 审批记录无真实代理数据时不展示代理人列。
  - 状态统一由 `StatusTag.js` 渲染，`StatusTag.jsx` 仅作转发。
- 项目级设计规范已固化在 `docs/UI_DESIGN_GUIDELINES.md`，并通过 `AI_RULES.md`、`CLAUDE.md` 设为个人工作台页面强制规范。
- “合约号码 ES 配给”页面已按最新要求调整：
  - 删除“5级及以上人员流程为自动发起”的红色提示。
  - 申请信息新增附件下载入口，原型下载真实生成的演示文本文件，不提供虚假下载地址。
  - 电话号码字段收窄为单列宽度，不再跨两列展示。
  - 审批信息删除流程线图，只保留审批历史表格、审批意见和操作按钮。
  - ES审批同意后生成“库管员领用”节点。
- 新增“合约号码库管员待办”并加入个人工作台：
  - 页面标题为“合约号码领用办理”，右上角展示申请单号。
  - 申请人信息展示当前仓库、使用人、联系电话、申请日期、公司、板块、办公区、成本中心、部门和单据备注。
  - 申请合约号码信息展示标签号、序列号、说明、城市、领用原因、数量、子公司、资费标准、号码状态和备注。
  - 当前仓库、城市、子公司、单据备注、备注可编辑。
  - 办理操作为确认领用、驳回、返回；驳回时办理意见必填。
  - 确认领用后号码状态更新为“已领用”，单据和流程节点更新为已完成。
- 合约号码演示数据存储键升级至 `mmp.contractNumberAllocation.applications.v2`，确保已访问过旧页面的浏览器也能加载新增库管员待办数据。

# 关键决定

- 老系统截图用于确认业务字段和流程，页面视觉结构统一按当前个人工作台规范重构，不照搬旧页面样式。
- 个人工作台页面遵循“页面标题 + 小尺寸业务 Card + 居中流程操作”的统一结构。
- 三列详情使用统一固定列宽；长文本、原因、说明、备注默认单独一行。
- 状态颜色由 `StatusTag` 集中维护，页面禁止自行声明状态颜色映射。
- 日期和部门格式由公共工具统一处理。
- 流程按钮使用纯文字，工具按钮才使用图标。
- 原型附件必须提供真实可执行的下载交互，不能配置不存在的服务器地址。
- 合约号码 ES 配给和库管员待办共用同一 mock、service 和 demoStorage 数据链路，页面禁止直接操作 localStorage。
- 新增默认演示数据结构时需要升级存储版本，避免旧 localStorage 阻止新节点和新字段出现。

# 上次停留位置

“合约号码 ES 配给”已完成附件、号码字段和审批布局调整；“合约号码库管员待办”已新增并挂载个人工作台，等待构建和页面验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 合约号码ES配给。
2. 确认申请信息不再展示5级及以上红色提示，并新增附件名称链接；点击后应下载演示附件。
3. 确认号码配给中的电话号码只占一列，话费套餐和号码状态分别占另外两列。
4. 确认审批信息没有 Steps 流程线图，只展示审批历史、审批意见和同意/驳回/延期/返回按钮。
5. 点击“选择号码”，确认号码选择弹窗支持 IMEI、标签号和电话号码查询并单选回填。
6. `/yewurules` → 个人工作台 → 合约号码库管员待办。
7. 确认申请人信息和申请合约号码信息按三列展示，部门使用“.”连接，备注字段单独一行。
8. 修改当前仓库、城市、子公司、单据备注和备注，确认输入交互正常。
9. 驳回不填写办理意见时应提示必填；确认领用后页面进入无待办状态。
10. 检查两个页面的 Card、按钮、状态标签、字段宽度和空状态符合 `docs/UI_DESIGN_GUIDELINES.md`。
