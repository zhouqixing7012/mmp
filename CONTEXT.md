# 当前状态

项目是产品演示用企业资产管理前端。当前开发分支：`feat/asset-management-updates`，基于最新 `main` 创建，仅用于资产管理相关页面修改。

# 当前正在做什么

后台“资产管理”按现系统截图逐页重构。

已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表
- 账面报废查询列表
- 资产处置查询列表
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

# 近期关键决定

- 本分支只修改“资产管理”相关页面，不处理个人工作台和库存管理页面。
- 资产报废、账面报废、资产处置入口统一为单据查询列表。
- 查询条件：申请单号、单据状态、公司、制单时间从/至。
- 列表字段：选择、申请单、单据状态、公司、制单人、制单时间、资产数量、备注、操作。
- 操作区：对应“创建申请单”按钮 + “删除”。
- 资产报废点击新建进入现有机房资产“报废申请单编辑页”，路由 `/BaofeiShenqing`，对应 `ScrapApplicationEdit`。
- 账面报废点击新建进入现有机房资产“账面报废申请页”，路由 `/`，对应 `AccountingScrapEdit`。
- 资产处置申请页字段尚未提供，因此保留新建按钮，但不猜表单字段。
- 列表统一把“共 X 条”放在列表标题右上角，操作按钮放下一行右侧。
- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

已确认资产报废、账面报废的新建按钮直接复用现有机房资产页面，不重新生成重复页面。当前代码已满足该跳转关系，无需额外修改业务代码。

# 验收路径

1. 打开 `/yewurules` → “资产管理”。
2. 点击“资产报废”，进入查询列表后点击“创建资产报废申请单”，应进入 `/BaofeiShenqing` 的机房资产报废申请单编辑页。
3. 点击“账面报废”，进入查询列表后点击“创建账面报废申请单”，应进入 `/` 的机房资产账面报废申请页。
4. 两个入口均复用现有页面，不新增重复报废申请页面。
