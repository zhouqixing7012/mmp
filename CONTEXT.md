# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产退库办理页面已调整：
  - “查看员工名下资产”按钮移动到申请人姓名后。
  - 公司、板块和部门层级统一使用“.”连接。
  - 退库资产信息增加备注、盘点状态、盘点执行人。
  - “仓库及鉴定信息”更名为“退库信息维护”。
  - 鉴定结果改为下拉框，可选“鉴定通过”“鉴定不通过”。
  - 页面不再展示确认状态字段，但员工确认和执行入库联动保持不变。
  - 维修记录弹窗改为当前退库资产的维修记录列表。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 页面处理完成后自动刷新并展示下一张待办；没有待办时显示空状态。
- 旧系统中组织层级展示使用“.”连接时，前端统一转换现有“/”格式，不修改底层模拟数据。
- 盘点字段无数据时展示“-”，不虚构盘点结果或执行人。

# 上次停留位置

资产退库办理页面已按最新字段和布局要求调整，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 资产退库办理。
2. 确认查看员工名下资产按钮位于申请人姓名后。
3. 确认公司.板块、部门层级均以“.”连接。
4. 确认退库资产信息包含备注、盘点状态、盘点执行人。
5. 确认“退库信息维护”中的鉴定结果为下拉框，且没有确认状态字段。
6. 点击“维修记录”，确认弹窗展示当前资产的维修记录列表。
