# 当前状态

项目是产品演示用企业资产管理前端。`feat/business-rule-updates` 已于 2026-08-09 合并到 `main`，库存管理当前在 `feat/inventory-management-updates` 分支持续开发。

# 当前正在做什么

后台“库存管理”按现系统截图逐页重构。

库存管理菜单固定为：
- 资产接收
- 耗材接收
- 入库
- 出库
- 移库
- 转移
- 库管员工作台

已完成库存页面：
- 资产接收：PO列表、待接收PO详情、已入库PO对应接收单列表
- 入库：查询、列表、创建/删除/批量打印入口
- 出库：查询、列表、创建/删除/出库打印/领用打印入口
- 移库：发起单据查询与列表、资产扫描输入、创建/删除入口；接收单据字段待确认
- 转移：查询、列表、创建转移单/删除入口
- 库管员工作台：扫码/刷卡/员工页面/刷新员工页面入口、资产出入库页签、查询条件和空列表

待确认页面：耗材接收，以及入库/出库/移库/转移各单据的新建和详情字段、库管员工作台行内操作详情。

# 近期关键决定

- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 资产接收首页按PO维度展示；待接收PO行操作为“接收”，已入库PO行操作为“查看”。
- 点击“接收”进入PO详情；点击“查看”进入该PO的接收单列表。
- 库存管理原型不展示旧系统“配置”按钮；仅保留截图中已确认且仍需要的业务操作。
- 入库查询字段：入库单号、入库类型、单据状态、PO单号、PR单号、资产标签号、制单人、制单日期从/至。
- 出库查询字段：出库单号、出库类型、单据状态、PO单号、申请单号、资产标签号、制单人、制单日期从/至、资产责任人。
- 移库保留“发起单据/接收单据”两个页签；当前只实现截图已确认的发起单据内容，接收单据不猜字段。
- 转移查询字段：转移单号、转移原因、单据状态、公司、转出部门、转出地点、板块、转入部门、转入地点、制单人、制单日期从/至。
- 库管员工作台仅保留“资产出入库”页签；查询字段为资产标签号、员工卡号、员工姓名、资产名称、单据编号、单据类型；列表字段为行号、申请单编号、单据类型、审批环节、申请人、申请时间、操作。
- 库管员工作台按截图保持空列表，不虚构待办数据；扫码聚焦资产标签号输入框，刷卡聚焦员工卡号输入框。
- 公司、板块、人员、部门、地点等选择型字段继续复用统一选择弹窗；截图未提供候选数据时保持空数据，不虚构。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

`feat/inventory-management-updates` 已完成资产接收、入库、出库、移库、转移、库管员工作台的列表/查询原型并接入库存管理菜单。下一步等待耗材接收或上述单据的新建/详情截图继续实现。

# 验收路径

1. 打开 `/yewurules` → “库存管理”。
2. “资产接收”可在PO列表进入接收详情或接收单列表。
3. “入库”展示截图中的9个查询条件、入库单列表和创建/删除/批量打印。
4. “出库”展示截图中的10个查询条件、出库单列表和创建/删除/出库打印/领用打印。
5. “移库”默认进入“发起单据”，展示查询、资产扫描、移库单列表和创建/删除；“接收单据”仅显示待确认空状态。
6. “转移”展示截图中的12个查询条件、转移单列表和创建转移单/删除。
7. “库管员工作台”展示顶部扫码/刷卡/员工页面/刷新员工页面，单一“资产出入库”页签，6个查询条件和空列表。
8. 上述页面均不展示“配置”按钮，未提供的新建/详情字段继续保持待确认状态。
