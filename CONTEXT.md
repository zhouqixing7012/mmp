# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

根据《资产退库/合约号码退库》PRD，新增两套可联动演示流程，并挂载到 `/yewurules` 的个人工作台。

## 资产退库

- “我的资产”新增单条/批量退库入口。
- 资产退库：选择资产、自动带出关联升级耗材、填写原因、按一项主资产一张单据拆分。
- 退库审批：领导审批与 MIS 鉴定两个待办页签，支持同意、驳回、返回、加签。
- 资产退库办理：仓库选择、资产标记、退库日期、员工确认、执行入库、驳回和放弃退库。
- 员工退库确认：支持扫码、刷卡和手工输入工号，校验申请人工号。
- 正常退库生成演示入库单；驳回或放弃退库结束流程并解除演示锁定。

## 合约号码退库

- 合约号码退库：本人名下号码多选提交，按一号一单拆分，支持附件。
- 合约号码退库办理：号码仓库选择、员工确认、执行入库、驳回和放弃退还。
- 员工号码退库确认：支持扫码、刷卡和手工输入工号。
- 正常退还生成演示号码入库单，号码终态按“在库（旧）”口径展示。

## 资产借用页面调整

- 借用申请页“需求说明”限制调整为 150 字以内，提示文本为“请填写需要借用资产的配置需求”。
- 借用配给页按旧系统截图收敛为申请人信息、借用资产信息和审批信息三块。
- 办理仓库改为弹窗选择，仓库弹窗支持编码、描述查询和单选回填。
- 资产选择弹窗调整为宽屏查询列表，补充 SN 号、配置等核心列。
- 借用审批页重新按审批截图调整：借用单信息仅保留申请人、申请时间、联系电话、邮箱和部门；借用资产信息仅保留资产类别、借用数量、借用原因、需求说明、借用开始日期和借用归还日期。
- 借用发放页按发放截图调整：当前仓库置顶，补充申请人、成本中心、部门、单据备注；每项资产按明细表单展示标签号、序列号、公司、板块、地点、用途、盘点信息和借用信息。
- 借用发放底部操作收敛为借用确认/执行出库、取消、返回，保留原有员工确认和出库联动。
- 员工借用确认页按截图调整：借用人信息仅展示使用人和部门；借用物资表格展示行号、资产标签号、物资说明、配置、申请数量、借用数量、资产用途和使用说明；底部改为刷卡输入、保管职责说明和二维码确认布局。

# 关键决定

- “原型”指 GitHub 仓库内可运行页面，不交付静态图片。
- 两套退库流程共用 `assetReturnMock` 和 `assetReturnService`，页面不直接操作 localStorage。
- 退库申请列表和办理状态使用同一份演示数据联动。
- 资产借用页面优先复用旧系统字段顺序和信息层级，但保持现有 Ant Design 视觉规范。
- 员工借用确认保留扫码和刷卡两种演示交互，确认后仍回到库管员发放节点执行出库。
- 服务号通知、真实扫码/刷卡、真实入库、并发锁定和 21 天超期任务仍为演示模拟。

# 上次停留位置

资产更换、资产退库、合约号码退库首版流程已完成；资产借用申请、配给、审批、发放和员工确认页面已按最新截图调整，下一步继续按页面验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 我的资产：选择资产并点击退库或批量退库。
2. 资产退库：填写原因提交，确认一项主资产生成一张单据。
3. 退库审批：按部门公用/退库 MIS 配置处理领导审批和 MIS 鉴定。
4. 资产退库办理：发起员工确认。
5. 员工退库确认：扫码、刷卡或工号确认。
6. 返回资产退库办理执行入库，确认生成入库单号。
7. 合约号码退库：选择号码提交，确认一号一单。
8. 合约号码退库办理 → 员工号码退库确认 → 执行入库。
9. 资产借用申请：确认需求说明最多 150 字及最新提示文本。
10. 借用配给：确认仓库弹窗、资产选择弹窗和字段布局。
11. 借用审批：确认借用单信息、借用资产信息和审批操作布局。
12. 借用发放：确认当前仓库、借用人信息、逐项资产明细、地点级联和借用确认/执行出库流程。
13. 员工借用确认：确认使用人/部门、借用物资字段、刷卡工号校验、二维码确认和保管职责说明。
