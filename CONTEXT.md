# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成员工自助新版第二阶段：ES 配给与汇总采购。旧版页面和旧版路由全部保留。

1. 新增员工自助新版资产配给
   - 业务审批完成后自动按申请行和数量拆分配给单。
   - 根据申请人公司匹配 ES 配给处理人。
   - 支持库存领用、统一采购、取消申请。
   - 库存领用必须选择符合公司板块权限、资产大类、在库状态、资产标记为空且未锁定的资产。
   - 支持查看申请人名下在用和未超期借用资产。
   - 配给结果回写原申请单任务状态。

2. 新增员工自助新版汇总采购
   - 统一采购配给单实时进入待汇总池。
   - 按公司和部门维度合并汇总数据。
   - 支持 ES 汇总说明、项目用途、部门申请明细、超标标识、ES 建议和附件。
   - 支持采购或驳回，驳回原因必填。
   - 展示部门现使用量、现人均量、申请采购量、采购后人均量和公司人均量。
   - 提交采购后回写原申请单进入采购与 PR 系统流程。

3. 新增统一演示数据与服务
   - `src/mock/employeeSelfServiceWorkflowMock.js`
   - `src/services/employeeSelfServiceWorkflowService.js`
   - 配给单、汇总单和原申请单共用演示存储并保持刷新后数据。

4. 新增路由和菜单
   - `/employee-self-service/asset-allocation`
   - `/employee-self-service/purchase-summary`
   - 顶部导航和 `/yewurules` 个人工作台内部菜单均可进入。

5. 修复第一阶段默认申请单
   - 补齐 `approvalRoute` 和参考价格，避免默认演示申请审批时无法推进。

# 本次关键决定

- 本期仍只实现资产，不实现耗材。
- 旧申请、旧审批、旧配给、旧汇总和旧领用页面全部保留。
- 配给单按申请行和数量拆分，一件资产对应一张配给单。
- 同一申请单允许同时存在库存领用和统一采购两条支线。
- 统一采购实时进入待汇总池，由 ES 集中提交采购。
- 库存领用配给完成后进入资产领用流程。

# 上次停留位置

员工自助新版第二阶段代码已提交到 `feat/business-rule-updates`。

下一阶段从资产领用开始：

1. 根据配给单生成资产领用单。
2. 支持库存领用和采购入库领用两类来源。
3. 支持当前仓库、领用资产、地点、用途和盘点信息。
4. 支持狐小 e 电子签或刷卡确认。
5. 库管员复核签字并执行出库。
6. 更新资产台账、出库单和原申请进度。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 选择弹窗使用 `SelectModal`。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能。
- 入口文件最后更新，避免模块未齐时破坏分支构建。

# 验收路径

1. 在新版业务审批页完成一张申请单审批，使任务状态变为“待配给”。
2. 打开 `/employee-self-service/asset-allocation` 并点击“刷新待办”。
3. 检查申请数量按一件一单拆分为多张配给单。
4. 选择库存领用，匹配资产并填写 ES 意见后提交。
5. 检查原申请单状态变为“待领用”或“领用/待汇总”。
6. 选择统一采购并提交，打开 `/employee-self-service/purchase-summary`。
7. 检查统一采购数据进入待汇总池。
8. 编辑汇总说明、项目用途和处理方式后提交采购。
9. 检查原申请单进入“采购流程”或“领用/采购流程”。
10. 刷新浏览器，确认配给和汇总结果仍存在。
