# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成员工自助新版第一阶段：资产申请与业务审批。旧版页面和旧版路由全部保留。

1. 新增员工自助新版资产申请
   - 展示申请须知和申请人信息。
   - 正式员工可申请，外包员工直接拦截。
   - 使用统一选择弹窗多选资产。
   - 支持数量、申请原因、申请用途和详细说明。
   - 个人超标资产展示红色标签，提交前二次确认。

2. 新增员工自助新版业务审批
   - 支持按申请单号或申请人查询。
   - 展示申请人、申请资产和审批记录。
   - 支持同意和驳回，驳回意见必填。
   - 超标申请依次经过直属领导、5级及以上领导、7级及以上领导。
   - 非技术人员申请苹果电脑继续逐级审批至 VP/CFO。
   - 业务审批完成后任务状态改为“待配给”。

3. 新增统一演示数据
   - `src/mock/employeeSelfServiceMock.js`
   - `src/services/employeeSelfServiceService.js`
   - 申请和审批读取同一份数据，刷新浏览器后保留。

4. 新增路由和菜单
   - `/employee-self-service/asset-apply`
   - `/employee-self-service/asset-approval`
   - 顶部导航和 `/yewurules` 个人工作台内部菜单均可进入。

5. 更新文档
   - 新增 `docs/PRD-EMPLOYEE-ASSET-SELF-SERVICE.md`。
   - 更新 `README.md` 和 `ARCHITECTURE.md`。

# 本次关键决定

- 本期只实现资产申请，不实现耗材申请。
- 旧申请、旧审批、旧配给和旧领用页面全部保留。
- 外包员工暂时不能发起申请。
- 7级及以上才有超标审批权限，超标申请必定经过5级和7级节点。
- 非技术人员申请苹果电脑按超标路径逐级审批至 VP/CFO。
- ES 配给后，统一采购实时进入待汇总池，由 ES 按月集中提交采购。
- 同一申请后续允许同时存在库存领用和统一采购两条支线。

# 上次停留位置

员工自助新版第一阶段代码和 PRD 已提交到 `feat/business-rule-updates`。

下一阶段从 ES 配给开始：

1. 根据公司维度匹配 ES 配给人。
2. 按申请行和数量拆分配给单。
3. 支持库存领用、统一采购、取消申请。
4. 库存领用选择匹配资产。
5. 统一采购进入待汇总池。
6. 配给结果回写原申请单进度。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 选择弹窗使用 `SelectModal`。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能。
- 入口文件最后更新，避免模块未齐时破坏分支构建。

# 验收路径

1. 打开 `/employee-self-service/asset-apply`。
2. 关闭申请须知，添加普通资产和苹果电脑。
3. 填写数量、申请原因和申请用途后提交。
4. 检查超标申请出现确认提示。
5. 打开 `/employee-self-service/asset-approval`。
6. 查询并进入刚提交的申请单。
7. 连续同意，检查节点按直属领导、5级、7级推进。
8. 检查苹果电脑申请继续进入 VP/CFO 节点。
9. 完成审批后检查任务状态变为“待配给”。
10. 刷新页面，确认申请和审批结果仍存在。
