# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产更换、资产退库、资产借用相关页面已按最新业务要求完成多轮字段、布局和交互调整。
- 个人工作台页面统一使用单层画布，`Descriptions` 统一固定列宽，保证跨 Card 三列对齐。
- 退库流程已新增节点附件能力，并在 Service 层校验当前节点、当前上传人删除权限。
- 退库审批页面本次调整：
  - 维修记录入口从资产信息 Card 右上角移至资产信息字段区最下方。
  - 字段名称和按钮名称统一为“维修记录”，点击后继续展示维修记录弹窗。
  - 附件信息表格只展示附件名称、附件大小和操作，不再展示上传节点、上传人、上传时间。
  - 上传节点、上传人等元数据仍在底层保留，仅用于附件删除权限校验。
- 资产借用页面已统一使用“资产”口径，借用配给和借用审批已删除审批记录中的代理人列，借用发放已删除“借用”资产用途枚举。

# 设计走查结论

当前页面整体已形成“页面标题 + 业务 Card + 底部操作”的主结构，但仍存在以下设计语言不一致：

1. 页面标题区有的展示单号、有的仅展示标题，容器结构和右侧信息不统一。
2. “申请时间/申请日期”仍混用，申请人字段集合和字段顺序在不同业务页不一致。
3. 部门层级分隔符仍有页面直接展示“/”，尚未全部统一为“.”。
4. 详情布局以三列为主，但部分审批页仍使用两列，跨页面信息密度不同。
5. 页面操作按钮的图标使用不统一，同类“同意/驳回/返回”在不同页面有的带图标、有的不带。
6. Card 名称存在“资产信息/物资信息/资产明细/物资明细”混用，需要按业务对象统一。
7. 状态 Tag 颜色仍在多个页面内手写，尚未完全复用统一状态组件。
8. 部分页面使用标准 Card，部分申请页使用自定义 section 容器，圆角、边框和标题栏细节有差异。
9. 维修记录、查看名下资产等关联入口存在普通按钮、文字链接、带图标链接等多种形态。
10. 空状态返回按钮存在“返回”和“返回工作台”两种文案。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上。
- 三列详情卡片必须共享相同标签列宽和固定表格布局，避免各 Card 根据内容自行计算列宽。
- 数量、总计等统计信息统一放在对应明细 Card 右上角，不占用申请信息字段位置。
- 附件删除权限由“业务单据 + 上传节点 + 上传人”共同判断；页面可以精简展示字段，但不能删除底层权限元数据。
- 维修记录属于资产信息的普通关联字段时，入口放在资产信息字段区，不占用 Card 右上角操作位。
- 资产借用业务只允许选择资产，页面不得继续保留耗材入口或“物资”口径。

# 上次停留位置

退库审批维修记录入口和附件表格已调整；设计走查已完成并形成统一性问题清单，尚未批量整改。

# 验收路径

1. `/yewurules` → 个人工作台 → 退库审批。
2. 确认资产信息 Card 右上角不再展示“查看”。
3. 确认资产信息最下方存在“维修记录”字段和“维修记录”按钮，点击后弹出维修记录。
4. 确认附件信息表格只展示附件名称、附件大小、操作。
5. 当前 MIS 节点本人上传的附件仍可删除；后续资产退库办理页面仍不可删除 MIS 节点上传的附件。
6. 刷新页面后附件记录和删除权限保持不变。
