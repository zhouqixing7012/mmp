# 当前状态

项目为企业资产管理产品演示前端。当前继续在 `feat/workspace-updates` 修改个人工作台，不新建分支或 worktree。

# 当前设计规范

- 个人工作台 PC 页面使用单层画布；页面标题使用 `Typography.Title level={4}`，蓝色竖块只用于业务 Card 标题。
- 业务分区使用 `Card size="small"`；详情优先 `DetailGrid + DetailItem`；列表使用 small bordered Table。
- 只读字段直接展示文本或 `StatusTag`，只有真实可编辑字段使用 Input / Select / DatePicker / TextArea。
- `DetailGrid` 整行字段必须从完整新行开始，避免 `span={3}` 串行。
- 员工确认全面取消电子签，统一使用刷卡/员工工号 + 狐小 e 扫码。
- 后续用户说“用附件组件”时，默认采用 `ReturnAttachmentCard` 这一套附件样式：**上传附件按钮在 Card 右上角**，下方 small bordered Table 字段为 **附件名称 / 附件大小 / 操作**。是否在具体页面展示附件仍以该页面业务要求为准。
- “查看名下资产”属于辅助查看动作，但需要明确可点击感；当前统一使用带边框的 small Button，不使用仅文字链接。

# 2026-08-14 个人工作台 UI 统一重画

- 本轮按走查结论统一处理四组页面：**物资申请主链路、耗材全链路、资产借用全链路、资产退库申请页**。
- 本轮原则是 **只调整页面结构、组件使用和视觉规范，不修改业务功能**。现有字段、校验、按钮行为、弹窗选择、状态流转、service 调用及本地原型数据语义保持原样。
- `WorkspaceContent` 增加 `data-workspace-page`，用于对个人工作台桌面业务页做作用域明确的样式收口；工作台首页和移动端工作台保持独立设计，不套用本轮桌面业务页规则。
- 桌面业务页统一去除历史页面重复的 `min-h-screen + 浅灰背景 + p-4` 外层画布；历史白底阴影页头在工作台内收口为普通页面标题；历史白底阴影底部操作条收口为普通居中按钮区。
- 业务 Card 标题统一使用紧凑蓝色竖线分组样式；已手写 `SectionTitle` 的页面通过样式去重，避免出现双蓝线。
- 历史桌面业务表格统一收紧到小号 B 端密度；新改页面显式补齐 `size="small"` / `bordered`。
- 物资申请已改为标准页面标题 + small Card + small bordered Table；预览/提交/返回等流程按钮取消图标，添加/搜索/删除等工具操作保留图标。
- 业务审批、资产配给的申请人信息和审批历史公共组件已切换到 `DetailGrid / StatusTag / small bordered Table`；同意/驳回/加签流程按钮取消图标，原审批及配给逻辑不变。
- ES前台领用已统一状态展示和选择资产弹窗样式；员工领用确认已改为领用人信息 / 领用物资明细 / 确认提示及保管职责 / 刷卡扫码确认四块结构，原确认行为不改。
- 资产借用申请、借用审批、员工借用确认已切换到单层页面结构；借用申请人公共 Card 使用 `DetailGrid`；借用配给和借用发放通过工作台公共样式同步去除旧画布、旧页头和旧操作条，原匹配资产、仓库、确认、出库逻辑不变。
- 耗材链路通过工作台公共样式统一旧 PageHeader / Card 标题 / 页面密度；员工耗材领用确认已改为标准 Ant Design `QRCode` + 刷卡/工号确认布局，继续调用原确认服务，职责勾选和员工校验逻辑不变。
- 统一申请汇总-资产保留原列表/明细/汇总/审批视图切换及导出、上传、保存、提交逻辑；历史自定义页头在工作台中收口为标准标题视觉，流程按钮图标按规范弱化。
- 资产退库申请页通过工作台公共样式同步去除旧二层画布、阴影页头和阴影底部操作条，并统一 Card 分组标题；原添加资产、资格校验、退库类型、退库原因和一资产一单逻辑不变。

# 2026-08-14 最新页面修正

- 退库审批页“退库资产信息”删除维修记录；资产说明调整到资产状态之前；附件信息 Card 整块删除，同时清理仅服务于附件/维修弹窗的页面层逻辑，审批提交逻辑不变。
- 资产更换申请“更换须知”不再展示“苹果笔记本、苹果一体机、组装机、工作站不支持通过本流程更换，如需新设备请发起资产申请”这一条；资产可更换资格判断本身不在本次修改范围内。
- 申请人信息中的“查看名下资产”统一保留明显交互样式，当前采用带边框的 small Button。
- 完成上述调整后，将 `feat/workspace-updates` 全量合并至 `main`。

# 个人工作台首页

- 资产页签：黑体展示品牌.型号，浅灰展示大类-小类；资产用途独立放在状态后；操作按物料编码配置规则显示退库/转移/更换，借用资产只显示退库；支持批量退库/批量转移/批量更换。
- 耗材页签：耗材信息黑体展示耗材说明、浅灰展示耗材小类；后续只展示耗材标签号、主资产标签号、主资产说明。
- 合约号码页签：业务字段只展示合约号码、合约号码说明、操作；支持复选框多选及批量退库，单条/批量均可带入合约号码退库申请页。

# 资产更换办理

- 更换物资信息右上角为“退库确认”，待发放资产信息右上角为“领用确认”，两个按钮均保持可点击。
- 点击退库确认进入员工资产确认时，携带 `replacementConfirmScene=旧资产退回`；点击领用确认时携带 `replacementConfirmScene=新资产领取`，同时携带当前申请单号。
- 员工资产确认页不再使用旧资产/新资产 Tabs；根据从办理页点击的入口，只展示对应一套明细：退库确认只展示“退库资产明细”，领用确认只展示“待发放资产明细”。
- 退库资产明细展示旧资产及更换原因、退库使用说明；待发放资产明细展示新资产及领用使用说明，不再出现借用开始日期/结束日期。
- 更换物资信息已删除鉴定结果/鉴定说明；备注、耗材信息位于仓库前。
- 仓库后顺序为：仓库、责任人、资产标记、退库日期、使用说明。责任人随仓库自动带出虚拟库管员工号-名称；退库日期取员工退库确认完成日期；使用说明单行可编辑。
- 待发放资产选中资产标签号后不出现删除按钮，选错直接重新打开选择弹窗覆盖选择。
- 盘点人/盘点状态只在当前资产同时有非空盘点人和盘点状态时展示；没有盘点数据时整组字段不出现。
- 页面底部审批操作只保留驳回、返回、加签。

# 资产退库办理

- “退库信息维护”已删除 **MIS鉴定 / 鉴定结果 / 鉴定说明 / 维修记录**。
- 当前维护字段为：**仓库 / 责任人 / 资产标记 / 退库日期 / 使用说明**。
- 当前页面**不展示附件信息**，并已移除该页的附件上传/删除入口及维修记录弹窗相关页面逻辑。
- `ReturnAttachmentCard` 组件本身继续保留，作为后续其他页面明确要求“附件组件”时的标准样式，不代表资产退库办理必须展示附件。

# 合约号码退库

## 申请页
- 申请信息只保留申请人、部门、申请日期、退库原因；政策提示已删除。
- 申请信息不出现独立滚动条；退库原因使用自动增高 TextArea，详情栅格隐藏横向滚动。
- “添加物资”弹窗和主页面“合约号码明细”字段都只保留：合约号码、合约号码说明。
- 添加物资仍从本人名下合约号码中多选；资格校验通过复选框禁用实现，不增加额外业务字段。

## 办理页
- “退库合约号码信息”只展示：合约号码、合约号码说明、号码状态。
- “退库信息维护”保留：退库仓库、责任人、退库日期、使用说明。
- 退库日期为员工退库确认完成日期，只读展示，不再由库管员手工选择。
- 使用说明改为单行 Input。
- 员工确认状态继续作为内部状态机驱动退库确认/确认入库，不作为页面字段展示。

## 员工合约号码退库确认
- 页面分为申请人信息、退库资产明细、退库确认提示、刷卡/扫码确认。
- 退库资产明细只保留：合约号码、合约号码说明、退库原因。
- 继续支持刷卡/管理员录入员工工号及狐小 e 扫码；工号必须与申请人一致。

# 资产借用

- 资产借用“添加资产”弹窗只有大类/小类两级，不展示品牌/型号/配置。
- 借用申请资产说明固定为大类.小类。
- 借用配给未选择资产标签号时仍显示大类.小类；选择具体资产标签号后，资产说明切换为该资产的品牌.型号。

# 耗材汇总

- 耗材汇总明细页不展示“导出申请明细”按钮。
- 汇总申请页中的“申请明细”Card 右上角保留导出按钮。

# 当前验收路径

1. 物资申请 → 业务审批 → 资产配给 → ES前台领用 → 员工领用确认：页面均不再出现重复浅灰画布/阴影页头；Card 分组标题统一；原申请、审批、配给、匹配资产、领用逻辑保持不变。
2. 资产借用 → 借用配给 → 借用审批 → 借用发放 → 员工借用确认：页面外壳统一为个人工作台单层结构，原仓库、匹配资产、员工确认、出库逻辑保持不变。
3. 耗材MIS鉴定 → 耗材审批 → 耗材配给 → 耗材领用 → 员工耗材领用确认 → 耗材汇总/审批：旧页头和 Card 标题已统一；员工确认使用 AntD QRCode；原耗材工作流 service 调用保持不变。
4. 个人工作台 → 资产退库：不再出现独立浅灰二层画布和阴影页头/底部操作条；原添加资产与退库资格校验保持不变。
5. 退库审批：退库资产信息不存在维修记录，资产说明在资产状态前；页面不存在附件信息 Card。
6. 资产更换申请：更换须知不再展示苹果笔记本/苹果一体机/组装机/工作站“不支持本流程更换”的提示行。
7. 资产配给 / 借用配给等存在“查看名下资产”入口的申请人信息：入口应显示为有边框的 small Button。
8. 资产更换办理 → 点击“退库确认” → 员工资产确认：只出现“退库资产明细”，不出现待发放资产明细或 Tabs。
9. 资产更换办理 → 点击“领用确认” → 员工资产确认：只出现“待发放资产明细”，不出现退库资产明细或 Tabs。
10. 个人工作台 → 资产退库办理：退库信息维护里不存在 MIS鉴定/鉴定结果/鉴定说明/维修记录；页面也不存在“附件信息”Card。
11. 合约号码退库：申请信息无政策提示、无独立滚动条；添加物资弹窗和合约号码明细都只有合约号码/合约号码说明。
12. 合约号码退库办理：退库合约号码信息只有合约号码/合约号码说明/号码状态；退库日期取员工确认完成日期，使用说明为单行输入。
13. 员工合约号码退库确认：退库资产明细只有合约号码/合约号码说明/退库原因。
