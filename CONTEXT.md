# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图只提取字段和业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 当前重点是 **库存管理 → 资产接收** 的 PO、接收单和接收明细链路。

# 上次停留位置

- PO 详情已有接收单时展示“查看接收单”，进入当前 PO 的接收单列表。
- 创建接收单必须先勾选 PO 物资行；创建成功后直接进入新接收单详情页。
- 接收单详情页已删除“导入”；“维护接收明细”改为页面级动作，不依赖接收物资行勾选。
- 进入维护接收明细后，按当前接收单全部接收物资的“本次接收数量”拆分成一台资产一条明细进行维护。
- 接收单明细页只保留扫描光标和接收资产明细表；生成标签号后可先扫标签号定位，再扫 SN 条码写入。
- 删除行、生成标签号、维护SN号以及扫描写入 SN 均实时保存并立即生效。
- 接收确认前必须校验全部资产明细都有资产标签号和 SN；SN 为“缺省”视为已维护，空值不算。
- 接收确认通过后生成采购接收入库单，并把接收单状态更新为“已完成”。

# 近期关键决定和原因

- 维护接收明细不是接收物资的单行操作，而是整张接收单的页面级维护入口；接收物资行勾选只服务于删除接收行等行级动作。
- 接收资产明细严格按照“本次接收数量”拆行，避免一条接收物资行代表多台资产却无法逐台维护标签号和 SN。
- 资产标签号、SN 是接收确认的硬校验条件；“缺省”是合法 SN 值，空字符串不是。
- 明细维护动作采用实时保存口径，用户操作后不再依赖额外的整页保存步骤。
- 扫描结果直接通过明细表筛选体现，不重复增加资产标签号、SN号、物资说明、配置四字段展示区。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”进入 PO 详情，“查看”进入接收单列表。
- 电子设备 PO 详情只显示“创建接收单”；服务器、服务器备件、网络设备、网络设备备件只显示“执行入库”。
- 接收单列表制单时间为单个日期范围查询，接收单号作为详情入口，不保留重复操作列。
- 入库、出库、移库、转移继续沿用此前已确认字段与状态规则，未在本轮改动。