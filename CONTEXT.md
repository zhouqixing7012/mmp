# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产退库办理页面已按最新要求调整字段、组织层级展示、鉴定结果和维修记录弹窗。
- 资产更换申请页面已调整必填标识、总计位置、更换须知颜色和勾选布局。
- 统一个人工作台页面画布：
  - 去掉个人工作台外层重复的白色卡片容器。
  - 由个人工作台统一提供页面背景和留白。
  - 自动移除新业务页面根节点重复的浅蓝色背景、整屏高度和内边距。
  - 后台基础配置页面保持原有卡片结构，不受影响。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 页面处理完成后自动刷新并展示下一张待办；没有待办时显示空状态。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上，禁止页面自身再套一层浅蓝背景。
- 页面背景统一在 `AdminContent` 和 `WorkspaceContent` 处理，不逐页重复修改。

# 上次停留位置

个人工作台整体背景层级已统一，等待检查各业务页面视觉效果。

# 验收路径

1. `/yewurules` → 个人工作台。
2. 依次打开资产借用、资产更换、资产退库、合约号码退库相关页面。
3. 确认页面不再出现浅蓝色区域套在更大的白色或灰色区域中的双层画布。
4. 确认卡片仍保留白色，页面背景统一为一层浅灰色。
5. 切换到后台基础配置，确认原有标签和白色容器样式不变。
