# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产退库办理页面已按最新要求调整字段、组织层级展示、鉴定结果和维修记录弹窗。
- 资产更换申请页面已调整必填标识、总计位置、更换须知颜色和勾选布局。
- 统一个人工作台页面画布，去掉业务页重复的浅蓝背景和外层留白。
- 资产更换办理页面已调整字段、默认值及审批记录列。
- 个人工作台内的 `Descriptions` 统一使用固定表格布局和固定标签列宽，保证不同 Card 之间三列网格对齐。
- 员工资产确认页面已完成卡片合并、提示精简、返回按钮及标题字号调整。
- 资产退库申请页面已完成资产总数位置及资产用途字段调整。
- 已撤销误改的“资产更换-MIS鉴定”页面，恢复至调整前版本。
- “退库审批”页面已按最新要求调整：
  - 部门层级使用“.”连接，不再展示“/”。
  - 申请人信息和资产信息均使用三列布局。
  - 资产说明单独占满一行。
  - 删除独立的“MIS鉴定处理”Card。
  - 鉴定通过时审批意见可为空，鉴定不通过时审批意见必填。
  - 资产信息 Card 右上角增加“查看”按钮，点击后展示当前退库资产的维修记录。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上。
- 审批页面先展示业务信息和审批流程，再展示审批意见和操作。
- 共用审批记录组件通过参数控制代理人字段，避免影响其他页面。
- 三列详情卡片必须共享相同标签列宽和固定表格布局，避免各 Card 根据内容自行计算列宽。
- 数量、总计等统计信息统一放在对应明细 Card 右上角，不占用申请信息字段位置。
- “资产更换-MIS鉴定”与“资产退库-退库审批/MIS鉴定审批”是两个独立页面，后续修改必须先确认所属业务模块。
- 退库审批页面由“鉴定通过/鉴定不通过”按钮直接确定鉴定结果，不再额外展示鉴定结果和鉴定说明输入区。

# 上次停留位置

资产更换 MIS 鉴定页面已恢复，退库审批页面已完成三列布局、维修记录入口和鉴定操作调整，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → MIS鉴定，确认页面已恢复到误改前版本。
2. `/yewurules` → 个人工作台 → 退库审批。
3. 确认部门层级使用“.”连接，申请人信息和资产信息均为三列。
4. 确认资产说明单独占满一行。
5. 确认页面不再展示“MIS鉴定处理”Card。
6. 点击资产信息右上角“查看”，确认弹出当前退库资产维修记录。
7. 点击“鉴定通过”时审批意见可为空；点击“鉴定不通过”时审批意见为空应提示必填。
