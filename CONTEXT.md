# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

根据《资产更换 PRD》完成资产更换原型，并全部挂载到 `/yewurules` 的个人工作台。

## 新增个人工作台页面

1. 我的资产：查询本人名下资产，单条或批量发起更换。
2. 资产更换申请：填写更换原因、查看退回资产、确认更换须知并提交。
3. 我的资产更换申请：查询申请进度并查看详情和完整流程记录。
4. MIS鉴定：查询待办，填写鉴定结果、鉴定说明和审批意见。
5. 资产更换办理：分阶段办理旧资产退回、新资产选择与发放。
6. 员工资产确认：分别完成旧资产退回和新资产领取的扫码/刷卡确认。

## 已实现流程

```text
我的资产
  ↓
资产更换申请
  ↓
MIS鉴定
  ↓
旧资产退回确认 → 执行入库
  ↓
选择同小类新资产
  ↓
新资产领取确认 → 执行出库
  ↓
流程完成
```

- 批量提交时，一个旧资产生成一张独立申请单。
- 只有正式员工或实习生本人名下、状态为“在用-使用中”、配置允许且未锁定的资产可以更换。
- 苹果笔记本、苹果一体机、组装机、工作站不展示更换能力。
- MIS 选择“资产更换”只能同意；选择“资产维修”或“员工取消”只能驳回。
- 旧资产入库完成后才开放新资产发放。
- 新资产限制为同小类、当前仓库可出库且未锁定资产。
- 旧资产退回和新资产领取分别确认、分别生成演示入库单和出库单。
- 旧资产已入库后放弃新资产领用，保留旧资产退库结果并结束流程。

# 关键决定

- “原型”在本项目中指 GitHub 仓库内可运行页面，不交付静态图片。
- 资产更换页面优先复用现有资产申请、资产配给、审批、员工名下资产和确认页面的结构。
- 新增统一 `assetReplacementMock` 和 `assetReplacementService`，页面不直接操作 localStorage。
- 申请单、MIS鉴定、退库、配给、员工确认、入库和出库状态使用同一份演示数据联动。
- 服务号通知、真实扫码/刷卡、并发锁定以及真实入库/出库接口仍为演示模拟。

# 上次停留位置

资产更换首版完整流程已完成代码实现，下一步按页面逐个验收和调整。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能和独立路由。

# 验收路径

1. 打开 `/yewurules`，展开个人工作台，确认新增 6 个资产更换入口。
2. 进入“我的资产”，验证查询、不可更换原因、单条更换和批量更换。
3. 进入“资产更换申请”，填写原因并提交，确认每个旧资产生成一张申请单。
4. 进入“MIS鉴定”，验证同意/驳回与鉴定结果组合校验。
5. 进入“资产更换办理”，发起旧资产确认。
6. 进入“员工资产确认”，完成扫码或刷卡确认；返回办理页执行入库。
7. 选择同小类新资产，维护发放信息并发起第二次员工确认。
8. 员工确认后返回办理页执行出库，确认申请状态更新为已完成。
9. 进入“我的资产更换申请”，查看入库单、出库单和完整流程记录。
