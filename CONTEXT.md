# 当前状态

- 当前工作分支：`feat/asset-inventory`，用于资产盘点原型改造。
- 2026-08-18 已按《资产盘点》需求继续完善“盘点项目”完整链路，页面视觉继续使用当前 Ant Design 规范，不复刻旧系统样式。
- 已补齐：初盘/抽盘/复盘关联链路分组、创建/编辑盘点项目、盘点规则、四类盘点方式、图片上传规则、盘点范围筛选、多次生成范围、资产清单、快照四页签、执行/未执行/未包含资产管理、默认/自定义生成盘点计划、计划人员配置、计划资产清单、标签扫描、图片审核、盘点进度、报告下载入口。
- 业务状态按需求控制主要工具栏：暂存 → 快照生成 → 生成盘点计划 → 盘点中 → 盘点关闭。
- “公司-账套对应关系”“盘点规则”独立菜单仍缺少独立维护页字段；需求中的盘点规则已落在盘点项目创建/编辑页，不补造独立维护页面。
- 演示数据仅用于原型展示；NO/MIS 扫描同步、消息推送、真实权限、随机抽样、真实导入导出等仍为前端演示。

# 上次停留位置

- 已完成本轮资产盘点原型代码整理并准备提交 `feat/asset-inventory`。
- 本轮需求原文同步保存至 `docs/PRD-资产盘点.md`。
- 重点验收路径：盘点项目列表 → 创建项目 → 生成快照 → 生成盘点计划 → 启动计划 → 查看计划资产清单/标签扫描；列表还可进入图片审核和项目进度。

# 当前全局设计规范

- PC 业务页使用单层画布；页面标题使用 `Typography.Title level={4}`；蓝色竖块只用于业务 Card 标题。
- 查询区域使用 `QueryBar / QueryItem`；每行最多 3 个条件；列表“共 X 条”放标题右上角，操作按钮放下一行右侧。
- 详情使用 `DetailGrid / DetailItem`；列表使用 small bordered Table；状态使用 `StatusTag`。
- 只读字段展示文本；真实可编辑字段才使用输入控件；选择人员统一使用 `SelectModal`。
- 未被需求明确支持的独立页面字段不补造。
