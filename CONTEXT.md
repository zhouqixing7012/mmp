# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图只提取字段和业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 当前重点是 **库存管理 → 资产接收 / 耗材接收** 的接收链路，以及入库、出库、移库、转移的细节统一。

# 上次停留位置

- 资产接收 PO 详情创建接收单必须先勾选 PO 物资行；“创建接收单”已放到 PO 物资明细 Card 右上角，创建成功直接进入接收单详情。
- 资产接收“维护接收明细”是页面级动作，按全部接收物资的本次接收数量逐件拆行；标签号/SN维护实时保存，接收确认要求标签号和 SN 均非空，“缺省”SN 视为已维护，确认后生成入库单并变为“已完成”。
- 耗材接收采购类型固定为“低值耐用品 / 耗材”。低值耐用品已改成与资产接收一致的逐件维护链路；普通耗材仍按数量/接收行维护。
- 耗材接收单状态统一只保留“草稿 / 已完成”，不再使用“接收待维护 / 接收完成”。
- 低值耐用品的“维护接收明细”改为页面级动作，覆盖当前接收单全部接收行并按本次接收数量逐件拆分；生成标签号、维护SN号、删除明细、扫描写入均实时保存。
- 低值耐用品接收确认前必须全部明细都有标签号和 SN；SN 为“缺省”有效，空值无效；确认后生成采购接收入库单并变为“已完成”。
- 创建出库单时“出库类型”固定默认为“领用出库”并只读；历史已有单据仍按其原出库类型只读展示。
- “库存管理 → 转移”列表的“创建转移单”按钮已改名为“创建”。

# 近期关键决定和原因

- 依赖表格勾选结果的动作放在对应表格 Card 操作区；因此 PO 详情“创建接收单”紧邻 PO 物资明细。
- 低值耐用品属于需要逐件形成标签号/SN的可管理实物，因此复用资产接收的逐件维护逻辑；普通耗材仍保留数量型接收，不强行逐件资产化。
- 接收单状态使用最小状态集“草稿 / 已完成”，明细维护是草稿期间的操作，不再额外制造中间状态。
- 接收明细的批量维护动作和扫码写入采用实时保存，避免再增加整页保存步骤。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”进入 PO 详情，“查看”进入接收单列表。
- 电子设备 PO 详情只显示“创建接收单”；服务器、服务器备件、网络设备、网络设备备件只显示“执行入库”。
- 资产/耗材接收单列表的制单时间为单个日期范围查询，接收单号作为详情入口，不保留重复操作列。
- 入库、出库、移库、转移继续沿用此前已确认字段与状态规则。