# 当前状态

- 项目为企业资产管理产品演示前端；默认继续在 `main` 迭代，用户明确要求时才使用独立分支。
- 当前工作分支：`feat/asset-inventory`，专门用于资产盘点原型改造。
- 2026-08-18 已根据用户提供的“资产系统 操作流程”轨迹包，将一级菜单“资产盘点”补齐 4 个子菜单：公司-账套对应关系、盘点规则、盘点项目、盘点差异报表。
- 本次轨迹只完整采集了“盘点项目”，因此该模块已按当前 UI 规范重构；其余 3 个子菜单只保留入口，不补造字段。
- “盘点项目”已覆盖：项目列表 → 创建项目/筛选范围 → 生成快照 → 快照清单 → 生成盘点计划 → 配置负责人/监督人/执行人 → 启动盘点计划。
- 页面统一复用 Ant Design、`QueryBar / QueryItem`、`DetailGrid / DetailItem`、`StatusTag`、`SelectModal`；截图仅用于确认字段与流程，不复刻旧系统视觉。
- 演示数据只取自本次轨迹截图；未确认的弹窗、配置项和业务字段不补造。

# 上次停留位置

- 资产盘点代码已提交到 `feat/asset-inventory`。
- Vercel 当前构建状态因 build rate limit 失败，尚未得到本次代码的实际构建结果。
- 下一步等待用户验收“盘点项目”页面；其余 3 个子菜单需新的页面轨迹或字段后再实现。

# 全局设计规范

- PC 业务页使用单层画布；页面标题使用 `Typography.Title level={4}`；蓝色竖块只用于业务 Card 标题。
- 业务分区使用 `Card size="small"`；详情优先 `DetailGrid + DetailItem`；列表使用 small bordered Table。
- 查询区域优先 `QueryBar / QueryItem`；每行最多 3 个条件；操作按钮放列表标题下一行右侧；“共 X 条”放标题右上角。
- 只读字段展示文本或 `StatusTag`；真实可编辑字段才使用 Input / Select / DatePicker / TextArea。
- 选择类字段优先 `SelectModal`；未采集选择数据源时禁止虚构选项。
- 空值显示 `-`；公司/板块分开；部门用 `.` 连接。
- 员工确认全面取消电子签，统一刷卡/员工工号 + 狐小 e 扫码。

# 原型标注规则

- PRD/需求先拆成最小 Requirement Atom，再绑定具体字段、按钮、Tab 或表头；具体规则不得为了省事回退到整个 Card。
- target 不依赖“共 X 件/条”、统计数字等动态文案；弹窗目标使用动态生命周期，关闭时不计为真未匹配。
- 标注保存采用“代码基线 + 用户覆盖层”；用户人工修改继续保留，Agent 新增基线可自动出现。

# 当前模块状态

- 个人工作台：资产申请、审批、配给、领用、借用、更换、退库、耗材、合约号码等主链路已建立。
- 资产管理：资产维护、耗材维护、合约号码维护、标签打印、跨公司转移、资产报废、账面报废、资产处置、员工资产信息查询已建立。
- 库存管理：资产接收、耗材接收、入库、出库、移库、转移、库管员工作台已有主要原型。
- 资产盘点：4 个子菜单入口已建立；“盘点项目”完整原型已建立，其余 3 个等待来源材料。
- 原型标注：全局页面作用域、细粒度 target、弹窗生命周期、target/display-anchor 分离、覆盖层保存和质量门均已建立。
