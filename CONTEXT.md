# 当前状态

- 项目在 `main` 持续迭代，除用户明确要求外不新建分支或 worktree。
- 页面继续按已确认 PRD/截图校准；历史 ERP 截图只作为字段和业务规则来源，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 页面级保存/提交/确认/返回等操作统一放页面底部居中；Card 内只保留对应分区的添加、删除、导入、导出等操作。
- `DetailGrid` 的整行字段已统一修正：`span={3}` 必须从新行开始，标签和值保持同一视觉行；备注、使用说明等多行文本按此规则展示。
- 库存管理“资产接收”PO 列表操作列已固定同时展示“接收 / 查看”；PO 单号和“接收”都进入 PO 详情，“查看”进入该 PO 的接收单列表。
- 资产接收 PO 详情已按 UI V2.1 重画：页面主标题取消蓝色竖块，PO 单信息改用 `DetailGrid`，流程按钮统一放页面底部居中；PO 物资明细按最新确认的完整字段集合展示。
- 资产接收按采购类型区分底部动作：电子设备只显示“创建接收单”，不显示“执行入库”；服务器、服务器备件、网络设备、网络设备备件只显示“执行入库”，不显示“创建接收单”。
- 库存管理“耗材接收”已完成 PO 查询/接收、REC、低值耐用品标签/SN维护和低耗耗材自动入库演示。
- 库存管理“入库”已完成新增入库、采购接收、退库入库、借用归还四类编辑页及对应添加/选择物资弹窗。
- 库存管理“出库”已新增独立 `OutboundPage.js`：创建后支持领用出库、借用出库两类编辑页，共用单据头，物资列表和添加物资维护字段按类型分叉。
- 库存管理“移库”已完成发起侧创建页、添加物资弹窗，以及接收侧接收单列表、接收详情和资产明细弹窗；字段均按 ERP 截图提取，视觉按现项目规范。
- 库存管理“转移”已切到独立 `TransferPage.js`：保留原查询列表，新增转移单创建页和添加转移物资弹窗，字段按 2026-09-10 ERP 截图提取。
- 移库“添加物资”弹窗物资信息已删除库区、货位、印刷号；弹窗顶部当前仓库为正常正文颜色；必填编辑字段统一使用红色星号。
- 工作台首页“物资申请”快捷按钮已改为进入个人工作台菜单下的“物资申请”页面；无工作台跳转状态直接打开 `/yewurules` 时，默认入口为“库存管理 → 资产接收”。
- ERP icon 库 `y8BydLFT5vQpnDMX9SpDzB` 节点 `1973:350` 已检查；当前后台四个一级菜单存在合适候选，本轮只做评估，尚未替换代码图标。

# 当前停留位置

- “库存管理 → 资产接收”当前重点为 PO 详情：PO 物资明细字段为选择、行号、操作、接收状态、物资总类、资产大类、物料编码、物料说明、PO单说明、配置、部件数量、部件说明、本次接收数量、采购数量、不含税单价、不含税金额小计、总税额、含税单价、含税小计、税率、已接收数量、草稿数量、约定到货日期、PR单/行号、SA单/行号、申请单号、部门、业务线。
- 资产接收 PO 列表的“接收”和 PO 单号行为一致，始终进入 PO 详情，不再根据接收状态切换去接收单列表；“查看”单独进入接收单列表。
- “库存管理 → 转移”已落地创建链路：查询列表点击“创建转移单”进入创建页，支持公司、备注、添加物资、Excel 导入入口、保存草稿和返回。
- 添加转移物资弹窗分为“选择物资 / 物资信息 / 转移单信息”三块；必填编辑字段使用红色星号，多行“使用说明”独占完整新行。
- “库存管理 → 移库 → 接收单据”已落地：支持按移库单号、单据状态、制单人、制单日期查询和资产扫描打开单据。
- `review/rd-review` 仍只保存阶段性已确认研发评审结论，不自动跟随 `main`。

# 近期关键决定

- 业务规则优先于设计系统；设计系统决定视觉，不反向改变字段、状态机和流程。
- 状态统一通过 `StatusTag`，详情字段优先使用 `DetailGrid`，查询区使用 `QueryBar + QueryItem`。
- 历史 ERP 页面继续只提取字段、表头和明确规则，不复刻旧版蓝色界面。
- 资产接收 PO 列表操作列有两个独立入口：**接收 / 查看**；接收=PO 详情，查看=接收单列表。PO 单号本身也固定进入 PO 详情。
- 资产接收 PO 详情的采购类型动作互斥：**电子设备 → 创建接收单；服务器 / 服务器备件 / 网络设备 / 网络设备备件 → 执行入库**。
- PO 详情页面结构统一为“页面标题 → PO 单信息 Card → PO 物资明细 Card → 底部流程操作”，不把流程按钮塞进信息 Card 右上角，也不在页面主标题前加蓝色竖块。
- “转移”是库存管理下独立菜单，不并入“移库”；现有 `InventoryDocumentPages.js` 中旧转移页保留为 legacy，正式菜单改为独立 `TransferPage.js`。
- 必填编辑字段的星号必须单独用红色展示，不能把 `*` 作为普通黑色标签字符；只读字段不因为历史截图带星号就机械标成必填编辑态。
- 只读资产信息直接展示文本；只有当前节点真实可维护字段使用输入控件。
- 多行文本字段必须独占完整新行，禁止通过空文本、拉宽相邻字段等方式补布局。
- Figma icon 库候选只有在明确决定替换后再导出/接入项目，不先混用临时图标。
