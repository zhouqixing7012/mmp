# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产更换申请、资产更换办理、员工资产确认等页面已按最新要求完成布局和交互调整。
- 个人工作台页面统一使用单层画布，`Descriptions` 统一固定列宽，保证跨 Card 三列对齐。
- 资产退库申请页面已完成资产总数位置及资产用途字段调整。
- 已撤销误改的“资产更换-MIS鉴定”页面，恢复至调整前版本。
- 退库审批页面已完成三列布局、维修记录入口和鉴定通过/不通过操作调整。
- 本次新增退库流程附件能力：
  - 退库审批在“审批信息”上方新增“附件信息”Card，可上传附件。
  - 附件记录上传节点、上传人、上传时间和文件大小，并通过统一 Service 持久化。
  - 当前节点当前审批人可以删除本人上传的附件，删除前二次确认。
  - 资产退库办理可以新增附件，只能删除“ES退库办理”节点由当前办理人上传的附件。
  - 上一节点“MIS鉴定”上传的附件在资产退库办理页面只读展示，不允许删除。
- 资产退库办理页面已调整：
  - 申请人信息中公司、板块拆分为两个字段，整体使用三列布局。
  - 退库资产信息改为三列，备注单独占满一行，关联耗材继续展示。
  - 退库信息维护字段调整为仓库、责任人、MIS鉴定、鉴定结果、鉴定说明、资产标记、退库日期、使用说明、维修记录。
  - 责任人、鉴定结果、鉴定说明为只读；维修记录通过“查看”按钮打开弹窗。
  - 附件信息位于退库信息维护下方。
  - 审批操作收敛为确认、驳回、返回；驳回时审批意见必填。
  - “确认”按钮保留员工退库确认流程：未发起时先发起员工确认，员工完成确认后再次点击确认执行入库。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上。
- 三列详情卡片必须共享相同标签列宽和固定表格布局，避免各 Card 根据内容自行计算列宽。
- 数量、总计等统计信息统一放在对应明细 Card 右上角，不占用申请信息字段位置。
- “资产更换-MIS鉴定”与“资产退库-退库审批/MIS鉴定审批”是两个独立页面，后续修改必须先确认所属业务模块。
- 附件权限由“业务单据 + 上传节点 + 上传人”共同判断，页面禁用与 Service 校验同时生效。
- 原型只保存附件元数据，不上传真实文件内容或调用外部文件接口。
- 资产退库办理虽然只展示一个“确认”按钮，但仍保留员工退库确认这一必要流程节点。

# 上次停留位置

退库审批附件入口、资产退库办理三列信息、节点附件权限及审批操作已完成，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 退库审批。
2. 在“审批信息”上方确认存在“附件信息”，上传一个文件后应展示名称、大小、节点、上传人和时间。
3. 当前 MIS 审批人上传的附件可删除，删除时有二次确认。
4. 鉴定通过后进入 `/yewurules` → 个人工作台 → 资产退库办理。
5. 确认申请人信息中公司、板块为独立字段；退库资产信息为三列，备注单独一行。
6. 确认退库信息维护字段和只读状态符合要求，维修记录按钮可正常打开弹窗。
7. 确认 MIS 节点附件展示为“不可删除”；在 ES 退库办理节点上传的新附件可删除。
8. 点击“确认”：未发起员工确认时应先发起员工退库确认；员工完成确认后再次点击应完成入库。
9. 点击“驳回”：审批意见为空时提示必填，填写后流程结束。
10. 刷新页面后附件和办理信息仍保留。
