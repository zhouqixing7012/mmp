# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产退库办理页面已按最新要求调整字段、组织层级展示、鉴定结果和维修记录弹窗。
- 资产更换申请页面已调整：
  - 更换原因增加必填标识，并保留提交校验。
  - 删除“更换数量”字段，将统计移动到“退回物资信息”右上角，名称改为“总计”。
  - “更换须知”统一使用红色字体。
  - “已阅读并同意”复选框居中展示。
  - 删除页面右上角“批量提交时，一个旧资产生成一张申请单”的提示。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 页面处理完成后自动刷新并展示下一张待办；没有待办时显示空状态。
- 页面统计信息优先放在对应业务卡片右上角，不占用表单字段位置。
- 必填字段同时保留视觉标识和提交校验，不能只做其中一种。

# 上次停留位置

资产更换申请页面已完成最新布局调整，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 资产更换申请。
2. 确认更换原因显示必填标识，未填写时不能提交。
3. 确认“退回物资信息”右上角展示“总计：X项”。
4. 确认更换须知为红色文字，“已阅读并同意”居中。
5. 确认页面右上角不再显示批量提交提示。
