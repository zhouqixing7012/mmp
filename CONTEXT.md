# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 退库审批页面：
  - 维修记录作为资产信息的普通字段放在最下方，使用小尺寸文字按钮。
  - 附件表格只展示附件名称、附件大小、操作，底层节点和上传人元数据继续用于权限校验。
  - 删除无实际数据的代理人列。
  - 申请日期、部门、状态标签和审批按钮样式完成统一。
- 资产更换、资产退库、资产借用三个流程完成设计语言批量整改：
  - 详情信息默认三列，长文本单独一行。
  - “申请时间/申请日期”按是否展示时分秒统一命名。
  - 部门层级通过公共方法统一使用“.”连接。
  - 只涉及固定资产的标题、表格列和弹窗统一使用“资产”，不再混用“物资”。
  - 提交、同意、驳回、确认、取消、返回、加签、转签等流程按钮统一去除图标。
  - 搜索、添加、上传、删除、刷新、扫码等工具操作保留含义明确的图标。
  - 审批记录无真实代理数据时不展示代理人列。
  - 借用申请的自定义 section 已替换为标准小尺寸 Card。
  - 维修记录、查看名下资产等字段级入口统一使用小尺寸文字按钮。
  - 空状态返回按钮统一为“返回工作台”。
  - 资产选择弹窗只保留一个主标题，不展示重复二级标题。
- 状态展示完成统一：
  - `StatusTag` 新增 `business` 类型，统一审批状态、资产状态、盘点状态、员工状态等颜色。
  - 借用审批记录、更换审批记录、退库审批记录和资产选择弹窗改为复用 `StatusTag`。
  - 修复统一状态组件在当前页面中出现状态文字空白的问题：状态数据仍保留，组件改为显式文字和颜色样式，并兼容 `value`、`status`、`children` 三种传值方式。
  - 修复部分页面未传 `type="business"` 时把审批状态按是/否渲染、统一显示“否”的问题。
  - `StatusTag` 默认改为自动识别：布尔值、0/1、是/否按是非状态处理；其他文本自动按业务状态原文展示，未知状态也不再降级成“否”。
  - 空状态值统一显示 `-`，状态字符串统一去除首尾空格。
- 新增公共展示格式工具：
  - `formatDepartment`：部门层级 `/` 转 `.`。
  - `formatDateText`：日期统一展示为 `YYYY-MM-DD`。
- 新增项目级设计规范 `docs/UI_DESIGN_GUIDELINES.md`，覆盖画布、Card、三列详情、日期、部门、命名、按钮、审批记录、状态标签、空状态和弹窗。
- `AI_RULES.md`、`CLAUDE.md` 已将该规范设为个人工作台页面的强制前置规则，新页面提交前必须执行规范检查清单。

# 关键决定

- 设计走查中确认的跨页面问题必须跨模块统一修复，不只调整示例页面。
- 个人工作台页面遵循“页面标题 + 小尺寸业务 Card + 居中流程操作”的统一结构。
- 三列详情使用统一固定列宽；长文本、原因、说明、备注默认单独一行。
- 状态颜色由 `StatusTag` 集中维护，页面禁止自行声明状态颜色映射。
- 状态组件必须同时保证颜色和文字可见，不能只依赖 UI 库预设色；公共组件变更后需覆盖审批、更换、退库、借用页面进行回归。
- `StatusTag` 默认采用自动识别模式，避免页面漏传 `type` 时把业务状态错误映射成“否”；明确的是/否字段仍应传 `type="yesNo"`。
- 日期和部门格式由 `src/utils/displayFormat.js` 统一处理，页面禁止重复实现。
- 流程按钮使用纯文字，工具按钮才使用图标。
- 只有存在真实代理审批业务和数据时才展示代理人列。
- 只涉及固定资产时使用“资产”；同时包含资产与耗材时才使用“物资”。
- 规范通过“设计文档 + AI 规则 + 公共组件/工具”三层固化，后续新增页面默认按该规范执行。

# 上次停留位置

审批状态统一显示“否”的问题已在公共 `StatusTag` 组件修复，等待构建和页面回归验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 退库审批：确认审批状态显示“已提交”等真实文字，不再显示“否”。
2. 进入资产更换办理、MIS鉴定、借用配给、借用审批：确认审批记录状态文字与颜色均正常。
3. 检查未显式传 `type="business"` 的状态展示，确认“处理中、待审批、已完成”等仍按原文展示。
4. 检查明确的是/否字段，确认仍显示“是”或“否”。
5. 检查资产状态、盘点状态和员工状态，确认文字未丢失，空值显示 `-`。
6. 刷新页面后再次确认状态显示正常，确保不是仅首次渲染有效。
7. 检查不同状态：处理中/已提交为蓝色，待审批/待确认为橙色，已完成/已同意为绿色，已驳回为红色。
8. 进入资产更换办理、MIS鉴定、借用配给、借用审批、资产退库办理：确认申请日期、部门分隔符和三列布局一致。
9. 新增页面前阅读 `docs/UI_DESIGN_GUIDELINES.md`，并按文末检查清单逐项验收。
