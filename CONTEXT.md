# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 退库审批、资产更换、资产退库、资产借用三个流程已完成统一设计语言整改：
  - 详情信息默认三列，长文本单独一行。
  - 部门层级统一使用“.”连接。
  - 流程按钮统一使用纯文字，工具按钮保留图标。
  - 审批记录无真实代理数据时不展示代理人列。
  - 状态统一由 `StatusTag.js` 渲染，`StatusTag.jsx` 仅作转发。
- 项目级设计规范已固化在 `docs/UI_DESIGN_GUIDELINES.md`，并通过 `AI_RULES.md`、`CLAUDE.md` 设为个人工作台页面强制规范。
- “合约号码 ES 配给”页面已按最新要求调整：
  - 删除“5级及以上人员流程为自动发起”的红色提示。
  - 申请信息新增附件下载入口，原型下载真实生成的演示文本文件。
  - 身份证号码改为普通单列，不再跨两列。
  - 电话号码字段收窄为单列宽度。
  - 号码配给删除“号码状态”，只保留电话号码和话费套餐。
  - 审批信息删除流程线图，只保留审批历史、审批意见和操作按钮。
  - ES审批同意后生成“库管员领用”节点。
- “合约号码库管员待办”已加入个人工作台：
  - 页面标题为“合约号码领用办理”。
  - 申请人信息和申请合约号码信息按三列展示。
  - 当前仓库、城市、子公司、单据备注、备注可编辑。
  - 办理操作调整为“发起领取确认、驳回、返回”。
  - 发起领取确认后单据进入“员工领取确认”节点，不再直接完成流程。
- 新增“员工合约号码领取确认”页面并加入个人工作台：
  - 领取信息展示使用人、部门、电话号码、标签号、话费套餐、当前仓库、领用原因和数量。
  - 确认提示使用红色强调。
  - 支持管理员输入员工工号视同刷卡确认，也支持点击二维码模拟扫码确认；页面不展示额外管理员操作提示。
  - 工号必须与申请人工号一致。
  - 确认成功后记录确认时间、确认方式、确认员工工号，号码状态更新为“已领用”，单据状态更新为“已完成”。
- 合约号码演示数据存储键升级至 `mmp.contractNumberAllocation.applications.v3`，新增独立的库管员待办和员工领取确认演示单据。

# 关键决定

- 老系统截图用于确认业务字段和流程，页面视觉结构统一按当前个人工作台规范重构，不照搬旧页面样式。
- 个人工作台页面遵循“页面标题 + 小尺寸业务 Card + 居中流程操作”的统一结构。
- 三列详情使用统一固定列宽；长文本、原因、说明、备注默认单独一行。
- 状态颜色由 `StatusTag` 集中维护，页面禁止自行声明状态颜色映射。
- 日期和部门格式由公共工具统一处理。
- 流程按钮使用纯文字，工具按钮才使用图标。
- 原型附件必须提供真实可执行的下载交互，不能配置不存在的服务器地址。
- 合约号码 ES 配给、库管员待办、员工领取确认共用同一 mock、service 和 demoStorage 数据链路，页面禁止直接操作 localStorage。
- 涉及员工本人确认的领取流程，上游办理节点只负责发起确认；员工完成刷卡或扫码后才最终更新号码和单据状态。
- 新增默认演示数据结构时需要升级存储版本，避免旧 localStorage 阻止新节点和新字段出现。

# 上次停留位置

合约号码 ES 配给字段已精简；员工合约号码领取确认页面已新增，库管员待办已改为发起员工确认，等待构建和页面验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 合约号码ES配给。
2. 确认身份证号码只占一列，附件位于相邻列。
3. 确认号码配给仅展示电话号码和话费套餐，不展示号码状态。
4. 确认审批信息没有 Steps 流程线图。
5. `/yewurules` → 个人工作台 → 合约号码库管员待办。
6. 点击“发起领取确认”，单据应进入员工领取确认节点，页面转为无待办状态。
7. `/yewurules` → 个人工作台 → 员工合约号码领取确认。
8. 确认领取信息按三列展示，部门使用“.”连接，提示为红色。
9. 输入非申请人工号应提示工号不匹配；输入申请人工号确认后应显示确认结果。
10. 点击二维码区域应按扫码方式完成确认。
11. 确认成功后号码状态更新为“已领用”，单据状态更新为“已完成”，刷新后不再出现在待确认列表。
12. 检查三个页面的 Card、按钮、字段宽度、空状态和弹窗符合 `docs/UI_DESIGN_GUIDELINES.md`。
