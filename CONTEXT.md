# 当前正在做什么

- `feat/contract-consumables-maintenance` 正在重做“资产管理 → 耗材维护”。耗材 PRD、独立业务页面、Mock 数据、保存边界和对抗性测试用例已完成，当前处于最终构建验证阶段。
- 合约号码维护 PRD 已入库，但页面尚未开始改造；当前阶段不修改合约号码业务代码。

# 上次停留位置

- 耗材维护已脱离通用 `LedgerMaintenancePage`，改为独立页面，覆盖查询、更多查询、列表排序/分页、耗材卡片、编辑、操作历史、模板下载、批量覆盖入口和导出。
- 耗材卡片严格只展示已确认的 29 个字段；板块、耗材说明、数量及其他非白名单字段不可编辑。
- 公司修改后仓库候选按当前公司实时刷新；跨公司旧仓库立即清空。公司未变化时，历史已保存但退出正常候选的仓库允许原样保留。
- 保存边界不信任前端派生值：公司编码、责任人姓名/部门、主资产说明均由系统数据重新推导；同时校验位置、仓库、状态、序列号、日期和主资产关系。
- 非 `SOHU` 普通责任人没有部门时不允许保存，并提示“该员工对应的部门为空，请联系管理员添加”；`SOHU` 虚拟用户继续走既有特殊逻辑。
- 单条编辑启用日期有值时保存页面值；留空时触发现有“根据购置日期计算启用日期”规则。原型没有该公式来源，不编造算法，留空时保留当前可见结果以表达“不会直接落空”。批量修改仍按覆盖策略，启用日期空白可覆盖为空。
- 批量修改已改为“上传 → 校验 → 展示结果 → 保存”两阶段闭环；校验失败展示行号、耗材标签号、失败原因，并禁止保存。原型不解析真实 Excel，文件名包含“校验失败”用于演示失败分支。
- 耗材导出完整字段清单已恢复到 PRD；卡片 29 字段限制只约束详情展示，不删除既有后台可导出字段。
- 既有公司+板块账套、成本中心回退、费用账户/内部费用账户重算规则已恢复到 PRD；具体算法继续复用现有后台实现，不在原型中编造。
- 单条保存成功后继续停留当前编辑页，并用保存后的最新数据刷新；无变化不更新时间、不生成历史。
- `src/services/assetManagementService.test.js` 已新增耗材维护保存边界对抗性测试用例，覆盖越权字段、SN 唯一性、仓库、位置、报废、主资产、派生字段伪造、启用日期留空和历史生成等场景。
- 当前连接环境无法直接拉取 GitHub 仓库执行 Jest，因此测试用例已固化但尚未在本地实际运行；Vercel 构建状态继续作为代码编译验证信号。

# 近期关键决定

- 批量上传采用覆盖策略：空白即清空；公司、责任人、City、Building 等必填字段为空直接失败；任一行失败则整份文件不保存。
- 批量模板中的板块、耗材说明、数量为只读核对字段，不允许通过批量上传写回修改。
- 耗材卡片字段只保留用户最终确认的 29 项，不增加品牌、用途、资产标记、主资产内存等额外字段。
- 主资产标签号允许维护但不得关联自身；主资产说明由所选主资产带出。
- 低值耐用品不提供 EBS 手工同步入口，也不生成自动 EBS 同步任务。
- 当前功能分支不主动合并 `main`；最终合并时只合并本分支差异，对 `src/services/assetManagementService.js` 等双方都修改的区域人工保留两边有效逻辑，禁止整文件覆盖。
