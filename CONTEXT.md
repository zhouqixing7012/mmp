# 当前正在做什么

- `feat/contract-consumables-maintenance` 已完成“资产管理 → 耗材维护”原型与 PRD 收口，并已完成“资产管理 → 合约号码维护”第一轮独立页面、保存边界、PRD 和对抗性测试收口。
- 当前处于合约号码维护最终构建验证阶段。

# 合约号码维护当前进度

- 已重建合约号码 Mock 数据，固定资产大类 `34`，覆盖合约号码、合约机、在用、在库、报废、责任人、套餐、合约期限、领用和操作历史等场景。
- 已新增独立 `contractNumberMaintenanceService.js`，合约号码保存规则不再继续堆叠到共享资产 Service；旧 `assetManagementService.js` 的合约号码导出已改为转发到专用 Service，避免历史调用方绕过新白名单。
- 合约号码页面已脱离通用 `LedgerMaintenancePage`，实现基础查询、更多查询、列表排序/分页、完整列表字段、标签号卡片、单条编辑、操作历史、模板下载、批量编辑演示和导出。
- 卡片固定为“详细信息 / 合约号码操作历史”两个页签，详情按资产身份、套餐与合约、状态与仓库、报废、人员、领用与扩展分组。
- 单条编辑按目标 PRD 使用严格白名单：使用公司、责任人、合约号码、合约号码说明、套餐内容、合约期限、金额、号码状态、仓库、备注、报废日期、报废原因、领用原因、领用说明。
- 旧页面曾允许修改但目标白名单未明确开放的身份证号码、子公司、领用日期、副卡、维修记录等字段，当前单条维护统一只读，接口同样拒绝直接越权提交。
- 责任人变化后，姓名、部门、子公司、员工职级和身份证号码统一根据人员主数据重新派生刷新；这些派生字段不信任页面提交值。非 `SOHU` 普通员工没有部门时禁止保存。
- 合约号码按 PRD 的 11 位手机号规则校验，支持 13/14/15/17/18 号段。
- 金额最多 8 位整数 + 2 位小数；备注最多 120 字；合约期限开始日期不得晚于结束日期。
- 状态/仓库规则：状态名称含“在用”时仓库必须为空；其他状态必须选择有效仓库，当前原型仓库为 `I10086.集团合约机库`。
- 报废状态必须填写报废日期和报废原因；非报废状态不允许残留报废字段。
- 查询已区分单值模糊和多值模糊：合约号码说明、套餐内容为单值模糊；标签号、合约号码、领用说明、申请单号、报废原因等按 PRD 支持多值。部门候选包含父级部门，选择父级时通过前缀匹配覆盖下级部门数据。
- 实际变化后生成来源为“合约号码台账维护”的修改事务；无实际变化不更新最后修改时间、不生成历史。
- 批量编辑继续沿用旧 11 列模板，空白表示保留原值；原型只演示“上传 → 全量校验 → 结果 → 保存”流程，不伪造真实 Excel 已解析落库。
- `src/services/contractNumberMaintenanceService.test.js` 当前固化 24 个保存边界对抗性用例，覆盖字段越权、客户端伪造派生值/最后修改时间、号码格式、金额、合约期限、状态/仓库、报废字段、责任人及身份证派生、公司编码派生、领用原因、不存在 ID、无变化和事务生成等场景。
- 合约号码 PRD 已按“背景 → 改造总结 → 通用规则 → 页面字段及交互 → 校验 → 后续说明”结构重写，旧代码现状与目标规则不再混写。
- 当前连接环境无法直接执行 Jest，因此测试用例已固化但未宣称实际 24/24 运行通过；Vercel 作为编译/部署验证信号。

# 耗材维护已确认口径

- 耗材卡片只保留最终确认的 29 个字段；板块、耗材说明、数量等非白名单字段不可改。
- “用途”不作为查询条件，也不进入批量模板。
- Floor 使用全部启用 Floor，不按 Building 收窄；Building 仍必须属于当前 City。
- 仓库候选要求：启用 + 当前公司 + 仓库用途为 `IU0001`（耗材库）或 `IU0003`（资产高耗库）；公司未变化时允许历史仓库原样保留。
- SN 唯一性比较去首尾空格并忽略英文字母大小写，但保存和展示保留实际输入大小写；`缺省`不参与唯一性冲突。
- 主资产新关联必须存在、未报废、不得关联自身，且责任人与当前耗材责任人一致。
- 批量上传采用覆盖策略；启用日期有值按 `yyyy/MM/dd` 读取；任一行失败整文件不保存。

# 分支规则

- 当前功能分支不主动合并 `main`。
- 最终合并时只合并本分支差异；双方同时修改的文件人工保留两边有效逻辑，禁止整文件覆盖。
- 当前原型不伪造真实权限、Excel 解析、数据库事务或外部系统集成能力；这些由正式研发按 PRD 接入。
