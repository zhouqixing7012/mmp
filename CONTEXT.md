# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产退库办理页面已按最新要求调整字段、组织层级展示、鉴定结果和维修记录弹窗。
- 资产更换申请页面已调整必填标识、总计位置、更换须知颜色和勾选布局。
- 统一个人工作台页面画布，去掉业务页重复的浅蓝背景和外层留白。
- MIS鉴定页面已调整申请人字段、三列布局及审批流程区。
- 资产更换办理页面已调整字段、默认值及审批记录列。
- 个人工作台内的 `Descriptions` 统一使用固定表格布局和固定标签列宽，保证不同 Card 之间三列网格对齐。
- 员工资产确认页面已调整：
  - 领用人信息、两个标签页和对应物资明细合并在同一个 Card 中。
  - 提示和保管职责说明合并在同一 Card 中。
  - 确认区为左侧员工工号输入、右侧二维码，原有刷卡和扫码确认逻辑保持不变。
  - 删除页面顶部“确认工号必须与申请人工号一致”说明。
  - 删除管理员录入工号、管理员扫码等操作提示文案。
  - 删除未确认状态下的蓝色操作提示条。
  - 返回按钮去掉图标，仅保留文字。
  - 两个标签页使用小尺寸样式；“维修物资明细”“待发放物资明细”改为与页面其他内容一致的普通小标题。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上。
- 审批页面先展示业务处理区，再展示审批流程，审批意见和操作统一放在流程记录之后。
- 共用审批记录组件通过参数控制代理人字段，避免影响其他页面。
- 三列详情卡片必须共享相同标签列宽和固定表格布局，避免各 Card 根据内容自行计算列宽。
- 管理员代确认属于操作逻辑，不在页面额外展示操作原理和校验规则，只保留必要控件及异常提示。
- 标签页和卡片内二级标题字号不得高于 Card 标题，避免信息层级倒置。

# 上次停留位置

员工资产确认页面的冗余提示、返回按钮图标和局部标题字号已完成调整，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 员工资产确认。
2. 确认页面顶部不再展示工号校验说明。
3. 确认刷卡/扫码区域不再展示管理员代录、代扫等说明文字，也不再展示未确认状态的蓝色提示条。
4. 确认返回按钮没有图标。
5. 确认两个标签页及“维修物资明细”“待发放物资明细”字号与页面其他普通内容协调，不再显得突出。
6. 确认员工工号校验、扫码确认及当前待确认节点校验逻辑仍正常生效。
