# 当前状态

项目为企业资产管理产品演示前端。当前继续在 `feat/workspace-updates` 修改个人工作台，不新建分支或 worktree。

# 当前正在做什么

个人工作台已完成一轮 PC 端详情/审批 UI 收敛，并正在补充员工侧移动端原型。当前重点是“物资申请”和“移动端工作台”。

## 本阶段已完成

### 物资申请
- 原“资产申请”统一更名为“物资申请”，菜单、页面标题、面包屑、预览标题及提示文案同步。
- 首次进入仍保留“申请须知”弹窗，仅删除正文中的“【申请原则】”标题，须知内容及“已阅读”动作保留。
- “添加物资”继续使用大类 / 小类 / 品牌 / 型号 / 配置五级选择。
- 长名称不使用 `...` 隐藏；自动换行完整展示，并保留 Tooltip。
- 演示目录支持“同品牌多型号、同型号多配置”。
- 长配置案例按一条完整配置展示：`转接线.绿联、CELINK、苹果.Type-C转（VGA/以太网/USB/Type-C）`，不拆分。
- 明细“详细说明”统一改为“申请原因”，底层暂保留 `detail` 字段。
- 重复选择同一物料时不新增第二行，原明细数量 +1，并给出合并提示。
- 耗材“关联主资产”使用独立弹窗，只能从当前员工本人名下已有资产中选择。
- 关联主资产弹窗可展示标签号、资产说明、配置、状态、用途；选择完成后编辑态与预览态均**只回显资产标签号**。

### 移动端工作台
- 个人工作台新增“移动端工作台”菜单，紧跟“工作台首页”。
- 当前菜单已切换到 `MobileWorkspacePageV2`，原移动端页面文件暂保留但不再作为菜单入口。
- 移动端原型放在桌面内容区中的独立手机画布内，内部自行管理滚动、底部导航和弹层。
- 移动端使用项目现有 **Ant Design 6** 组件重绘，并参考飞书类 App 的清爽信息架构：白色卡片、浅灰页面底色、蓝色主色、弱边框、紧凑留白、轻量状态标签。
- 未引入 `antd-mobile` 新依赖；基于仓库已有 `antd@6.4.5` + `ConfigProvider` 做移动端主题适配。
- 首页一级数据分类固定为：**资产 / 耗材 / 合约号码**。
- 首页顶部“物资概览”保留轻量渐变概览卡，资产 / 耗材 / 合约号码分别使用图标 + Statistic 数量卡展示，点击数量卡可直接切换对应分类；已删除“我的资产服务”“在用物资”两处辅助文案，仅保留资源数量信息。
- 首页一级分类使用 Ant Design `Tabs` 的线条式页签；单据“未完成 / 已完成”、盘点“未盘 / 已盘”、物资申请“资产 / 耗材”同步使用同一套线条页签语言。
- 资产卡片单条操作固定为：**退库 / 转移 / 更换**；三个按钮保持同级默认样式，“更换”不再高亮；资产详情页底部同步提供三项同级操作。
- 长按任一资产约 520ms 进入批量选择模式；可继续点选多项资产，底部显示 **批量退库 / 批量转移 / 批量更换** 操作栏，并提供取消批量操作入口；页面不再额外显示“长按任一资产可进入批量……”说明文案。
- 耗材卡片展示数量和关联主资产标签号。
- 合约号码卡片展示号码、运营商、套餐、状态；号码退库仍从号码卡片进入，不放到全局“+”快捷入口。
- 底部导航保留：首页 / 单据 / + / 盘点 / 扫一扫。
- 中间“+”快捷入口**只提供：物资申请、资产借用**，不提供合约号码退库。
- 历史单据支持未完成/已完成及按物资申请、资产借用、资产转移、资产更换、资产退库、合约号码退库筛选。
- 移动端继续提供资产详情、资产退库、资产转移、资产更换、资产借用、物资申请、合约号码退库、资产盘点和扫一扫模拟查询。
- 扫一扫在原型中继续使用资产标签号输入模拟，不声称接入真实摄像头。
- 移动端“物资申请”不再使用简单物料卡片列表，改为与 PC 端同一业务结构的 **大类 → 小类 → 品牌 → 型号 → 配置** 五级目录筛选。
- 五级目录在移动端采用“逐级钻取”而不是横向五列：顶部用五个可回跳的层级胶囊展示当前路径，主体一次只展示当前层级选项，选择后自动进入下一级；已选路径同时以面包屑方式展示，可快速返回上级修改。
- 配置层允许多选；选中的物料进入底部“购物车”。底部固定显示购物车数量和“下一步填写”，点击购物车打开 Ant Design `Drawer` 底部抽屉查看、删除或清空已选物料。
- 点击“下一步填写”后，按已选物料逐卡填写数量、申请用途、申请原因；耗材关联主资产使用底部 `Drawer` 从本人名下资产中选择，回显仍只显示资产标签号。
- 移动端目录与 PC 端共用相同物料样例口径，包括同品牌多型号、同型号多配置和长配置名称案例；长配置在移动端允许自然换行完整展示。

### 其他已完成且需保持
- 合约号码流程：`合约号码ES配给 → 合约号码配给主管审批 → 合约号码库管员待办 → 员工合约号码领取确认`。
- 合约号码 ES 配给新增必填配给附件；主管审批页只读展示 ES 配给结果。
- 资产更换申请、MIS 鉴定、资产更换办理已统一到个人工作台单层画布和 `DetailGrid` 视觉规范。
- MIS 鉴定结果仅保留“资产维修 / 资产更换”。
- 资产更换办理保持原流程，不增加 Steps、不做阶段解锁、不改动态主按钮、不调整审批历史。
- 新员工领用单及新员工领用员工确认已新增到个人工作台。
- 本次重构全面取消电子签，员工确认统一使用刷卡/工号和狐小 e 扫码。

# 近期关键决定

- 个人工作台 PC 页继续使用单层画布；业务页自身不再叠加 `min-h-screen`、独立灰底和额外外层 padding。
- 多 Card 三列详情需要严格跨 Card 对齐时统一使用公共 `DetailGrid`。
- 主要业务 Card 标题使用紧凑蓝色竖向分组标识。
- “物资申请”作为资产+耗材统一入口，相关菜单与页面命名保持一致。
- 长配置名称属于核心业务信息，不用省略号隐藏。
- 用户明确给出的长字符串若定义为“一个配置名称”，必须作为一个值展示，不能自行拆分成品牌/接口/配置。
- “借用”是业务流程，不是员工持有物资分类；移动端首页一级分类固定为资产 / 耗材 / 合约号码。
- 旧移动端截图只用于理解原系统信息架构和交互习惯，不要求像素级复刻；新移动端允许使用 Ant Design / 飞书类现代 App 视觉重绘。
- 移动端主要平级内容分类统一优先使用线条式 `Tabs`，不再用大块 `Segmented` 作为一级导航。
- 资产单条变更能力固定为退库 / 转移 / 更换，三个动作视觉同级；长按资产进入批量选择模式，批量动作与单条动作保持同一业务语义。
- 全局“+”只承载主动申请入口：物资申请、资产借用；合约号码退库属于已有号码的变更动作，从号码卡片进入。
- 移动端物资选择需要保留 PC 端五级目录语义，但不能在窄屏机械压缩成五列；统一采用“逐级钻取 + 可回跳路径 + 底部购物车”的移动交互。
- 购物车只负责收集待申请物资，业务字段填写放到下一步逐物料完成，避免在目录筛选阶段同时塞入数量、用途、原因等大量表单控件。
- 关联主资产选择完成后的回显只显示资产标签号。

# 当前验收路径

1. 打开个人工作台 → 物资申请：首次仍出现“申请须知”，不显示“申请原则”标题。
2. 添加物资：长大类/小类/品牌/型号/配置允许完整换行；长配置案例保持一条完整配置。
3. 重复选择同一物料：原行数量自动 +1，不新增重复行。
4. 耗材关联主资产：通过弹窗从本人名下资产选择；确认后明细与预览只显示标签号。
5. 打开个人工作台 → 移动端工作台：首页分类必须为“资产 / 耗材 / 合约号码”。
6. 顶部物资概览不显示“我的资产服务”“在用物资”，仅展示“物资概览”、资源总量和三类 Statistic 数量卡。
7. 首页分类使用线条式 Tabs；单据、盘点、物资申请页保持同类 Tab 语言。
8. 每个资产卡片显示“退库 / 转移 / 更换”三个同级按钮，“更换”不高亮；资产详情页同步存在三项同级操作。
9. 页面不显示长按批量操作说明；实际长按资产仍可进入批量选择，底部出现“批量退库 / 批量转移 / 批量更换”。
10. 底部“+”只显示“物资申请 / 资产借用”，不出现合约号码退库。
11. 合约号码退库仍可从合约号码卡片进入。
12. 进入移动端“物资申请”，应先看到资产/耗材 Tab、搜索框及大类/小类/品牌/型号/配置五级路径，而不是简单物料卡片列表。
13. 点击大类后逐级进入小类、品牌、型号、配置；任一已选层级可点击回跳，配置长名称完整换行展示。
14. 配置层可多选，底部购物车数量实时变化；点击购物车弹出已选物资底部抽屉，可删除单项或清空。
15. 购物车有物资后点击“下一步填写”，按物料卡片填写数量、申请用途、申请原因；耗材通过底部抽屉选择关联主资产且只回显标签号。
16. 单据页可切换未完成/已完成，并包含资产转移等当前业务类型筛选。
17. 盘点与扫一扫仍可交互；扫一扫以标签号输入模拟。
