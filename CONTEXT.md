# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 项目级设计规范已固化在 `docs/UI_DESIGN_GUIDELINES.md`，个人工作台页面统一使用小尺寸 Card、三列详情、统一 StatusTag、部门“.”分隔和无图标流程按钮。
- 员工确认方式已统一：本次资产系统重构全面取消电子签确认。
- 已逐一核对个人工作台当前员工确认页面：员工领用确认、员工合约号码领取确认、员工借用确认、员工资产确认、员工退库确认、员工合约号码退库确认、员工耗材领用确认；均只保留刷卡/员工工号确认和狐小 e 扫码确认。
- 已清理耗材流程早期聚合页面中的旧电子签实现，耗材 Service 和 Mock 中已删除电子签字段，演示数据存储版本升级为 `v2`。
- 在不覆盖现有“耗材领用”页面的前提下，新增“耗材领用方案一（物料编码）”和“耗材领用方案二（标签号）”两套独立对比原型。
- 账面报废申请页已开始重构：公司改为统一选择弹窗，新增“是否公司间转移”“已到报废期报废原因”“未到报废期报废原因”“丢失报废原因”。
- “是否公司间转移=是”时展示“公司间转移明细”，字段为资产标签号、新责任人、新公司、新板块、新成本中心、City、Building、Floor、调账后仓库。
- 公司间转移明细与上方资产明细严格一一对应：资产标签号由资产明细自动带入，不提供新增/删除入口，也不可修改；资产明细新增或删除后转移明细同步变化。
- 公司间转移明细除“调账后仓库”外均必填，提交时逐行校验。

# 耗材流程当前口径

- 个人工作台不展示独立“耗材申请”入口，耗材原型从 MIS 鉴定及后续流程开始。
- 耗材 MIS 鉴定使用“鉴定通过、鉴定不通过”，本轮不拆分独立“MIS 鉴定处理”Card。
- 耗材审批只展示耗材说明、配置、数量、申请原因和详细说明。
- 耗材配给匹配状态只包含“库存领用、统一采购”，不存在“驳回类型”。
- 耗材汇总明细按申请人/申请单分 Card 展示，不存在“是否通过”字段，驳回入口位于 Card 右上角。
- 耗材领用不展示“领用确认方式”和“确认状态”；点击“领用确认”后进入员工耗材领用确认。
- 员工耗材领用确认只支持刷卡/工号和扫码确认。
- 两个耗材领用方案为页面布局对比原型，不覆盖现有耗材领用业务页面，也不改变正式流程状态。

# 账面报废当前口径

- 账面报废申请页文件：`src/pages/AccountingScrapEdit.js`。
- 公司使用项目统一 `SelectModal`，数据复用 `mockCompanies`。
- 是否公司间转移默认“否”；选择“是”后才展示公司间转移明细。
- 公司间转移明细中的资产标签号来自资产明细，禁止直接编辑和直接删除行。
- 新公司同样复用公司选择弹窗；新责任人、新板块、新成本中心、City、Building、Floor 当前为必填输入项；调账后仓库非必填。
- 当前阶段只修改账面报废申请页，账面报废审批页尚未调整。

# 关键决定

- 用户在资产系统项目中说“原型”“原型图”或“放到个人工作台”时，默认交付仓库内可运行网页，不生成静态图片。
- 老系统截图用于确认业务字段和流程；视觉结构统一按当前个人工作台规范重构。
- 页面不能因为数据对象已有字段就全部展示；展示字段以用户当前确认的页面口径为准。
- 独立“驳回”按钮不代表需要新增“驳回类型”等辅助字段；只有用户或 PRD 明确要求时才增加业务枚举。
- 汇总明细的整单驳回通过申请人 Card 右上角按钮完成，不在明细表中重复增加“是否通过”字段。
- 上游办理页负责发起员工确认并跳转确认页；确认方式不在办理页重复展示。
- 本次重构不存在支持电子签的员工确认页面；不得根据旧代码、旧 Mock、旧 PRD 或其他模块自行恢复电子签。
- 涉及员工本人确认的领取或退库流程，员工确认后库管员才能执行最终出库或入库。
- 同一业务需要多套页面方案用于比较时，新增独立菜单和配置化共享组件，不覆盖当前已验收页面，不复制完整页面文件。
- 公司间转移明细不是独立选资产入口，资产集合永远以上方账面报废资产明细为唯一来源。

# 上次停留位置

账面报废申请页第一阶段已完成，等待继续确认公司间转移字段交互、审批流程及后续账面报废规则。

# 验收路径

1. 打开 `/` → “账面报废申请单”。
2. 点击“公司”，应打开公司选择弹窗，选择后回填“编码.名称”。
3. 页面应展示“是否公司间转移”和三类报废原因字段。
4. “是否公司间转移=否”时不展示公司间转移明细；切换为“是”后展示。
5. 公司间转移明细行数与资产明细一致，资产标签号完全由资产明细带入且不可编辑，明细内无新增/删除按钮。
6. 上方资产明细新增或删除资产后，公司间转移明细同步新增或删除对应行。
7. 公司间转移明细的新责任人、新公司、新板块、新成本中心、City、Building、Floor 任一为空时提交应拦截；调账后仓库允许为空。
8. 当前账面报废审批页保持不变。
