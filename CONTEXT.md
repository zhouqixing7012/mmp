# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图只提取字段和业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- 当前重点是 **库存管理 → 资产接收 → 接收单详情 → 维护接收明细**。
- 已新增 `docs/PRD-库存管理-资产接收.md` 记录当前实现口径。

# 上次停留位置

- 接收单明细页已删除“合并行”。
- “删除行 / 生成标签号 / 维护SN号”只在接收单状态为 **待接收** 时展示；接收完成不展示。
- 删除行只作用于勾选的接收明细。
- 生成标签号一次给全部接收明细生成标签号，完成后按钮消失；标签号全部生成后才展示“打印标签号”。
- 维护SN号一次把全部明细 SN 设置为“缺省”，完成后按钮消失。
- 扫描光标在生成标签号前禁用；生成后按两步处理：先扫资产标签号筛选并锁定明细，再扫序列号条形码写入该资产 SN。
- 扫描输入框会按阶段提示“请扫描资产标签号”或“已定位 xxx，请扫描SN号”，不新增额外流程按钮。

# 近期关键决定和原因

- 接收明细的标签号生成、缺省 SN 维护属于 **整批动作**，不依赖行勾选；删除属于 **行级动作**，必须勾选。
- 扫码枪只是一种输入方式，仍使用同一个输入框 + 回车；通过明确的两步状态避免标签号和 SN 被错误写反。
- 已完成接收单只保留查看/打印能力，不再展示会修改接收明细结构或初始数据的批量动作。
- 页面级操作继续放底部居中；表格分区级操作留在对应 Card 内。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”进入 PO 详情，“查看”进入接收单列表。
- 电子设备 PO 详情只显示“创建接收单”；服务器、服务器备件、网络设备、网络设备备件只显示“执行入库”。
- 接收单列表制单时间为单个日期范围查询，接收单号作为详情入口，不保留重复操作列。
- 入库、出库、移库、转移继续沿用此前已确认字段与状态规则，未在本轮改动。
