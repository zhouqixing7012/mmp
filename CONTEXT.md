# 当前状态

项目为企业资产管理产品演示前端。当前继续在 `feat/workspace-updates` 修改个人工作台，不新建分支或 worktree。

# 当前设计规范

- 个人工作台 PC 页面使用单层画布，不在业务页重复增加 `min-h-screen`、独立灰底、额外外层 padding 或阴影标题盒。
- 页面标题使用标准 `Typography.Title level={4}`；蓝色竖块仅用于业务 Card 标题。
- 业务分区使用 `Card size="small"`；三列详情优先使用公共 `DetailGrid + DetailItem`；列表统一使用 small bordered Table。
- 只读字段直接展示文本或 `StatusTag`，不使用 readOnly / disabled Input、TextArea 伪装只读；只有真实可编辑字段保留表单控件。
- `DetailGrid` 的整行字段必须从完整新行开始，避免在未填满三列时插入 `span={3}` 导致串行/错位。
- 流程按钮使用纯文字；工具型动作可使用图标。员工确认全面取消电子签，统一使用刷卡/工号 + 狐小 e 扫码。

# 本阶段已完成

## 物资申请
- “资产申请”统一更名为“物资申请”，菜单、页面标题、面包屑、预览标题同步。
- 首次进入保留“申请须知”弹窗，仅删除“申请原则”标题。
- 大类 / 小类 / 品牌 / 型号 / 配置五级选择保留，同品牌多型号、同型号多配置；长配置名称完整换行展示。
- 重复选择同一物料时原明细数量 +1，不新增重复行。
- 耗材关联主资产使用独立弹窗，只能选择本人名下已有资产；选择后只回显资产标签号。

## 移动端工作台
- 首页一级分类固定为：资产 / 耗材 / 合约号码。
- 底部导航：首页 / 单据 / + / 盘点 / 扫一扫；“+”只提供物资申请、资产借用。
- 资产单条操作：退库 / 转移 / 更换；长按进入批量退库 / 转移 / 更换。
- 移动物资申请使用“大类 → 小类 → 品牌 → 型号 → 配置”逐级钻取 + 底部购物车 + 下一步填写。

## 个人工作台首页
- 资产页签操作按钮按“物料编码配置规则”中的 `allowReturn / allowTransfer / allowReplace` 控制显隐；借用资产无论普通物料规则如何，**仅展示退库**。
- 资产支持多选；选择两项及以上时，根据所选资产是否全部支持对应动作，显示**批量退库 / 批量转移 / 批量更换**。
- 退库、更换入口会携带当前所选资产标签号进入对应新增申请页，目标页根据 `prefillAssetTags` 自动带入资产；转移入口同样向转移页传递 `prefillAssetTags`。
- 合约号码页签只展示：**合约号码、合约号码说明、操作**；操作只保留“退库”，点击进入合约号码退库申请页。
- 资产信息列不展示电脑/显示器等小图标；主信息黑体展示原资产说明，即**品牌.型号**。
- 资产信息第二行使用浅灰字展示**大类-小类**，大类与小类之间统一使用 `-`，不再拼接资产用途。
- **资产用途**从资产信息第二行拆出，作为独立字段放在“状态”之后。

## 号码控制
- 页面使用 QueryBar + Card + small bordered Table + 标准 Modal。
- 查询条件：姓名/工号、授权状态；授权状态仅已授权 / 未授权。
- 列表字段：工号、姓名、所属部门、授权状态、操作。
- 操作统一为发送通知、授权 / 停止授权。

## 合约号码申请与领用
- 原“号码管理”菜单统一更名为“合约号码申请”。
- 合约号码申请页按当前 UI 规范重画，保留身份证号码、申请原因、附件、实名认证提示及提交功能。
- 合约号码流程：`合约号码ES配给 → 合约号码配给主管审批 → 合约号码库管员待办 → 员工合约号码领取确认`。
- 合约号码库管员待办：当前仓库只读、无单据备注、城市弹窗选择；城市+子公司一行，申请原因、备注各独占一行。
- 员工领取确认：领用人信息 / 领用物资明细 / 保管职责 / 刷卡/扫码确认；明细仅标签号、电话号码、说明、申请数量、领用数量、申请原因。

## 耗材汇总
- **耗材汇总明细页不展示“导出申请明细”按钮**。
- 汇总申请页中的“申请明细”Card 右上角保留**导出**按钮，支持导出当前通过的申请明细 CSV。
- 导出字段：序号、部门、申请单号、申请人、物资类别、物料说明、采购数量、预计费用（元）、申请原因、ES建议。

## 资产借用
- 资产借用“添加资产”弹窗与物资申请弹窗区分：**只保留大类 / 小类两级选择**，不展示品牌 / 型号 / 配置。
- 弹窗支持按大类或小类搜索，小类可多选，右侧仅回显已选资产小类及所属大类。
- 借用申请按资产大类 / 小类形成需求，后续 ES 配给时按大类 + 小类筛选实际库存资产，不再要求与申请阶段某个品牌、型号、配置的 materialId 精确一致。
- **资产借用申请页的“资产说明”固定展示“大类.小类”**。
- **借用配给页在未选择资产标签号时，“资产说明”同样展示“大类.小类”；选择具体资产标签号后，资产说明切换为该资产的“品牌.型号”**。
- 历史演示单据仍兼容原 materialId 精确匹配逻辑，并通过物料目录回填大类 / 小类展示。

## 资产退库流程
- 退库审批、资产退库办理、员工退库确认均已按当前 UI 规范重画。
- 退库审批“退库资产信息”已修复三列串行问题；普通字段先凑满三列，再展示资产说明、配置、备注等整行字段。
- 资产退库办理“退库资产信息”再次修正整行字段布局：**配置 / 备注 / 关联耗材**前先保证城市 / 建筑 / 楼层构成完整三列行，三项整行字段连续展示后再放盘点状态 / 盘点执行人，避免整行字段在只填满两列时被 CSS Grid 自动拆成标签和值串行。
- 资产退库办理中责任人、MIS鉴定、鉴定结果、鉴定说明改为纯文本只读；仓库、资产标记、退库日期、使用说明保留编辑控件。
- 员工退库确认分为申请人信息 / 退库资产明细 / 刷卡/扫码确认；申请人信息只保留申请人、部门；退库资产明细包含行号和退库原因。

## 合约号码退库流程
- **合约号码退库** 已按当前 UI 规范重画为单层画布 + 小 Card + 蓝色竖块标题 + DetailGrid + small bordered Table。
- 合约号码退库页面**不提供附件上传**；顶部 Card 统一命名为**申请信息**，只保留：**申请人、部门、申请日期、退库原因、政策提示**。
- “政策提示”使用 Ant Design `Alert` 的**单行紧凑提示样式**：标题与说明合并在同一行，不使用 description 双层结构，并收紧上下内边距。
- 合约号码明细字段固定为：**合约号码、标签号、合约号码说明、套餐内容、金额、号码状态**。
- 合约号码明细默认不直接铺出本人全部号码；通过**“添加物资”**按钮打开“选择本人名下合约号码”弹窗，从个人工作台当前员工名下的合约号码中多选后带回退库明细。
- 个人工作台名下可选合约号码均按当前持有资源口径展示为 **“在用-使用中”**；业务处理中已被其他退库单锁定的号码仍可在选择弹窗中看到，但禁止再次勾选。
- **合约号码退库办理** 已改用 DetailGrid；申请人及号码信息均为纯展示，退库信息维护只保留真实维护字段。
- 合约号码退库办理的“退库信息维护”字段为：**退库仓库、责任人、退库日期、使用说明**，**不展示确认状态**；员工确认状态仍只作为流程状态机驱动按钮行为。
- 合约号码退库办理的号码信息统一展示：合约号码、标签号、合约号码说明、套餐内容、金额、号码状态；不再展示申请附件。
- **员工合约号码退库确认** 已拆分为独立 Card：**申请人信息 / 退库资产明细 / 退库确认提示 / 刷卡/扫码确认**。
- 员工合约号码退库确认的申请人信息只展示申请人、部门；退库资产明细单独使用 standard small bordered Table 展示合约号码信息及退库原因。
- 员工合约号码退库确认同时支持刷卡/管理员录入工号和狐小 e 扫码确认，使用 Ant Design `QRCode`，不包含电子签。

# 当前验收路径

1. 个人工作台 → 工作台首页 → 资产：资产信息黑体为“品牌.型号”，浅灰为“大类-小类”，行内无电脑/显示器小图标；“资产用途”作为状态后的独立列。
2. 工作台首页 → 资产：普通资产的退库 / 转移 / 更换按物料配置规则显示；借用资产只显示退库。
3. 工作台首页 → 资产多选：两项及以上时显示所选资产共同支持的批量退库 / 批量转移 / 批量更换；退库、更换进入申请页后自动带入所选资产标签号。
4. 工作台首页 → 合约号码：列表只显示合约号码 / 合约号码说明 / 操作，操作只有退库并进入合约号码退库申请页。
5. 个人工作台 → 耗材汇总 → 查看：耗材汇总明细页**不出现“导出申请明细”按钮**。
6. 耗材汇总 → 下一步：申请明细 Card 右上角存在“导出”按钮，点击后导出当前通过的申请明细 CSV。
7. 个人工作台 → 资产借用 → 添加资产：弹窗只有大类 / 小类两级，不出现品牌 / 型号 / 配置；选择小类后可添加到借用明细，资产说明显示“大类.小类”。
8. 借用配给：未选择资产标签号时资产说明显示“大类.小类”；选择具体资产标签号后，资产说明立即显示所选资产的“品牌.型号”。
9. 个人工作台 → 合约号码退库：顶部 Card 标题为“申请信息”，只显示申请人 / 部门 / 申请日期 / 退库原因；政策提示为一行紧凑 Alert；页面无附件上传。
10. 合约号码退库 → 合约号码明细：初始为空，点击“添加物资”打开“选择本人名下合约号码”弹窗；从本人当前持有号码多选后带回明细，明细只显示合约号码 / 标签号 / 合约号码说明 / 套餐内容 / 金额 / 号码状态，号码状态统一展示“在用-使用中”。
11. 个人工作台 → 合约号码退库办理：退库信息维护中不出现“确认状态”；责任人直接文本展示，退库仓库、退库日期、使用说明保持可维护。
12. 个人工作台 → 员工合约号码退库确认：申请人信息与退库资产明细必须是两个独立 Card；申请人信息只显示申请人和部门；刷卡、扫码两种确认方式均存在。
13. 个人工作台 → 退库审批：退库资产信息不出现字段串行或错位。
14. 个人工作台 → 资产退库办理：退库资产信息中的配置 / 备注 / 关联耗材均为独立完整整行，不再出现标签和值串行；只读字段为普通文本，真实维护字段才显示输入控件。
