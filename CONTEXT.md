# 当前状态

项目是产品演示用企业资产管理前端。`feat/business-rule-updates` 已于 2026-08-09 合并到 `main`，后续新功能原则上从最新 `main` 创建独立分支；当前按用户指定继续在 `feat/workspace-updates` 修改个人工作台。

# 当前正在做什么

个人工作台正在统一收敛详情/审批/办理页面的 UI 语言。合约号码 ES 配给与主管审批已完成 `DetailGrid` 公共详情栅格封装；资产更换申请、MIS 鉴定、资产更换办理已迁移到同一套视觉规范；已新增“新员工领用单”及配套“新员工领用员工确认”页面。本轮继续优化原“资产申请”页面并统一更名为“物资申请”。

本次已完成：
- 个人工作台原“资产申请”菜单更名为“物资申请”，菜单 key 与 label 同步修改；个人工作台面包屑使用 `activeSubMenu` 展示，因此同步显示“首页 / 个人工作台 / 物资申请”。
- “物资申请”页面中的可见文案同步收敛：预览标题改为“物资申请预览”，员工身份限制提示改为“暂不支持发起物资申请”，超标提示改为“超标物资申请”。
- “添加物资”弹窗继续保留大类 / 小类 / 品牌 / 型号 / 配置五级选择，但长名称不再使用 `...` 截断；改为自动换行完整展示，同时保留 Hover Tooltip 便于快速查看完整名称。
- “关联主资产”不再使用下拉框，也不再混入“本次申请”的主资产；改为搜索按钮打开“选择关联主资产”弹窗。
- “选择关联主资产”弹窗仅展示当前员工个人工作台中本人名下已有资产，支持按资产标签号、资产说明、配置搜索，并展示资产标签号、资产说明、配置、资产状态、资产用途。
- 关联主资产确认后在申请明细中回填“资产标签号 / 资产说明”，预览页也按同一口径展示；耗材仍保留“必须关联主资产”的原提交校验。
- `MY_EXISTING_ASSETS` 演示数据补充资产标签号、资产说明、配置、状态、用途字段，供关联主资产弹窗展示。
- 多次打开“添加物资”弹窗重复选择同一物料时，不再新增重复明细行；系统在原明细行基础上将数量自动 +1，并提示重复物资已合并。一次确认同时包含新物料和重复物料时，新物料正常新增、重复物料分别合并计数。
- 个人工作台新增“新员工领用单”菜单，位置在“ES前台领用”之后。
- 个人工作台新增“新员工领用员工确认”菜单，位置在“新员工领用单”之后、“员工领用确认”之前。
- “新员工领用单”采用单层画布、Card 分组、蓝色竖向分组标题和公共 `DetailGrid` 三列详情栅格。
- “使用人信息”保留截图字段：当前仓库、领用单号、使用人、单据状态、公司、板块、成本中心、业务线、部门、制单时间、City、Building、Floor、单据类型、出库单、资产配置、备注。
- City / Building / Floor 保留必填校验，并使用当前系统 Select 级联交互；备注可编辑。
- “领用物资明细”保留截图字段：选择、行号、资产标签号、SN序列号、物资总类、大类、物资说明、实际盘点人、盘点状态、申请数量、领用数量、City、Building、Floor、资产状态、PR单号、操作。
- 明细支持新增、勾选删除和选择资产；资产标签号搜索入口与操作按钮均可打开现有资产候选数据进行选择。
- 底部流程按钮统一放在同一操作区，顺序为“保存 / 领用确认 / 弃领 / 返回”；保存不再放在信息区中间，按用户要求与“领用确认”同级。
- “新员工领用员工确认”按旧系统截图保留“领用人信息 / 领用物资明细 / 保管职责 / 刷卡确认”核心结构，并用当前设计规范重绘。
- “新员工领用员工确认”领用人信息仅展示使用人、部门；领用物资明细保留行号、资产标签号、物资说明、配置、申请数量、领用数量、用途、使用说明。
- 员工确认不恢复电子签，统一提供刷卡/工号确认和狐小 e 扫码确认；手工录入工号时校验必须与领用人员工工号一致。
- 确认成功后页面展示识别员工、确认时间、确认方式和已确认状态；底部“返回”回到“新员工领用单”。
- “资产更换申请”去除页面自身灰色画布、额外 padding、白底阴影标题框和底部阴影操作框，改为个人工作台单层画布。
- “资产更换申请”的更换信息说明改用公共 `DetailGrid`；更换原因保持必填标识和原提交校验。
- “资产更换申请”的退回资产表格保持原字段和功能，资产标签号固定左侧，资产说明/配置/部件/耗材信息增加 ellipsis，减少横向滚动时的视觉干扰。
- “资产更换申请”的更换信息说明、退回资产信息、更换须知 Card 标题统一增加蓝色竖向分组标识。
- “MIS鉴定”去除页面自身灰色画布、额外 padding 和白底阴影标题框，空状态也改为单层 Card。
- “MIS鉴定”的申请人信息、更换资产信息、MIS 鉴定处理改用公共 `DetailGrid`，Card 标题统一增加蓝色竖向分组标识。
- “MIS鉴定”鉴定结果删除“员工取消”，当前仅保留“资产维修 / 资产更换”；同意/驳回决策逻辑和审批历史保持原实现。
- “资产更换办理”去除页面自身灰色画布、额外 padding 和白底阴影标题框，空状态同步改为单层 Card。
- “资产更换办理”的申请人信息、更换资产信息、待发放资产信息改用公共 `DetailGrid`，Card 标题统一增加蓝色竖向分组标识。
- “资产更换办理”补充原逻辑中已实际使用但页面未展示的“退回仓库”字段；旧资产未入库时可维护，已入库后只读，避免使用不可见默认仓库完成入库。
- “资产更换办理”保留原办理阶段逻辑、原“同意”主按钮、原可编辑时机和原审批历史；本轮明确不增加 Steps、不做阶段解锁、不改动态主按钮、不改审批历史。
- `docs/UI_DESIGN_GUIDELINES.md` 已补充个人工作台主要业务 Card 的蓝色竖向分组标题规范。

合约号码此前已完成：
- “合约号码ES配给”页面在“号码配给”区块内新增“附件信息”必填字段，支持上传/重新上传附件。
- ES 配给“同意”时同时校验电话号码和配给附件，校验通过后进入“合约号码配给主管审批”。
- 个人工作台新增“合约号码配给主管审批”菜单，位于“合约号码ES配给”下方。
- 主管审批页复用 ES 配给已填写内容并只读展示；审批操作保留同意、驳回、返回。
- 主管审批同意后进入原“合约号码库管员待办”，后续员工领取确认流程保持不变。
- ES 配给与主管审批页面按个人工作台设计规范收敛为单层画布、Card 分区、三列详情和居中流程按钮。
- 合约号码演示数据版本升级为 `v5`，清除浏览器中已推进到后续节点的旧 `v4` 状态影响。
- 默认演示数据同时提供一张“ES审批”待办和一张“合约号码配给主管审批”待办，两个菜单首次打开都直接展示完整业务页面，不再互相依赖前一个页面先提交。

后台“资产管理”此前已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表
- 账面报废查询列表
- 资产处置查询列表
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

新增一级菜单“库存管理”，位置在“无形资产”下方，子菜单固定为：
- 资产接收
- 耗材接收
- 入库
- 出库
- 移库
- 转移
- 库管员工作台

# 近期关键决定

- 当前用户明确指定在 `feat/workspace-updates` 修改，因此本次不新建分支、不创建 worktree。
- 个人工作台原“资产申请”统一改名为“物资申请”，因为同一入口同时支持资产和耗材申请；菜单名称、面包屑及页面可见标题应保持一致。
- 物料五级选择中，大类 / 小类 / 品牌 / 型号 / 配置属于核心决策信息，名称较长时优先换行完整展示，可辅以 Tooltip，不使用省略号隐藏关键选择内容。
- 耗材“关联主资产”只能从当前员工本人名下已有资产中选择，不允许把“本次申请”的新主资产作为关联对象；交互统一使用独立弹窗，不使用下拉框承载长列表。
- 物资申请明细以“物料唯一标识”为合并维度；重复选择同一物料时只增加原行数量，不允许产生两条相同物料明细，同时必须给用户明确合并提示。
- 个人工作台业务页统一使用单层画布，避免页面自身再叠加灰底、外层内边距和阴影容器。
- 多 Card 详情需要严格三列对齐时统一使用公共 `DetailGrid`；简单且不要求跨 Card 对齐的详情仍可使用 AntD `Descriptions`。
- 个人工作台主要业务 Card 标题统一使用紧凑蓝色竖向分组标识，不扩展为大面积底色或装饰性图形。
- 旧系统页面复刻时保留原字段、信息分组和关键操作关系，但控件与视觉统一替换为当前 Ant Design / 个人工作台规范。
- “新员工领用单”的“保存”属于底部流程操作，与“领用确认”同级展示，不在信息区域单独占一行。
- “新员工领用员工确认”延续当前员工确认统一规则：不提供电子签，只保留刷卡/工号与狐小 e 扫码两类确认方式。
- 资产更换本轮只做已确认的 UI/UE 收敛，不修改 MIS 同意/驳回决策模型，不增加资产更换办理 Steps、阶段解锁、动态主按钮，也不调整审批历史。
- MIS 鉴定结果当前只保留“资产维修 / 资产更换”，不展示“员工取消”。
- 资产更换办理的退回仓库原本已存在于状态和入库逻辑中，本轮仅补充可见维护入口，不改变原流程状态机。
- 合约号码 ES 配给新增的附件属于“号码配给”结果的一部分，与申请信息中的原附件并存，不替换原申请附件。
- 新流程为：`合约号码ES配给 → 合约号码配给主管审批 → 合约号码库管员待办 → 员工合约号码领取确认`。
- 产品演示菜单需要独立可验收：新增流程节点后，应为新增节点准备对应的默认待办数据，不能只依赖上游页面运行后才能看到目标页面。
- `main` 仍作为后续并行分支开发的统一基线；`feat/business-rule-updates` 已完整纳入 `main`。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

`feat/workspace-updates` 已将个人工作台“资产申请”更名为“物资申请”，并完成添加物资长名称完整展示、耗材关联主资产弹窗化及本人名下资产范围限制；重复选择同一物料时会自动合并到原明细并将数量 +1。此前新增的“新员工领用单”“新员工领用员工确认”及资产更换三页 UI 收敛也已完成。

# 验收路径

1. 打开“个人工作台”，原“资产申请”菜单应显示为“物资申请”；点击后顶部标签与面包屑应同步显示“物资申请”。
2. 进入“物资申请”后，预览状态标题显示“物资申请预览”，员工身份和超标提示中不再使用“资产申请”作为页面业务名称。
3. 点击“添加物资”，大类 / 小类 / 品牌 / 型号 / 配置中的长名称应允许换行完整展示，不出现 `...`；Hover 时仍可通过 Tooltip 查看完整文本。
4. 添加一条耗材后，“关联主资产”字段应为只读输入框 + 搜索按钮，不再展示 Select 下拉框。
5. 点击“关联主资产”搜索按钮，应打开“选择关联主资产”弹窗；弹窗只显示本人名下已有资产，不显示“本次申请”的主资产。
6. 关联主资产弹窗支持搜索资产标签号、资产说明、配置；选中并确定后申请明细回填“资产标签号 / 资产说明”。
7. 耗材未关联主资产时仍不可预览/提交，原业务校验保持不变。
8. 第一次添加任意物料后再次打开“添加物资”并选择同一物料，确认后申请明细仍只有一行该物料，原行数量自动从 N 变为 N+1，并显示重复物资已合并提示。
9. 一次确认同时选择已存在物料和新物料时，已存在物料数量 +1，新物料新增一行，两类处理结果应在提示中明确说明。
10. 打开“新员工领用单”，页面底部应按同一层级展示“保存 / 领用确认 / 弃领 / 返回”。
11. 打开“新员工领用员工确认”，员工确认仅提供刷卡/工号和狐小 e 扫码，不出现电子签。
12. 打开“MIS鉴定”，鉴定结果仅显示“资产维修 / 资产更换”，不显示“员工取消”；原同意/驳回逻辑保持不变。
13. 打开“资产更换办理”，仍保持原“同意/驳回/返回/转签”和原办理逻辑，不出现 Steps、不做阶段解锁、不改动态按钮文案、不调整审批历史。
