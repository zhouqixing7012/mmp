# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成员工自助新版第三阶段：资产领用、员工确认和库管复核出库。旧版页面和旧版路由全部保留。

1. 新增员工自助新版资产领用
   - 库存领用配给完成后自动生成领用单。
   - 已汇总的统一采购数据模拟入库后生成采购入库领用单。
   - 支持当前仓库、资产标签号、City、Building、Floor、用途、使用说明、盘点人和盘点状态。
   - 支持狐小e电子签或刷卡/工号确认方式。
   - 支持发送领用通知。

2. 新增员工自助新版领用确认
   - 展示资产保管职责，员工必须先点击已阅读。
   - 支持手写电子签名、清除和重新签名。
   - 支持刷卡或手工输入工号，并校验是否与申请人工号一致。
   - 确认完成后进入库管复核。

3. 新增库管复核与出库
   - 库管员可查看员工签名。
   - 签名不合规时可填写意见并退回员工重新签名。
   - 复核通过前校验资产所在仓库与当前仓库一致。
   - 出库后生成出库记录，更新资产状态、责任人、部门、成本中心、地点、用途和使用说明。
   - 回写原申请单进度，相关领用分支全部完成后申请单变为已完成。

4. 新增统一演示数据与服务
   - `src/mock/employeeSelfServiceClaimMock.js`
   - `src/services/employeeSelfServiceClaimService.js`
   - `src/pages/employeeSelfService/SignaturePad.js`
   - 领用单、员工签名、出库记录和原申请单共用演示存储并保持刷新后数据。

5. 新增路由和菜单
   - `/employee-self-service/asset-claim`
   - `/employee-self-service/asset-claim-confirm`
   - 顶部导航和 `/yewurules` 个人工作台内部菜单均可进入。

6. 更新文档
   - 新增 `docs/PRD-EMPLOYEE-ASSET-SELF-SERVICE-PHASE3.md`。

# 本次关键决定

- 本期仍只实现资产，不实现耗材。
- 旧申请、审批、配给、汇总、领用和确认页面全部保留。
- 在职员工默认使用狐小e电子签，库管员可切换为刷卡/工号确认。
- 采购流程在演示系统中以“汇总已提交后模拟采购入库”的方式生成领用单。
- 只有库管复核通过并执行出库后，领用单和原申请单才可以完成。
- 出库必须校验资产所在仓库与当前仓库一致。

# 上次停留位置

员工自助新版第三阶段代码已提交到 `feat/business-rule-updates`。

下一阶段进入全流程进度查询与收尾：

1. 新增“我的申请”列表和详情。
2. 展示申请、审批、配给、汇总采购、领用、员工确认和出库的全流程记录。
3. 支持按单号、日期和状态查询。
4. 统一申请单、配给单、汇总单、领用单和出库单状态口径。
5. 补充整条链路演示数据重置与验收脚本。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 选择弹窗使用 `SelectModal`。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能。
- 入口文件最后更新，避免模块未齐时破坏分支构建。

# 验收路径

1. 在新版资产配给中完成一张“库存领用”配给单。
2. 打开 `/employee-self-service/asset-claim`，点击“刷新领用单”。
3. 进入领用单，维护仓库、地点、用途、盘点信息和确认方式。
4. 点击“发送领用通知”。
5. 打开 `/employee-self-service/asset-claim-confirm`，刷新待办。
6. 阅读保管职责并完成电子签，或输入与申请人一致的工号。
7. 提交领用确认。
8. 返回新版资产领用页面，查看签名并执行库管复核。
9. 点击“确认并出库”，检查领用单变为已完成。
10. 检查原申请单状态和资产台账字段已更新。
11. 对统一采购分支完成汇总后，刷新资产领用页，检查生成采购入库领用单。
12. 刷新浏览器，确认领用、签名和出库结果仍存在。
