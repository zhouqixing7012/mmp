# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台删除“我的资产”“我的资产更换申请”两个菜单页面。
- 资产更换申请提交和返回不再跳转已删除页面，统一返回工作台首页。
- MIS鉴定删除待办列表，进入菜单后直接展示第一张待鉴定单的审批页。
- 资产更换办理删除待办列表，进入菜单后直接展示第一张待办理单的办理页。
- 资产退库删除“我的退库申请”标签页，仅保留退库申请页面。
- 退库审批删除待办列表和领导审批入口，仅保留MIS鉴定审批页；MIS鉴定处理区保持不变。
- 资产退库办理删除待办列表，进入菜单后直接展示第一张待办理单。
- 合约号码退库删除“我的退库申请”标签页，仅保留退库申请页面。
- 合约号码退库办理删除待办列表，进入菜单后直接展示第一张待办理单。
- 员工退库确认、员工号码退库确认原本已是直接确认页，本次保持不变。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 页面处理完成后自动刷新并展示下一张待办；没有待办时显示空状态。
- 已删除菜单不保留内部跳转入口，避免出现菜单不存在但页面仍跳转的问题。

# 上次停留位置

上述页面精简已完成，等待逐页验收。

# 验收路径

1. `/yewurules` → 个人工作台：确认“我的资产”“我的资产更换申请”已删除。
2. MIS鉴定、资产更换办理：确认进入菜单直接展示审批/办理页。
3. 资产退库、合约号码退库：确认不再显示“我的退库申请”标签。
4. 退库审批：确认直接展示MIS鉴定审批页，MIS鉴定处理区未变化。
5. 资产退库办理、合约号码退库办理：确认直接展示办理页。
6. 员工退库确认、员工号码退库确认：确认仍直接展示确认页。
