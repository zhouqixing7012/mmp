# 当前状态

- 当前分支为研发评审专用 `review/rd-review`，只保存阶段性已经确认、准备交给研发评审的 PRD 与原型结论；日常产品迭代继续在 `main`。
- 本分支不会自动跟随 `main`。后续 PRD 或原型变化由用户手动选择性同步，避免尚未确认的改动进入研发评审版本。
- 当前评审版本为 `2026.08.19-01`，页面启动时展示“研发评审版本更新说明”，分别列出 PRD 变化和原型变化。
- 个人工作台、资产管理、库存管理、资产盘点主要原型已建立。
- 员工自助 10 个主要业务模块均已建立基线标注与 Coverage Ledger，当前合计 854 条研发规则。
- Coverage Ledger 固定使用 `bound / review / skip`；`review` 表示 PRD 与原型/实现存在真实差异或暂无可靠承载位置，不能通过粗绑 Card 假装完成。
- 原型标注已支持页面 scope、Button/字段/Tab/表头等细粒度 target、Descriptions、Modal/Drawer/SelectModal 动态浮层、拖动、重绑、代码基线 + 浏览器覆盖层、Markdown、PRD 覆盖中心、匹配质量、评审模式、碰撞避让和运行诊断。
- “号码控制 → 发送通知”规则已经修正为行内 Button 标注，不再落到“授权人员列表”整个 Card。

# 当前停留位置

- 等待当前评审分支 Vercel Preview 构建完成，构建成功后可直接将该 Preview 地址发给研发。
- 下一阶段不继续扩张 resolver 兼容逻辑，重点逐项处理 Coverage 中的 `review`。
- 处理顺序固定为：先确认 PRD 是否仍有效 → 再确认原型是否应修改 → 再确认是否属于真实后端/流程能力 → 最后更新 Coverage 状态。
- 每次后续手动同步评审分支时，同步更新 `src/review/review-release.js` 的版本号、PRD 变化和原型变化。

# 关键决定

- 标注生成固定流程：PRD 拆 Requirement Atom → 找最细业务 target → 对照当前实现 → 写 annotation → 写 Coverage Ledger → Granularity/Coverage Check。
- action-rule 必须落到 Button；field-rule 必须落到字段/控件；tab-rule 必须落到 Tab；table-column-rule 必须落到表头。找不到稳定目标时进入 `review`，不回退模块。
- 同名按钮只有在规则确实属于重复行内动作时才允许使用“首个可见同语义按钮”作为展示锚点；普通审批按钮仍遵循“当前页面文案唯一才做语义兼容”。
- target 不依赖动态文案；弹窗/抽屉等暂时未挂载 DOM 的目标单独归类为动态目标。
- 标注保存继续使用“代码基线 + 用户覆盖层”。
- 评审更新说明以 `review-release.js` 中人工确认内容为唯一来源，不自动分析 `main` 的 Git 差异。

# 当前模块状态

- 个人工作台：主要业务链路已建立；10 个员工自助业务模块均已进入 PRD 深审和 Coverage 体系。
- 资产管理：资产维护、耗材维护、合约号码维护、标签打印、跨公司转移、资产报废、账面报废、资产处置、员工资产信息查询已建立。
- 库存管理：资产接收、耗材接收、入库、出库、移库、转移、库管员工作台已有主要原型。
- 资产盘点：盘点项目已覆盖创建、范围、快照、计划、扫描、图片审核、进度和报告入口；其余维护页等待明确需求。
- 原型标注：底座能力已基本完成，当前任务重点转向 `review` 差异收敛和研发评审交付。
