# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成第九轮整改：对 `yewurules` 拆分结果进行页面级回归审计，并修复拆分过程中遗漏或误加的交互。

1. 完成后台配置全部 34 个业务视图的结构审计
   - 对照拆分前完整 `yewurules.js`。
   - 核对查询条件、操作栏、按钮状态、新增/编辑弹窗、批量弹窗、选择弹窗和表格列。
   - 审计清单记录在 `docs/BACKGROUND_CONFIG_AUDIT.md`。

2. 修复费用账户规则按钮状态
   - 删除第四阶段重写时误加的启用/停用置灰条件。
   - 恢复为拆分前可点击状态。
   - 删除类按钮仍按原页面要求在未勾选时置灰。

3. 恢复业务权限规则完整页面
   - `CompanyPlateAssetAuthView.js`
   - `NODeviceAssetAuthView.js`
   - `CompanyBelongingAuthView.js`
   - 恢复完整查询区、操作栏、新增/编辑弹窗、批量修改和选择弹窗。
   - `permission/index.js` 已切换到独立完整页面。

4. 恢复资产折旧规则完整页面
   - 恢复“计算”入口。
   - 恢复完整查询条件、编辑按钮和新增/编辑表单。

5. 已完成此前发现的费用及会计页签修复
   - 账套内容维护
   - 物资申请超标配置
   - 费用账户规则
   - 成本中心与科目映射
   - 物料大类与子目映射
   - NO 一级服务与科目映射
   - 员工与项目映射

6. 更新文档
   - 更新 `README.md` 的按钮状态说明和分阶段优化计划。
   - 新增 `docs/BACKGROUND_CONFIG_AUDIT.md`。

# 已完成的后台拆分

- 后台框架：`components/`
- 菜单与页签配置：`config/`
- 个人工作台映射
- 物料模块：`modules/material/`
- 业务映射模块：`modules/mapping/`
- 仓库模块：`modules/warehouse/`
- 地点模块：`modules/location/`
- 权限模块：`modules/permission/`
- 会计模块：`modules/accounting/`
- 费用模块：`modules/expense/`
- `yewurules.js` 入口约 200 行

# 按钮状态原则

- 不在拆分过程中自行给启用/停用按钮增加 `disabled` 条件。
- 删除按钮在原页面要求先选择数据时，未选中状态保持置灰。
- 物料大类、物料小类、品牌、型号和配置页面的停用按钮未选中时置灰，是拆分前原有表现。
- 后续实现真实状态操作时，按钮可以保持可点击；点击后若未勾选数据，应给出明确提示，而不是静默无响应。

# 下一步优化计划

## 第一优先级：交互闭环

1. 查询、重置、分页真实生效。
2. 启用、停用、删除增加选中校验、二次确认、loading 和成功提示。
3. 新增/编辑弹窗增加必填校验、提交 loading、关闭重置和防重复提交。
4. 同步、导入、批量修改和下载模板补充明确演示反馈。

## 第二优先级：后台配置数据层

1. 建立统一的后台配置 mock service。
2. 页面不再直接把 mock 数组作为最终数据源。
3. 新增、编辑、启停、删除写入统一演示存储。
4. 提供统一的演示数据重置能力。

## 第三优先级：组件治理

1. 拆分超过 300 行的页面。
2. 拆分超过 80 行的 Modal。
3. 抽取通用列表工具栏、状态批量操作和选择字段。
4. 将查询字段、表格列和下拉枚举迁入配置文件。

## 第四优先级：业务链路和测试

1. 申请、列表和审批读取同一份申请数据。
2. 费用账户映射与资产台账演示数据建立联动。
3. 补充后台配置关键页签的 Playwright 冒烟用例。

# 近期关键决定

- 不修改 `package.json`，不新增依赖。
- 保留现有菜单、页签名称和 mock 数据字段。
- 新模块使用目录 `index.js` 统一出口。
- 原始完整 `yewurules.js` 是拆分回归审计的交互基准。
- 后续不再以简化模板替代已有页面结构。
- 入口文件最后更新，避免模块未齐时破坏分支构建。

# 验收建议

1. 本地执行 `npm run build`。
2. 检查费用账户规则启用/停用按钮不再置灰。
3. 检查三个权限页签的查询区、操作栏和弹窗。
4. 检查资产折旧规则的计算入口和新增/编辑弹窗。
5. 按 `docs/BACKGROUND_CONFIG_AUDIT.md` 抽查其他后台页签。
