# 当前状态

项目是产品演示用企业资产管理前端。当前开发分支：`feat/asset-management-updates`，基于最新 `main` 创建，仅用于资产管理相关页面修改。

# 当前正在做什么

后台“资产管理”按现系统截图逐页重构。

已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表
- 账面报废查询列表
- 资产处置查询列表
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

# 近期关键决定

- 本分支只修改“资产管理”相关页面，不处理个人工作台和库存管理页面。
- 资产报废、账面报废、资产处置入口统一为单据查询列表。
- 查询条件：申请单号、单据状态、公司、制单时间从/至。
- 列表字段：选择、申请单、单据状态、公司、制单人、制单时间、资产数量、备注、操作。
- 操作区：对应“创建申请单”按钮 + “删除”。
- 资产报废点击新建后，不再跳转独立路由，直接在“资产管理 → 资产报废”内容区嵌入现有机房资产报废申请单编辑页 `ScrapApplicationEdit`。
- 账面报废点击新建后，不再跳转独立路由，直接在“资产管理 → 账面报废”内容区嵌入现有机房资产账面报废申请页 `AccountingScrapEdit`。
- 嵌入页返回/退出后恢复当前菜单的查询列表，不离开 `/yewurules` 后台框架。
- 现有 `/BaofeiShenqing` 和 `/` 独立路由继续保留，不删除，避免影响已有演示入口。
- 资产处置申请页字段尚未提供，因此保留新建按钮，但不猜表单字段。
- 列表统一把“共 X 条”放在列表标题右上角，操作按钮放下一行右侧。
- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

资产报废、账面报废的新建操作已改为后台框架内嵌入现有机房资产申请页面。下一步继续处理资产管理下的公司间转移、资产处置或其他页面。

# 验收路径

1. 打开 `/yewurules` → “资产管理” → “资产报废”。
2. 点击“创建资产报废申请单”，左侧菜单和后台顶部保持不变，内容区直接显示“报废申请单”。
3. 点击报废申请单底部“退出”，应回到“资产报废”查询列表，不发生路由跳转。
4. 打开“账面报废”，点击“创建账面报废申请单”，内容区直接显示“账面报废申请单”。
5. 点击账面报废申请页底部“返回”，应回到“账面报废”查询列表，不发生路由跳转。
6. 切换其他资产管理子菜单后再次回来，应默认显示查询列表，不保留上一次嵌入编辑状态。
