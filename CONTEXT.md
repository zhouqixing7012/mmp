# 当前状态

项目是产品演示用企业资产管理前端。`main` 已包含库存管理和上一轮资产管理改动；当前继续在 `feat/asset-management-updates` 分支修改资产管理。

# 当前正在做什么

后台“资产管理”和“库存管理”按现系统截图逐页重构。

资产管理已完成：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 公司间转移查询列表 + 嵌入式公司间转移申请页
- 资产报废查询列表 + 嵌入式报废申请编辑页
- 账面报废查询列表 + 嵌入式账面报废申请页
- 资产处置查询列表 + 嵌入式资产处置编辑页
- 员工资产信息查询

库存管理已完成：资产接收、入库、出库、移库、转移、库管员工作台的已确认列表/查询原型；耗材接收及部分新建/详情页仍待字段确认。

# 近期关键决定

- 公司间转移列表页与账面报废列表页保持同一结构；申请页去掉报废原因、报废资产明细和“是否公司间转移”，只保留可添加/删除物资的公司间转移明细。
- 资产报废、账面报废、资产处置入口统一为单据查询列表，点击新建后在资产管理内容区嵌入编辑页。
- 报废申请单仅调整 UI/UE：标题、基本信息 Card、Descriptions、明细 Card、表格、底部操作栏统一为账面报废页面风格；原字段、必填校验、公司选择、报废资产/关联配件页签、导入Excel、增行、删行、保存、提交、退出功能均保持不变。
- 资产处置基本信息仅保留制单人、制单时间、处置说明、附件；资产明细字段与账面报废申请页一致。
- 嵌入页返回/退出后恢复当前菜单查询列表；现有独立路由继续保留。
- 查询列表统一把“共 X 条”放在列表右上角，操作按钮放下一行右侧。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

公司间转移已完成；报废申请单 UI/UE 已统一到账面报废风格，业务字段和功能未改。完成 Review 后合并到 `main`。

# 验收路径

1. `/yewurules` → “资产管理” → “资产报废” → 创建资产报废申请单。
2. 报废申请单整体视觉应与账面报废申请页一致，仍展示原有全部字段。
3. 公司、资产大类、资产所在地仍为必填；保存/提交校验保持不变。
4. 报废资产/关联配件页签、导入Excel、增行、删行保持可用；报废资产表格字段不变。
5. 底部退出、保存、提交保持可用；嵌入状态下退出回资产报废列表。
6. “公司间转移”列表及申请页继续保持上一轮确认结构。
