# 当前状态

项目是产品演示用企业资产管理前端。资产管理上一轮改动已合并到 `main`；当前继续在 `feat/asset-management-updates` 分支修改资产管理，完成后合并回 `main`。

# 当前正在做什么

后台“资产管理”和“库存管理”按现系统截图逐页重构。

资产管理已完成：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、标签清单、打印历史、生成标签）
- 跨公司转移查询列表 + 嵌入式跨公司转移申请页
- 资产报废查询列表 + 嵌入式报废申请编辑页 + 跨公司转移第二新建入口
- 账面报废查询列表 + 嵌入式账面报废申请页
- 资产处置查询列表 + 嵌入式资产处置编辑页
- 员工资产信息查询

库存管理已完成：资产接收、入库、出库、移库、转移、库管员工作台的已确认列表/查询原型；耗材接收及部分新建/详情页仍待字段确认。

# 近期关键决定

- 标签预打印查询中的“创建时间从/创建时间至”合并为一个创建时间区间选择器；新增“资产标签号”文本查询条件。
- 标签预打印“详细 → 标签清单”按现系统“查看标签”页面重构为弹窗：查询字段为标签号、是否打印；保留打印所选、打印全部；移除“配置”；返回按钮放到弹窗底部居中；列表字段为选择、标签批次、标签号、打印次数、是否已打印、操作，操作为打印历史。
- 标签预打印“生成标签”进入独立生成标签页面。页面继续保留选择账套、常规标签规则、高耗标签规则、特殊规则-家具、特殊规则-手机、特殊规则-备件全部业务字段。
- 生成标签的标签规则区按 B 端表单规范改为固定 4 列，每列固定宽度，控件使用固定且适中的宽度，不再默认铺满整列；规则类型单独占左侧固定列，所有规则字段保持同一列基线。
- 生成标签页面删除“标签大小”字段；保留打印标签数量、备件数量、标签批次、操作员、当前日期、备注说明。返回、生成标签统一放页面底部居中。
- 生成标签页面只优化 UI/UE，不补造截图未提供的资产类型、高耗类型、家具类型、备件类型枚举；这些选择项保持空候选。
- 原“公司间转移”统一改名为“跨公司转移”；资产管理菜单、查询列表标题、新建按钮和申请页标题使用新名称。
- 跨公司转移列表页与账面报废列表页保持同一结构；菜单内跨公司转移申请页只保留跨公司转移明细，并提供添加物资、删除物资。
- 资产报废列表保留“创建资产报废申请单”，新增第二个“创建跨公司转移申请单”按钮。
- 资产报废列表进入的跨公司转移页以报废申请单 UI 为基础：基本信息隐藏“资产大类”“资产所在地”，其余报废申请单基本字段保持；原“报废资产明细”替换为与跨公司转移申请页一致的“跨公司转移明细”。
- 报废申请单编辑页去除“关联配件”页签和对应逻辑，仅保留报废资产明细；其他字段、导入 Excel、增行、删行、保存、提交、退出和必填校验保持不变。
- 资产报废、账面报废、资产处置入口统一为单据查询列表，点击新建后在资产管理内容区嵌入编辑页。
- 资产处置基本信息仅保留制单人、制单时间、处置说明、附件；资产明细字段与账面报废申请页一致。
- 嵌入页返回/退出后恢复当前菜单查询列表；现有独立路由继续保留。
- 查询列表统一把“共 X 条”放在列表右上角，操作按钮放下一行右侧。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

标签清单弹窗和生成标签页面的 B 端布局已再次收敛：移除配置和标签大小，固定 4 列规则区，底部统一返回/生成操作。完成 Review 后合并到 `main`。

# 验收路径

1. `/yewurules` → “资产管理” → “标签打印” → “预打印”。
2. 查询区显示标签批次、是否已打印、创建人、订单编号、资产标签号、创建时间；创建时间为单个区间选择器。
3. 点击任一预打印记录“标签清单”，弹出“查看标签”；查询字段为标签号、是否打印。
4. 标签清单顶部只保留打印所选、打印全部，不显示配置；返回按钮位于弹窗底部居中。
5. 标签清单列表展示标签批次、标签号、打印次数、是否已打印、打印历史；可勾选标签执行打印所选/打印全部。
6. 点击标签清单或预打印列表的“打印历史”仍进入现有打印历史弹窗。
7. 预打印页点击“生成标签”，进入生成标签页面；标签规则区固定 4 列、列宽固定，选择框和输入框使用固定宽度。
8. “标签生成”不再显示标签大小；保留打印标签数量、备件数量、标签批次、操作员、当前日期、备注说明。
9. 生成标签页底部居中显示“返回”“生成标签”；截图确认的手机标签前缀为 NE、当前最大序号为 3792。
10. 返回预打印、返回标签打印均保持当前后台框架，不发生路由跳转。
