# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 当前重点

账面报废申请页与账面报废审批页正在同步重构：
- 申请页：`src/pages/AccountingScrapEdit.js`
- 审批页：`src/pages/ApprovalPage.js`

# 账面报废当前口径

## 申请页

- 公司使用统一 `SelectModal` 弹窗选择。
- 新增：是否公司间转移、已到报废期报废原因、未到报废期报废原因、丢失报废原因。
- “是否公司间转移”使用普通单选按钮，默认“否”；选择“是”才展示公司间转移明细。
- 公司间转移明细字段：资产标签号、新责任人、新公司、新板块、新成本中心、City、Building、Floor、调账后仓库。
- 资产标签号由上方资产明细自动带入，不可编辑；公司间转移明细不能独立新增或删除资产，资产集合始终与资产明细一一对应。
- 除“调账后仓库”外，其余公司间转移字段均必填。
- 新公司、新板块、新成本中心、City、Building、调账后仓库均使用弹窗选择；Floor 使用下拉框。
- 新板块复用后台配置板块数据，新成本中心复用成本中心数据，City/Building 复用地点基础数据，调账后仓库复用仓库信息数据。
- City 变化时清空 Building/Floor；Building 变化时清空 Floor，避免保留不匹配地点。
- 页面顶部说明文字已删除；公司间转移明细原说明条已删除。“与资产明细保持一一对应”改为 Card 标题旁气泡提示，hover 展示。
- 申请页以 Ant Design Card / Descriptions / Table 为主，字段、必填规则、资产增删联动逻辑保持不变。
- 基本信息、报废原因、资产明细、公司间转移明细等 Card 标题与审批页统一使用“蓝色竖条 + 16px 加粗标题”样式。

## 审批页

- 审批页按申请页已填写结果只读展示，不再重复提供公司间转移相关字段编辑能力。
- 当前演示单据“是否公司间转移=是”。
- 基本信息只读展示：申请单号、公司、单据状态、制单人、制单时间、是否公司间转移、备注、附件。
- 报废原因只读展示三类：已到报废期报废原因、未到报废期报废原因、丢失报废原因。
- “公司间转移明细”只读展示资产标签号、新责任人、新公司、新板块、新成本中心、City、Building、Floor、调账后仓库；资产标签号按普通黑色文本展示。
- 报废资产明细展示字段保持原口径不变，并继续按“已到报废期 / 未到报废期 / 丢失”三类分别汇总和展示。
- 每个报废类型继续展示该类型总数量、原值合计、净值合计，并在类型内部继续按服务器、服务器（配件）、网络设备、网络设备（配件）等分组汇总。
- 服务器、服务器（配件）、网络设备、网络设备（配件）等分组默认折叠，只展示组名及数量/原值/净值汇总；点击分组后再展开原明细表格。切换报废类型时重新收起分组。
- 基本信息、报废原因、报废资产明细、公司间转移明细、审批意见等 Card 标题统一使用更明显的蓝色竖条 + 加粗大字号分区标题。
- 附件在 hover 时展示下载按钮，点击可触发演示下载并给出反馈。
- 审批意见保留；驳回时审批意见必填。

# 其他已确认规则

- 本次资产系统重构全面取消电子签确认，员工确认统一只保留刷卡/员工工号和狐小 e 扫码。
- 页面不能因为数据对象存在字段就全部展示，展示字段以当前确认口径为准。
- 老系统截图用于确认业务字段和流程，视觉结构按当前项目规范重构。
- 用户在项目语境中说“原型/原型图/放到个人工作台”时，默认交付仓库内可运行页面。

# 上次停留位置

账面报废申请页和审批页已完成公司间转移场景及分区标题样式统一，等待页面验收及后续审批流程规则调整。

# 验收路径

## 申请页

1. 打开 `/` → “账面报废申请单”。
2. 页面顶部不展示说明文案。
3. “是否公司间转移”为普通“否/是”单选按钮。
4. 选择“是”后展示公司间转移明细，标题旁信息图标 hover 显示“与资产明细保持一一对应”。
5. 新板块、新成本中心、City、Building、调账后仓库点击后均打开对应选择弹窗；Floor 为下拉框。
6. 公司间转移明细无说明条、无新增/删除按钮，资产标签号只读且随资产明细同步。
7. 除调账后仓库外，任一必填字段为空时提交应拦截。
8. 基本信息、报废原因、资产明细、公司间转移明细标题均展示蓝色竖条和 16px 加粗标题。

## 审批页

1. 打开 `/approval` → “账面报废审批”。
2. 基本信息、报废原因、报废资产明细、公司间转移明细、审批意见标题均有明显蓝色竖条和加粗标题。
3. 基本信息、三类报废原因均按申请结果只读展示，“是否公司间转移”为“是”。
4. 附件 hover 时出现下载按钮，点击有下载动作和成功反馈。
5. 报废资产明细仍分“已到报废期 / 未到报废期 / 丢失”三个标签页展示，并保留类型级数量/原值/净值汇总。
6. 各资产分组默认折叠，只显示组名及数量/原值/净值汇总；点击后展开原审批页明细表格。
7. 报废资产明细字段与原审批页保持一致。
8. 页面展示只读“公司间转移明细”，资产标签号使用普通黑色文本且与报废资产明细对应。
9. 审批意见可填写；驳回时未填写审批意见应拦截。
