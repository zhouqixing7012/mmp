# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 退库审批页面：
  - 维修记录作为资产信息的普通字段放在最下方，使用小尺寸文字按钮。
  - 附件表格只展示附件名称、附件大小、操作，底层节点和上传人元数据继续用于权限校验。
  - 删除无实际数据的代理人列。
  - 申请日期、部门、状态标签和审批按钮样式完成统一。
- 资产更换、资产退库、资产借用三个流程完成设计语言批量整改：
  - 详情信息默认三列，长文本单独一行。
  - “申请时间/申请日期”按是否展示时分秒统一命名。
  - 部门层级通过公共方法统一使用“.”连接。
  - 只涉及固定资产的标题、表格列和弹窗统一使用“资产”，不再混用“物资”。
  - 提交、同意、驳回、确认、取消、返回、加签、转签等流程按钮统一去除图标。
  - 搜索、添加、上传、删除、刷新、扫码等工具操作保留含义明确的图标。
  - 审批记录无真实代理数据时不展示代理人列。
  - 借用申请的自定义 section 已替换为标准小尺寸 Card。
  - 维修记录、查看名下资产等字段级入口统一使用小尺寸文字按钮。
  - 空状态返回按钮统一为“返回工作台”。
  - 资产选择弹窗只保留一个主标题，不展示重复二级标题。
- 状态展示完成统一：
  - `StatusTag.js` 作为唯一真实实现，`StatusTag.jsx` 仅转发，避免同名组件分叉。
  - `business`、`workflow` 状态按原文展示；明确的是/否字段才按是非状态展示。
- 新增公共展示格式工具：
  - `formatDepartment`：部门层级 `/` 转 `.`。
  - `formatDateText`：日期统一展示为 `YYYY-MM-DD`。
- 新增项目级设计规范 `docs/UI_DESIGN_GUIDELINES.md`，并通过 `AI_RULES.md`、`CLAUDE.md` 设为个人工作台页面强制规范。
- 新增“合约号码 ES 配给”页面并加入个人工作台：
  - 按旧系统截图保留申请人信息、申请信息、号码配给、审批历史和审批意见。
  - 页面按项目规范改为单层画布、小尺寸 Card、三列详情和无图标流程按钮。
  - 号码选择弹窗支持按 IMEI、标签号或电话号码查询，单选后回填电话号码和话费套餐。
  - 同意前必须选择号码；驳回和延期时审批意见必填。
  - 同意、驳回、延期均写入统一演示数据并刷新审批历史。
  - 新增 `src/mock/contractNumberAllocationMock.js`、`src/services/contractNumberAllocationService.js`、`src/pages/contractNumber/ContractNumberAllocationPage.js`、`ContractNumberSelectModal.js`。

# 关键决定

- 个人工作台页面遵循“页面标题 + 小尺寸业务 Card + 居中流程操作”的统一结构。
- 三列详情使用统一固定列宽；长文本、原因、说明、备注默认单独一行。
- 状态颜色由 `StatusTag` 集中维护，页面禁止自行声明状态颜色映射。
- 同一模块路径下禁止保留 `.js` 与 `.jsx` 两个同名公共组件实现；必须保留单一真实实现，其他扩展名只允许转发或删除。
- 日期和部门格式由 `src/utils/displayFormat.js` 统一处理。
- 流程按钮使用纯文字，工具按钮才使用图标。
- 只有存在真实代理审批业务和数据时才展示代理人列。
- 老系统截图用于确认业务字段和流程，视觉结构统一按当前个人工作台规范重构，不照搬旧页面样式。
- 合约号码配给使用独立 mock 与 service，通过 `demoStorage` 持久化，不在页面直接操作 localStorage。

# 上次停留位置

“合约号码 ES 配给”页面已加入个人工作台，等待构建和页面验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 合约号码ES配给。
2. 确认申请人信息按三列展示：申请人、部门、职级、手机、分机、入职时间。
3. 确认申请信息展示申请原因、身份证号码和红色流程提醒。
4. 在号码配给区点击“选择号码”，弹窗只保留“选择合约号码”一个标题。
5. 输入 IMEI、标签号或电话号码查询，选择一条号码并确认，电话号码和话费套餐应回填。
6. 未选择号码直接同意时应提示先选择电话号码。
7. 驳回或延期未填写审批意见时应提示必填。
8. 同意、驳回或延期后审批历史应更新；刷新页面后操作结果仍保留。
9. 检查审批状态显示真实文字，不得显示成统一的“否”。
10. 检查页面 Card、三列详情、按钮图标、部门分隔符和弹窗标题符合 `docs/UI_DESIGN_GUIDELINES.md`。
