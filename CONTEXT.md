# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。

# 本次完成

- 个人工作台已删除“我的资产”“我的资产更换申请”及各业务待办列表页，菜单直接进入审批、办理或确认页面。
- 资产退库、合约号码退库已删除“我的退库申请”标签页。
- 资产更换申请、资产更换办理、员工资产确认等页面已按最新要求完成布局和交互调整。
- 个人工作台页面统一使用单层画布，`Descriptions` 统一固定列宽，保证跨 Card 三列对齐。
- 资产退库申请页面已完成资产总数位置及资产用途字段调整。
- 已撤销误改的“资产更换-MIS鉴定”页面，恢复至调整前版本。
- 退库审批页面已完成三列布局、维修记录入口和鉴定通过/不通过操作调整。
- 退库流程已新增节点附件能力，并在 Service 层校验当前节点、当前上传人删除权限。
- 资产退库办理页面已完成申请人信息、退库资产信息、退库信息维护、附件信息和审批操作调整。
- 资产借用页面已调整：
  - “添加物资”统一改为“添加资产”，添加弹窗只展示资产目录，不再展示耗材标签页。
  - 空状态、添加成功提示、重复选择提示和校验文案统一使用“资产”。
  - 资产保管职责正文改为红字。
  - 阅读确认项改名为“我已阅读并确认资产保管职责”，并在 Card 内居中展示。
  - 新建借用单不再默认写入“借用”资产用途。
- 借用配给页面已调整：
  - 申请人信息仅保留申请人、申请日期、公司、办公区、联系电话、邮箱、部门。
  - 部门组织层级使用“.”连接，不再展示“/”。
  - 资产标签号选择弹窗删除“资产查询列表”蓝色二级标题，弹窗主标题改为“选择资产”。
  - 审批信息删除代理人字段。
- 借用审批页面的审批信息删除代理人字段。
- 借用发放页面已调整：
  - 资产用途选项只保留员工用机、部门公用、其他用途、专业用途，不再提供“借用”。
  - 历史借用单中原有“借用”用途进入页面时自动置空，需重新选择有效用途。
  - 资产标签号选择弹窗同步删除“资产查询列表”蓝色二级标题。

# 关键决定

- 原型菜单点击后直接进入业务详情、审批或确认页面，不再额外展示待办列表。
- 个人工作台只保留一层页面画布，业务卡片直接展示在统一浅灰背景上。
- 三列详情卡片必须共享相同标签列宽和固定表格布局，避免各 Card 根据内容自行计算列宽。
- 数量、总计等统计信息统一放在对应明细 Card 右上角，不占用申请信息字段位置。
- “资产更换-MIS鉴定”与“资产退库-退库审批/MIS鉴定审批”是两个独立页面，后续修改必须先确认所属业务模块。
- 附件权限由“业务单据 + 上传节点 + 上传人”共同判断，页面禁用与 Service 校验同时生效。
- 原型只保存附件元数据，不上传真实文件内容或调用外部文件接口。
- 资产借用业务只允许选择资产，页面不得继续保留耗材入口或“物资”口径。
- “借用”是业务场景，不是资产用途枚举；借用发放时仍需选择真实资产用途。
- 借用审批信息统一复用 `BorrowingApprovalHistory`，代理人字段从共用组件中删除，确保借用配给与借用审批同时生效。

# 上次停留位置

资产借用、借用配给、借用审批和借用发放的文案、申请人字段、弹窗标题、审批记录列及资产用途已完成调整，等待验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 资产借用。
2. 确认右上角按钮和弹窗标题均为“添加资产”，弹窗中不再出现“耗材”标签页。
3. 确认资产保管职责为红字，阅读确认项居中，文案为“我已阅读并确认资产保管职责”。
4. `/yewurules` → 个人工作台 → 借用配给。
5. 确认申请人信息仅展示申请人、申请日期、公司、办公区、联系电话、邮箱、部门，部门使用“.”连接。
6. 点击资产标签号搜索按钮，确认弹窗主标题为“选择资产”，不再出现“资产查询列表”蓝色标题。
7. 确认借用配给的审批信息表格不再展示代理人列。
8. `/yewurules` → 个人工作台 → 借用审批，确认审批信息同样不展示代理人列。
9. `/yewurules` → 个人工作台 → 借用发放。
10. 确认资产用途下拉没有“借用”，历史“借用”值不会被继续回填；未选择用途时无法发起确认。
11. 点击借用发放的资产标签号搜索按钮，确认同样不再出现“资产查询列表”蓝色标题。
