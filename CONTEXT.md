# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图和已整理的历史逻辑文档只提取字段与业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- B 端统一动效已进一步收口：公共弹窗、后台菜单/Tab、侧边栏、顶部下拉、React Router 路由、按钮进入详情/编辑/创建页、查询/重置结果刷新、可点击操作块和表格结果反馈均已形成公共能力，不要求业务页面各自维护动画参数。
- 新页面动效规则已固化到 `AGENTS.md` 和 `docs/UI_MOTION_GUIDELINES.md`，后续生成页面默认按统一动效规范检查。
- 当前库存重点已推进到 **库存管理 → 出库 / 移库** 的正式库存处理链路，并继续承接资产接收、耗材接收及员工申领/借用/耗材领用的上下游结果。
- Mock 数据开始统一引用真实基础数据：`src/mock/reference/materialCatalog.js` 提供物料参考记录，`src/mock/reference/warehouseCatalog.js` 收录全部启用仓库；后续物料、仓库及关联属性禁止脱离真实记录自行拼接。

# 上次停留位置

- 全局动效 Token 保持 100 / 140 / 180 / 220ms 四档，统一缓动，并支持 `prefers-reduced-motion`。
- `src/components/Modal.js` 和 `src/components/SelectModal.jsx` 已增加淡入 + 轻微上移/缩放的进入动画，以及约 140ms 的真实退出动画；关闭后再卸载 DOM。
- 新增 `src/components/PageMotionBoundary.jsx` 作为整页动效统一出口：初次进入播放 `mmp-page-motion`；同 URL 内部视图切换使用“语义标题变化 + 页面出口第一层主要业务区块替换”双判定，覆盖 list/detail/editor/create/返回等整块视图变化。
- `PageMotionBoundary` 的结构判定只认 Card / Table / Form / Descriptions 等主要业务区块；表格行更新、输入值变化、局部提示/按钮显隐不会触发整页动画。
- `PageMotionBoundary` 已排除 Ant Design Modal / Drawer / Popover / Dropdown，以及项目自定义 `mmp-motion-overlay` / `[role="dialog"]` 内部标题和结构，避免打开浮层时误触发整页页面动效。
- `PageMotionBoundary` 根节点增加 `data-mmp-page-motion-boundary`，供页面内局部交互定位自己的结果区域，不依赖具体业务页面 DOM。
- `src/App.js` 已改为普通 React Router 路由统一挂 `PageMotionBoundary`，并按 `location.key` 重新挂载；以后无论 `Link` 还是按钮内 `navigate()`，只要进入普通路由都会自动有统一页面进入动效。
- `/yewurules` 不在 App 层重复播放整页动画，由 `AdminContent` 内部 `PageMotionBoundary` 接管；`yewurules.js` 中角色、字典、个人工作台、资产管理、库存管理、盘点和后台基础配置全部位于该出口内，因此菜单切换与同一菜单内本地 `view/viewMode/currentView` 的详情/编辑/创建切换均统一覆盖。
- `src/components/PageViewMotion.jsx` 保留为特殊兜底：只有自动边界既无法通过语义标题区分，也没有可识别主要业务区块替换时才显式使用；优先可在视图根节点声明 `data-page-view-key`。
- `src/components/QueryBar.jsx` 已统一处理查询/重置结果反馈：默认按钮和自定义 `buttons` 中的“查询 / 重置”都会自动识别，并对同一页面/弹窗中该查询区后面的首个 `Table / List` 播放约 140ms 的淡入 + 2px 轻位移动效；如果结果不是普通 Table/List，可用 `data-mmp-query-result` 显式标记。
- 查询/重置不会触发整页 `PageMotionBoundary` 动效，也不会人为制造 Loading；查询条件 Card 本身保持静止，真实异步等待才使用 Spin/Skeleton。
- `AdminSidebar` 二级菜单通过 CSS Grid 平滑展开/收起，Chevron 使用统一旋转过渡。
- 顶部 `Navbar` 路由下拉保留约 140ms 的淡入 + 4px 位移；路由页面进入由 App / AdminContent 的公共边界统一负责。
- `mmp-interactive-card` 作为明确可点击卡片/操作块的统一 hover/press 动效；普通信息 Card 不增加上浮。
- `src/hooks/useTransientRowHighlight.js` 为新增/修改成功后的目标表格行提供约 900ms 的短暂品牌色高亮；查询/分页/删除不触发。
- `docs/UI_MOTION_GUIDELINES.md` 与 `AGENTS.md` 已将查询/重置结果刷新纳入新页面硬规则，避免以后生成查询列表时再次出现结果瞬间替换、交互生硬的问题。
- 资产接收 PO 详情创建接收单必须先勾选 PO 物资行；“创建接收单”放在 PO 物资明细 Card 右上角，创建成功直接进入接收单详情。
- 资产接收 PO 物资“编辑”弹窗已与耗材接收统一：物料、配置、接收数量、是否部件、部件数量、部件描述；接收数量受剩余可接收数量约束。
- 资产接收接收单状态已统一为 **草稿 / 已完成**；电子设备创建接收单后直接进入草稿，机房资产 PO“接收确认”直接生成已完成接收单。
- 电子设备创建草稿接收单后立即占用 PO“草稿数量”；可接收数量固定按 `采购数量 - 已接收数量 - 草稿数量` 计算。删除接收行、删除拆分资产明细、删除草稿接收单或取消接收都会释放对应占用数量。
- 资产接收部分接收时整张 PO 继续保持“待接收”；只有所有 PO 行累计接收数量达到采购数量后才变为“已接收”，后续全部执行入库后才进入“已入库”。
- “取消接收”已明确为：作废当前草稿接收单并释放全部 PO 草稿占用数量，不引入第三个接收单状态。
- 普通资产接收“维护接收明细”按全部接收物资的本次接收数量逐件拆行；删除一条拆分资产会同步把来源接收行数量减 1，并释放 1 个 PO 草稿占用数量。
- 标签号/SN维护实时保存；“维护SN号”只给空白 SN 写“缺省”，不得覆盖已有真实 SN；真实 SN 必须唯一，“缺省”不参与唯一性冲突，扫描、明细编辑保存和接收确认均按该规则校验。
- 普通资产接收确认要求标签号和 SN 均非空，“缺省”SN 视为已维护；确认成功后记录当前操作人为接收人、当前时间为接收时间，同时将草稿占用转入已接收数量并生成草稿采购接收入库单。
- 接收单的制单人/制单时间取创建草稿时；接收人/接收时间取接收确认成功时，草稿阶段接收人和接收时间为空。
- 资产接收 PO 详情的申请批次为选填；有值时随接收单保存，并继续传到后续采购接收入库单及入库物资明细。板块同样随接收单和入库单传递。
- 服务器、服务器备件、网络设备、网络设备备件的 PO 详情使用勾选行后的“接收确认”，并生成 **已完成接收单 + 草稿采购接收入库单**；部分接收不提前改变整张 PO 为已接收。
- 资产接收、耗材接收的正式“物资接收单”仅已完成接收单允许打印；打印沿用现有表头、明细、联次和签字区，打印/重打不改变接收、PO、入库或库存状态。
- 耗材接收采购类型固定为“低值耐用品 / 耗材”；低值耐用品沿用逐件标签/SN维护，普通耗材按数量型接收。
- 低值耐用品“维护接收明细”的扫描交互已与资产接收统一：扫描光标使用 QueryBar 的【查询 / 重置】；首次扫描标签号后仅展示并勾选命中行，第二次扫描 SN 成功后实时保存、恢复全量列表并保留刚处理行勾选；重置会清空扫描输入、定位、过滤和勾选状态。
- 耗材接收的 PO 单列表、接收单列表均在 Card 右上角固定展示当前查询结果“共 X 条”；接收单列表存在删除等分区操作时，数量与操作按钮并列展示，不因有操作按钮省略数量。
- 入库列表现有演示单据已覆盖新增入库、采购接收、退库入库、借用归还四类可查看详情数据；已完成单据使用真实只读展示。
- 入库物资的资产标签号为蓝色详情入口；蓝色 link Button 文本现在同时支持光标拖选复制，点击能力继续保留。
- 只读“入库物资信息”不展示与下方信息重复的“选择物资”区域。
- 新增入库、退库入库、借用归还的只读物资详情沿用各自维护字段口径，但只读栅格标签宽度已加宽，确保“原资产标签号 / 主资产标签号”等长字段名完整展示。
- 采购接收入库物资只读详情已收敛字段：删除品牌、规格型号、库区、货位、级别、合同编号、小服务、Cabinet、ILO、ILOPW、IP1、IP2、IP3；`NO位置` 紧跟在 Floor 后，`服务` 紧跟在费用账户后；删除独立“NO设备信息”分区，将剩余两个 NO 字段并入“物资信息”。
- 采购接收入库物资只读详情已从历史截图临时采用的 4 列布局统一回项目标准 **3 列 DetailGrid**；历史 ERP 截图的信息密度不作为当前系统的特殊布局规则。
- 纯查看型“入库物资信息”弹窗只保留右上角 X 关闭，不再重复展示右下角“关闭”按钮；新增/编辑/确认类 Modal 继续保留业务 footer。
- 采购接收入库明细“使用说明”按采购订单行使用部门生成：部门层级用 `.` 连接，超过3段保留前3段，不足3段按实际层级，部门为空则为空；该值用于接收/入库链路展示及入库单“入库说明”打印。
- 采购接收入库正式建卡时 **不将上述使用说明写入资产卡片“使用说明”**，资产卡片该字段保持现有建卡规则，不由本次采购接收入库覆盖。
- 入库打印保持现有类型标题和字段：新增入库=“新增入库单”、采购接收=“接收入库单”、领用退库=“领用退库单”、借用归还=“借用退库单”；采购接收入库打印不新增SN列。
- 创建出库单时“出库类型”固定默认“领用出库”并只读；历史已有单据仍按原出库类型展示。
- 出库单状态已扩展为 **草稿 / 审批中 / 已完成**。只有核心出库页手工创建的领用出库需要新增审批；上游员工资产申领、资产借用、耗材领用自动生成已完成出库单后不重复审批。
- 普通资产手工领用审批固定为 **责任人确认 → 责任人直属7级及以上领导审批 → ES出库管理员确认 → 正式出库**；机房资产手工领用固定为 **责任人直属7级及以上领导审批 → 正式出库**。
- 手工出库禁止普通资产与机房资产混单；普通资产还要求责任人、直属领导、ES出库管理员一致，机房资产要求直属领导一致，否则提示拆分出库单。
- 出库审批驳回后单据恢复草稿，保留物资和历史审批记录，不产生正式库存或台账结果；重新点击“执行出库”发起新一轮审批。
- 出库手工资产选择按当前仓库、可出库状态和业务锁定过滤；草稿修改当前仓库且已有物资时二次确认并清空已有物资。添加物资使用“取消 / 添加并继续 / 添加并关闭”，不再保留重复的连续添加复选框。
- 出库已完成演示数据已覆盖普通资产申领、借用出库、低值耐用品和普通耗材；资产标签/耗材标签作为详情入口，耗材和低值耐用品使用耗材业务字段口径。
- 凡出库物资属于采购接收来源，正式出库完成后发送采购专员服务号通知；通知失败不回滚已完成出库，并在已完成详情提供“重新发送采购通知”。
- 出库列表支持出库打印、领用打印和导出；领用出库及借用出库的已完成单据均支持领用打印，草稿或审批中不得打印正式凭证。
- 出库打印保持现状：物资出库单最后一列继续使用现有“备注”口径（业务线/新资产地点/启用日期等），本次不改为资产卡片“使用说明”；员工领用单继续区分“用途”和“领用说明”，借用场景使用现有借用凭证口径。
- 移库 PRD 和 PC 原型已同步收口为 **资产 / 低值耐用品逐件移库**，普通耗材当前不允许移库；运输方式、预计到达时间不再属于移库单字段。
- 移库发起和接收已改为共享同一套移库单数据：草稿点击“移库提交”后真实进入 **出库待接收**，物资仓库切到 **V00001-集团在途总库**，接收页自动展示同一张单，不再维护两套互不联动的 Mock。
- 移库单状态按明细结果统一计算：**全部已接收=已完成；全部已驳回=已驳回；仍有待接收=出库待接收；无待接收且已接收/已驳回等终态混合=已完成**。
- 移库驳回按当前勾选待接收行执行，驳回原因必填且最多200字；被驳回行不直接退回来源仓，而是自动生成交换来源/目标仓的反向移库单，并记录来源移库单号。
- 新建移库单添加物资不要求先保存单头，但必须先选择当前仓库；添加物资支持资产/低值耐用品选择、批量选择、重复校验和业务锁过滤，被其他业务锁定的资产禁止加入或提交。
- 移库接收已拆开“验证”和“正式接收”：扫码可直接验证当前待接收行，手工验证必须填写说明；只有 **已验证 + 待接收** 的勾选行允许执行移库接收，接收后生成事务25并记录接收人/时间。
- 移库现有演示数据已覆盖普通资产草稿、低值耐用品草稿、部分验证待接收、正常完成、部分接收+部分驳回、整单驳回及反向待接收等主要分支；移库物资详情统一使用3列只读展示并读取历史快照。
- 「库存管理 → 转移」列表主按钮统一为“创建”。
- 资产管理 → 资产维护已从通用轻量台账页收敛为独立业务原型，正式目标文档为 `docs/PRD-资产管理-资产维护.md`；业务范围覆盖普通资产和机房资产的全部正式资产卡片，页面本身不新增、不删除资产卡片。
- 资产维护列表字段固定为：标签号、公司、板块、资产大类、资产小类、资产说明、数量、原值、净值、资产责任人、资产状态、成本中心、仓库、启用日期、资产类型；标签号进入资产卡片，右侧固定操作列仅保留【编辑】，不再通过勾选后顶部编辑；除选择列、操作列外的业务列支持表头升降序排序。
- 资产维护查询拆分为基础条件和默认收起的更多条件；公司、部门、责任人、成本中心、品牌使用多选弹窗，品牌不使用自由文本；PC 查询区统一一行 3 个条件，更多条件展开后继续按三列向下排列；选择仅保留当前页，不跨分页保留。正式查询接口对公司、部门、责任人、成本中心、位置等使用稳定编码/ID，页面只负责显示名称；文本多值统一支持中英文逗号和换行分隔。Floor 查询可独立选择有效 Floor，不要求先选择 Building，也不按 Building 做强关联过滤。
- 资产维护查询进一步收口：公司、部门和成本中心在原型中均以稳定编码作为实际选择值，名称或“编码.名称”只负责回显；正式“部门包含下级组织”必须通过组织树的部门编码/ID展开，不得按部门名称前缀推断上下级。数量/金额排序同样区分空值与真实 `0`，空值不得为了排序被转换成 0。
- 资产卡片继续使用 Modal，不改独立详情页；PC 端中等宽度并允许弹窗内容区纵向滚动。**查看态**固定保留详细信息、价值信息、资产维修记录、资产盘点历史、资产操作历史、RFID资产操作历史六个页签；**编辑态不展示页签导航，只展示详细信息内容及取消/保存操作**。
- 资产卡片详细信息的每个分组标题左侧增加蓝色竖向区块；基础信息固定展开，账务信息、NO信息、报废信息、耗材信息、部件信息支持各自独立展开/收起，打开新资产时默认全部展开，折叠不清空数据或编辑值。
- 资产卡片基础信息字段顺序固定为：资产标签号、序列号、公司、部门、资产大类、资产小类、资产说明、配置、数量、单位、主资产标签号、仓库、原值、新增类型、购买日期、启用日期、资产状态、资产标记、资产责任人、用途、City、Building、Floor、盘点标识、ES实物报废期、升级日期、升级金额、PR单号、申请单号、入库单号、PO单号、使用说明、备注；**配置占2列宽度，可与资产说明共同行；使用说明、备注独占整行**。
- 资产卡片真正的整行字段必须从完整新行开始；“入库单号/PO单号→使用说明”之间使用真正的空结构单元补齐3列，不用空格文本占位。资产责任人历史脏数据只显示实际存在的编号/姓名，均空时显示空值；价值页折旧年限/剩余月份真实值 `0` 分别显示 `0年 / 0个月`，不按空值处理。
- 资产卡片账务信息字段顺序固定为：资产编号、成本中心、板块、业务线、项目、内部费用账户、费用账户；成本中心在该分组原位置进入编辑态。资产卡片不展示品牌、型号、账簿、税额、资产入库批次、资产更换日期、可用标志、资产要求。
- 资产卡片中的 NO 信息只展示 **NO位置 / 服务**；普通资产或无关联时显示“无 NO 信息”。正式系统机房资产识别复用系统统一分类口径，原型 `isMachineRoom` 仅为 Mock。报废、耗材、部件等无数据分组也不隐藏，分别展示空状态。
- 资产维护单条编辑只展示详细信息分组，并在字段原位置把 **成本中心、City、Building、Floor、资产状态、资产序列号、备注、资产标记、使用说明、资产用途** 10 个字段切换为可编辑态，其余字段始终只读。**成本中心、City、Building、资产状态必填**；Floor、SN、备注、资产标记、使用说明、用途非必填。成本中心当前只校验必填及基础资料有效性，暂不按责任人、使用部门或机房资产限制候选范围；Floor 只校验有效基础资料，不与 Building 做强关联；当前资产为正式报废状态时资产状态在编辑态直接显示只读文本，不使用 disabled Select。
- 资产维护允许“再利用-待报废”继续通过资产维护编辑；“已报废-待处置 / 已报废-已处置”等正式报废状态不能通过资产维护人工设置，必须由报废业务产生；资产当前已经处于正式报废状态时，资产维护中的“资产状态”锁定，不允许改回其它状态。
- 单条资产编辑正式保存使用乐观锁及请求幂等：资产卡片、手工修改事务、最后修改时间、修改人及前后值在同一本地事务中原子提交；服务端身份和服务端时间作为审计依据。原型 Service 生成的资产维护事务已保存全部10个开放编辑字段的 `beforeValues / afterValues / changedFields`，无实际变化时不更新时间、不生成事务。费用账户/账簿等保存前关键联动失败会阻止本地提交；财务系统推送改为本地提交后的可靠同步任务，外部推送失败记录并重试，不回滚已经成功的本地资产与历史。
- 资产维护价值页普通资产固定展示 **原值、净值、EBS净值、EBS原值、折旧年限、折旧剩余月份**；机房资产额外展示 **配置参考价值**；价值页不展示税额。金额无记录与真实 0 明确区分。维修记录和 RFID 历史均只读，维修单号暂不下钻。
- 资产盘点历史与资产盘点模块口径统一：只展示项目状态为 **盘点关闭** 的记录，项目类型为 **初盘 / 抽盘 / 复盘**，按盘点项目启动时间倒序；盘点状态在资产维护历史中固定为 **已盘 / 代盘**。导入方式按狐小e扫码 / 狐小e快速扫描资产 / 人工上传盘点结果 / 扫码枪映射为 APP导入 / 快速盘点导入 / 空白 / 扫码枪导入。资产卡片“盘点标识”根据盘点关闭项目中的最近一次有效盘点历史动态取得年份；盘点历史行自己的“盘点标识”仍为项目“盘点类型-期间”。
- 资产操作历史按操作日期倒序；同秒事务继续按事务创建序号/主键倒序保证稳定顺序，相邻事务字段变化用浅黄色标识；页面与 Service 均按同一二级排序，不再由页面只按时间重新排序覆盖 Service 顺序。不展示资产担保人；正式业务事务不允许只有申请单号没有单据编号，手工资产维护事务允许单据编号和申请单号同时为空。后台审计需保留全部10个开放编辑字段的修改前后值及实际变化字段集合，包括当前历史表格未展示的资产标记。
- 资产维护 Excel 批量修改采用固定11列表头（资产标签号+10个维护字段）和覆盖式更新；**资产标签号、成本中心、City、Building、资产状态必填**，必填单元格为空或全空格时整批失败；Floor、SN、备注、资产标记、使用说明、用途为空时覆盖原值为空。成本中心当前只校验有效基础资料，不加责任人/部门范围限制；Floor 非空时只校验有效基础资料，不与 Building 做强关联。公式单元格、非法表头直接整文件失败。正式报废资产状态为锁定例外但仍必填且只能填写原状态。真实 SN 唯一性按整批保存后的最终状态校验；无实际变化行不更新时间、不生成事务或财务同步任务，并在批次记录中单独计数。本地整批保存原子化并做并发校验，批次使用提交幂等键，不能仅用文件哈希判重；财务推送失败进入重试，不回滚已成功的本地整批事务。当前原型不实现真实 Excel 解析和后台事务。
- 批量修改模板填写口径已明确：新模板继续采用11列单表直接填写；资产标签号和资产序列号列预设为 Excel 文本格式；成本中心填写“编码.名称”并以编码作为稳定识别值；City / Building / Floor 面向用户填写名称，后台映射为位置 ID，其中 Building 仍校验属于当前 City，Floor 只校验自身有效、不与 Building 强关联；状态/资产标记/用途优先通过标准值下拉辅助录入。名称无法唯一映射、成本中心编码不存在、编号被 Excel 科学计数法或前导零破坏后无法识别时均按实际值校验失败，不允许后台猜测恢复。
- 资产维护导出：当前页有勾选时仅导出勾选资产；未勾选时导出当前查询条件下全部结果。正式 xlsx 固定拆为 **资产卡片 / 耗材信息 / 部件信息** 3 个工作表，资产卡片一张资产只占一行，关联明细用“所属资产标签号”关联，避免一对多重复主行影响统计；标识字段按 Excel 文本写出并处理公式注入风险。历史明细页签不纳入本次资产卡片主数据导出；当前原型仍只验证范围和确认文案。
- 资产维护权限继续完整写入 PRD：除“公司 + 板块”数据隔离外，保留集团台账、上海媒体分公司台账、广州媒体分公司台账对应的现有查看/编辑人员口径；当前 PC 原型不模拟权限身份和按钮显隐。
- 对抗性测试明确不处理两个点：主资产标签号保持普通只读字段，不恢复下钻；编辑中点击资产卡片右上角 X 允许直接关闭，不增加未保存修改二次确认。
- 新增统一 Mock 基础参考源：物料源保留 67 条真实记录，覆盖全部启用“物料总类 + 物料大类”组合并每组最多取 3 个不同小类；仓库源收录 72 条全部启用仓库。两个源都提供按编码获取和按关键属性过滤的公共方法。

# 近期关键决定和原因

- B 端动效以“解释状态变化”为目标，不做明显弹跳和长距离位移；公共入口统一实现，业务页面不自行定义动画参数。
- 页面切换统一由两层公共边界处理：普通 React Router 页面由 `App.js + PageMotionBoundary`；`/yewurules` 由 `AdminContent + PageMotionBoundary`。公共边界同时负责同 URL 内语义视图切换，不再要求每个历史页面逐个套动画组件。
- `PageMotionBoundary` 优先看页面标题和主要 Card 标题；标题相同时再判断页面出口第一层是否替换了 Card/Table/Form/Descriptions 等主要业务区块。这样既覆盖同标题的详情/编辑切换，也避免普通数据刷新触发整页动画。
- 如果两个真实整页视图仍无法被公共边界区分，使用 `data-page-view-key` 或 `PageViewMotion` 显式兜底；这条已写入新页面规范。
- 查询/重置属于“数据结果刷新”，不是“页面切换”：统一由 `QueryBar` 给结果区做 140ms 短反馈，不重播整页动画，不使用固定延时伪造 Loading。
- 查询结果反馈默认寻找当前查询区后面的首个 Ant Design `Table / List`；特殊自定义结果容器用 `data-mmp-query-result` 显式声明，保持公共能力可控而不是依赖页面逐个手写。
- 新页面不依赖会话记忆判断动效，而以 `AGENTS.md + docs/UI_MOTION_GUIDELINES.md` 作为固定项目规则。
- 当前需求用 React + CSS + Ant Design / React Router 自带能力已能稳定完成，因此本阶段不引入 Motion for React。
- 页面切换只做进入过渡，不做复杂双页面叠加退出，避免后台高密度页面出现闪烁、布局重叠或状态管理复杂化。
- 表格新增/修改反馈抽成 `useTransientRowHighlight`，业务页面只负责在数据真正成功落地后触发。
- 卡片动效只作用于明确可点击对象，查询 Card、详情 Card、表格 Card 等纯信息容器保持静止。
- 依赖表格勾选结果的动作放在对应表格 Card 操作区。
- 业务列表 Card 的结果数量属于统一信息层：默认在右上角展示当前筛选结果“共 X 条”；即使同一区域还有新增、删除等分区操作，也不省略结果数量。
- 详情/只读 Modal 默认沿用项目 3 列 DetailGrid；只有明确业务或视觉要求时才采用其他列数，不因历史截图较密就单独保留 4 列例外。
- 蓝色编号/标签等 link Button 既承担下钻入口，也应允许用户拖选复制文本，不能因做成按钮失去基础文本复制能力。
- 接收数量属于可逆占用：创建草稿先占用，删除/取消释放，接收确认再由草稿占用转成已接收数量；不能只改页面状态而不维护数量账。
- PO 状态属于累计数量结果：部分接收保持待接收，全部接收才已接收，全部入库才已入库。
- 机房资产接收确认的业务结果是“接收已完成、入库待执行”，因此生成已完成接收单 + 草稿采购接收入库单。
- 原型列表已经提供可点击详情入口时，演示数据必须覆盖主要业务分支并带完整详情行数据。
- 已完成单据属于纯查看态，字段必须切换为真实只读文本/状态展示。
- 纯查看弹窗没有确认、保存等业务动作时，避免保留两个等价关闭入口；默认只保留右上角 X。
- Mock 基础数据以 `src/mock/reference` 为统一事实源：物料、仓库及其关联属性只要能够从基础源取得，就必须使用同一条真实记录；参考集未覆盖具体物料时先补录真实记录，禁止临时拼接品牌、型号、分类、配置或仓库地点关系。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”都进入 PO 详情；“查看”进入该 PO 的接收单列表，不能根据 PO 接收状态改变 PO 单号的去向。
- 电子设备 PO 详情使用“创建接收单”；服务器、服务器备件、网络设备、网络设备备件使用“接收确认”。
- 资产接收接收单状态固定为草稿 / 已完成；耗材低值耐用品同样沿用草稿 / 已完成。
- 资产/耗材接收单列表的制单时间为单个日期范围查询条件，接收单号作为详情入口，不保留重复操作列。
- 移库当前正式状态固定为 **草稿 / 出库待接收 / 已完成 / 已驳回**；明细状态为草稿 / 已取消 / 待接收 / 已接收 / 已驳回，反向移库继续走同一套接收闭环。
- 入库、出库、移库、转移继续沿用各自已确认字段与状态规则。
- 员工资产信息查询已完成原版 PRD / 当前原型 / 新版 PRD 三方校准：原版基线固定为 `docs/员工资产信息查询+原版PRD.md`，新版目标为 `docs/PRD-资产管理-员工资产信息查询.md`；当前原型已改为人员单选选择器、按页签动态查询条件、资产/耗材/合约号码严格按当前责任人归属、单据信息支持申请人/责任人/payPersonId/入出库明细人员多关系查询，并按原版字段重构耗材与合约号码列表、补齐详情入口/三状态/倒序/空状态。正式仍待明确资产状态 3/6 名称、合约号码有效状态集合、数据权限和历史单据旧系统跳转规则。
- 员工资产信息查询补充确认：资产状态代码 `3=在用-借用中`、`6=在用-使用中`；合约号码仅查询 `在用-使用中`；离职员工允许在人员选择器中查询并继续查看其关联资产/耗材/合约号码/单据；历史单据统一跳转旧系统对应单据详情页，PRD 不维护具体 URL；本功能暂不增加公司/板块/部门等数据权限限制，拥有菜单权限即可查询全部员工。当前仅待明确资产/耗材/合约号码默认排序。
