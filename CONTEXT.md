# 当前正在做什么

- `main` 持续校准库存管理原型，历史 ERP 截图和已整理的历史逻辑文档只提取字段与业务规则，视觉统一按 `docs/UI_DESIGN_GUIDELINES.md` V2.1。
- B 端统一动效已进入第二阶段：公共弹窗、后台页面切换、侧边栏展开/收起、顶部路由下拉、一级路由 View Transition、明确可点击操作块和表格结果反馈能力已统一收口，不改业务逻辑、不新增动画依赖。
- 新页面动效规则已固化到 `AGENTS.md` 和 `docs/UI_MOTION_GUIDELINES.md`，后续生成页面默认按统一动效规范检查。
- 当前库存重点仍是 **库存管理 → 资产接收 / 耗材接收** 的接收链路，以及接收结果与入库草稿的衔接。

# 上次停留位置

- 全局动效 Token 保持 100 / 140 / 180 / 220ms 四档，统一缓动，并支持 `prefers-reduced-motion`。
- `src/components/Modal.js` 和 `src/components/SelectModal.jsx` 已增加淡入 + 轻微上移/缩放的进入动画，以及约 140ms 的真实退出动画；关闭后再卸载 DOM。
- `AdminContent` 已按 `activeMenu / activeSubMenu / activeTab` 组成的页面 scope 统一触发约 180ms 的页面淡入 + 6px 上移动效，个人工作台、资产管理、库存管理、资产盘点和后台配置共用同一出口。
- `AdminSidebar` 二级菜单已改为保持挂载后通过 CSS Grid 平滑展开/收起，Chevron 使用统一旋转过渡，不再瞬间出现/消失。
- 顶部 `Navbar` 路由下拉已增加约 140ms 的淡入 + 4px 位移，并为 `Link` 启用 React Router 7 原生 `viewTransition`。
- `mmp-interactive-card` 继续作为明确可点击卡片/操作块的统一 hover/press 动效；普通信息 Card 不增加上浮。
- 新增 `src/hooks/useTransientRowHighlight.js`：新增/修改成功后可通过 `highlightRow(key)` + Table `rowClassName` 对目标行做约 900ms 的短暂品牌色高亮；查询/分页/删除不触发。
- 新增 `docs/UI_MOTION_GUIDELINES.md`，明确页面切换、弹窗、菜单、下拉、可点击 Card、表格反馈、Loading、禁止项和新页面 8 项检查清单；`AGENTS.md` 已将其设为新增页面必须遵守的规则。
- 资产接收 PO 详情创建接收单必须先勾选 PO 物资行；“创建接收单”放在 PO 物资明细 Card 右上角，创建成功直接进入接收单详情。
- 资产接收 PO 物资“编辑”弹窗已与耗材接收统一：物料、配置、接收数量、是否部件、部件数量、部件描述；接收数量受剩余可接收数量约束。
- 服务器、服务器备件、网络设备、网络设备备件的 PO 详情不再使用“执行入库”，改为 PO 物资明细 Card 右上角的“接收确认”；必须先勾选目标 PO 行。
- 机房资产“接收确认”一次生成 **已完成接收单 + 草稿采购接收入库单**；已处理 PO 行更新为已接收，草稿入库单可在「库存管理 → 入库」继续编辑并执行入库。
- 普通资产接收“维护接收明细”仍是页面级动作，按全部接收物资的本次接收数量逐件拆行；标签号/SN维护实时保存，接收确认要求标签号和 SN 均非空，“缺省”SN 视为已维护。
- 耗材接收采购类型固定为“低值耐用品 / 耗材”。低值耐用品沿用资产接收逐件维护链路；普通耗材仍按数量/接收行维护。
- 耗材接收单状态只保留“草稿 / 已完成”；接收明细已去除“资产标记”字段。
- 低值耐用品的生成标签号、维护SN号、删除明细、扫描写入均实时保存；接收确认前全部明细必须有标签号和 SN，SN 为“缺省”有效。
- 入库列表现有演示单据已补齐详情 Mock：**新增入库、采购接收、退库入库、借用归还**四种类型至少各有一张可进入详情并看到物资明细；现有两张退库入库演示单也都补有明细，避免点击任一历史演示单出现空详情。
- 入库单状态为**已完成**时，详情页全部按只读样式展示；入库类型、是否计费、当前仓库直接使用只读文本，不再用 disabled Select 模拟只读。
- 入库物资表中的**资产标签号**统一为蓝色链接文本；点击后打开只读“入库物资信息”弹窗。
- 只读“入库物资信息”不再展示与下方信息重复的“选择物资”区块；该区块只保留在新增/编辑流程中用于定位来源物资。
- 新增入库、退库入库、借用归还的只读物资详情继续沿用对应添加物资弹窗的业务字段口径，只切换为只读展示；采购接收作为已确认例外，详情字段按历史页面截图展示完整 **物资信息 + NO设备信息**，不再复用“待入库物资”选择列表的精简字段。
- 采购接收入库物资详情“物资信息”包含物资说明、分类、品牌型号、配置、库区/货位、申请批次、数量、合同、金额、标签/SN、级别/状态、位置、组织责任、新增属性、费用、日期、PR/申请/PO、部件、供应商、使用说明、备注；“NO设备信息”包含服务、小服务、NO位置、Cabinet、ILO、ILOPW、IP1、IP2、IP3。
- 创建出库单时“出库类型”固定默认为“领用出库”并只读；历史已有单据仍按原出库类型只读展示。
- 「库存管理 → 转移」列表的“创建转移单”按钮已改名为“创建”。

# 近期关键决定和原因

- B 端动效以“解释状态变化”为目标，不做明显弹跳和长距离位移；公共入口统一实现，业务页面不自行定义动画时长和曲线。
- 新页面不依赖会话记忆判断动效，而以 `AGENTS.md + docs/UI_MOTION_GUIDELINES.md` 作为固定项目规则；生成页面时必须逐项检查页面切换、弹窗、可点击 Card、菜单/下拉、表格反馈和 reduced motion。
- 当前需求用 React + CSS + Ant Design / React Router 自带能力已能稳定完成，因此本阶段不引入 Motion for React；等出现共享布局、多阶段编排等真实需求再评估。
- `/yewurules` 内页面切换继续只做进入过渡，不做复杂双页面叠加退出，避免后台高密度页面出现闪烁、布局重叠或状态管理复杂化。
- 一级 React Router 路由使用官方 `viewTransition`；浏览器不支持时由平台自然回退，不额外维护第二套路由动画逻辑。
- 表格新增/修改反馈抽成 `useTransientRowHighlight`，业务页面只负责在数据真正成功落地后触发，不把查询、分页、排序等普通刷新做成动画。
- 卡片动效只作用于明确可点击对象，查询 Card、详情 Card、表格 Card 等纯信息容器保持静止，避免误导可点击性。
- 依赖表格勾选结果的动作放在对应表格 Card 操作区；因此电子设备“创建接收单”和机房资产“接收确认”都紧邻 PO 物资明细。
- 机房资产接收确认的业务结果是“接收已完成、入库待执行”，因此直接落一张已完成接收单，同时生成一张草稿采购接收入库单，而不是把两步混成已完成入库。
- 低值耐用品属于需要逐件形成标签号/SN的可管理实物，因此复用资产接收逐件维护逻辑；普通耗材仍保留数量型接收。
- 接收明细批量维护动作和扫码写入采用实时保存，避免再增加整页保存步骤。
- 原型中列表已经提供可点击详情入口时，演示数据必须覆盖所有主要业务分支并带完整详情行数据，不能只在列表层造单据头导致详情页为空。
- 已完成单据属于纯查看态，字段必须切换为真实只读文本/状态展示，而不是保留一组灰色 disabled 控件；详情表中的关键编号可继续保留蓝色链接作为下钻入口。
- 只读详情不保留仅服务于选择/编辑的重复区块；详情字段默认与对应维护场景保持业务口径一致，但用户提供独立历史详情字段集时，以该已确认详情字段集为准。

# 其他已确认库存管理规则

- 资产接收 PO 列表固定“接收 / 查看”；PO 单号和“接收”进入 PO 详情，“查看”进入接收单列表。
- 电子设备 PO 详情使用“创建接收单”；服务器、服务器备件、网络设备、网络设备备件使用勾选行后的“接收确认”，并生成已完成接收单 + 草稿采购接收入库单。
- 资产/耗材接收单列表的制单时间为单个日期范围查询，接收单号作为详情入口，不保留重复操作列。
- 入库、出库、移库、转移继续沿用此前已确认字段与状态规则。
