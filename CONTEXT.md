# 当前状态

- 项目为企业资产管理产品演示前端，当前持续在 `main` 迭代；除用户明确要求外不新建开发分支或 worktree。
- 个人工作台、资产管理、库存管理、资产盘点主要原型已建立，继续按 PRD、截图和已确认业务口径逐页校准。
- 员工自助标注体系已经从“搭能力”进入“PRD 深审 + 差异收敛”阶段：个人工作台、资产申请、新员工领用、合约号码、耗材、资产借用、资产更换、资产转移、资产退库、合约号码退库共 10 个业务模块均已建立基线标注与 Coverage Ledger，当前合计 854 条研发规则。
- Coverage Ledger 固定使用 `bound / review / skip`；`review` 表示 PRD 与原型/实现存在真实差异或暂无可靠承载位置，不能通过粗绑 Card 假装完成。
- 原型标注已支持页面 scope、Button/字段/Tab/表头等细粒度 target、Descriptions、Modal/Drawer/SelectModal 动态浮层、拖动、重绑、代码基线 + 浏览器覆盖层、Markdown、PRD 覆盖中心、匹配质量、评审模式、碰撞避让和运行诊断。
- 2026-08-19 修正“号码控制 → 发送通知”标注：该规则属于行内按钮动作，即使同一表格有多个同名按钮，也必须绑定 `发送通知` Button，不允许退回“授权人员列表”整个 Card；当前使用表格内首个可见同语义按钮的稳定无后缀 target。
- 2026-08-19 完成个人工作台 Coverage 首轮人工评审收口：36 条 Requirement Atom 中 `review` 已清零，人员准入/页签显隐/实习生借用入口、资产实时业务锁、约60秒待办刷新均确认“PRD 正确、前端原型无需模拟、研发按规则实现”；待审批/已审批/已申请确认“PRD 正确、需要补前端原型”，现已补齐三个正式页面 scope。
- 资产盘点“盘点项目”主链路已建立；“公司-账套对应关系”“盘点规则”“盘点差异报表”仍等待明确字段，不补造业务内容。
- 2026-08-19 保留现有“盘点项目”原型不变，并新增“盘点项目（方案二）”作为对照版本。方案二不再重新实现整套页面，而是直接复用原“盘点项目”页面：仅在创建/编辑阶段隐藏“盘点规则”和“图片上传规则配置”，生成快照后补充“盘点方式设置”，并继续展示原有“图片上传规则配置”；除此之外列表、快照、计划、扫描、审核、进度、报告等页面及交互与原方案保持同源一致。

# 当前停留位置

- 正在把标注体系用于研发评审，而不是继续堆兼容匹配逻辑。
- 个人工作台 `review` 已完成一轮人工决策并收口；下一模块可继续按同样方式逐组确认。
- 个人工作台新增三个正式原型入口：待审批、已审批、已申请。待审批/已审批按 PRD 共用查询与列表结构；已申请覆盖九类员工自助申请记录并提供只读详情/审批记录查看入口。
- 人员类型、实时资产锁、定时刷新等后端/运行时能力不为了“看起来已实现”而在原型中补假逻辑；对应研发规则继续通过标注保留。
- 资产盘点当前可并行对比“盘点项目”原方案与“盘点项目（方案二）”；方案二重点验收路径为：创建项目 → 生成快照 → 配置盘点方式 / 图片上传规则 → 生成盘点计划。
- 下一阶段重点是逐项处理其他 Coverage 中的 `review`：先确认 PRD 口径是否仍有效，再决定修改原型/实现、更新 PRD，或明确保留为后端/流程能力。
- 用户决定新增独立研发评审分支：`main` 继续个人迭代；`review/rd-review` 只保存阶段性已确认结论。后续 PRD 或原型发生变化时，由用户手动选择性同步到评审分支，不自动跟随 `main`。
- 评审分支需要独立 Vercel Preview，并在打开页面时展示版本更新通知，按“PRD 变化 / 原型变化”说明本次同步内容。

# 关键决定

- 标注生成固定流程：PRD 拆 Requirement Atom → 找最细业务 target → 对照当前实现 → 写 annotation → 写 Coverage Ledger → Granularity/Coverage Check。
- action-rule 必须落到 Button；field-rule 必须落到字段/控件；tab-rule 必须落到 Tab；table-column-rule 必须落到表头。找不到稳定目标时进入 `review`，不回退模块。
- Coverage 人工评审结论分三类处理：PRD/原型冲突时按最新业务口径修改一方；PRD 正确但属于权限、实时锁、定时刷新等研发运行时规则时退出 `review`、保留研发标注但不补假前端实现；用户可见页面/交互确实缺失时补原型后再退出 `review`。
- 同名按钮只有在规则确实属于重复行内动作时才允许使用“首个可见同语义按钮”作为展示锚点；普通审批按钮仍遵循“当前页面文案唯一才做语义兼容”，避免误绑。
- target 不依赖动态文案；`共 X 件/条`、统计数字、运行时业务值不得进入稳定 key。
- 弹窗/抽屉等暂时未挂载 DOM 的目标单独归类为动态目标，不计入真未匹配。
- 标注保存继续使用“代码基线 + 用户覆盖层”，避免本地快照挡住后续基线升级。
- 员工确认全面取消电子签，统一刷卡/员工工号 + 狐小 e 扫码，并校验到真实申请人。
- 资产盘点流程方案调整采用“保留原方案 + 同源派生方案”的方式：方案二直接复用原页面实现，只在用户明确要求的节点做差异化处理，避免复制后因独立重写造成字段、布局或交互漂移。

# 当前模块状态

- 个人工作台：工作台首页、待审批、已审批、已申请均已建立正式原型；Coverage 共 36 条规则，当前 `review = 0`，其中权限/实时锁/刷新类规则保留为研发实现规则而不要求原型模拟。
- 资产管理：资产维护、耗材维护、合约号码维护、标签打印、跨公司转移、资产报废、账面报废、资产处置、员工资产信息查询已建立。
- 库存管理：资产接收、耗材接收、入库、出库、移库、转移、库管员工作台已有主要原型，继续按业务字段和流程校准。
- 资产盘点：原“盘点项目”完整保留；“盘点项目（方案二）”直接复用原页面，只将创建阶段的盘点规则/图片规则后置，其中快照后新增盘点方式设置并保留原图片上传规则；其余维护页等待明确需求。
- 原型标注：底座能力已基本完成，当前任务重点转向其他模块 `review` 差异收敛和评审交付。
