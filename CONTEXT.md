# 当前状态

项目是产品演示用企业资产管理前端。`feat/business-rule-updates` 已于 2026-08-09 合并到 `main`，后续新功能原则上从最新 `main` 创建独立分支；当前按用户指定继续在 `feat/workspace-updates` 修改个人工作台。

# 当前正在做什么

个人工作台正在统一收敛详情/审批/办理页面的 UI 语言。合约号码 ES 配给与主管审批已完成 `DetailGrid` 公共详情栅格封装；本轮继续将资产更换申请、MIS 鉴定、资产更换办理页面迁移到同一套单层画布、Card 分组、蓝色竖向分组标题和三列严格对齐的详情布局。

本次已完成：
- “资产更换申请”去除页面自身灰色画布、额外 padding、白底阴影标题框和底部阴影操作框，改为个人工作台单层画布。
- “资产更换申请”的更换信息说明改用公共 `DetailGrid`；更换原因保持必填标识和原提交校验。
- “资产更换申请”的退回资产表格保持原字段和功能，资产标签号固定左侧，资产说明/配置/部件/耗材信息增加 ellipsis，减少横向滚动时的视觉干扰。
- “资产更换申请”的更换信息说明、退回资产信息、更换须知 Card 标题统一增加蓝色竖向分组标识。
- “MIS鉴定”去除页面自身灰色画布、额外 padding 和白底阴影标题框，空状态也改为单层 Card。
- “MIS鉴定”的申请人信息、更换资产信息、MIS 鉴定处理改用公共 `DetailGrid`，Card 标题统一增加蓝色竖向分组标识。
- “MIS鉴定”的原鉴定结果、鉴定说明、同意/驳回决策逻辑和审批历史保持不变；本轮明确不做简化鉴定决策。
- “资产更换办理”去除页面自身灰色画布、额外 padding 和白底阴影标题框，空状态同步改为单层 Card。
- “资产更换办理”的申请人信息、更换资产信息、待发放资产信息改用公共 `DetailGrid`，Card 标题统一增加蓝色竖向分组标识。
- “资产更换办理”补充原逻辑中已实际使用但页面未展示的“退回仓库”字段；旧资产未入库时可维护，已入库后只读，避免使用不可见默认仓库完成入库。
- “资产更换办理”保留原办理阶段逻辑、原“同意”主按钮、原可编辑时机和原审批历史；本轮明确不增加 Steps、不做阶段解锁、不改动态主按钮、不改审批历史。
- `docs/UI_DESIGN_GUIDELINES.md` 补充个人工作台主要业务 Card 的蓝色竖向分组标题规范。

合约号码此前已完成：
- “合约号码ES配给”页面在“号码配给”区块内新增“附件信息”必填字段，支持上传/重新上传附件。
- ES 配给“同意”时同时校验电话号码和配给附件，校验通过后进入“合约号码配给主管审批”。
- 个人工作台新增“合约号码配给主管审批”菜单，位于“合约号码ES配给”下方。
- 主管审批页复用 ES 配给已填写内容并只读展示；审批操作保留同意、驳回、返回。
- 主管审批同意后进入原“合约号码库管员待办”，后续员工领取确认流程保持不变。
- ES 配给与主管审批页面按个人工作台设计规范收敛为单层画布、Card 分区、三列详情和居中流程按钮。
- 合约号码演示数据版本升级为 `v5`，清除浏览器中已推进到后续节点的旧 `v4` 状态影响。
- 默认演示数据同时提供一张“ES审批”待办和一张“合约号码配给主管审批”待办，两个菜单首次打开都直接展示完整业务页面，不再互相依赖前一个页面先提交。

后台“资产管理”此前已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表
- 账面报废查询列表
- 资产处置查询列表
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

新增一级菜单“库存管理”，位置在“无形资产”下方，子菜单固定为：
- 资产接收
- 耗材接收
- 入库
- 出库
- 移库
- 转移
- 库管员工作台

# 近期关键决定

- 当前用户明确指定在 `feat/workspace-updates` 修改，因此本次不新建分支、不创建 worktree。
- 个人工作台业务页统一使用单层画布，避免页面自身再叠加灰底、外层内边距和阴影容器。
- 多 Card 详情需要严格三列对齐时统一使用公共 `DetailGrid`；简单且不要求跨 Card 对齐的详情仍可使用 AntD `Descriptions`。
- 个人工作台主要业务 Card 标题统一使用紧凑蓝色竖向分组标识，不扩展为大面积底色或装饰性图形。
- 资产更换本轮只做已确认的 UI/UE 收敛，不修改 MIS 决策模型，不增加资产更换办理 Steps、阶段解锁、动态主按钮，也不调整审批历史。
- 资产更换办理的退回仓库原本已存在于状态和入库逻辑中，本轮仅补充可见维护入口，不改变原流程状态机。
- 合约号码 ES 配给新增的附件属于“号码配给”结果的一部分，与申请信息中的原附件并存，不替换原申请附件。
- 新流程为：`合约号码ES配给 → 合约号码配给主管审批 → 合约号码库管员待办 → 员工合约号码领取确认`。
- 产品演示菜单需要独立可验收：新增流程节点后，应为新增节点准备对应的默认待办数据，不能只依赖上游页面运行后才能看到目标页面。
- `main` 仍作为后续并行分支开发的统一基线；`feat/business-rule-updates` 已完整纳入 `main`。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

`feat/workspace-updates` 已完成资产更换申请、MIS 鉴定、资产更换办理三页的 UI 收敛，等待用户验收视觉与字段布局。明确暂不继续改 MIS 决策方式和资产更换办理的流程阶段交互。

# 验收路径

1. 打开“个人工作台” → “资产更换申请”，页面应为单层画布，不再出现内嵌灰底和独立阴影标题/底部操作框。
2. “更换信息说明 / 退回资产信息 / 更换须知”标题前应有统一蓝色竖向分组标识；更换原因仍显示必填标识，未填写不可提交。
3. 退回资产表格字段和功能保持不变，资产标签号固定左侧，长内容列超出时截断展示。
4. 打开“MIS鉴定”，申请人信息、更换资产信息、MIS鉴定处理应使用统一三列详情栅格；原资产维修/资产更换/员工取消与同意/驳回逻辑保持不变。
5. MIS 审批信息/审批历史保持原实现，不新增决策简化。
6. 打开“资产更换办理”，申请人信息、更换资产信息、待发放资产信息应使用统一三列详情栅格；各主要业务 Card 标题前有统一蓝色竖向标识。
7. “更换资产信息”中应可看到“退回仓库”；旧资产未入库时可选择，旧资产已入库后不可修改。
8. 资产更换办理仍保持原“同意/驳回/返回/转签”和原办理逻辑，不出现 Steps、不做阶段解锁、不改动态按钮文案、不调整审批历史。
