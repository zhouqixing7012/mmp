# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。当前分支：`feat/business-rule-updates`。

任务开始前必须阅读：`AI_RULES.md`、`CLAUDE.md`、`CONTEXT.md`、`lessons.md`。涉及个人工作台页面时还必须阅读 `docs/UI_DESIGN_GUIDELINES.md`。

# 本次完成

- 退库审批、资产更换、资产退库、资产借用三个流程已完成统一设计语言整改：
  - 详情信息默认三列，长文本单独一行。
  - 部门层级统一使用“.”连接。
  - 流程按钮统一使用纯文字，工具按钮保留图标。
  - 审批记录无真实代理数据时不展示代理人列。
  - 状态统一由 `StatusTag.js` 渲染，`StatusTag.jsx` 仅作转发。
- 项目级设计规范已固化在 `docs/UI_DESIGN_GUIDELINES.md`，并通过 `AI_RULES.md`、`CLAUDE.md` 设为个人工作台页面强制规范。
- “合约号码 ES 配给”页面已按最新要求调整：
  - 删除“5级及以上人员流程为自动发起”的红色提示。
  - 申请信息新增附件下载入口。
  - 身份证号码改为普通单列，不再跨两列。
  - 电话号码字段收窄为单列宽度。
  - 号码配给删除“号码状态”，只保留电话号码和话费套餐。
  - 审批信息删除流程线图，只保留审批历史、审批意见和操作按钮。
- “合约号码库管员待办”和“员工合约号码领取确认”已加入个人工作台，并形成完整领取确认流程。
- “合约号码退库办理”已重构为个人工作台可运行网页原型：
  - 申请人信息、退库合约号码信息、退库信息维护和审批信息使用标准小尺寸 Card。
  - 申请人及号码信息按三列 Descriptions 展示，部门统一使用“.”连接。
  - 退库信息维护包含退库仓库、责任人、退库日期、确认状态和使用说明。
  - 审批信息展示审批历史、审批意见和居中操作按钮。
  - 主操作根据状态分阶段展示“发起退库确认 / 等待员工确认 / 确认入库”。
  - 驳回时审批意见必填，流程按钮均不带图标。
- “员工合约号码退库确认”已重构为个人工作台可运行网页原型：
  - 展示使用人、部门、电话号码、标签号、话费套餐、当前仓库、退库原因和数量。
  - 退库确认提示使用红色强调。
  - 支持输入员工工号视同刷卡确认，也支持点击二维码模拟扫码确认。
  - 确认成功后展示识别工号、确认时间、确认方式和确认结果。
  - 菜单名称统一为“员工合约号码退库确认”。
- 合约号码退库演示数据存储键升级至 `mmp.contractReturn.applications.v2`，确保页面默认存在可验收的办理单据。

# 关键决定

- 用户在资产系统项目中说“原型”“原型图”或“放到个人工作台”时，默认交付仓库内可运行网页，不生成静态图片；只有明确要求图片、截图或视觉稿时才使用图片生成。
- 老系统截图用于确认业务字段和流程，页面视觉结构统一按当前个人工作台规范重构，不照搬旧页面样式。
- 个人工作台页面遵循“页面标题 + 小尺寸业务 Card + 居中流程操作”的统一结构。
- 三列详情使用统一固定列宽；长文本、原因、说明、备注默认单独一行。
- 状态颜色由 `StatusTag` 集中维护，页面禁止自行声明状态颜色映射。
- 日期和部门格式由公共工具统一处理。
- 流程按钮使用纯文字，工具按钮才使用图标。
- 涉及员工本人确认的退库流程，上游办理节点只负责发起确认；员工完成刷卡或扫码后，库管员才能执行最终入库。
- 新增默认演示数据结构时需要升级存储版本，避免旧 localStorage 阻止新节点和新字段出现。

# 上次停留位置

合约号码退库办理和员工合约号码退库确认已改为个人工作台可运行网页原型，等待页面验收。

# 验收路径

1. `/yewurules` → 个人工作台 → 合约号码退库办理。
2. 确认页面包含申请人信息、退库合约号码信息、退库信息维护、审批信息四个 Card。
3. 确认详情默认三列，部门路径使用“.”连接，长文本单独一行。
4. 点击“发起退库确认”，按钮应切换为等待状态，单据进入员工确认节点。
5. `/yewurules` → 个人工作台 → 员工合约号码退库确认。
6. 输入非申请人工号应提示工号不匹配；输入申请人工号或点击二维码应完成确认。
7. 确认成功后展示确认工号、时间、方式和“已确认”状态。
8. 返回合约号码退库办理，主按钮应变为“确认入库”。
9. 点击确认入库后单据进入完成状态；驳回未填写审批意见时应提示必填。
10. 检查两个页面的 Card、按钮、状态标签、字段宽度和空状态符合 `docs/UI_DESIGN_GUIDELINES.md`。
