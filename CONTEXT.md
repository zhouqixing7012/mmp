# 当前状态

项目是产品演示用企业资产管理前端。当前开发分支：`feat/asset-management-updates`，基于最新 `main` 创建，仅用于资产管理相关页面修改。

# 当前正在做什么

后台“资产管理”按现系统截图逐页重构。

已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表 + 嵌入式报废申请编辑页
- 账面报废查询列表 + 嵌入式账面报废申请页
- 资产处置查询列表 + 嵌入式资产处置编辑页
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

# 近期关键决定

- 本分支只修改“资产管理”相关页面，不处理个人工作台和库存管理页面。
- 资产报废、账面报废、资产处置入口统一为单据查询列表。
- 查询条件：申请单号、单据状态、公司、制单时间从/至。
- 列表字段：选择、申请单、单据状态、公司、制单人、制单时间、资产数量、备注、操作。
- 操作区：对应“创建申请单”按钮 + “删除”。
- 资产报废点击新建后，直接在“资产管理 → 资产报废”内容区嵌入现有机房资产报废申请单编辑页 `ScrapApplicationEdit`。
- 账面报废点击新建后，直接在“资产管理 → 账面报废”内容区嵌入现有机房资产账面报废申请页 `AccountingScrapEdit`。
- 资产处置点击新建后，直接在“资产管理 → 资产处置”内容区嵌入 `AssetDisposalEditPage`。
- 资产处置基本信息仅保留：制单人、制单时间、处置说明、附件。
- 资产处置资产明细字段与账面报废申请页资产明细保持一致；操作仅保留添加物资、删除物资及单行删除。
- 嵌入页返回/退出后恢复当前菜单的查询列表，不离开 `/yewurules` 后台框架。
- 现有 `/BaofeiShenqing` 和 `/` 独立路由继续保留，不删除，避免影响已有演示入口。
- 列表统一把“共 X 条”放在列表标题右上角，操作按钮放下一行右侧。
- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

资产处置编辑页已完成，并与资产报废、账面报废一样采用后台框架内嵌方式打开。下一步继续处理资产管理下的公司间转移或其他待确认页面。

# 验收路径

1. 打开 `/yewurules` → “资产管理” → “资产处置”。
2. 点击“创建资产处置申请单”，左侧菜单和后台顶部保持不变，内容区直接显示“资产处置申请单”。
3. 基本信息只显示制单人、制单时间、处置说明、附件。
4. 资产明细展示与账面报废申请页一致的明细字段，并提供“添加物资”“删除物资”。
5. 点击底部“返回”应回到资产处置查询列表，不发生路由跳转。
6. 资产报废、账面报废的嵌入式新建方式继续保持不变。
