# 当前状态

项目为企业资产管理产品演示前端。当前继续在 `feat/workspace-updates` 修改个人工作台，不新建分支或 worktree。

# 当前正在做什么

个人工作台已完成一轮 PC 端详情/审批 UI 收敛，并正在补充员工侧移动端原型。当前重点是“物资申请”“移动端工作台”“号码控制”、合约号码申请及资产退库流程页面规范化。

## 本阶段已完成

### 物资申请
- 原“资产申请”统一更名为“物资申请”，菜单、页面标题、面包屑、预览标题及提示文案同步。
- 首次进入仍保留“申请须知”弹窗，仅删除正文中的“【申请原则】”标题，须知内容及“已阅读”动作保留。
- “添加物资”继续使用大类 / 小类 / 品牌 / 型号 / 配置五级选择。
- 长名称不使用 `...` 隐藏；自动换行完整展示，并保留 Tooltip。
- 演示目录支持“同品牌多型号、同型号多配置”。
- 长配置案例按一条完整配置展示：`转接线.绿联、CELINK、苹果.Type-C转（VGA/以太网/USB/Type-C）`，不拆分。
- 明细“详细说明”统一改为“申请原因”，底层暂保留 `detail` 字段。
- 重复选择同一物料时不新增第二行，原明细数量 +1，并给出合并提示。
- 耗材“关联主资产”使用独立弹窗，只能从当前员工本人名下已有资产中选择。
- 关联主资产弹窗可展示标签号、资产说明、配置、状态、用途；选择完成后编辑态与预览态均**只回显资产标签号**。

### 移动端工作台
- 个人工作台新增“移动端工作台”菜单，紧跟“工作台首页”。
- 当前菜单已切换到 `MobileWorkspacePageV2`，原移动端页面文件暂保留但不再作为菜单入口。
- 移动端原型放在桌面内容区中的独立手机画布内，内部自行管理滚动、底部导航和弹层。
- 移动端使用项目现有 **Ant Design 6** 组件重绘，并参考飞书类 App 的清爽信息架构：白色卡片、浅灰页面底色、蓝色主色、弱边框、紧凑留白、轻量状态标签。
- 未引入 `antd-mobile` 新依赖；基于仓库已有 `antd@6.4.5` + `ConfigProvider` 做移动端主题适配。
- 首页一级数据分类固定为：**资产 / 耗材 / 合约号码**。
- 首页顶部“物资概览”保留轻量渐变概览卡，资产 / 耗材 / 合约号码分别使用图标 + Statistic 数量卡展示，点击数量卡可直接切换对应分类；已删除“我的资产服务”“在用物资”两处辅助文案，仅保留资源数量信息。
- 首页一级分类使用 Ant Design `Tabs` 的线条式页签；单据“未完成 / 已完成”、盘点“未盘 / 已盘”、物资申请“资产 / 耗材”同步使用同一套线条页签语言。
- 资产卡片单条操作固定为：**退库 / 转移 / 更换**；三个按钮保持同级默认样式，“更换”不再高亮；资产详情页底部同步提供三项同级操作。
- 长按任一资产约 520ms 进入批量选择模式；可继续点选多项资产，底部显示 **批量退库 / 批量转移 / 批量更换** 操作栏，并提供取消批量操作入口；页面不再额外显示“长按任一资产可进入批量……”说明文案。
- 耗材卡片展示数量和关联主资产标签号。
- 合约号码卡片展示号码、运营商、套餐、状态；号码退库仍从号码卡片进入，不放到全局“+”快捷入口。
- 底部导航保留：首页 / 单据 / + / 盘点 / 扫一扫。
- 中间“+”快捷入口**只提供：物资申请、资产借用**，不提供合约号码退库。
- 移动端“物资申请”采用与 PC 端同一业务结构的 **大类 → 小类 → 品牌 → 型号 → 配置** 五级目录，移动端用逐级钻取 + 可回跳路径 + 底部购物车表达。

### 号码控制
- “号码控制”页面按当前个人工作台 UI 规范重新收敛为单层画布，统一使用 Ant Design `Space + QueryBar + Card + Table + Modal`。
- 查询条件为“姓名/工号”“授权状态”，授权状态枚举固定为：**已授权 / 未授权**。
- 列表字段顺序统一为：**工号、姓名、所属部门、授权状态、操作**；状态通过公共 `StatusTag` 展示。
- 原“权限状态”统一改名为“授权状态”；操作原“启用 / 停用”统一改名为“授权 / 停止授权”，停止授权保留二次确认。
- “发送通知 / 授权 / 停止授权”使用表格内文字 Link；未授权人员的“发送通知”不可操作。
- “新增授权人员”使用标准 Ant Design Modal + QueryBar + Table，保留工号、姓名、部门查询、多选、分页及已选人数。

### 合约号码申请
- 个人工作台原“号码管理”菜单统一更名为 **“合约号码申请”**，页面标题同步改为“合约号码申请”。
- 页面按当前 UI 规范重画为单层画布，顶部只保留页面标题和申请单号，业务内容放入 `Card size="small"`。
- “申请信息”使用蓝色竖向分组标识 + `DetailGrid`，保留原有身份证号码、申请原因、上传附件、实名认证提示、电信号码使用协议入口及提交功能。
- 附件继续限制单文件、最大 10M；原页面不再使用独立灰底、最大宽度白盒和 Divider 作为主要层级。

### 资产退库流程 UI 规范化
- 现有“退库审批”页面按当前 UI 规范重画：单层画布、标准标题、小尺寸 Card、蓝色竖向分组标题、`DetailGrid` 三列详情、small bordered Table、标准 Modal。
- 退库审批保留申请人信息、退库资产信息、附件、审批历史、审批意见、鉴定通过/不通过、维修记录等原有字段和功能；页面标题与菜单统一为“退库审批”。
- “资产退库办理”同步重画，保留申请人信息、退库资产信息、退库信息维护、附件、审批意见、确认/驳回、员工确认状态机和维修记录功能；不再使用二层灰色画布或阴影标题盒。
- 退库办理的详情区域改用公共 `DetailGrid`，状态统一走 `StatusTag`；附件 Card 也统一使用蓝色竖向分组标题。
- “员工退库确认”按员工确认页规范重画为：**申请人信息 / 退库资产明细 / 刷卡/扫码确认**。
- 员工退库确认的“申请人信息”仅保留：**申请人、部门**，不再使用“退库确认信息/确认信息”命名。
- “退库资产明细”使用标准 small bordered Table，保留原退库资产核对字段，并明确包含 **行号、退库原因**。
- 员工退库确认同时保留两种确认方式：**刷卡/管理员录入工号确认**、**狐小 e 扫码确认**；不包含电子签，工号仍校验为申请人本人。
- 扫码原型改用 Ant Design `QRCode`，确认成功后继续展示识别员工、确认时间、确认方式和确认结果。

### 合约号码库管员待办 / 员工领取确认
- “合约号码库管员待办”收敛到个人工作台单层画布和当前 Card / DetailGrid 设计语言。
- 当前仓库只读；单据备注删除；城市使用弹窗选择。
- “申请合约号码信息”字段顺序固定为：**标签号、序列号、说明、数量、金额、城市、子公司、申请原因、备注**；城市 + 子公司单独一行，申请原因独占一行，备注独占一行。
- 原“办理操作”Card 改名为“审批意见”，主按钮改名为“领用确认”。
- “员工合约号码领取确认”按：**领用人信息 / 领用物资明细 / 保管职责 / 刷卡/扫码确认** 四块展示。
- 员工领取确认页的“领用物资明细”只保留：**标签号、电话号码、说明、申请数量、领用数量、申请原因**。

### 其他已完成且需保持
- 合约号码流程：`合约号码ES配给 → 合约号码配给主管审批 → 合约号码库管员待办 → 员工合约号码领取确认`。
- 合约号码 ES 配给新增必填配给附件；主管审批页只读展示 ES 配给结果。
- 资产更换申请、MIS 鉴定、资产更换办理已统一到个人工作台单层画布和 `DetailGrid` 视觉规范。
- MIS 鉴定结果仅保留“资产维修 / 资产更换”。
- 新员工领用单及新员工领用员工确认已新增到个人工作台。
- 本次重构全面取消电子签，员工确认统一使用刷卡/工号和狐小 e 扫码。

# 近期关键决定

- 个人工作台 PC 页继续使用单层画布；业务页自身不再叠加 `min-h-screen`、独立灰底和额外外层 padding。
- 多 Card 三列详情需要严格跨 Card 对齐时统一使用公共 `DetailGrid`。
- 主要业务 Card 标题使用紧凑蓝色竖向分组标识。
- 页面标题与 Card 标题分层：页面标题使用标准 `Typography.Title level={4}`，蓝色竖块只用于业务 Card 标题。
- 员工确认页只展示当前确认动作真正需要核对的信息，确认方式统一为刷卡/工号 + 狐小 e 扫码，不恢复电子签。
- “合约号码申请”作为原“号码管理”的新名称，菜单和页面可见文案不再使用“号码管理/电话卡申请”。
- “退库审批 / 资产退库办理 / 员工退库确认”与其他个人工作台流程页使用同一 UI 设计语言，不再保留旧独立页面容器。
- 关联主资产选择完成后的回显只显示资产标签号。

# 当前验收路径

1. 个人工作台 → 物资申请：首次出现申请须知但无“申请原则”标题；五级目录、长配置、重复物资合并、关联主资产回显规则保持不变。
2. 个人工作台 → 移动端工作台：首页分类为资产 / 耗材 / 合约号码；底部“+”只有物资申请 / 资产借用；资产支持退库 / 转移 / 更换与长按批量操作。
3. 个人工作台 → 号码控制：查询“姓名/工号 + 授权状态”，状态仅已授权/未授权，操作为发送通知、授权/停止授权。
4. 个人工作台 → 合约号码申请：菜单和页面标题均显示“合约号码申请”，申请原因、身份证、附件、实名认证提示与提交功能可用，页面为单层 Card 结构。
5. 个人工作台 → 退库审批：页面为单层画布 + 蓝色 Card 标题 + DetailGrid；审批历史、审批意见、附件、鉴定通过/不通过和维修记录功能可用。
6. 个人工作台 → 资产退库办理：申请人信息、退库资产信息、退库信息维护、附件、审批意见、确认/驳回均可用；员工确认流程状态机保持不变。
7. 个人工作台 → 员工退库确认：第一块标题为“申请人信息”，只显示申请人和部门；退库资产明细包含行号与退库原因；刷卡/工号和狐小 e 扫码两种确认方式均存在且无电子签。
8. 合约号码库管员待办：当前仓库只读、无单据备注、城市弹窗选择；城市+子公司一行、申请原因和备注各独占一行。
9. 员工合约号码领取确认：四块标准结构，领用物资明细只包含标签号 / 电话号码 / 说明 / 申请数量 / 领用数量 / 申请原因。
