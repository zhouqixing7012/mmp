# 当前状态

项目是产品演示用企业资产管理前端，当前分支：`feat/business-rule-updates`。

# 当前正在做什么

后台“资产管理”按现系统截图逐页重构。

已完成页面：
- 资产维护
- 耗材维护
- 合约号码维护
- 标签打印（含预打印、打印历史）
- 资产报废查询列表
- 账面报废查询列表
- 资产处置查询列表
- 员工资产信息查询

“公司间转移”仍等待截图和字段。

新增一级菜单“库存管理”，位置在“无形资产”下方，子菜单固定为：
- 资产接收
- 耗材接收
- 入库
- 出库
- 移库
- 转移
- 库管员工作台

当前库存管理仅建立菜单和页面入口，字段未确认前不补业务内容。

# 近期关键决定

- 资产报废、账面报废、资产处置入口统一为单据查询列表。
- 查询条件：申请单号、单据状态、公司、制单时间从/至。
- 列表字段：选择、申请单、单据状态、公司、制单人、制单时间、资产数量、备注、操作。
- 操作区：对应“创建申请单”按钮 + “删除”。
- 资产报废新建跳转现有 `/BaofeiShenqing`；账面报废新建跳转现有 `/`。
- 资产处置申请页字段尚未提供，因此保留新建按钮，但不猜表单字段。
- 列表统一把“共 X 条”放在列表标题右上角，操作按钮放下一行右侧。
- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

资产报废、账面报废、资产处置查询入口以及库存管理菜单已落地。下一步等待用户继续提供公司间转移、库存管理各子页面或资产处置新建页的截图和字段。

# 验收路径

1. 打开 `/yewurules` → “资产管理”。
2. 点击“资产报废 / 账面报废 / 资产处置”，均应进入同结构的单据查询列表。
3. 三页均展示查询、重置、创建、删除和空列表；“共 X 条”位于列表右上角。
4. “资产报废”创建按钮进入 `/BaofeiShenqing`；“账面报废”创建按钮进入 `/`。
5. 左侧“无形资产”下方出现一级菜单“库存管理”。
6. 展开“库存管理”应按顺序展示 7 个子菜单，默认进入“资产接收”。
7. 库存管理各子菜单当前只展示待确认空状态，不出现未经确认的业务字段。
