# 当前状态

项目是产品演示用企业资产管理前端。`feat/business-rule-updates` 已于 2026-08-09 合并到 `main`，库存管理当前在 `feat/inventory-management-updates` 分支持续开发。

# 当前正在做什么

后台“库存管理”按现系统截图逐页重构。

库存管理菜单固定为：
- 资产接收
- 耗材接收
- 入库
- 出库
- 移库
- 转移
- 库管员工作台

已完成库存页面：
- 资产接收：PO列表、待接收PO详情、已入库PO对应接收单列表

待确认页面：耗材接收、入库、出库、移库、转移、库管员工作台。

# 近期关键决定

- 老系统截图用于确认业务字段和流程；视觉结构按当前项目规范重构。
- 页面不能因为数据对象已有字段就全部展示，以用户当前确认字段为准。
- 资产接收首页按PO维度展示；待接收PO行操作为“接收”，已入库PO行操作为“查看”。
- 点击“接收”进入PO详情；点击“查看”进入该PO的接收单列表。
- 资产接收相关页面不展示“配置”按钮，不从旧系统截图自行补充未确认操作。
- PO详情按截图保留“创建接收单 / 执行入库 / 返回”，物资明细保留行内“编辑”。
- 接收单列表保留“删除接收单”和行内“查看”。
- 公司、板块、供应商等选择型字段复用统一选择弹窗；演示数据仅使用用户截图中已出现的数据。
- 本次重构全面取消电子签，员工确认统一保留刷卡/工号和狐小 e 扫码。

# 上次停留位置

`feat/inventory-management-updates` 已完成资产接收原型，并已接入库存管理“资产接收”菜单。下一步等待用户提供库存管理其他页面截图和字段继续实现。

# 验收路径

1. 打开 `/yewurules` → “库存管理” → “资产接收”。
2. 首页展示公司、板块、PO单号、供应商、接收状态、采购类型查询条件和PO列表。
3. 待接收PO行显示“接收”，点击进入PO详情；PO2103040001展示用户截图中的接收单信息和物资明细。
4. PO详情操作区仅有“创建接收单 / 执行入库 / 返回”，无“配置”。
5. 已入库PO行显示“查看”，PO2606030001点击后进入接收单列表并展示 REC-202606110001。
6. 接收单列表有“删除接收单”和行内“查看”，无“配置”。
