# 当前状态

项目是产品演示用的企业资产管理前端，不是生产系统。

任务开始前必须先阅读：
- `AI_RULES.md`
- `CLAUDE.md`
- `CONTEXT.md`

# 本次完成

完成资产借用首轮页面收敛，统一向现有资产申请、资产配给和业务审批页面靠拢。

- 借用申请：移除申请人信息；物资弹窗改为与资产申请一致的五级分栏和已选物资区；开始、结束日期合并为日期范围选择器，并在面板底部提供快捷期限。
- 借用配给：申请人信息字段与资产配给一致；申请明细和 ES 配给处理分区展示；资产选择弹窗改为与库存领用物资列表一致的查询、表格和单选交互。
- 借用审批：移除顶部审批规则提示；申请人信息精简；申请及配给表格将当前仓库、资产标签号放在前两列，并删除 SN 号和资产状态。

# 关键决定

- 资产借用相关原型全部放在个人工作台下，便于逐页验收。
- 借用页面优先复用现有资产申请、资产配给和审批页面的结构与交互，不单独设计另一套样式。
- 借用物资范围由后端控制，前端选择弹窗保持与资产申请一致。
- 同一明细数量大于 1 时，提交后按数量拆成多行，但仍属于同一借用单。
- 续借不在本轮范围内；到期和逾期只保留规则，不改变单据状态或资产标记。
- 这是前端演示，通知、真实扫码、刷卡、库存锁定和出库接口均为页面流程模拟。

# 上次停留位置

借用申请、借用配给和借用审批已按最新反馈完成代码调整，下一步继续逐页验收借用发放和员工借用确认。

# 不变约束

- 不修改 `package.json`，不新增依赖。
- 页面优先使用 Ant Design。
- 查询区域使用 `QueryBar` 和 `QueryItem`。
- 页面不直接操作 localStorage。
- 不删除已有功能和独立路由。

# 验收路径

1. 进入“资产借用”，确认页面不展示申请人信息。
2. 点击“添加物资”，确认弹窗结构与资产申请一致，并支持五级分栏、多选和已选物资展示。
3. 确认借用日期使用一个范围选择器，面板底部可选择近 7 天、15 天、1 个月、2 个月和 3 个月。
4. 进入“借用配给”，确认申请人信息字段与资产配给一致，申请物资和 ES 配给处理分区展示。
5. 点击资产标签号后的搜索按钮，确认物资列表查询和表格样式与资产配给库存领用一致。
6. 进入“借用审批”，确认无顶部提示区域，申请人信息已精简。
7. 确认审批表格前两列为当前仓库、资产标签号，且不展示 SN 号和资产状态。
